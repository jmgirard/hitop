# M078: The per-scale tables the package ships or returns join on one column shape

- **Status:** in-progress
- **Priority:** normal
- **Depends on:** —
- **Driving RR:** —
- **Principles touched:** GP2, GP3, IP1
- **Branch/PR:** `m078-column-shape`

## Goal

A caller joining the package's per-scale tables to each other or to scored output — the shipped `*_scales`, `hitopsr_subscales` and `*_devstats` datasets, a `reliability_*()` return, `available_scales()` — meets one integer `nItems` and a `camelCase` stem column on every one of them, and the two devstats tables and the reliability return spell their display-name column `Scale` as `available_scales()` does, with no value moved.

## Scope

User-facing tier: the deliverable changes the storage type of a column in four documented datasets (six tibbles), renames a column in two, and adds a column to three exported functions' return value.

**In:** (a) `nItems` becomes integer in `hitopsr_scales`, `hitopsr_subscales`, `hitopbr_scales` and each element of `pid_scales`, by editing the builders (`data-raw/hitopsr_info.R:20,39`, `data-raw/hitopbr_info.R:36`, `data-raw/pid_info.R:20,35,50,75` — `purrr::map_dbl` → `map_int`, `as.numeric` → `as.integer`) and re-running them; every other column of every object those scripts save is identical to the object before the run. `available_scales()` drops the `as.integer()` coercion and its comment (`R/available_scales.R:50-53`). (b) `reliability_pid5()`, `reliability_hitopsr()` (full and `module` path) and `reliability_hitopbr()` return a `camelCase` column between `Scale` and `nItems`, read from the keying table's `camelCase` column on the same row `Scale` is read from (D-046's supplier pattern: `reliability_engine()` takes a `scale_stems` argument parallel to `scale_names`, guarded to the same length; `module_engine_inputs()` supplies it from `kept$camelCase`). (c) `hitopsr_devstats` and `hitopbr_devstats` rename their first column `scale` → `Scale`, by editing `data-raw/hitopsr_devstats.R` and `data-raw/hitopbr_devstats.R` and re-running them; `data-raw/verify_hitopsr_devstats.R` and `verify_hitopbr_devstats.R` read the new name. Roxygen (`R/data.R` `@format` blocks; the three `@return` blocks), `cairn/DESIGN.md:33,44`, tests, `cairn/LESSONS.md`'s M037 line, and NEWS follow. Both column changes take D-018's migration path — one release, no dual column, no shim — with the pre-1.0 deprecation cycle waived by Jeff at the 2026-09-01 plan gate (D-053, written with this plan). No scored, reliability, interval or devstats value moves; no keying content moves (IP1 sign-off for regenerating the four keying-table files given at the same gate).

**Out:** `p-Factor` capitalization → its own candidate row; PID-5 item-column naming → its candidate row; a `Scale` column on `pid_scales` (whose display column is `Facet` or `Domain` by version) → not planned, the reliability return is where the PID-5 gets a `Scale`; `interval_*()`, which match on `camelCase` and read no renamed column → untouched, their existing tests stand as the guard; the `hitop_module()` object, whose `nItems` is already integer → untouched; the M061 NEWS text in the 0.2.0 section saying the tables store a double → left as the record of that release, the new entry says what changed since.

## Acceptance criteria

- [ ] AC1. Every shipped dataset that is a data frame with an `nItems` column, or a list whose elements are, stores `nItems` as integer: a test walks `utils::data(package = "hitop")$results[, "Item"]`, loads each with `getExportedValue()`, descends one list level, asserts `is.integer(x$nItems)` on every hit, and asserts the hits are exactly `hitopsr_scales`, `hitopsr_subscales`, `hitopbr_scales`, `hitopsr_devstats`, `hitopbr_devstats` and the three elements of `pid_scales`, so the walk is shown non-empty. The evidence is the test under `devtools::check()`, whose install makes the index the branch's; a run against a stale install is not evidence.
- [ ] AC2. Only the type or the name changed: for each of `hitopsr_scales`, `hitopsr_subscales`, `hitopbr_scales`, `pid_scales`, `hitopsr_devstats` and `hitopbr_devstats`, with `old` loaded from the merge base through `tests/testthat/helper-merge-base.R` and `new` the committed object, a test asserts `identical(new, old)` after applying to `old` exactly the change this milestone makes to it (`old$nItems <- as.integer(old$nItems)`, per element for `pid_scales`; `names(old)[1] <- "Scale"` for the two devstats tables). The test skips without a distinct merge base and skips when the base already carries the change (the M059 `skip_without_rename_base()` shape), so it runs on this branch and never fails a later one.
- [ ] AC3. `reliability_pid5()` for each of `FULL`, `SF`, `BF`, `reliability_hitopsr()` with and without a `module`, and `reliability_hitopbr()` — each under the default flags, under `omega = FALSE`, and under `alpha = FALSE` — return columns named exactly `Scale`, `camelCase`, `nItems`, then whichever of `alpha`, `omega` the flags request, in that order; on every row `camelCase` equals the keying table's `camelCase` on the row where that table's display column (`Facet` for FULL/SF, `Domain` for BF, `Scale` otherwise) equals the row's `Scale`; and for each of the six calls, `paste0(prefix, camelCase)` with the matching `score_*()` default `prefix` names a column of that `score_*()`'s output on the same data. Tests assert all three on each of the six calls.
- [ ] AC4. No `Scale`, `nItems`, `alpha` or `omega` value, no row position, and no signalled condition class differs from the merge base on any cell of this matrix: `reliability_pid5()` by version on `sim_pid5`, `sim_pid5sf`, `sim_pid5bf` and `ku_pid5sf`; `reliability_hitopsr()` on `sim_hitopsr` and `ku_hitopsr`, full and on two modules (one scale; four scales); `reliability_hitopbr()` on `sim_hitopbr` and `ku_hitopbr`; each under `alpha`/`omega` in all four settings, default `srange`. The branch side is compared with its new `camelCase` column dropped.
- [ ] AC5. `hitopsr_devstats` and `hitopbr_devstats` have names exactly `Scale`, `camelCase`, `type`, `nItems`, `reliability`, `reliabilityType`, `mean`, `sd` (a test asserts the vector), and `data-raw/verify_hitopsr_devstats.R` and `data-raw/verify_hitopbr_devstats.R` run to completion reporting no discrepancy against the pinned proof PDF — maintainer-local evidence, since the shelf under `cairn/references/sources/` is gitignored, recorded as M041's was.
- [ ] AC6. `devtools::document()` produces no diff, `devtools::test()` is clean, `devtools::check()` reports 0 errors / 0 warnings; the four retyped tables' `@format` entries say `nItems` is an integer and the two devstats entries name `Scale`; the three `reliability_*()` `@return` blocks name `camelCase`; `grep -rn 'double' R/ tests/ cairn/DESIGN.md cairn/LESSONS.md` returns no line saying a `*_scales` table stores `nItems` as a double (each hit read); `NEWS.md` carries a Breaking-changes entry naming the `scale` → `Scale` rename in both devstats tables and the new `camelCase` column (with the column-position consequence), and an Improvements entry for the integer `nItems` naming the four datasets and that `identical()` against `available_scales()$nItems` now holds.

## Coverage

- AC1 → T1
- AC2 → T1, T3
- AC3 → T2
- AC4 → T2
- AC5 → T3
- AC6 → T1, T2, T3, T4

## Tasks

- [x] T1. Tests first: the AC1 walk and the AC2 merge-base identity test (both devstats tables included, red until T3); prove AC2's test able to fail by planting, on a copy, one moved list-column value and one reordered row per object family and seeing red, controls green (record in the work log). Edit `data-raw/pid_info.R:20,35,50,75`, `hitopsr_info.R:20,39`, `hitopbr_info.R:36` to build integer `nItems`; re-run the three scripts (they need {snakecase}, {purrr}, {dplyr} locally, per `Config/Needs/data-raw`); compare every object each script saved against its merge-base object (M026 lesson: `load()` from `git show`, never the `.rda` bytes) and `git checkout` back any object with no change; if any column other than `nItems` differs, stop and surface it at the amendment gate rather than commit. Drop the coercion and comment at `R/available_scales.R:50-53`; update `tests/testthat/test-available_scales.R:14-17` (comment and comparison); update the four `@format` `nItems` lines in `R/data.R` (`pid_scales`, `hitopsr_scales`, `hitopsr_subscales`, `hitopbr_scales`).
- [x] T2. Tests first for AC3 (extend `tests/testthat/test-reliability.R:174`'s exact-names assertion; add the six-call × three-flag column-order, table-alignment and join-into-`score_*()` tests). Add `scale_stems` to `reliability_engine()` (`R/reliability_engine.R`) with the same length guard as `scale_names`; supply it from `pid_scales[[version]]$camelCase` (`R/reliability_pid5.R:63-69`), `hitopbr_scales$camelCase` (`R/reliability_hitopbr.R:52`), `hitopsr_engine_inputs()` and `module_engine_inputs()` (`R/module.R`). Update the three `@return` blocks. Run AC4's differential as a scratch script against a `git archive` export of the merge base (M031 lesson), recording value and condition classes together per cell (M037 lesson); its cell count and verdict go in the work log.
- [x] T3. Rename the shipped column in `data-raw/hitopsr_devstats.R` (`scale =` at `:85,91,108`; `$scale` reads at `:124,129,130,135,143`) and `data-raw/hitopbr_devstats.R` (`:85,101,106,107,112`) — the local `package_names$scale` join key renamed with it; re-run both, confirm AC2's identity for both; move `data-raw/verify_hitopsr_devstats.R` and `verify_hitopbr_devstats.R` to `$Scale` and run both against the shelf PDF; update `tests/testthat/test-interval_hitopsr.R` (the `package_names()` helper at `:9-32`, `merge(by = "scale")` at `:81,129`, reads at `:34-99`) and `test-interval_hitopbr.R` (`:18-87`); `grep -rnw scale R/ tests/ data-raw/ vignettes/ devel/ pkgdown/ README.Rmd cairn/DESIGN.md` and read each hit for a devstats read; update the two devstats `@format` blocks in `R/data.R`; add the AC5 names test.
- [ ] T4. NEWS (two entries), `cairn/DESIGN.md:33,44`, correct `cairn/LESSONS.md`'s M037 line in place (the mismatch it warns of no longer exists; AC1's test enforces the type, so review may retire it), the AC6 `double` grep, `devtools::document()`, `devtools::test()`, `devtools::check()`.

## Work log

- 2026-09-01: created by /milestone-plan; absorbs the 2026-08-21/2026-08-28 column-shape candidate row (lineage M043, M061, M068). Waiver, keying-file sign-off and the subscales inclusion chosen by Jeff at the plan gate; D-053 records them.
- 2026-09-01: criteria audit ran in full mode ([O] fresh reader, before the gate); 21 findings, 20 fixed in the draft before the gate saw it — goal narrowed to the tables that gain `Scale`; AC1 loads through `getExportedValue()` with `check()` as its evidence; AC2 skips once the base carries the change and T1 plants defects; AC3 names the display column per table and the flag settings; AC4 restated as a property of the return with `ku_*` data, `FALSE/FALSE` and a second module added and conditions recorded; AC5 asserts the names directly and marks the verify-script evidence maintainer-local; AC6's grep set widened to `tests/` and `LESSONS.md`; coverage and cited line ranges corrected. One finding kept as drafted: AC3's table-alignment clause, which catches a row-misaligned supply.
- 2026-09-01: plan gate chose waiving the deprecation cycle (pre-1.0) over a one-release dual-column warning because two column sets would be documented and tested for a release on a package with no known external callers; falsified by a downstream caller breaking on the release.
- 2026-09-01: plan gate chose re-running the `data-raw/*_info.R` builders over coercing the shipped objects in place because the builders must emit an integer next run anyway and the identity test bounds the risk; falsified by a builder run that changes a column other than `nItems`.
- 2026-09-01: plan gate chose including `hitopsr_subscales` over the row's three tables because the promise then covers every shipped table with an `nItems` column; falsified by nothing short of a reason to keep one table double.
- 2026-09-01: plan chose placing `camelCase` second, after `Scale`, over appending it last because `available_scales()` already orders `Scale`, `camelCase`, `nItems`; falsified by a caller relying on `nItems` at position two.

- 2026-09-01: T1 done. AC1 walk and AC2 identity tests in `tests/testthat/test-column-shape.R`; discrimination proof on merge-base copies: 3 controls identical, 6 planted defects (one moved list-column value or cell, one reordered row, per family: flat keying table, `pid_scales`, devstats) all reported. Builders rebuilt with `map_int`/`as.integer`; every other object the three scripts save (`pid_items`, `pid_domains`, `hitopsr_items`, `hitopsr_definitions`, `hitopbr_items`) loaded identical to its merge-base object, `pid_items.rda` checked back out (bytes only); the four retyped tables identical after `as.integer(nItems)`. `available_scales()` coercion dropped, its test and four `@format` lines updated. `devtools::test()`: only the two devstats identity tests fail, red by plan until T3.
- 2026-09-01: T2 done. AC3 tests (six calls × three flag settings: column order, row-aligned stem lookup by display name, `prefix + camelCase` names a `score_*()` column) in `test-column-shape.R`, omega estimator mocked there for speed; `reliability_engine()` takes `scale_stems` with the same length guard as `scale_names` (guard test added beside the M061 one); suppliers read `camelCase` from the same table row as the display name; three `@return` blocks updated. AC4 differential (48 cells: 12 data×call combinations × 4 flag settings) running as a scratch script against a `git archive` export of the merge base; verdict logged when both sides finish.
- 2026-09-01: T3 done. `scale` → `Scale` in both devstats builders (the local `package_names` join key with it) and both verifiers; both tables rebuilt and identical to the merge base after the rename (AC2 tests green, none skipped); `verify_hitopsr_devstats.R` (372 cells over 93 rows, type vs indentation 93 rows) and `verify_hitopbr_devstats.R` (32 cells over 8 rows) each report every shipped cell matching Table 1 of the pinned proof PDF (sha256 1c2112…, matches). Interval tests moved to `Scale` (the HiTOP-BR oracle list's own `scale` field left as is: not a devstats read); the `-w scale` sweep's remaining hits are `pid_norms`' stem column, `type == "scale"` literals and prose. Two devstats `@format` blocks updated; AC5 names test added. `devtools::test()` clean.
## Decisions

## Review
