# M125: hitop-form's `link.html` fills its fields from a study link's `c` parameter, so a link can be edited and another page can hand a module over

**Status:** done (2026-09-25, PR #135 https://github.com/jmgirard/hitop/pull/135; hitop-form PR #11 https://github.com/jmgirard/hitop-form/pull/11, green and approved, open at archive time for Jeff to merge from his terminal)

**Goal:** Make hitop-form's link builder read the `?c=` parameter the form page already reads, fill its controls from it, refuse one it cannot read by name, and show its place in the flow with links to the module builder and the online-collection tutorial.

**Outcome:** In hitop-form `link.html`: `prefill()` in the module script, run before `showKind()`, reads `c` from `location.search`, decodes it with form.js's `decodeConfig()`, and refuses three faults into `#err` by name ("The link's c parameter could not be read." / "does not hold a form." / "names an instrument this page does not offer: <value>.", each followed by "Fill in the form above to make a new link."), the instrument check reading the select's own options. A readable `c` fills every control it names (string fields only, `shuffle` and `prolific` as `=== true`, a plain-object `module` as indented JSON, a `webhook` or `supabase` store into its kind's group); a wrong-type field is skipped and the rest filled. The call is wrapped in try/catch: a throw past the checks (a module nested some 6,000 deep stops `JSON.stringify`) gives `f.reset()` and the first refusal, so the submit handler still attaches. Above the form, `ol.steps` (choose the instrument, where the responses go, make the link) links the Module Builder and the online-collection tutorial; the module hint links the builder; `nav.upnav` links the package site. Tests L16 to L19 (round trips per store choice, instrument-only and instrument-plus-module against a no-`c` load, seven bad loads each asserting its message and a live submit handler, the list and links) and N8 (only `link.html` and `form.js` requested). README paragraph and test rows; hitop NEWS. Suite 216 tests.

**Decisions:** none milestone-local. Plan gate: prefilled hand-off links over merging the link builder into the module builder, and prefill over bare cross-links, both falsifiers in the work log. Question gate: a wrong-type field is skipped rather than the whole `c` refused.

**Review:** three-lens fan-out, one pass, user-facing tier, return count 0. Criteria audit at plan: 36 findings, 31 repaired. Claim audit at implement: 60 claims, 10 corrected. The history and prior-review lenses found nothing, both PR-comment probes empty. The diff-bug lens: 17 findings, none falsifying a criterion. Fixed at the gate (hitop-form ba06da5): F1 the refusal said "form below" with `#err` under the fields; F2 an uncaught throw in `prefill()` left the page without its submit handler, so "Make the link" would have put every field into the address; F4 L18 asserted only "c parameter"; F17 a stale network header. Three plants red. Candidate row (F3, F10, F6): a crafted `c` presets store and completion addresses, the README privacy paragraph omits the edit route, an alert filled before load may go unannounced. Rejected: F5 and F14 (the gate's skip-and-fill choice), F7 and F8 (the form page's wording), F9 (unverified), F11 and F12 (identical values), F13 (on record), F15 and F16 (forward risks). hitop test FAIL 0 PASS 20375, check 0/0/0, CI 8 of 8 on #135; hitop-form CI green on #11. The `gh pr checks --watch` hit the harness ceiling once; the session resumed by the record. Nothing graduated or retired; the plain-English lint hook's hits on the tracking files were left as cairn's record grammar.
