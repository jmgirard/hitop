# M101: The builder tells users to score shuffled Word forms with `layout = "printed"`

**Status:** done (2026-09-21, PR #111 https://github.com/jmgirard/hitop/pull/111 tracking, code in jmgirard/hitop-builder PR #17 https://github.com/jmgirard/hitop-builder/pull/17)

**Goal:** The HiTOP-SR Module Builder tells users of a shuffled Word form to score printed-order columns with `read_module()` and `layout = "printed"`. It no longer says that they must reorder the columns by hand.

**Outcome:** In hitop-builder, the `#shuffleNote` first paragraph gives two routes. Printed-order columns take `read_module()` and `score_hitopsr(..., layout = "printed")`. Columns in HiTOP-SR order take the default layout. The wrong layout "raises no error". `bundleReadme(format, stem, version, shuffle = false)` adds one paragraph to a shuffled Word bundle's README.txt. The call in `download()` passes the existing `shuffle` constant, and every other bundle README is byte-identical to before. `tests/prose.mjs` lists `readme:docx-shuffled`, and its writer ledger is unchanged at 18. README.md §The scoring file, §Shuffling the Word form and the page walkthrough name the argument. The help-page sentence now points at the `randomize` paragraph of `generate_docx_hitopsr()`. This repo: tracking only.

**Decisions:** none cross-cutting. The work log records four plan-gate choices. They are a sentence in the shuffled-bundle README, three served bundles as the scoring proof, the download-button race kept separate, and a two-route notice. The proof reads the printed order from the `.docx` (the M046 oracle lesson). AC4 was amended at implement because it counted four `crosswalkSentence()` branches where there are three. Two re-audits ran, and Jeff chose the final wording. The claim audit read 16 claims and corrected 2.

**Review:** three-lens fan-out. AC1 to AC5 by fresh runs: the served notice on four builds, the eight-build README diff, and the scoring run on three bundles (all.equal TRUE, default-layout control differs). AC6 by ste_lint 0 and smoke, local and PR #17 CI. Gate: `cairn_validate` green, `document()` no diff, `check_pkgdown()` clean, `check()` 0/0/0. Blame-history and prior-review lenses: no finding. Diff-bug lens, seven findings. Fixed now: F1, a README walkthrough line that still said "not scored as it stands". F2, "says the same" pointing at passages without the layout advice. F5, the notice's opening, which now reads as a condition. F3 was the process wait for CI. Rejected: F4 positions tip, F6 accurate reordering wording, F7 pre-existing README text. Both PR conversations empty. Nothing graduated or retired.
