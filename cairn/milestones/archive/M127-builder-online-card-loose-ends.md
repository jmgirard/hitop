# M127: The module builder removes the link-builder link on a format change, announces the saved file through the status line, and repairs A16, plant (o) and the README pointer

**Status:** done (2026-09-25, PR #138 https://github.com/jmgirard/hitop/pull/138; code in jmgirard/hitop-builder PR #23 https://github.com/jmgirard/hitop-builder/pull/23, CI green on 6867beb, Jeff merges it from his terminal)

**Goal:** Close the six loose ends M126's review deferred on the builder's Online form card: two page fixes a visitor meets, three checker repairs and one README pointer.

**Outcome:** In hitop-builder `index.html`, `setFormat()` calls `removeLinkBuilder()` when the format changes, so the Word, Qualtrics and REDCap cards take the link out and hide `#onlineNext`, and an Online re-press keeps it. `removeLinkBuilder()` resets `#status` to "Ready." when it removed an anchor. The online branch of `download()` ends on "Ready. The scoring file is saved. The link to the link builder is under the button." inside the selection guard and on "Ready." outside it. Smoke test: `saveOnline()` awaits the download event, then any "Ready.", and returns the status; `readLinkState()` reads the anchor count, the hidden attribute, the status and the button state in one call; A16 re-aimed at a second online save pressed with a link present; A18 (card press removes, re-press keeps) and A19 (the status after the first save, the guarded save and the untick) added. Plants: (o) parses the JSON; (u) and (v) re-aimed; (x), (x2), (y), (z) added; matrix 27 of 27. README "The online form": the every-scale pointer to "Ticking every scale", the card press among the removal triggers, the status text. This repo: tracking only.

**Decisions:** none cross-cutting. Implement refined T1's reset condition to "an anchor was removed" alone, because the ticks made during a build find none.

**Review:** one pass. AC1 to AC6 by local smoke, the matrix (each of the eight milestone plants failing the assertions the plan named), a prose compare against a `--ref main` baseline naming the four predicted passages, a scratch probe of the boot, no-link tick and bundle statuses, and PR #23 CI; `check()` 0/0/0, PR #138 8/8. Claim audit before review: 45 claims read, 7 corrected. Three-lens fan-out: prior-review 0 (empty PR-comment probe), blame 2, diff-bug 11. Fixed before merge: the A17 exact-text wait (it made A19's mid-tick read unfalsifiable and plant (v) a 240 s timeout), download-event waits in the online saves, one anchor filter shared by both A16 reads, six comments and README sentences. Rejected: the deleted post-Word-build anchor read (the plan gate chose re-aiming A16, and A18 covers the press) and naming the online file in "Ticking every scale" (a code token there breaks AC5's pinned compare list). Noted: A18's before-press hidden reads show the selector matches. One lesson (exact-text hard waits before soft reads); the M029 line pruned for budget. Nothing retired.
