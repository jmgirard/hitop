# M067: The modules article and the generator help pages describe what the generators do

**Status:** done (2026-08-30, PR #73 https://github.com/jmgirard/hitop/pull/73)

**Goal:** The modules article and the three HiTOP-SR generator help pages state the module behavior the code has shipped since M048, M049 and M065, with the corrected sentences locked by a test.

**Outcome:** `vignettes/articles/modules-hitopsr.Rmd` gains three passages: the module Word header (`HiTOP-SR Module (v1.0)` against `HiTOP-SR (v1.0)`) with `title =` named as the override and stated to print verbatim on either form; the refusal of `include_subscales` together with `module`, which the sweep of the generators' five `module` conditionals found and the plan had not named; and a rewritten `randomize` paragraph confining the printed-number crosswalk to a renumbered module form, naming `renumber = FALSE` and the full instrument as the two cases without one. The reorder recipe `collected[order(item_order)]` now carries its precondition — the collected columns are in the order the form printed, first column the paper's item 1, and applying it to instrument-ordered columns scrambles them — at both article sites and in `@param randomize` of `generate_docx_hitopsr()`. The `@param descriptor` of all three `generate_*_hitopsr()` functions says the descriptor's path is announced on the console after its form's message. `tests/testthat/test-module-doc-prose.R` (6 blocks, 154 lines) locks all of it, slicing the article by heading and the Rd files by `\item{}` with both cut boundaries asserted found. No behavior changed. `module.R:154` and `:267` stay out of the article deliberately, logged with reasons. One NEWS bullet.

**Decisions:** none.

**Review:** All seven criteria verified fresh; gate clean (`cairn_validate` exit 0, `document()` no diff, `test()` 15642 passing, `check()` Status OK, `check_pkgdown()` clean, line-endings passed); 8 CI jobs green. Three lenses run inline, not in fresh-context subagents (session configured not to spawn agents); blame-history and prior-review found no conflict, the GitHub inline-comment probe returned 0 repo-wide. Three findings, none a criterion failure: the recipe block's bare `order(` anchor, which any `order(` in the section satisfied (fixed at the gate to `collected[order(`, shown red on a renamed recipe the old anchor passed); the article's `include_subscales = TRUE` wording against a guard that refuses any truthy value (logged — it matches the help page it describes); a stray source line break (logged). Defect returns: 0. Nothing retired; the M046 prose-guard lesson extended.
