# M120: hitop-form's saved-file screens carry a "Save the file" button that saves the same file again from the participant's own click

**Status:** done (2026-09-24, PR #131 https://github.com/jmgirard/hitop/pull/131; hitop-form PR #8 https://github.com/jmgirard/hitop-form/pull/8, open at archive time for Jeff to merge from his terminal, the deployed-page dispatch after it)

**Goal:** Both saved-file screens of hitop-form gain a "Save the file" button that saves the same CSV again from a click of the participant's own, so a download the browser blocked or the participant dismissed can be recovered before they leave.

**Outcome:** In hitop-form: `saveCsv()` returns the file's name and text; `showSaved()` takes both, appends "If the file did not appear, press Save the file." to the trail paragraph and renders a `button` "Save the file" between that paragraph and the completion link, its click calling `saveFile(name, text)`; `saveFile()` revokes its blob URL after 60 s rather than 10. The sent screen is unchanged. Tests: helpers `SAVE_AGAIN`, `expectSaveAgain()` (one button; a first and a second click each yield a download with the Finish download's suggested name and bytes, each wait 15 s), `savedScreenOrder()`/`screenOrder()` (the `main > *` tags and classes, the file-name paragraph marked `P>CODE.filename`); T19 in the five-outcome loop and the T16 order, T20 in the T5 loop, the supabase loop and T15's held-request snapshot, S21 on the HiTOP-BR case and the S16 order. README: the participant section and the save and send rows. In hitop: the online-collection article's saved-file sentences (the sheet walkthrough's step 2 and the Prolific route) and NEWS. Suite 197 tests.

**Decisions:** none. D-072(c) governs: outcome-screen copy is page copy outside IP1, recorded at the plan gate.

**Review:** three-lens fan-out, one pass, user-facing tier. Criteria audit at plan (full mode): 5 findings, 3 fixed, 2 to the gate. Claim audit at implement: 27 claims, 6 corrected (the send's wait "can outlast" the user activation; the suggested file name, not the name on disk; the button before the completion link; the five outcomes with a send address; the article's step named). The history and prior-review lenses found nothing, both PR comment probes empty. The diff-bug lens: 11 findings; Jeff chose the proposed triage. Fixed at the gate (hitop-form a341cc2): the 15 s save-again waits, the unasserted "a browser can block a download that late" sentence dropped from README, NEWS and the article, S21 reads `code.filename`, the order token marks the file-name paragraph, S16 and T16 assert the trail sentence, the sent-screen count trims and includes (planted red against T15), the 60 s blob URL, the article's order sentence. Noted: the hang walk's clicks after its timing assertion. Candidate row: no on-page confirmation after a press. Rejected: quoting the button's label in the sentence. Six plants red in all. CI 8 of 8 on #131. Nothing graduated or retired; the plain-English lint hook's hits on the tracking files were left as cairn's record grammar.
