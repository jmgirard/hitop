# M079: The online exports pad item numbers to the instrument's width, not to the export's own

**Status:** done (2026-09-01, PR #85 https://github.com/jmgirard/hitop/pull/85)

**Goal:** `build_qualtrics_txt()` and `build_redcap_zip()` zero-pad item numbers to the width of the instrument's largest item number, stated by the wrapper that calls them, rather than to the width of whichever items that one call exports.

**Outcome:** Both internal builders take a required `max_n` — the instrument's largest item number — and build names through `item_names()`, replacing the Qualtrics builder's inline `sprintf` width computation (the M077 review's finding 5). All ten `generate_{qualtrics,redcap}_*` wrappers pass their own item table's maximum (405, 45, 220, 100, 25); `rlang::check_required()` aborts a call omitting it, with no fallback to the exported items' own largest number. A HiTOP-SR module of low-numbered items would now be `hsr_007`, a name `label_hitopsr()` matches, where it would have been `hsr_07` — latent today, since the narrowest buildable module is still three digits wide. No shipped file moved: all eleven generator-backed manifest exports rebuild to identical item names. `tests/testthat/test-export-padding-width.R` is new — direct-call width and required-argument tests on both builders, a module smoke test through both HiTOP-SR generators, and the rebuild lock, which also asserts that `hitophsum_qualtrics.qsf` is the sole manifest export this package has no generator for. Padding comments in `R/util.R` and both builders corrected, and the two HiTOP-SR online generators' `module` help now states that a module export keeps the full instrument's width.

**Decisions:** D-054. Three plan-gate and three implement-gate alternatives are recorded in the milestone file's work log (deriving the width inside `apply_module()`, comparing whole files rather than item names, narrowing the candidate row instead of fixing; passing a digit count, R's own missing-argument error, covering only the nine numbered exports).

**Review:** Two rounds. Round 1 verified AC1/AC2/AC4 and returned an amendment on AC3, whose domain covered a manifest file this package ships but cannot build; the criterion was narrowed at a mini gate to the manifest's generator-backed rows. Round 2 verified all four fresh. Three-lens fan-out in round 1 (blame-history and prior-PR-comments: no findings); the [O] lens re-spawned in round 2 against the amended criteria, the two [S] lenses not re-spawned on a byte-identical code diff — a logged deviation. Fourteen findings, none reaching the return floor: five fixed on the branch at Jeff's direction — the rebuild test's search-path generator lookup replaced by `getNamespaceExports()`/`getExportedValue()` (the question AC3's domain is stated in), an explicit reader route in place of a bare `else`, five stale test comments in `test-generate_{qualtrics,redcap}.R`, an overreaching `R/util.R` comment, and a vacuous assertion. One recorded as D-054 (the shipped-file comparison is a no-regression lock, not an oracle), six deferred to a candidate row, one rejected. `cairn_validate` exit 0, `check()` 0/0/0, CI green on all eight checks.
