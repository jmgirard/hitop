# M122: hitop-form's "Save the file" button confirms each press in a status message a screen reader announces

**Status:** done (2026-09-24, PR #133 https://github.com/jmgirard/hitop/pull/133; hitop-form PR #10 https://github.com/jmgirard/hitop-form/pull/10, green and approved, open at archive time for Jeff to merge from his terminal)

**Goal:** A press of the "Save the file" button on either saved-file screen writes a short confirmation into a status region the screen renders empty, so a participant using a screen reader hears that the file was saved again while focus stays on the button.

**Outcome:** In hitop-form: `showSaved()` renders `p.saved-again[role="status"]` empty between the button and the completion link or version line, and the button's handler calls `saveFile(name, text)` then sets the region's text to "The file was saved again." on every press. Focus is not moved. `.saved-again:empty { margin: 0; }` keeps the empty region in the accessibility tree with no gap (never `display: none`, which the `[role="alert"]:empty` rule uses). Tests: helpers `SAVED_AGAIN`, `expectStatusEmpty()` (one region, the role, empty text), the `P.saved-again` token in `savedScreenOrder()`, and `expectSaveAgain()` rewritten (first press by focus and Enter with an `activeElement` assertion, second by click under a MutationObserver installed before it, the file name, bytes and sentence asserted after each). Called on the four saved screens (no store and unconfirmed send, each with and without a link) and the `completeSaved` screen. README: the participant paragraph and the save and send rows. In hitop: NEWS. Suite 201 tests.

**Decisions:** none. Plan gate: the sentence "The file was saved again." over a file-named or "offered" wording; focus left on the button over a move to the region; both falsifiers in the work log. Outcome-screen copy stays under M120's reading of D-072(c).

**Review:** three-lens fan-out, one pass, user-facing tier, return count 0. Criteria audit at plan: 5 findings on this file, 4 fixed, 1 settled by wording. Claim audit at implement: 34 claims, 6 corrected ("meant to announce" as a property of the role; focus qualified to the keyboard press). The history and prior-review lenses found nothing, both PR-comment probes empty. The diff-bug lens: 10 findings, none falsifying a criterion. Fixed at the gate (hitop-form 0fa05ac): the empty region's margin (probe: 0px before a press, 16px after), the form.js focus comment, the `completeSaved` screen's order and empty-region assertions. Rejected: re-announcement of identical text and the sentence's reach past the download outcome (both the plan gate's choices with falsifiers logged), the focus check by tag and text, the undisconnected observer, the D-072(c) reading's record, line wrapping. Two plants red at T2. hitop test FAIL 0 PASS 20099, check 0/0/0, CI 8 of 8 on #133. Nothing graduated or retired; the plain-English lint hook's hits on the tracking files were left as cairn's record grammar.
