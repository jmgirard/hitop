# M092: The HiTOP-SR Module Builder's prose reads as plain human English

**Status:** done (2026-09-11, PR #100 https://github.com/jmgirard/hitop/pull/100 tracking; code in jmgirard/hitop-builder PR #16 https://github.com/jmgirard/hitop-builder/pull/16)

**Goal:** Every sentence a user reads on the builder page, in the bundle's README.txt, and in the builder repo's README meets the plain-English house rules and reads as written by a person.

**Outcome:** In hitop-builder, `index.html`'s static text, every runtime message (`status()`, `log()`, `abandonBoot()`, the tally, recap, settings summaries, *Select all* label, crosswalk sentence, `FORMATS[].label/.button`, the `nomodule` message) and the `bundleReadme()` template, and `README.md`, are rewritten under the rules: sentences of at most 25 words, active voice, simple tenses, no semicolons, em dashes, bold lead-ins or decorative triplets. The three `<strong>` notice lead-ins are gone; no element id, `data-*` attribute, pinned `Ready.` string or plant anchor changed. New `tests/prose.mjs` lists every user-visible string in four parts (body text and `placeholder`/`aria-label` attributes, script strings, the three bundle READMEs by running the page's own code, README.md sections), keeps a ledger of the 18 writer sites and refuses to run when a grep for `textContent`/`innerHTML`/`setAttribute` disagrees, and with `--ref`, `--text`, `--json` and `--compare` extracts from a git ref, writes linter input, and compares per-passage fact multisets (URLs, code tokens, ids, `data-*` values, numbers). Linter baseline 49 (page) and 122 (README) went to 0. Claim audit corrected three stale statements (Word radios are markup, the status line names the format only while idle, Pages copies three files). This repo: tracking only.

**Decisions:** none cross-cutting. Implement-gate choices in the work log: the linter runs from the plugin path, never copied in; echoed R calls in log lines stay verbatim as one code token; README.md passages are sections, not paragraphs. AC3 was amended twice at implement (byte-identity narrowed to entry names plus bytes with docx timestamps, zip mtimes and shuffle order set aside) and once at review (the reverse-key marker `(R)` added to the set-aside run class), Jeff choosing each.

**Review:** three-lens fan-out. AC1 by `prose.mjs` plus `ste_lint.py --type descriptive` at 0 on both surfaces; AC2 by `--compare` against the merge base, 151 of 153 passages identical and the two differences logged; AC3 by smoke 1/1 and probes 2/2 locally, PR #16 CI green three times, sorted id/`data-*`/`Ready.` sets identical, and a same-day eight-bundle capture on the merge base and the branch matching under the amended rules. Gate: validate green, document() no diff, pkgdown clean, check 0/0/0. Blame-history and prior-review lenses: zero findings. Diff-bug lens ten: F1 fixed now (`--compare` keyed passages by group alone, so 39 runtime passages went unchecked; now paired by group and ordinal, proven by a tamper probe); F4–F9 six prose slips fixed now; F10 fixed in the evidence line; F2/F3 (writer grep too narrow, `prose.mjs` not in npm scripts or CI) → candidate row. Nothing graduated or retired.
