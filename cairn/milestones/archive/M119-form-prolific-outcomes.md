# M119: The Prolific route of hitop-form gives each outcome its own end: a second completion address for a saved file, a sent screen before the redirect, and a doubled parameter read as its filled value

**Status:** done (2026-09-24, PR #130 https://github.com/jmgirard/hitop/pull/130; hitop-form PR #7 https://github.com/jmgirard/hitop-form/pull/7, open at archive time for Jeff to merge from his terminal, the deployed-page dispatch after it)

**Goal:** Three follow-ons close the Prolific route M118 opened: a link's `completeSaved` field names the address the saved-file screens link to, a confirmed send draws the sent screen before it goes to `complete`, and a Prolific parameter present twice in the address reads as its first filled value.

**Outcome:** In hitop-form: `parseLink()` reads `completeSaved` through `checkCompleteUrl()` under its own `bad` message, refused with no `complete` beside it. `readProlific()` reads `params.getAll(name)` and takes the first value that is neither blank nor a `{{%…%}}` placeholder. `finish()` draws the sent screen, with "Continue to <host>." linking to `complete` in place of "You can close this page.", before `location.assign(complete)`. `showSaved()` links `completeSaved ?? complete`. `link.html` gains the "Completion URL after a saved file" field, refused as the page refuses it and beside an empty completion field, and the Prolific box's hint names Prolific's "I'll use URL parameters" option. Tests G12, S18 to S20, T15 and N7 rewritten to read the document at a held navigation request through a MutationObserver, T17, T18, L14. In hitop: the article's Prolific route, `prolific2026help.md`'s fifth page (article 445170) with two quoted sentences, NEWS.

**Decisions:** none. D-072 governs, recorded at the plan gate. Its context paraphrases article 445170's "clear description" clause as "clear label"; the source note holds the verbatim text.

**Review:** three-lens fan-out, one pass. The history and prior-review lenses found nothing, PR-comment probes empty. The diff-bug lens: twelve findings. Fixed at the gate: T15 and N7 counted `nav button`, a selector matching nothing (the page's buttons are `.nav button`), now `.nav button` with the buttons asserted seen on the form, planted red; T1 asserts "You can close this page." without `complete`. Jeff accepted AC5's split of its two sentences across the new field's hint and the Prolific box's hint. Rejected: the call order inside `finish()` (unobservable), a hand-typed first value winning (the plan-gate choice), the observer channel race (a false red only), the unproven `completeSaved` route (proven on `complete`), a second GET on a click of the sent screen's link, the builder's lone-field refusal without the value. Out of scope: Back after the redirect, no scroll to the top. CI 8 of 8 on #130. Lesson M078 pruned for the M119 line; nothing graduated.
