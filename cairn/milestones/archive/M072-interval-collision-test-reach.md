# M072: The HiTOP-BR interval tests and the collision-ordering sweep fail on the defects they claim to catch

**Status:** done (2026-08-30, PR #78 https://github.com/jmgirard/hitop/pull/78)

**Goal:** Close the four test-reach gaps M068's and M069's reviews left open, so each guard goes red on the defect its comment says it catches.

**Outcome:** No package behavior changed. `test-interval_hitopbr.R` gained a two-column block converting `hbr_antagonism` and `hbr_internalizing` in one call, expectations from the `br_reference` Table 1 constants rather than `hitopbr_devstats`, which alone reports 4 failures under `refstats[hit[[1]], ]`; and `pairing_residue(devstats, scales)`, returning the printed scale names whose `camelCase` is not the stem `hitopbr_scales` pairs with them, with an exchanged-stem probe asserting both names come back where `setequal()` over the same columns still passes. `test-append-collision.R` gained one warnings-before-abort block per scoring export (`score_pid5`, `score_hitopsr`, `score_hitopbr`) at `calc_se = TRUE`, each with a non-colliding control raising `hitop_deprecated_calc_se`, plus a block deriving the covered domain from the `calc_se` formal over `append_exports()` and asserting the table equals it. `data-raw/characterize_calc_se.R`'s `capture_call()` now returns the call's value and muffles the deprecation instead of recording a condition set, the channel having captured nothing on any of the matrix's 48 cells. `cairn/ORACLES.md`'s O-005 and O-006 anchors were corrected to `:170` and `:29` and O-005's provenance extended to name the multi-column companion at `:526`.

**Decisions:** None cross-cutting; no `DECISIONS.md` entry. Two plan-gate choices, both with recorded falsifiers: hand-writing the three `score_*` call sets rather than sweeping them (falsified by an eighth appending export with a warning output path — the domain block now derived from the `calc_se` formal closes that), and deleting the condition channel rather than making it discriminating (falsified by a `calc_se` change whose only visible effect is a condition the package stops signalling).

**Review:** Three fresh-context reviewers on distinct evidence bases ([O] diff-bug, [S] blame-history, [S] prior-PR-comments) returned eleven findings; none met the return floor. Four fixed on the branch: stale `ORACLES.md` anchors with the new block unrecorded; the `calc_se` sweep able to generate zero blocks and stay green; the AC3 controls changing `append = FALSE` alongside the collision rather than being the same call; and the AC1 block's comment claiming a mechanism `score_cols <- data[scores]` rules out. Six rejected with reasons. One routed to `DESIGN.md` Known issues 9: deleting the condition channel gives up catching a warning the package silently stops signalling, accepted at the plan gate. `devtools::test()` FAIL 0 / PASS 16191, `devtools::check()` 0/0/0, `cairn_validate` exit 0, all eight CI checks green. Nothing graduated or retired at hygiene.
