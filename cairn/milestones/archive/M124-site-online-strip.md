# M124: The instrument download pages keep three cards per row and gain an online-form strip, and the site lists the steps from instrument to scores

**Status:** done (2026-09-25, PR #136 https://github.com/jmgirard/hitop/pull/136)

**Goal:** Return each download page's card row to three cards, give the online route a strip under the row that holds the study-link button and the JSON export, reach the link builder from the Instruments menu, and write the four steps from instrument to scores on the overview page.

**Outcome:** `online_strip(instrument, stem, json_link)` in `vignettes/articles/_download-helpers.R` renders a `<section class="hitop-online" aria-labelledby>` under the card row: an `h4.h5` title, a four-sentence paragraph, a `btn-primary` link to `https://jmgirard.github.io/hitop-form/link.html?c=<p>` where `<p>` is `.base64url()` (over `jsonlite::base64_enc()`, newlines stripped, `+/` to `-_`, padding dropped, equal to hitop-form's `encodeConfig()`) of `{"instrument":"<stem>"}`, the JSON export through `.button_renderer()` (hoisted out of `download_cards()`, badge and `download` attribute per D-033), and a quiet link to the online-collection article; it stops when `stem` and the JSON link disagree. The five form-backed pages drop the JSON card and call the strip in their `downloads` chunk with a literal `dl_link()`; the HSUM page is unchanged. `.hitop-online` CSS in `pkgdown/extra.css`. `_pkgdown.yml`: "Make a study link" after "Build a HiTOP-SR Module". `overview.Rmd`: an `<ol class="hitop-steps">` of four steps under a "From instrument to scores" heading. `online-collection.Rmd`: one sentence on a prefilled link builder. `tests/testthat/test-download-pages.R` (six tests, 92 expectations): each page's `downloads` chunk purled and evaluated, three cards, one strip or none, `c` decoded to the exact JSON text, manifest anchors by set equality, the menu read with `yaml::read_yaml()` ({yaml} in Suggests, D-074), the overview steps with every site-relative href resolved and each `#fragment` matched to a pandoc heading id; the download-attribute test in `test-artifacts.R` renders the strip. NEWS entry.

**Decisions:** D-074 ({yaml} in Suggests for the navbar test; its Consequences corrected at review to name the covr job as the CI enforcer, since the source-checkout-only tests skip under R CMD check). Plan gate: the strip over a quiet JSON line and over four narrower cards; the overview step list plus the menu entry. Question gate: title, one sentence and three links; the step list below the cards.

**Review:** three-lens fan-out, user-facing tier, defect returns 0. Criteria audit at plan: repairs written into the file. Claim audit at implement: 50 claims, 2 corrected. Prior-review and blame lenses 0 findings; diff-bug lens 15, ten fixed at the gate (D-074 wording, the strip's paragraph, landmark, heading level, hidden and literal emoji, the stem guard with a test, the fragment-resolving overview test, a redundant skip, a re-wrap) and five rejected (an external-link cue, the tutorial sentence's content, step 3's link target, the `hitop-steps` hook class, a reused regex). Full suite FAIL 0, check 0/0/0, pkgdown clean, CI 8 of 8 on #136; the merge landed in a second review pass because the desktop app's PR rule bars a CI watch from the session. Nothing graduated or retired; the plain-English lint hook's hits on the tracking files were left as cairn's record grammar.
