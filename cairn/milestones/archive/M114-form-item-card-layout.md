# M114: hitop-form draws a wrapped item's text inside its card

**Status:** done (2026-09-23, PR #124 https://github.com/jmgirard/hitop/pull/124; code in hitop-form PR #4 https://github.com/jmgirard/hitop-form/pull/4, merged as 5702a3a).

**Goal:** Change the item card's layout in `jmgirard/hitop-form` so that an item's text renders as a block inside the card, below its top border and within its width, with the fieldset and legend kept for assistive technology.

**Outcome:** In hitop-form, `fieldset.item legend` floats left at `width: 100%` with `padding: 0` and a bottom margin, and `fieldset.item .options` clears it, so the legend leaves the border notch and renders as a block under an unbroken top border while staying the fieldset's accessible name. `itemNode()` puts a space text node between the number span and the text span, so the group's name reads "1. I felt…". `tests/layout.spec.js`: Y1 walks every page of the HiTOP-SR and the PID-5 at 320 px, 375 px and the default width, measuring each legend against its card's padding box on four sides at 0.5 px, its `scrollWidth` against `clientWidth`, the options' top against the legend's bottom, the page's `scrollWidth` against the viewport, and at least one wrapped legend per walk; Y2 finds every first-page item of the five forms by `getByRole('group', { name, exact: true })` with the export item its `data-number` names; Y3 refuses Next with a blank item and reads four error-coloured borders plus the legend placement, with an answered item as control. README test-table row. Suite 111 → 123, no prior test edited. hitop changed `cairn/` only. Plants: the old CSS reds 7 tests on "legend top", the missing space reds all 5 name tests.

**Decisions:** none cross-cutting. Plan gate: keep the bordered card with the text inside over dropping the border. Implement: `float: left; width: 100%` over a `div[role=group]` rewrite, keeping the fieldset semantics and selectors.

**Review:** one pass, three lenses, user-facing tier. [S] blame-history: none (the notch layout traces to the first commit with no stated reason). [S] prior-review: none (seven archives read, the PR-comment probe empty). [O] diff-bug: 13, none a broken page. Fixed at the gate (hitop-form 1fa2282): F2 Y2 paired cards with export items by index, F3 no check that the options sit below the legend, F6 comment wording, F8 no check against sideways scroll. F1 browsers: Jeff chose a cross-browser run; WebKit 2215 passed 12/12, Firefox 1495 never launched on macOS 27 (compositor crash at start), so Chromium and WebKit are the browsers measured. Noted: F5 (the number-to-text gap grew by one space width), F10 (light scheme only in Y3), F11 (whole-pixel overflow check). Rejected: F4, F7, F9, F12, F13. Gate: `cairn_validate` exit 0, `document()` no diff, `check_pkgdown()` clean, `check()` 0/0/0, hitop CI 8/8, hitop-form CI green, the dispatched deployed-page run 123/123. The Playwright installer's extraction hung under the command sandbox; both browser archives were unzipped into the cache by hand (LESSONS, M095 line). Hygiene: the browser-pane lesson (M056) trimmed to fit the LESSONS byte budget; nothing graduated.
