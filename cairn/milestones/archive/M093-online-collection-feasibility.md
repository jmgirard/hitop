# M093: A feasibility evaluation of an online response-collection form as an alternative to Qualtrics and REDCap

**Status:** done (2026-09-20, PR #101 https://github.com/jmgirard/hitop/pull/101)

**Goal:** Write down what it would take, and cost, to offer an online form that collects responses to the HiTOP instruments in place of Qualtrics or REDCap, and record Jeff's decision on whether and how to build one.

**Outcome:** One synthesis note, `cairn/references/online-collection.md` (INDEX line added): eight requirements derived from the package's surfaces (descriptor format and `itemOrder`, `item_names()`, the four `*_instructions` objects, the HSUM's 638 gated fields, IP1 and D-016 for a served form, GP4); four architectures (A device-only page, B static page posting to researcher-owned Supabase/Firestore/Apps Script storage, C hosted service, D self-hosted kit) compared on seven dimensions under a solo maintainer and an institutional operator; a compliance section (controller/processor, BAA, GDPR, the IRB story, consent and identifiers); a build breakdown of A 6, B 11, C 14, D 12 sessions; a twelve-cell hosting-cost table; a Sources table of 20 dated vendor and regulator pages. Finding the plan did not anticipate: a static page cannot hold a REDCap API token, so B has no REDCap route. Recommendation: B, built renderer-first. Jeff's disposition: a ROADMAP candidate row to build B in two steps. Also shipped: the macOS check pinned to R 4.5 in `R-CMD-check.yaml` because CRAN's R 4.6 macOS binaries are zstd archives pak 0.11.1 cannot extract; a candidate row tracks the revert.

**Decisions:** none cross-cutting. Gate choices in the work log: B's candidate row over a revisit row or a rejection; F13 rejected (AC3's "in the package or the builder page" read as examples of a home); parking on the red macOS check superseded by the CI workaround on the branch.

**Review:** first pass, internal tier, one [O] lens on the docs-only diff: 16 findings, 13 fixed now (the wrong `score_hitopsr()` call, B's REDCap store listed after the note ruled it out, 24 not 20 artifact files, the Supabase add-on price, a DESIGN citation, three unused Sources rows, INDEX order and the milestone number in titles, a status assertion in the note, 2 KB versus a 10 KB Firestore document, C's hand-off home, a daily-quota comparison), F13/F14/F15 rejected with reasons. Second pass after the CI pin, three lenses: [O] four findings, three fixed in the workflow comment and one carried by the revert candidate row; blame-history and prior-review lenses zero. Gate: validate green, document() no diff, pkgdown clean, check 0/0/0; CI 8/8 green after the pin. Post-merge: ROADMAP's superseded confidence-interval row absorbed into the norms-schema row and M090's row pruned early to keep the caps. Nothing graduated or retired.
