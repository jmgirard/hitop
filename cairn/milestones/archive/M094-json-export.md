# M094: The package ships a JSON export of the HiTOP-SR and HiTOP-BR items, response options and instructions as a checksum-locked artifact

**Status:** done (2026-09-20, PR #102 https://github.com/jmgirard/hitop/pull/102)

**Goal:** Ship one JSON file per HiTOP form holding its items, response options and administration instructions as the package's tables hold them, locked to the tables by test and to the manifest by checksum, so a page outside the package can render the instrument.

**Outcome:** `data-raw/json_export.R` (sourced by `data-raw/artifacts.R`, or run as a script) writes `inst/extdata/hitopsr.json` (51,477 bytes) and `inst/extdata/hitopbr.json` (6,255 bytes) through a binary connection. Top level: `format` `"1.0"`, `package`, `packageVersion`, `buildDate`, `stem`, `maxItem`, `instructions` (`start`, and `options` as `{value, label}`) and `items` as `{number, name, text}` in table order, built from `hitopsr_items`/`hitopbr_items`, the `*_instructions` objects and `item_names()`. Item names carry the stem (`hitopsr_001`), not the scoring prefix (`hsr_001`). Keying is not exported (D-039 rebuilds it). `hitop_artifacts` gains two `json` rows (41 rows), and byte-identical copies under `pkgdown/assets/downloads/` serve at `downloads/<stem>.json` (D-033). `tests/testthat/test-json-export.R` compares each file field by field against the tables and `hitop:::*_instructions`, with six plants red by name; `test-artifacts.R` admits `json`. Also: a one-sentence JSON card on both download pages, a `json` label in `_download-helpers.R`, `"json"` in `hitop_artifacts`' roxygen, DESIGN.md's generator list and artifact-versioning paragraph, and a NEWS entry.

**Decisions:** D-063 declares format 1.0's fields a public contract. Plan-gate choices in the work log: HiTOP-SR and HiTOP-BR only (the PID-5 and HSUM exports went to the online-form candidate row); the tables without keying over a self-contained descriptor.

**Review:** two passes, three lenses each, user-facing tier. Pass 1: 18 [O] findings and an amendment return on AC5 (the card wording as planned demanded a claim about a page that does not exist until M095), re-audited twice and reworded at the mini gate; seven fix-now items taken with the amendment as T5 (script-mode load order and header claim, manifest note, roxygen format list, staged-file count, DESIGN wording, NEWS address). Pass 2: eight new [O] findings, three fixed at the gate (card wording, DESIGN sentence order, the writer's `file` argument renamed `path`), G4/G5/G7 rejected with reasons; blame and prior-review lenses zero on both passes. Follow-ups: F2 (stem-named items versus the `hsr_` prefix) and F17 (no field telling the export from the module descriptor) to the online-form row for M095/M096; F3 and F6 (a version bump reds the suite until `artifacts.R` reruns; `Sys.Date()` churn) to the release row; F7, F8, F9, F10, F16, G6 (test reach: an unreachable type note, a shared tempfile, `auto_unbox` plants, a duplicated helper, no direct writer test, unnamed extra keys ship green) dispositioned at hygiene; F14 (four cards wrap) rejected as cosmetic. Gate: validate green, document() no diff, pkgdown clean, check 0/0/0; CI 8/8 green. Post-merge: M091's row pruned; one LESSONS line added, one pruned. Nothing graduated or retired.
