# M116: A study link can ask hitop-form to show each participant the items in a fresh random order

- **Status:** review
- **Priority:** normal
- **Depends on:** M114, M115
- **Driving RR:** —
- **Principles touched:** IP1, GP3
- **Resolves:** —
- **Surface tier:** user-facing — the participant-facing form page and its link builder
- **Branch/PR:** `m116-form-random-order` (hitop); `m116-form-random-order` in jmgirard/hitop-form

## Goal

A link field `shuffle: true` makes `jmgirard/hitop-form` draw a new random order of the planned items on each load, while the row and the file keep the item columns in the instrument's order under the official item names and record the shown order in an `item_order` column.

## Scope

**In:** `parseLink()`, `planItems()` and the render in `form.js`; `buildCsv()`, `buildRow()` and `storeSql()`; a checkbox in `link.html`; the README; Playwright tests and two SQL fixtures. In hitop: one captured fixture with a test, on this milestone's branch.

**Out:** one fixed order per link for a whole instrument → not planned (a module's `itemOrder` is that route). Random order of the response options → not planned. A start-screen sentence about the order → none, so no participant-facing text changes. A "Save the file" button → its candidate row.

## Acceptance criteria

- [x] AC1: A link with `shuffle: true` renders the planned items (the export's items, or the module's `items`) in an order drawn on each load by a Fisher–Yates shuffle over `crypto.getRandomValues`, so that the shown order is a rearrangement of them, and the position printed beside each item is its place in the shown order, 1 to n. Two loads of a HiTOP-BR link render different orders, and two loads of a link carrying `module-shuffled.json` render different orders. A link with no `shuffle`, or with `shuffle: false`, renders as today: export order, or a module's `itemOrder` when present and otherwise its `items`. A `shuffle` that is not `true` or `false`, `null` included, is refused with a message naming the field and the value. Tests: the two double loads, an unshuffled module, and refusals for `"true"`, `1` and `null`.
- [x] AC2: Under `shuffle`, the saved CSV and the posted row carry the five lead columns, then `item_order` holding the shown order as item numbers joined by single spaces, then one column per planned item in the export's order (a module's `items` order), named by the official item name and holding the answer chosen for that item. Without `shuffle`, the file and the row have no `item_order` column and are unchanged. Tests: a HiTOP-BR shuffle walk that saves a file, one that posts to the webhook route, one that inserts through the Supabase route, and a shuffled module walk that saves, each checked by item number against the answers the walk chose at each shown position, and an unshuffled walk against the existing fixture.
- [x] AC3: The SQL the link builder shows for a Supabase store under `shuffle` has a text column `item_order` after `submitted` and the item columns in the export's order (a module's `items` order). Tests: the SQL compared to two hand-written fixtures, a HiTOP-BR and the shuffled module, and a shuffled Supabase walk whose posted keys equal the SQL's column names in order.
- [x] AC4: `link.html` has a checkbox "Show the items in a random order" that sets `shuffle: true`. Its hint says that each participant sees a new order, that the file and the table list the items in the instrument's order under their item names, that `item_order` records the order seen, and that a module's printed order is not followed. A link built with the box checked opens a page that renders a rearrangement, by AC1's check.
- [x] AC5: Every sentence that a grep for `order` finds in hitop-form's README, `link.html` and `form.js` comments and in the package's `?read_form_responses` and online-collection article, and that says the columns follow the order shown, is still true under `shuffle` or is rewritten to say what holds. The README says to start a new sheet for a shuffled link, because the Apps Script builds the header from the first row. The saved file of a shuffled HiTOP-BR walk is copied into hitop's `tests/testthat/fixtures/` as LF with a provenance row, and a test reads it, checks `item_order` against the file's text, and scores it with `score_hitopbr()` against means recomputed per row from the file's text with `hitopbr_items$Reverse` and `hitopbr_scales$itemNumbers`.

## Coverage

- AC1 → T1
- AC2 → T2
- AC3 → T3
- AC4 → T3
- AC5 → T4

## Tasks

- [x] T1: `parseLink()` (`form.js:60`) reads `shuffle`; `planItems()` (`form.js:344`) returns the canonical list and a shown permutation; the render and the shuffle; the render tests of AC1.
- [x] T2: `buildCsv()` and `buildRow()` (`form.js:360`, `form.js:389`) write `item_order` and the canonical columns; the save and send tests of AC2.
- [x] T3: The `link.html` checkbox and hint, `storeSql()` (`form.js:304`), the two SQL fixtures, the link and SQL tests of AC3 and AC4.
- [x] T4: The grep sweep of AC5 across both repos, the README sections, the fixtures README, the captured fixture and its test in hitop on this milestone's branch.
- [ ] T5: At `/milestone-review`'s merge step, after the user's approval: the hitop-form PR, its CI and the dispatched deployed-page run after the merge; the hitop PR with the fixture, the test and tracking. Both branches are ready on disk; nothing is pushed before that gate.

## Work log

- 2026-09-23: created by /milestone-plan, with M114 and M115. Depends on M115 for the reader and on M114 because both edit `itemNode()`.
- 2026-09-23: criteria audit ran in full mode on a fresh [O] reader (shared with M114 and M115): eleven findings on this file. Ten fixed in the wording: the "two loads differ" claim is asserted on the 45-item and 21-item cases only, the module case must differ between loads, the refusal is probed with a string, a number and `null`, both store routes get a walk, the Supabase test compares keys to SQL columns, two SQL fixtures, the prose claim rests on a grep, the README's new-sheet note, M115 as a dependency, and the package fixture as a task on hitop's own branch. One carried to the gate: the IP1 sign-off. The reader's objection to recomputing means from the package tables was not taken, on M113's AC2 precedent.
- 2026-09-24: from M115's review. `vignettes/pid5_scoring.Rmd` line 202 and `vignettes/articles/modules-hitopsr.Rmd` line 420 say a file has five lead columns. When the page writes `item_order`, those two sentences need the sixth.
- 2026-09-23: plan gate chose a fresh order per participant over one fixed order per link (or both), because counterbalancing order effects is what random order is for and a module's `itemOrder` already gives a fixed order; falsified by a researcher needing one fixed random order for a whole instrument.
- 2026-09-23: plan chose ignoring a module's `itemOrder` under `shuffle` over refusing the combination, because every builder-saved descriptor of a shuffled Word form carries one; falsified by a researcher surprised that the printed order was not followed.
- 2026-09-23: plan dropped the drafted start-screen sentence about random order, so no participant-facing text changes and IP1's text clause is not touched.
- 2026-09-23: IP1 sign-off: Jeff signed off at the plan gate on shuffling the display order, with the stored columns labelled by the official item number and the participant's display number saved in `item_order` (D-070). The escalation to a review brief offered at the gate was not taken.
- 2026-09-24: implement started. Branches `m116-form-random-order` in both repos. Question gate skipped: the field name, the column shape, the module rule and the fixture route are fixed by the criteria and D-070.
- 2026-09-24: T1 done in hitop-form. `parseLink()` refuses a `shuffle` outside the two booleans by name. `planItems(exp, module, shuffle)` returns `{ items, shown }`; `shuffleItems()` is a Fisher–Yates over `crypto.getRandomValues` with rejection above the largest multiple of the range. Four render tests (R7 to R9) and three refusal tests (G9). Plants: a no-op shuffle reds R7 and R8; an accept-anything field reds the three G9 tests. Suite 123 to 130, all green.
- 2026-09-24: T2 done in hitop-form. The record carries `items: plan.items` and, under shuffle, `itemOrder` from `plan.shown`; `buildCsv()` and `buildRow()` write `item_order` sixth when the record has one. `expectShuffled()` in helpers.mjs checks a header and a row by item number against the pattern at the shown position. Tests: S8 (HiTOP-BR and module saves), S9 (the committed capture `responses-hitopbr-shuffled.csv` agrees with its own `item_order`), T12 (webhook and Supabase posts). Plants: columns in shown order reds S8 and T12 (4 tests); a dropped `item_order` key reds both T12 tests. Suite 135 green.
- 2026-09-24: T3 done in hitop-form. `storeSql(table, items, shuffle)` adds `"item_order" text` after `submitted`; `link.html` has the checkbox (`name="shuffle"`), its hint, and passes the flag to `planItems()` and `storeSql()`. Fixtures `supabase-hitopbr-shuffle.sql` and `supabase-module-shuffle.sql` written by rule, rows in the fixtures README. Tests: L7 gains the two shuffle fixtures, L9 the box, hint, both links and a rearranged render; T12's Supabase case compares the posted keys to the SQL fixture's 51 column lines. The first L7 run went red on the module case because the builder called `planItems()` without the flag, so its columns followed `itemOrder`; fixed. The hint said "module descriptor's", which made L3's `hasText` locator match two hints; reworded to "A module's printed order". Checkbox layout checked in a Playwright screenshot. Suite 138 green.
- 2026-09-24: T4 done. Grep for `order` read in hitop-form's README, `link.html` and `form.js` comments and in `?read_form_responses`, the online-collection article, the modules article and `vignette("pid5_scoring")`. Rewritten where the claim fails under shuffle: the README's step 4, file section, sheet keys, SQL sentence and table-of-its-own sentence; a new step 5 for the box; the new-sheet paragraph in the sheet section; the help page's module sentence and `item_order` paragraph; the sheet-download sentence and the `item_order` sentence in the online-collection article; the modules article's descriptor sentence, five-lead-columns sentence and printed-layout sentence; the PID-5 vignette's five-lead-columns sentence. NEWS entry. `responses-hitopbr-shuffled.csv` copied into hitop as LF (hitop-form d2501bf) with a fixtures README row; one test reads it, checks `item_order` against the file's text, each item value against the pattern at its shown position, and scores it through `expect_hitopbr_export()`. Plant: two swapped item cells red the test. `document()` regenerated the Rd; `devtools::test()` 0 failures, 19898 passes.
- 2026-09-24: minor amendment: T5 reworded to say review's merge step opens the PRs, waits on CI and dispatches the deployed-page run, after the user's approval (D-138). It stays unchecked at the implement close; both branches are ready on disk.
- 2026-09-23: the work-log lines above dated 2026-09-24 were written this session, on 2026-09-23 local time (2026-09-24 UTC); the local date is used from here on.
- 2026-09-23: claim audit: 74 claims read, 4 corrected — hitop-form README.md (a table made with the box accepts a row without `item_order`, so only the reverse needs a new table; the module gloss), form.js (the rejection bound is "at or above"), tests/fixtures/README.md (dates); hitop R/read_form_responses.R and man/, vignettes/articles/modules-hitopsr.Rmd (the module gloss: the descriptor's `items` order, which `write_module()` writes ascending, not "ascending" as such), tests/testthat/fixtures/README.md (date).
- 2026-09-23: the reader re-read the four corrected passages once: all hold. Final heads: hitop-form 7546e56 (suite 138 green), hitop 04e1df48 (`devtools::test()` 0 failures). Status set to review; T5 runs at review's merge step.
- 2026-09-23: review ran. Five criteria ticked against fresh evidence, gate clean, three lenses read. Of the diff-bug lens's 11 findings, Jeff chose at the gate: F2, F7 and F9 fixed now (hitop-form a5056a0 and the online-collection article), F1 a candidate row, the rest rejected with reasons in the Review section.
- 2026-09-23: step-7 approval: m116-form-random-order approved for merge, in hitop-form and then in hitop. T5 runs from here: push, PRs, CI, merges, the deployed-page dispatch.

## Decisions

## Review

- 2026-09-23 sync: `origin/main` is unmoved under both branches (hitop head 07c4c731, hitop-form head 7546e56). No PR exists in either repo. `cairn_validate` exits 0 with 24 advisories (dangling early D-ids and one references staleness), none a gate item.
- AC1 evidence: the hitop-form suite ran this session, 138 passed in 55 s. R7 loads a HiTOP-BR link twice and checks each load is a rearrangement of the 45 export numbers, that neither equals the export order, and that the two differ. R8 does the same for `module-shuffled.json` with 21 items against its `items`. R9 checks `shuffle: false` renders the export order and a module's `itemOrder`. The no-`shuffle` module tests are unchanged and green. G9 refuses `"true"`, `1` and `null` with the message naming the field and the value, with no Begin button and no items. The draw is `shuffleItems()` at `form.js:369`, a Fisher–Yates over `crypto.getRandomValues` with rejection at or above the largest multiple of the range. L9 checks the printed positions equal 1 to 45.
- AC2 evidence: same run. S8 walks a HiTOP-BR and a shuffled module and checks the header is the five lead columns, `item_order`, then the planned items' official names in export or `items` order. It checks `item_order` equals the walked order joined by spaces and each item value equals the pattern at its shown position, through `expectShuffled()`. S9 checks the committed capture agrees with its own `item_order`. T12 runs the same checks on the posted row for a webhook and for a Supabase store. The five unshuffled fixture saves and the unshuffled sends are unchanged and green, so a file without `shuffle` has no `item_order` column.
- AC3 evidence: same run. L7 compares the shown SQL byte for byte to `supabase-hitopbr-shuffle.sql` (51 column lines, `"item_order" text` sixth after `"submitted"`) and to `supabase-module-shuffle.sql` (27 column lines, item columns in the descriptor's `items` order 11, 20, 64 onward, not its `itemOrder`). T12's Supabase case checks the posted keys equal the fixture's 51 column names in order.
- AC4 evidence: same run. L9 finds the checkbox by its label "Show the items in a random order", unchecked by default, and checks the four hint clauses: a new order per participant, the file and table in the instrument's order under item names, `item_order` records the order seen, a module's printed order not followed. A link built unchecked carries no `shuffle` field. One built checked decodes to `shuffle: true`, and opening it walks 45 items whose shown numbers are a rearrangement of 1 to 45 with positions 1 to 45.
- AC5 evidence: a grep for `order` was read this session. Hits in hitop-form: README lines 38 to 45, 82, 119 to 123, 134 to 136, 216 to 219, 264, 274, 287. `link.html` lines 85, 91, 131. `form.js` comment lines 91, 244, 341 to 346, 396, 422, 601, 760. Hits in hitop: `R/read_form_responses.R` lines 18 to 31, 41, 51, 61. `vignettes/articles/online-collection.Rmd` lines 82 to 85, 108 to 112, 116. Every sentence saying the columns follow the order shown is now qualified by the random-order case, or states the instrument order and `item_order`. The README's sheet section (lines 214 to 215) says the script builds the header from the first row. It says to start a new sheet for a shuffled link. The fixture `tests/testthat/fixtures/responses-hitopbr-shuffled.csv` holds 0 CR bytes and `data-raw/check_line_endings.R` passes. The fixtures README carries its provenance row (hitop-form commit d2501bf). `devtools::test()` ran this session: 0 failures, 0 warnings, 15 skips, 19898 passes. The new test reads the fixture and compares `item_order` to the file's own cell through `store_rows()`. It checks the cell is a rearrangement of 1 to 45 and each item value equals the pattern at its shown position. It scores through `expect_hitopbr_export()`, which calls `score_hitopbr()`. That helper compares to `table_means()` built from `hitopbr_items$Reverse` and `hitopbr_scales$itemNumbers`.
- Consistency gate: `cairn_validate` exit 0. `devtools::document()` leaves no diff. `README.md` and `README.Rmd` share one commit date (2026-08-01) and the branch touches neither. `pkgdown::check_pkgdown()` finds no problems. NEWS.md carries the entry, with no milestone number. No new top-level file. No DESIGN principle changed, so `cairn_impact` is skipped. `devtools::check()` ran this session in 4 m 43 s: 0 errors, 0 warnings, 0 notes.
- Independent review, three fresh lenses. [S] blame-history: no findings. It read the six form.js commits, the reader's history, D-036, D-064, D-069 and D-070, and the M111 to M115 archives. [S] prior-review: no findings and no prior-review evidence contradicted. It read the M096, M108 and M111 to M115 archived Review sections and LESSONS. Both repos' PR-comment probes returned empty, and the thread walk was skipped. M115's carried-forward five-lead-columns item is closed by this diff. [O] diff-bug: no acceptance criterion failing, no logic bug in the shuffle or the column contract, the unshuffled path unchanged. Its 11 findings, ranked as reported, with the gate's disposition:
  - F1 (defect outside the criteria): `checkModule()` does not require a module's `items` to be ascending, and the modules article says a shuffled module file "has its columns in instrument order". A hand-edited descriptor with non-ascending `items` scores wrong under the default layout with no error. `write_module()` and the Module Builder, which calls the package's generate functions, both write `items` ascending. Disposition: follow-up, a ROADMAP candidate row (Jeff, 2026-09-23).
  - F2 (test gap, AC2): no save or send walk runs under `shuffle: false`, and a regression writing `item_order` for that value passes. Disposition: fixed now. `save.spec.js` S10 walks the HiTOP-BR under `shuffle: false` against `responses-hitopbr.csv`. Plant: `config.shuffle !== undefined` in `runForm()` reds it with a header one column longer, then reverted. Save and link specs 35 green at hitop-form a5056a0.
  - F3 (test gap, AC1): no test checks the draw uses `crypto.getRandomValues` or Fisher-Yates. Disposition: rejected. AC1 names the tests it requires and they exist, and the method is read in the code.
  - F4 (test gap, AC1): positions are read from `data-position`, not the printed text. Both come from one variable. Disposition: rejected, one source for both.
  - F5 (test gap, AC2): S8 and T12 derive positions from the walk index. A mismatch with `data-position` still fails. Disposition: rejected, the reviewer's own account says a mismatch fails.
  - F6 (test weakness): the answer pattern repeats every 4 positions, so a column mix-up preserving position mod 4 passes. The committed plants did turn red. Disposition: rejected, the plants on record turned red.
  - F7 (doc): the module hint at `link.html:85` still says items show "in the descriptor's printed order when it records one", unqualified. Display order, so outside AC5's predicate. Disposition: fixed now, the hint ends "unless the random-order box below is checked", and L3 is green.
  - F8 (doc): AC3 calls the SQL fixtures "hand-written" and the fixtures README says a loop wrote the column names by rule. Disposition: rejected, the fixtures are independent of the page and the README records how they were made.
  - F9 (doc): the online-collection article states the new sheet as a requirement, the README as the case that needs one. Disposition: fixed now, the article says what the README says: an existing sheet appends the column, the reader reads it there, a new sheet puts it sixth.
  - F10 (question): `link.html:234` runs a shuffle whose result the builder discards. Disposition: rejected, harmless.
  - F11 (doc): NEWS has two adjacent entries about `item_order`, both accurate. Disposition: rejected, both accurate.
