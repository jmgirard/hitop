# M077: The shipped HiTOP datasets, `rename_hitopsr_items()` and the `label_*()` family name item columns as the REDCap export does

- **Status:** review
- **Priority:** normal
- **Depends on:** —
- **Driving RR:** —
- **Principles touched:** GP2, GP3
- **Branch/PR:** `m077-item-column-names` · https://github.com/jmgirard/hitop/pull/83

## Goal

One item-column naming pattern for the HiTOP instruments — the one `generate_redcap_*()` already writes — so data collected through the package's own exports, the shipped example datasets, and the rename and label helpers agree, and a reader keeps one selection idiom across every HiTOP-SR and HiTOP-BR example.

## Scope

User-facing tier: the deliverable renames columns in four exported datasets and changes what three exported functions build or match.

The pattern is the item variable `build_redcap_zip()` writes at `R/generate_redcap.R:288-292`: lowercase stem, underscore, item number zero-padded to the width of the instrument's largest item number — `hsr_001`..`hsr_405`, `hbr_01`..`hbr_45`.

**In:** rename the item columns of `ku_hitopsr` and `sim_hitopsr` to `hsr_001`.. and of `ku_hitopbr` and `sim_hitopbr` to `hbr_01`.., by a committed `data-raw/` script that loads, renames and re-saves the shipped objects (the sim scripts draw with no seed and `data-raw/ku_data.R` reads a network drive, so neither regenerates here), while `data-raw/sim_hitopsr.R:8`, `data-raw/sim_hitopbr.R:8` and `data-raw/ku_data.R:9,34,55,60,65` are edited to build the same names next time they run. One internal helper builds the pattern for `build_redcap_zip()`, `rename_hitopsr_items()` (`R/rename_hitopsr_items.R:56,99`), `label_hitopsr()` (`R/label_hitopsr.R:33`) and `label_hitopbr()` (`R/label_hitopbr.R:34`) — today those three paste an unpadded integer for every `prefix`, so this is a padding change for all callers plus a default `prefix` change to `"hsr_"` / `"hbr_"`, with no deprecation cycle (waived by Jeff at the 2026-09-01 plan gate, D-052). A consequence to declare: `label_hitopsr(x, target = "scales")` with no `prefix` now matches `score_hitopsr()`'s default output. The `@format` blocks in `R/data.R`, the two scoring vignettes, the three articles referencing the datasets, the roxygen examples and the tests move to the new names; `cairn/DESIGN.md:45` stops saying `HSR_*`; NEWS Breaking-changes entry.

**Out:** the PID-5 datasets (`pid_1` today, `pid5_001` in the exports) → the candidate row added 2026-09-01; a `label_*()` that also accepts unpadded or differently cased names → rejected at planning (work log), reopened by a user report; naming guidance in `vignettes/articles/import-instructions.Rmd` → not planned; the Qualtrics and REDCap generators' own variable names and the locked artifacts → untouched, they are the reference the datasets move to.

## Acceptance criteria

- [x] AC1. The item columns of `ku_hitopsr`, `sim_hitopsr`, `ku_hitopbr` and `sim_hitopbr` are named by the pattern stated independently of any generator: a test asserts `names(ku_hitopsr)[-(1:2)]` and `names(sim_hitopsr)` are identical to `sprintf("hsr_%03d", hitopsr_items$HSR)`, and `names(ku_hitopbr)[-(1:2)]` and `names(sim_hitopbr)` to `sprintf("hbr_%02d", hitopbr_items$HBR)`; the REDCap dictionary tests in `tests/testthat/test-generate_redcap.R` keep asserting the dictionary's item variables against the same two expressions.
- [x] AC2. Only the names changed, and each column kept its item: for each of the four datasets, with `old` loaded from `git show 7e06f40e:data/<name>.rda` and `new` the committed object, `identical(unname(as.list(new)), unname(as.list(old)))` is `TRUE`, and the trailing integer of every old item name (`sub("^\\D+", "", x)`) equals the trailing integer of the new name in the same position.
- [x] AC3. For any `prefix`, `rename_hitopsr_items()` (both methods), `label_hitopsr()` and `label_hitopbr()` build item names zero-padded to the instrument's width: tests assert items 1, 10 and the last item — `hsr_001`, `hsr_010`, `hsr_405`; `hbr_01`, `hbr_10`, `hbr_45` — from each rename method and from each label function with the default `prefix` against `ku_hitopsr` / `ku_hitopbr`; a test asserts `label_hitopsr(x, prefix = "HSR_")` labels a frame named `HSR_001`..`HSR_405`; and a test asserts `label_hitopsr(score_hitopsr(sim_hitopsr, items = 1:405, append = FALSE), target = "scales")` labels every scale column with no `prefix` given.
- [x] AC4. `grep -rnE` over `R/`, `vignettes/`, `tests/`, `data-raw/` and `man/` for `hsr[0-9]{3}`, `hsr%0?3?d`, `hsr_[0-9]{1,2}\b`, `HSR_[0-9]{1,2}\b`, `hbr[0-9]{2}`, `hbr%0?2?d`, `hitopbr_[0-9]`, `HBR_[0-9]\b` and `num_range\("h(s|b)r"` returns no line. This claims only those nine spellings; names built at run time from a stem and a number are covered by AC6's executed examples and articles, not by this sweep.
- [x] AC5. `NEWS.md` carries a Breaking-changes entry naming the four renamed datasets, the three functions whose item names are now zero-padded for every `prefix` (so a caller passing a custom `prefix` against unpadded columns stops matching), their new default `prefix`, and the `target = "scales"` consequence; `cairn/DESIGN.md`'s utilities line stops saying `HSR_*`.
- [x] AC6. `devtools::document()` produces no diff, `devtools::test()` is clean, `devtools::check()` reports 0 errors / 0 warnings (building `vignettes/*.Rmd` and running every roxygen example), and `pkgdown::build_articles()` renders with no chunk error (`vignettes/articles/` is not registered as vignettes, so nothing else executes them).

## Coverage

- AC1 → T2, T5
- AC2 → T2
- AC3 → T3
- AC4 → T4
- AC5 → T5
- AC6 → T4, T5

## Tasks

- [x] T1. Add an internal helper (`R/util.R`) that builds `paste0(tolower(stem), "_", zero-padded n)` with the width taken from the instrument's largest item number, unit-tested on both widths; route `build_redcap_zip()` (`R/generate_redcap.R:288-292`) through it with `tests/testthat/test-generate_redcap.R` unchanged and green.
- [x] T2. Write `data-raw/rename_item_columns.R`: load each of the four objects from `data/`, rewrite item names positionally from each name's trailing integer through the helper (no literal old spelling, so AC4's sweep covers the script), assert the AC2 identity and pairing against `git show 7e06f40e:data/<name>.rda` (M026 lesson: compare loaded objects, never the `.rda` bytes), `usethis::use_data(overwrite = TRUE)`; edit `data-raw/sim_hitopsr.R:8`, `data-raw/sim_hitopbr.R:8`, `data-raw/ku_data.R:9,34,55,60,65` to build the new names; update the four `@format` blocks in `R/data.R`; add the AC1 test.
- [x] T3. Tests first for AC3, then route `rename_hitopsr_items()` (`:56,99`), `label_hitopsr()` (`:33`) and `label_hitopbr()` (`:34`) through the helper and set their default `prefix` to `"hsr_"` / `"hbr_"`; update their roxygen (`@param prefix`, examples) and `tests/testthat/test-rename_hitopsr_items.R`, `test-label_scales.R`.
- [x] T4. Sweep: `vignettes/hitopsr_scoring.Rmd` (`:30,64-69,82,105,119,249-254`), `vignettes/hitopbr_scoring.Rmd` (`:65,70,182,190-195`), `vignettes/articles/modules-hitopsr.Rmd` (`:193,245`), `download-hitopsr.Rmd`, `download-hitopbr.Rmd`, roxygen examples in `R/score_hitopsr.R:68`, `R/reliability_hitopsr.R:48`, `R/module.R:52`, and every test named by the AC4 grep plus the run-time constructions the audit listed (`test-module_file.R:283`, `test-generator-descriptor.R:195`, `test-deprecated.R:16`, `test-scale-name-hitopsr.R:164-169`, `test-item-guards.R:16-17,42-43`, `helper-fixtures.R:160,175`, `test-test-pipeline_hitopsr.R:14`); run the AC4 grep and `pkgdown::build_articles()`.
- [x] T5. NEWS Breaking-changes entry (AC5), `cairn/DESIGN.md:45`, `devtools::document()`, `devtools::test()`, `devtools::check()`.

## Work log

- 2026-09-01: created by /milestone-plan; absorbs the 2026-08-21 naming candidate row (lineage M044). Pattern, scope, deprecation waiver and regeneration route chosen by Jeff at the plan gate; D-052 records the pattern and the waiver.
- 2026-09-01: criteria audit ran in full mode ([O] fresh reader) after the gate, since the gate was posed as an assessment; it returned five findings, all fixed here: AC1 now uses the independently stated pattern rather than the generator as oracle (IP2 direction); AC2 gained the column-to-item pairing fact and lost its review-re-run clause; AC3 restated as a padding change at four sites with probes at items 1, 10 and last, both rename methods, and the `target = "scales"` consequence pinned; AC4 narrowed to the nine grep spellings with run-time constructions routed to AC6; AC5 names three functions and the wider breaking surface, and lost its instrument-bound clause; AC6 names `pkgdown::build_articles()`.
- 2026-09-01: plan gate chose the REDCap export's padded lowercase pattern over the sims' unpadded `hsr_1` because package-fielded data then matches shipped data and `label_*()` can read it unrenamed; falsified by users reporting hand-named unpadded data as the common case.
- 2026-09-01: plan gate chose a strict single pattern in `label_*()` over lenient matching (any padding or case after the literal prefix) because one pattern is one promise to test; falsified by a user report of `label_*()` refusing data the package's own vignettes once taught.
- 2026-09-01: plan gate chose renaming the shipped objects in a `data-raw/` script over seeding and regenerating the sims because the sims' values would change and every pinned example output with them; falsified by a value-level defect found in the shipped sims.
- 2026-09-01: plan gate chose waiving the deprecation cycle (pre-1.0) over a one-release warning because the warning would fire on every default-prefix call; falsified by a downstream caller breaking on the release.
- 2026-09-01: audit chose the independently written pattern as AC1's oracle over the generator's dictionary because a generator change would move both sides; falsified by the dictionary and the datasets diverging while both tests stay green.- 2026-09-01: /milestone-implement started; branch `m077-item-column-names` cut from pushed main. Implement gate (one question): module REDCap exports keep padding to the widest item in the module, so the generator's output is unchanged on this branch; the small-module mismatch with `label_hitopsr()` is a new candidate ROADMAP row (both online generators). Jeff asked mid-session whether module items should carry a marker such as `hsr_m001`; advised against, since the original item number is what `score_hitopsr(module =)` and `label_hitopsr()` match on. ROADMAP rows compressed to stay under the byte budget.- 2026-09-01: T1 done. `item_names(prefix, n, max_n)` in `R/util.R` pastes `prefix` literally (so the label/rename helpers can honor a user's `HSR_`) and pads to the width of `max_n`; five assertions in `test-util.R` on both widths; `build_redcap_zip()` routed through it passing the export's own widest item, `test-generate_redcap.R` untouched and green.- 2026-09-01: T2 done. `data-raw/rename_item_columns.R` loads each object at 7e06f40e via `git show`, renames from the trailing integer through `item_names()`, asserts the AC2 identity and pairing, and re-saves all four; the sim and KU scripts build `sprintf("hsr_%03d")`/`"hbr_%02d"` names next run (`num_range()` selections replaced by `all_of()`, since AC4 bans `num_range("hsr`); four `@format` blocks updated; AC1 test in `test-data-item-names.R`.
- 2026-09-01: T3 done. Three helpers routed through `item_names()` with `max_n` the instrument's largest item; defaults `"hsr_"`/`"hbr_"`; roxygen states the padded pattern. AC3 tests: both rename methods at items 1/10/405 plus a literal `HSR_` prefix, `label_*()` on `ku_*` at 1/10/last with no prefix, `target = "scales"` on `score_hitopsr()` output. Minor reorder: the test half of T4 (fixtures, pipeline, item-guard, module and scale-name tests) moved into T2/T3 so each checkpoint's suite is clean; the vignette/article/example half also landed here. `devtools::test()` clean.- 2026-09-01: T4 done. AC4 grep over `R/`, `vignettes/`, `tests/`, `data-raw/`, `man/` returns no line (the last hit, `data-raw/characterize_calc_se.R`, moved to the padded names). `pkgdown::build_articles()` first failed because the *installed* hitop still carried the old dataset names (the Rmd's `library(hitop)` reads the installed copy, so `score_hitopbr()` aborted with `items` not found in `data`); after `devtools::install()` from this branch every article renders, none of the three rendered pages carries an old spelling.
- 2026-09-01: T5 done. NEWS Breaking-changes entry names the four datasets, the three padded helpers and their new defaults, the custom-prefix consequence and the `target = "scales"` consequence; `cairn/DESIGN.md` utilities line names the pattern and `item_names()`. `devtools::document()` no diff; `devtools::test()` clean; `devtools::check()` 0 errors / 0 warnings / 0 notes (7m47s). Status → review.
- 2026-09-01: /milestone-review started; main unmoved since the branch cut; draft PR #83 opened. `cairn_validate` failed `weight caps` (ROADMAP at 60 lines, cap <60 — the candidate row added at the implement gate pushed it over); remedied on the branch by merging the two M061 column-shape candidate rows into one (no content dropped), 59 lines, validate all-green. Logged here rather than returned, since the remedy is the candidate-row hygiene any skill may perform; surfaced at the approval gate.

## Decisions

## Review

Fresh evidence, 2026-09-01, on branch `m077-item-column-names` at the PR #83 head (main unmoved since the cut).

- AC1 — pass. A script under `devtools::load_all()` evaluated the four `identical()` comparisons against `sprintf("hsr_%03d", hitopsr_items$HSR)` / `sprintf("hbr_%02d", hitopbr_items$HBR)`: all four `TRUE`. `tests/testthat/test-generate_redcap.R` is unchanged on the branch and still asserts the dictionary's item variables against the same two expressions.
- AC2 — pass. For each of the four datasets, `old` loaded from `git show 7e06f40e:data/<name>.rda` and `new` from the branch: `identical(unname(as.list(new)), unname(as.list(old)))` `TRUE` all four; trailing integers (`as.integer(sub("^\\D+", "", x))`) equal position-by-position all four. Old spellings were `hsr001`, `hsr_1`, `hbr01`, `hitopbr_1`; column counts unchanged (407/405/47/45).
- AC3 — pass. Tests: `test-rename_hitopsr_items.R` ("zero-pads item numbers to three digits (both methods)": `original` and `text` at items 1/10/405, plus `prefix = "HSR_"` → `HSR_001`/`HSR_010`/`HSR_405`), `test-label_scales.R` (`label_hitopsr(ku_hitopsr)` and `label_hitopbr(ku_hitopbr)` at items 1/10/last with no prefix; `HSR_001`..`HSR_405` frame under `prefix = "HSR_"`; `label_hitopsr(score_hitopsr(sim_hitopsr, items = 1:405, append = FALSE), target = "scales")` labels every column). A direct probe confirmed each: 405/45 item columns labelled by default, 405 under `HSR_`, 76 of 76 scale columns.
- AC4 — pass. The nine-pattern `grep -rnE` over `R/ vignettes/ tests/ data-raw/ man/` returned no line (exit 1).
- AC5 — pass. `NEWS.md` "Breaking changes" entry read: names `ku_hitopsr`, `sim_hitopsr`, `ku_hitopbr`, `sim_hitopbr`; names `rename_hitopsr_items()`, `label_hitopsr()`, `label_hitopbr()` as zero-padding "for every `prefix`", the custom-prefix-against-unpadded-columns consequence, the new `"hsr_"`/`"hbr_"` defaults, and the `target = "scales"` consequence. `cairn/DESIGN.md` utilities line now reads `hsr_001`..`hsr_405` and cites `item_names()`; `HSR_*` gone.
- AC6 — pass. `devtools::document()` then `git status`: no diff. `devtools::test()`: 0 failures, 4 skips (all pre-existing: the SDTD OQ-1 skip and three Body Focus merge-base skips in `test-scale-name-hitopsr.R`). `devtools::check()`: 0 errors / 0 warnings / 0 notes (vignettes built, examples run). `pkgdown::build_articles()` after `devtools::install()` from the branch: every article rendered, no chunk error.

Consistency gate: `cairn_validate` all checks pass after the ROADMAP row merge (22 advisories, all pre-existing dangling-id and staleness warnings); no IP/GP principle changed on the branch, so `cairn_impact` was skipped; `document()` no diff; README.Rmd/README.md untouched by the branch and last changed in the same commit; `pkgdown::check_pkgdown()` passed; NEWS entry present; no new top-level files (`check()` 0 notes). Driving RR: none.

Independent review, three fresh-context lenses:

- [S] prior-review lens — no prior-review evidence regressed, zero findings. Archived `## Review` sections with hits (M044, M050, M073, M075) and every LESSONS line were read; the M044 candidate row is what this milestone closes; `data-raw/rename_item_columns.R` follows the M026 and M035 lessons; the PR-comment probe returned no inline comments repo-wide, so the thread walk was skipped.
- [S] blame-history lens — two findings. (1) `data-raw/characterize_calc_se.R:61,67,73` still built `paste0("hsr_", 1:405)` / `paste0("hitopbr_", 1:45)` for the sim datasets (the `ku_*` lines had moved; run-time constructions outside AC4's nine spellings and outside AC6's executed surface, so the M069 maintainer-run harness would abort on its next run) — no criterion fails; triaged fix-now, the three lines moved to `sprintf("hsr_%03d")` / `sprintf("hbr_%02d")` on the branch. (2) The T2 task line's "M026 lesson" citation said to be wrong — rejected: `cairn/LESSONS.md` line 12 is the 2026-07-30 (M026) `use_data()` lesson that says compare the loaded objects, so the citation stands. Everything else read against history (D-052, D-026, D-018; the M12-era `hitopbr_` rename; every changed `expect_*` line) came back as a deliberate change, none undone.
- [O] diff-bug lens — nine findings, ranked by the reviewer; verified against the code before triage.
  1. `label_hitopsr()` / `label_hitopbr()` on unpadded columns match partially (items 100+ / 10+, since padding changes nothing there) with no warning, while NEWS said they "no longer match and warn that no column matched" — verified: `prefix = "HSR_"` on `HSR_1`..`HSR_405` labels 306 of 405. AC5's claim is inaccurate, not its named content; triaged fix-now as a documentation correction: NEWS, both `@param prefix` blocks and both scoring vignettes now state the partial match and that the warning fires only when nothing matched. The behavior itself (no partial-match warning) is left as is and put to the maintainer at the gate.
  2. Roxygen (three files), both scoring vignettes and NEWS said the Qualtrics export writes the lowercase pattern; it writes `HSR_001` / `HBR_01` (`R/generate_qualtrics.R:101,29`) — verified; fix-now: every site now names the REDCap export and says what Qualtrics writes and which `prefix` matches it.
  3. `item_names(prefix, integer(0))` returned the bare prefix — verified; fix-now: returns `character(0)`, one test added in `test-util.R`. Unreachable from any current caller.
  4. `item_names()` validates nothing (a double `2.7` pads to `002`; character `n` mis-widths) — rejected: internal helper, every call site passes the keying tables' numeric item numbers; no criterion names it.
  5. `generate_qualtrics_hitopsr()` still computes the width inline (`R/generate_qualtrics.R:287`) — follow-up: the Qualtrics generator is out of scope by the plan ("untouched, they are the reference"); absorbed into the existing 2026-09-01 module-padding candidate row, which already names both generators.
  6. NEWS migration examples named two of the four old spellings — fix-now: all four named.
  7. `num_range()` → `all_of()` in `data-raw/ku_data.R` aborts on a missing item where the old selection silently shortened — rejected: intentional (AC4 bans `num_range("hsr`; the T2 work-log line records the swap) and a maintainer-run script; the bare-vector `select()` at `:55` is pre-existing.
  8. HiTOP-BR test fixtures (`test-score_hitopbr.R`, `test-reliability.R:188,196`, `helper-fixtures.R:221`) still build `HBR_`-prefixed unpadded frames — rejected: self-named frames that never touch shipped data or the label/rename helpers, outside the sweep the plan lists and outside AC4's spellings; noted for the maintainer.
  9. `test-data-item-names.R` slices two id columns positionally — rejected: AC1's own text specifies `[-(1:2)]`.
  Checked correct by the lens: AC1/AC2/AC3 independently re-verified (AC3's assertions fail under the old behavior), `build_redcap_zip()` refactor behavior-preserving, `rename_hitopsr_items()` and `label_*()` agree on `max_n` for any prefix, a wider grep over `devel/`, `inst/`, `pkgdown/`, `.github/`, README, and the hitop-builder repo found no stale spelling.

Fix-now commits after the pre-gate checkpoint: `c7f74add` (characterize script), `5e1ffea2` (docs, NEWS, `item_names()` empty case). After them: `document()` no diff, AC4 grep no line, the three touched test files green; full `test()` and `check()` re-run on the final head recorded below.
Final-head re-run (`73ac3200`, code last touched at `5e1ffea2`): `devtools::test()` 0 failures; `devtools::check()` 0 errors / 0 warnings / 0 notes; `document()` no diff; AC4 grep no line. AC5 and AC6 evidence above stands against this head.

Gate round 1 (2026-09-01): Jeff chose "fix before merge" for diff-bug finding 1's behavior and approved the merge, so approval is re-requested after the fix. Commit `e01ab24e`: `label_hitopsr()` / `label_hitopbr()` now warn, under the condition class `hitop_unpadded_items`, naming any column that carries the prefix and a bare number but is not among the padded item names (`unpadded_item_cols()` and `warn_unpadded_items()` in `R/util.R`); such columns stay unlabelled, a padded subset such as a module's items warns nothing, and an empty match still reports "No columns matched". Tests first in `test-label_scales.R` (warning class, labelled/unlabelled split, module-subset silence, empty-match control) for both instruments; roxygen, both scoring vignettes and NEWS restated. `document()` no diff; AC4 grep no line; the label and util test files green; full `test()` and `check()` on this head recorded below.
Head `e01ab24e` (code final): `devtools::test()` 0 failures; `devtools::check()` 0 errors / 0 warnings / 0 notes. AC3, AC5 and AC6 evidence stands against this head; AC1, AC2 and AC4 are untouched by the fix commits (datasets and grep unchanged, grep re-run empty).

