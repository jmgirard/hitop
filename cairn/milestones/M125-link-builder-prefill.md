# M125: hitop-form's `link.html` fills its fields from a study link's `c` parameter, so a link can be edited and another page can hand a module over

- **Status:** review
- **Priority:** normal
- **Depends on:** —
- **Driving RR:** —
- **Principles touched:** GP3
- **Resolves:** —
- **Surface tier:** user-facing — a researcher-facing page on the deployed hitop-form site
- **Branch/PR:** `m125-link-builder-prefill` (hitop, tracking and NEWS; hitop-form, the code)

## Goal

Make hitop-form's link builder read the `?c=` parameter the form page already reads, fill its controls from it, refuse one it cannot read by name, and show its place in the flow with links to the module builder and the online-collection tutorial.

## Scope

**In:** `link.html` and `form.js` in `jmgirard/hitop-form`: on load, decode a `c` parameter with `decodeConfig()`, fill every control it names, show the chosen store kind's field group, and write a refusal into `#err` for a parameter that cannot be read or names an instrument the page does not offer. A three-step list above the form. A link to the module builder in the module field's hint and a link to the tutorial. The README paragraph. Playwright tests.

**Out:** the builder's "Online form" card → M126. The package site's strip, menu entry and step list → M124. A module for an instrument other than the HiTOP-SR → DESIGN Known issue 11 stands. Re-normalising a store or completion URL on prefill → not needed, since the values come from links the page wrote.

## Acceptance criteria

- [x] AC1: Opening `link.html?c=<p>` fills each control from the config that `<p>` encodes, where `<p>` is `encodeConfig(config)`. Pressing "Make the link" then yields a link whose `c` decodes to a config deep-equal to the one opened, and the chosen store kind's field group is shown with the other hidden. A test in `tests/link.spec.js` shows this once per store choice: no `store` field, a `webhook` store, and a `supabase` store. One run sets `participant`; another sets `prolific: true` with no participant. Each run sets `module`, `shuffle`, `complete` and `completeSaved`. Every URL and text value in the opened config is already in the form `link.html` writes.
- [x] AC2: A `c` whose config carries only `instrument` selects that instrument, and every other input's `value` and every checkbox's `checked` equal the same control's on a load with no `c`. A config carrying `instrument` and `module` also fills the module textarea with JSON text whose `JSON.parse` is deep-equal to the descriptor. Two tests in `tests/link.spec.js` show this.
- [x] AC3: A `c` that `decodeConfig` cannot decode, one that decodes to a value that is not a plain object, and one whose `instrument` is not one of the select's five values each write a message containing the text `c parameter` into `#err` and leave every control at its no-`c` value. A load with no `c` leaves `#err` empty. Tests in `tests/link.spec.js` show this for seven loads: a string that is not base64url, a base64url string that is not JSON, a JSON array, JSON `null`, JSON `"x"`, an object with `instrument` `"hitophsum"`, and no `c`.
- [x] AC4: Above the form sits an ordered list of three items whose text contains, in document order, "Choose the instrument", "where the responses go" and "Make the link". The module field's hint carries a link with href `https://jmgirard.github.io/hitop-builder/`, and the page carries a link with href `https://jmgirard.github.io/hitop/articles/online-collection.html`. A test in `tests/link.spec.js` asserts the two hrefs and the three items' order.
- [x] AC5: A load of `link.html` with AC1's Supabase config requests nothing but `link.html` and `form.js` from navigation to the first network idle. A test in `tests/network.spec.js` records every request over that window and compares URLs with the query dropped, as its `recorded()` helper does.
- [ ] AC6: The Playwright suite is green locally and on the pull request's CI run. `README.md` gains a paragraph under "Make a study link" stating that a study link's `c` parameter opened on `link.html` fills the fields for editing, and that a page can hand a module over that way.

## Coverage

- AC1 → T1, T3
- AC2 → T1, T3
- AC3 → T1, T3
- AC4 → T2, T3
- AC5 → T1, T3
- AC6 → T4

## Tasks

- [x] T1: In `link.html` (`link.html:151-160`, the module script) read `c` from `location.search` on load, decode it with `decodeConfig()` (`form.js:62`), fill each control, call the kind-group toggle (`link.html:170-175`) after setting the store kind, and write the AC3 refusals into `#err`. The instrument check reads the select's own options.
- [x] T2: Add the three-step list above the form, the builder link in the module hint (`link.html:84-87`), the tutorial link, and an up-nav link to the package site in the builder's shape (`hitop-builder/index.html:437-440`).
- [x] T3: Tests: the AC1 round trips, the AC2 comparisons against a no-`c` load, the seven AC3 loads, the AC4 hrefs and order in `tests/link.spec.js`, and the AC5 request window in `tests/network.spec.js`. Written red before T1 and T2 land; this task plants a defect against each new assertion class and runs the full suite.
- [x] T4: The README paragraph; run the suite; push the hitop-form branch and read its push-triggered test run (the PR opens at review).

## Work log

- 2026-09-24: created by /milestone-plan.
- 2026-09-24: criteria audit ran in full mode (fresh [O] reader, user-facing tier) over the three drafts; 36 findings, 31 repaired before writing (store kinds, participant beside Prolific, pre-normalised values, hidden kind groups, checkbox state, unknown instrument, seven malformed loads, exact list phrases, the network window, the README's tense), five accepted as test-run gates.
- 2026-09-24: plan gate chose prefilled hand-off links over merging the link builder into the module builder because every link build would then wait on the builder's webR boot and the five-instrument page would collapse to the HiTOP-SR; falsified by a researcher report that two pages with hand-offs still read as disjointed.
- 2026-09-24: plan gate chose prefill over cross-links with no parameter reading because the pasted descriptor is the step researchers lose; falsified by a report that the prefilled link fails where the paste worked.
- 2026-09-24: implement started; hitop branch `m125-link-builder-prefill` cut from main (equal to origin/main at f42a8ca4), hitop-form branch of the same name cut from its main (equal to origin/main at e97aa35, hitop-form #10 merged).
- 2026-09-24: question gate: a readable `c` whose other field has the wrong type skips that field and fills the rest (over refusing the whole `c` through `parseLink()`, which would add a fourth refusal); the three messages "The link's c parameter could not be read." / "does not hold a form." / "names an instrument this page does not offer: <value>.", each followed by "Fill in the form below to make a new link."; T4 pushes the hitop-form branch and reads its push-triggered run, the PR opening at review (minor amendment to T4's text). Minor amendment: the tests are written red before T1 and T2, T3 holds the plants and the full run.
- 2026-09-24: T1 (hitop-form fe1d39d): `prefill()` in the module script, run before `showKind()`. Tests L16–L18 and N8 red on the unchanged page (11 failed, the no-`c` load's empty `#err` passing), 13 passed after; the three messages read off the page match the gate's wording. L19 stays red until T2.
- 2026-09-24: T2 (hitop-form 9daa8cf): `nav.upnav` above the heading, `ol.steps` between the lede and the form, the Module Builder linked in step 1 and the module hint, the tutorial linked in step 3. L19 and L3 green; the page read in the browser with a Supabase `c` (fields filled, the Supabase group shown) and with `instrument: "hitophsum"` (the alert under the store select, every field empty).
- 2026-09-24: T3: nine plants on `link.html`, each red on its own assertion then reverted, nothing committed: the study written into the participant field (L16 value); `prefill()` run after `showKind()` (L16 Supabase group hidden); the message without "c parameter" (L18); the plain-object check removed (L18 `null`, `#err` empty); the shuffle box checked unless `false` (L17 deep-equal); an export fetch on prefill (N8, one extra URL); the tutorial href replaced (L19 count 0); steps two and three swapped (L19 item text); the list moved below the form (L19 bounding box). A sed plant that reworded step 2 without moving "where the responses go" stayed green, as it should. Full suite started after the plants.
- 2026-09-24: T4: README paragraph under "Make a study link" and the link and network test rows (hitop-form 2ce1a33); NEWS entry in hitop. hitop-form `npx playwright test` 215 passed (1.3 min) on the code head 9daa8cf.
- 2026-09-24: claim audit: 60 claims read, 10 corrected — NEWS.md (hitop), README.md, link.html, tests/link.spec.js, tests/network.spec.js (hitop-form). The ten: "every field left empty" on a refused `c` at two sites (the instrument select keeps its first option; now "no field is filled from it"); "read as the form page reads it" (only the decoding is shared); the L16 header's field-group and participant counts; "seven loads" at two sites (six bad values of `c` plus the no-`c` load); N8's comment naming L16's config and a build the test never runs. Re-read once: all hold. Left: "up to the first network idle" (the set is read just after it), "for each store kind" (the file choice is a kind in the builder's words), the up-nav link unmentioned in NEWS and README.
- 2026-09-24: correction to the gate's third premise: hitop-form's `tests.yml` runs on `push` only for `branches: [main]` (its header comment says "a push to main"), so the branch push (hitop-form 2245410, on origin) started no run, and `gh run list --branch` is empty. AC6's CI evidence is the pull request's run, read at review as the gate's second option described. T4 done otherwise.
- 2026-09-24: completion: hitop-form `npx playwright test` 215 passed on 9daa8cf, the later commits touching README and comments only (`node --check form.js` clean); hitop `devtools::test()` FAIL 0 WARN 0 SKIP 15 PASS 20375 on the NEWS change. Status → review. hitop-form head 2245410, pushed, no PR.
- 2026-09-25: review: AC1 to AC5 ticked on 2245410 (215 passed); gate green; three lenses, 0, 0 and 17 findings; four fixed at the gate (hitop-form ba06da5), the suite 216 passed on it, L18 now seven bad loads. The lint hook's hits on this file were left as cairn's record grammar.
- 2026-09-25: step-7 approval: m125-link-builder-prefill approved for merge (hitop, with hitop-form ba06da5; Jeff merges the hitop-form PR from his terminal, LESSONS M116).

## Decisions

## Review

- 2026-09-25: sync: hitop's branch merge base with origin/main is f42a8ca4, which is origin/main; hitop-form's is e97aa35, which is its origin/main. main has no unpushed commits in either repo. No merge was needed.
- AC1: hitop-form `npx playwright test` at 2245410, run 2026-09-25: 215 passed, 0 failed, 1.4 minutes; `tests/link.spec.js` and `tests/network.spec.js` alone: 53 passed. The three L16 tests (link.spec.js:749) open `link.html?c=` with `encodeConfig(config)` once per store choice: no `store` with `participant: "l16"`, a `webhook` store under `prolific: true` with no participant, and a `supabase` store with `participant: "l16s"`. Every run's config carries `module` (the `module-plain.json` descriptor), `shuffle: true`, `complete` (`COMPLETE_URL`, an `https://app.prolific.com` address) and `completeSaved` (`COMPLETE_SAVED_URL`, an `https://saved.example.org` address); the store URLs are `https://` origins with no trailing slash, the Supabase key and table are the builder's own trimmed form. Each test asserts every control's value against its field, `#webhookFields` and `#supabaseFields` visible only for their kind, presses "Make the link", asserts `#err` empty and `decodeLink(href)` deep-equal to the opened config. PASS.
- AC2: same run. L17 (link.spec.js:812, :825): `controls()` reads every `#f input, #f select, #f textarea` as name plus value, or `checked` for a checkbox, on a no-`c` load, then on `{instrument: "pid5sf"}`; the instrument reads `pid5sf` and every other control deep-equals the no-`c` load. The second test opens `{instrument, module}` with `module-shuffled.json`, asserts `JSON.parse` of the textarea deep-equal to the descriptor and every control but the two deep-equal to the no-`c` load. PASS.
- AC3: same run. L18 (link.spec.js:870) runs six loads: `%%%not-base64url%%%` raw, `not json` base64url-encoded raw, and the encodings of `[]`, `null`, `"x"` and `{instrument: "hitophsum"}`; each asserts `#err` contains `c parameter` and `controls()` deep-equal to a no-`c` load taken first. The seventh (link.spec.js:884) loads with no `c` and asserts `#err` has empty text and is hidden. The page (link.html:194-217) refuses in that order: a `decodeConfig` throw, a non-plain-object, an `instrument` outside the select's own options. PASS.
- AC4: same run. L19 (link.spec.js:893): `main ol li` count 3, item texts contain "Choose the instrument", "where the responses go", "Make the link" in that order, the list's bounding box bottom at or above the form's top, one `a[href="https://jmgirard.github.io/hitop-builder/"]` inside the Module descriptor label's `.hint`, one `a[href="https://jmgirard.github.io/hitop/articles/online-collection.html"]` on the page. Read at link.html:72-76: the `ol.steps` sits between the lede and `form#f`. PASS.
- AC5: same run. N8 (network.spec.js:198) records every request from `page.goto(link.html?c=<AC1's Supabase config with participant "n8">, {waitUntil: "networkidle"})`, asserts the Supabase table field filled, and the sorted recorded set equal to `[link.html, form.js]`; `recorded()` (network.spec.js:47) drops the query for the page's own address and `link.html` only. PASS.
- AC6, local half: the 215-passed run above at 2245410; `README.md:113-122` in hitop-form is the paragraph under "## Make a study link" (README.md:23), stating that a link's `c` opened on `link.html` fills the fields so a link can be edited and made again, and that a page can hand a module over with a `c` carrying only `instrument` and `module`. The CI half is the pull request's run, which opens after the merge gate (the workflow runs on `pull_request` and on `push` to `main` only, work log 2026-09-24); ticked when that run is green.
- Gate, universal: `cairn_validate.py` exit 0, all checks passed, 24 advisory warnings (23 dangling pre-migration D-ids, 1 references staleness), none new to this branch. No DESIGN principle changed, so `cairn_impact.py` was skipped.
- Gate, r-package: `devtools::document()` produced no diff. README.Rmd untouched on the branch, README.md unchanged, in sync. `pkgdown::check_pkgdown()`: no problems found. NEWS.md carries the entry (AC6). No new top-level file. The branch touches no R code, so `NAMESPACE`, `man/` and `data/` are unchanged. `devtools::test()` at b51b9b18: FAIL 0, WARN 0, SKIP 15, PASS 20375. `devtools::check()`: 0 errors, 0 warnings, 0 notes, 5m 56s. Gate PASS.
- Review, [S] blame-history: no findings. Read `git blame main` and `git log -p` over the touched lines of the four hitop-form files (16 commits, 2520e23 to 2245410), the archives M095, M098, M111, M112, M116, M118, M119, and D-038, D-064, D-071, D-072 whole. The `recorded()` widening in network.spec.js exempts only the two pages' own addresses, `showKind()` still runs after `prefill()`, the module hint's sentence L3 asserts is unchanged, and `prefill()` runs the first three of `parseLink()`'s checks by design, `build()` still running the rest.
- Review, [S] prior-review record: no prior-review evidence contradicted. Read the Review and Outcome paragraphs of the fifteen archives matching the touched files (M095 to M123); consistent with M095's per-screen alert, M116's module-hint clause, M112's double-submit guard and M098's Known issue 11 deferral. Both PR-comment probes (`hitop-form`, `hitop`) returned empty arrays, so no thread walk.
- Review, [O] diff-bug, ranked, none falsifying a criterion; F1 and F2 verified at review. F1 every refusal ends "Fill in the form below to make a new link." but `#err` (link.html:146) sits after every field, above the button, so the form is above the message and the message is below the first screen on load. F2 a throw inside `prefill()` after the three checks (a `module` nested 10,000 deep, an 80 KB `c`, makes `JSON.stringify` throw "Maximum call stack size exceeded", reproduced in node) stops the module script before `showKind()` and the submit listener, so "Make the link" then makes a native GET submit that puts every field, a Supabase key included, into the address the host sees. F3 the channel fills every field, so a crafted `c` can preset a store or completion address a researcher may not notice, and any text reaches the alert through `JSON.stringify(config.instrument)` (text, not markup). F4 L18 asserts only "c parameter", never which of the three messages, so a fault routed to the wrong message passes. F5 a wrong-type field or a store kind outside the two is skipped in silence and lost on rebuild; the README does not say so. F6 `#err` is filled before load, and a screen reader may not announce a live region's content present at load. F7 a `c` with no `instrument` reads "names an instrument this page does not offer: undefined." F8 `c=` (empty) is refused as "could not be read." where `parseLink()` treats it as no link, so the comment's "decoded as the form page decodes it" is loose. F9 a return to the page without the back-forward cache re-runs `prefill()` over edits (unverified). F10 the README privacy paragraph does not say that opening `link.html?c=` sends the config, key included, to the host again. F11 N8's participant is "n8" where AC5 says AC1's config ("l16s"). F12 the L16 tests encode with helpers.mjs's `encodeConfig`, not form.js's (same output). F13 the up-nav link is untested and undocumented. F14 `shuffle: false` and `prolific: false` round-trip to no field. F15 a field added to `parseLink()` later and not to `prefill()` would be dropped on edit. F16 a repeated `c` (first wins) is untested. F17 network.spec.js:1 still says "three recorded walks" over N1 to N8.
- Triage at the gate (Jeff, 2026-09-25). F1 fix now: the refusal ends "Fill in the form above to make a new link." F2 fix now: `refuse()` hoisted out of `prefill()`, the call wrapped in try/catch, a throw giving `f.reset()` then the "could not be read." refusal. A module nested 6,000 deep throws in node at a 16 KB `c`, which the test server refuses (ERR_HTTP_RESPONSE_CODE_FAILURE at 26 KB) and the weekly run against the deployed page would refuse too, so L18's seventh load provokes the throw with an `addInitScript` that makes `JSON.stringify` throw on a module carrying `throwOnStringify: true`, the study filled first. F4 fix now: each L18 load asserts its full message, and after the refusal presses "Make the link" and asserts the handler's "Give the study a name." with no `instrument` in the address. F17 fix now: the header names N1 to N7 and N8. Three plants red then restored: the try/catch removed (message empty), `f.reset()` removed (study kept), "below" restored (all seven). Full suite after the fixes: 216 passed. F3, F10, F6 follow-up: one candidate row, written at hygiene. Rejected: F5 and F14 are the question gate's skip-and-fill choice; F7 and F8 match the form page's wording, and its `!c` reading is the no-link case; F9 unverified and common to any prefilled form; F11 and F12 name identical values; F13 left on record at the claim audit; F15 and F16 forward risks with no present defect.
