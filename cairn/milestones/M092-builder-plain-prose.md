# M092: The HiTOP-SR Module Builder's prose reads as plain human English

- **Status:** review
- **Priority:** normal
- **Depends on:** —
- **Driving RR:** —
- **Principles touched:** —
- **Resolves:** —
- **Surface tier:** user-facing — the prose a builder user reads on the page, in the bundle README, and in the repo README
- **Branch/PR:** `m092-builder-plain-prose` (this repo, tracking), and `m092-plain-prose` in jmgirard/hitop-builder (code)

## Goal

Every sentence a user reads on the builder page, in the bundle's README.txt, and in the builder repo's README meets the plain-English house rules and reads as written by a person.

## Scope

**In:** In the sibling repo `jmgirard/hitop-builder`: a rewrite of the page's static text, every runtime message, the bundle README template, and `README.md` under the house rules. The rules are the plain-English rules the session hook states: sentences of at most 25 words, active voice, simple tenses, no semicolons, no em dashes, no decorative triplets, no bold lead-ins, one word per meaning. An extraction script in the builder's `tests/` that enumerates the user-visible strings. This repo: tracking only, as M090 did.

**Out:** Naming M091's `layout = "printed"` in the builder text → candidate row (lands after M091 ships). The hitop package's own documentation prose → not in scope. Any change to element ids, `data-*` attributes, the `Ready.` status strings the smoke test pins, or page behavior → refused (AC3). The scale names and definitions the page renders from the package → hitop's content (D-038, IP1), untouched.

## Acceptance criteria

- [x] AC1: Every user-visible prose string meets the house rules. The domain has four parts. (a) The text nodes of `<body>` outside `<script>` and `<style>`. (b) Every string that reaches the page as user-visible text: the arguments of `status()` and `log()` and the runtime notice writers, `FORMATS[].label` and `.button`, `selectionSentence()`, `settingsSummary()`, `refreshSelectAllLabel()`, the `<script nomodule>` message, and the `placeholder` and `aria-label` attributes. (c) The `bundleReadme()` output for each of the three formats. (d) `README.md`. Excluded: the scale names and definitions rendered from the package (`s.Scale`, `s.Brief`), and the pinned `Ready.` and `Ready. <label> chosen.` strings. An extraction script in the builder's `tests/` enumerates (a) to (c), and asserts its writer count against a grep for `textContent`, `innerHTML`, and `setAttribute` writers. The linter at `~/.claude/plugins/marketplaces/local-desktop-app-uploads/simple-english/evals/ste_lint.py`, run with `--type descriptive` on the extraction (code tokens in backticks, URLs bare) and on `README.md`, reports 0 in every rule. Baseline at that type on 2026-09-11: 18 hits over the static text, 122 over `README.md`.
- [x] AC2: Every fact survives. A second extraction pass collects, per passage, the URLs, code tokens, element ids, `data-*` values, and numbers as an ordered multiset. Run from the branch against a merge-base checkout, the two multisets match passage by passage. Any deliberate removal is listed in the work log with its reason.
- [ ] AC3: The behavior the smoke spec and the eight bundle-table builds cover is unchanged. `npm run smoke` is green locally and `tests/smoke.spec.js` is green in the builder PR's CI. `git diff` of `index.html` against the merge base of `m092-plain-prose` and `main` changes no element id, no `data-*` attribute, and no pinned `Ready.` status string, and every `from` string in `tests/plants.mjs` and every selector in `tests/smoke.spec.js` still matches the branch's `index.html`, so the plant matrix is not re-run. For each of the eight builds in `README.md`'s bundle table, with every other setting at its default and the "some scales" builds using the first scale row in both the branch build and the merge-base build, the bundle's entry names match a capture taken from that merge base on the same calendar day. The capture is a no-regression lock under D-054, not an oracle, and the locked property is separately derived in hitop's `test-generate_docx.R`, `test-generate_qualtrics.R` and `test-generate_redcap.R`. So do the decompressed bytes of every entry except `README.txt`, where the `.docx` and the REDCap `-upload.zip` are themselves zips and are compared by their own entry names and decompressed entry bytes, once the two things that differ between any two runs of the same page are set aside. The first is the timestamps in the Word file's `docProps/core.xml`. The second is the random order on a shuffled Word form, where the scoring file's `itemOrder` lists the same multiset of item numbers and `word/document.xml` holds the same multiset of text runs once the printed number that opens each item row's text is removed and the runs made only of numbers, commas and arrows (the crosswalk and the scoring table's item lists) are set aside. This comparison does not check the crosswalk's or the scoring table's numbers. `itemOrder` and the item set carry that. A diff of the two `README.txt` entries shows only the rewritten paragraphs, quoted in the review.

## Coverage

- AC1 → T1, T2, T3
- AC2 → T1, T4
- AC3 → T4

## Tasks

- [x] T1: The extraction script (`tests/prose.mjs`): the four-part domain of AC1, the writer-count assertion, the per-passage fact multiset of AC2, and a `--baseline` mode that writes both to JSON. Run it on the merge base and commit nothing from that run.
- [x] T2: Rewrite the page (`index.html`): static text, runtime messages, and the bundle README template. Keep every id, `data-*` value, and pinned status string. Lint to zero.
- [x] T3: Rewrite `README.md`. Keep its tables and dated verification records. Lint to zero.
- [x] T4: Evidence. Smoke locally, the eight-build entry-wise comparison against the merge-base capture, the plant re-run for touched anchors, then the builder PR and this repo's tracking PR.

## Work log

- 2026-09-11: created by /milestone-plan, from Jeff's remark at the M091 plan gate that the builder's visuals improved but the prose reads as AI-written. Not a hotfix: a rewrite is not a bug, and not a direct commit: the page is a runtime surface.
- 2026-09-11: criteria audit ran in full mode (fresh Opus reader) and returned nine findings, all fixed. The linter clause named `--type descriptive` (the draft's carve-out was self-contradicting). The enumeration names every user-visible writer (the draft covered `status()` and `log()` only). Package-rendered scale text and the pinned `Ready.` strings are excluded. The bundle comparison is entry-wise against a same-session merge-base capture (no stored baseline exists, and zip bytes are not stable). The CI command is the smoke spec CI runs. The fact check is a per-passage multiset. The plant requirement narrows to touched anchors. The draft's fourth criterion, a human read, moved into the review gate.
- 2026-09-11: plan gate chose a milestone over `/hotfix` because a prose rewrite is not a user-visible bug and needs judgment at review; falsified by nothing, the tier is a routing rule.
- 2026-09-11: implement gate. The linter runs from the plugin path named in AC1 and is not copied into the builder (md5 `d20daa68bc83384d4652a53eb7a59b17` for `ste_lint.py`, `5811fd8013baf9423af23c3e245fc93b` for `slop.tsv`). Log lines that echo an R call (`> generate_…`, `> zip::zip(…)`, `> library(hitop)`, `> installPackages(…)`) stay verbatim and the extraction wraps each whole line in backticks, so the linter reads one code token.
- 2026-09-11: T1 done, builder commit 5cd97b1. `tests/prose.mjs` takes `--ref` for a git ref, `--text` for the linted passages, `--json` for passages with facts, and `--compare` for the fact check (the plan's `--baseline` is these two flags). README.md passages are its sections, one per heading, because a paragraph-level rewrite reflows paragraphs. Facts compare as multisets, sorted on both sides. Merge-base run: 153 passages, 18 writer sites, the linter at 57 over parts (a) to (c) (the plan's 18 counted the static body text alone) and 122 over `README.md`.
- 2026-09-11: T2 done, builder commit c11e95e. Page extraction lints 0 in every rule and every passage keeps its facts. The three `<strong>` lead-ins in the notices are removed (a house rule the linter cannot see, and no CSS targets them). Local smoke 1/1 and runtime probes 2/2 green on the rewritten page. No plant anchor is touched (the plants anchor code lines only).
- 2026-09-11: T3 done, builder commit 80a650e. `README.md` lints 0 in every rule, longest sentence 25 words. Two deliberate fact changes: the quoted `README.txt` block follows the rewritten template (its last sentence), and the layout table gains the `tests/prose.mjs` row. Bold in the README became italic or plain.
- 2026-09-11: claim audit: 52 claims read, 3 corrected — index.html, README.md. Corrected: the second-step hint said every setting comes from the package (the Word radios are markup), the README said the status line names the format after every card press (only while idle), and the layout table said the Pages workflow publishes `index.html` alone (it copies three files). Five wording slips fixed in the same commit (182f96a): a never-printed crosswalk stated as existing, a lost antecedent, two counterfactuals, the tally's comma splice. The reader re-read all six once: all hold.
- 2026-09-11: amendment (substantive, gated): AC3 asked for byte-identical bundle entries, and six of eight bundles differ between any two runs of the unchanged page (docx `dcterms:modified`, REDCap zip header mtime, the random shuffle order). Jeff chose the narrowing at the mini gate over keeping the text or stopping.
- 2026-09-11: re-audit: AC3 (full) — nine findings: docx inner zip headers are build-stamped, "eight default builds" undefined, "page behavior" a universal, the plant clause vacuous and instrument-bound, no prose plant, "item line" and ordered-vs-multiset unspecified, the number-only rule a blind spot, "same session" not bounding the date, the IP2 objection unstated. Eight adopted as narrowings. Declined: a prose plant (widens the criteria set).
- 2026-09-11: re-audit: AC3 (full) — seven findings on the revised wording: the plant inference did not cover its domain, "both sides" and the merge base unnamed, the `-upload.zip` header exception inert, `dcterms:created` unguarded, `itemOrder` set vs multiset, D-054's second clause unmet, no named diff for `README.txt`. All seven adopted. Further churn went to Jeff, who chose the final text.
- 2026-09-11: T4 done. `npm run smoke` 1/1 and the runtime probes 2/2 green on the final page (builder 182f96a). `git diff` against the merge base touches no id, `data-*` or `Ready.` string. Full plant matrix 8/8 red plus the unplanted control green (run before the last edit). Eight bundles built on the merge base and on the branch the same day with the first scale row for the module builds: entry names match on all eight, bytes match on both Qualtrics files and six scoring files, the REDCap inner zips match entry by entry, the four docx differ inside only in `docProps/core.xml` timestamps, and the two shuffled forms hold the same multiset of text runs and the same `itemOrder` multiset. `README.txt` differs in one sentence: "In R, the hitop package reads it back with read_module() when you score the data." became "When you score the data in R, the hitop package reads it back with read_module()." The comparison scripts live in the session scratchpad, not the repo. Builder branch `m092-plain-prose` is four commits ahead of main and unpushed. Status → review.

## Decisions

## Review

- 2026-09-11 AC1 (verified): `node tests/prose.mjs` on builder 182f96a lists 153 passages over the four parts and finds 18 writer sites in the source against 18 in the ledger. `ste_lint.py --type descriptive` (md5 d20daa68…) on the `--text` extraction (1687 words, 205 sentences, longest 25) and on `README.md` (3340 words, 258 sentences, longest 25) reports 0 in every rule on both. Merge base 0a5c065 at the same settings: 49 over the extraction, 122 over `README.md`. `<strong>`/`<b>` count in `index.html`: 0; bold lead-ins in `README.md`: 0; em dashes: 0 in both. Evidence: AC1 ✓.
- 2026-09-11 AC2 (first pass, withdrawn): `--compare` keyed passages by group alone, so the 25 `log`, 5 `status`, 8 `abandonBoot` and 6 `FORMATS` passages collapsed to one each and 39 passages went unchecked (diff-bug lens F1, confirmed by a tamper probe that went unreported). Fixed on the builder branch: passages pair by group and ordinal.
- 2026-09-11 AC2 (verified): with the fix, `node tests/prose.mjs --compare` against the merge-base JSON (`--ref 0a5c065`) pairs all 153 passages and reports 151 identical fact multisets; a tamper probe (one extra token on one `log` passage) is reported. The two that differ are the deliberate changes: the quoted `README.txt` block in `README.md` (its last sentence follows the rewritten template) and the *Repository layout* table (gains `tests/prose.mjs`, and names `LICENSE.md` and `README.md` as the files the Pages workflow copies, the claim-audit correction). Evidence: AC2 ✓.
- 2026-09-11 AC3 (evidence recorded, box pending the gate): `npm run smoke` 1/1 and runtime probes 2/2 green locally on builder 182f96a; `tests/smoke.spec.js` green in builder PR #16 CI (1m18s). Sorted `id=`, `data-*` and `Ready.` string sets of `index.html` identical between the merge base and the branch. All 10 `from` strings in `tests/plants.mjs` match the branch's `index.html`; the smoke selectors resolve. Eight bundles captured today on the merge base and on the branch (first scale row for the module builds): entry names match on all eight; both Qualtrics files, all six unshuffled scoring files and the REDCap `-upload.zip` inner entries are byte-identical; the four `.docx` differ inside only in `docProps/core.xml` timestamps; both shuffled scoring files hold the same `itemOrder` multiset. `README.txt` differs in one sentence on every bundle (the `read_module()` sentence the work log names). Shuffled full form: the text-run multisets match once the scoring-table item lists are set aside, but two of those runs (`5, 29, 166(R), 335, 391` and its counterpart) carry the reverse-key marker `(R)` and so are not "made only of numbers, commas and arrows" as the criterion literally states. Those runs change between any two runs of the unchanged page, so the criterion's filter wording, not the rewrite, is what fails. Put to the gate as an amendment question on AC3.
- 2026-09-11 consistency gate: `cairn_validate` all checks passed (24 advisories, pre-existing dangling D-ids and one references-staleness line); no principle changed, `cairn_impact` skipped. `devtools::document()` no diff; `pkgdown::check_pkgdown()` no problems; the branch touches no package file, so README/NEWS sync are n/a; `devtools::check()` result recorded below.
- 2026-09-11 independent review: three-lens fan-out. Blame-history lens: zero findings. Prior-review lens: no prior-review evidence regressed (both PR-comment probes empty; archives M045–M090 read). Diff-bug lens: ten findings, triaged at the gate below.
- 2026-09-11 `devtools::check()`: 0 errors, 0 warnings, 0 notes. Pre-gate checkpoint.
