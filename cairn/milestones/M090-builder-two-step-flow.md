# M090: A two-step builder flow with the format settings folded away

- **Status:** review
- **Priority:** normal
- **Depends on:** M089
- **Driving RR:** —
- **Principles touched:** GP3 (page behavior under D-038; every setting keeps its gated default)
- **Resolves:** —
- **Surface tier:** user-facing — the public builder page's step flow and controls
- **Branch/PR:** `m090-builder-two-step-flow` (tracking, this repo); code on `m090-two-step-flow` in jmgirard/hitop-builder

## Goal

The HiTOP-SR Module Builder asks for scales, then for a format, and downloads from that second screen, with every format setting behind a closed disclosure that shows its current values.

## Scope

**In:** In `jmgirard/hitop-builder`, the three `section.step` blocks become two — *Choose scales* and *Choose a format and download* — and the step bar carries two buttons. A format card press (`[data-choose]`) marks the card `aria-current`, swaps in its `.fmtpanel` and relabels `#downloadBtn`, and no longer moves the step; the *Choose a different format* button goes. Each `.fmtpanel`'s settings — Word's paper size, numbering and shuffle; Qualtrics' block name and ID prefix; REDCap's form name and required flag — sit inside one `<details>` per format, closed on first paint and after every card press, whose `<summary>` names the current values and updates on change. The start-up log line at `index.html:1525`, the two `README.md` passages on ticking every scale (`README.md:179-181`, `283-285`), and the identifier `tilesExactly` are rewritten to say only what the probe establishes (absorbing the ROADMAP candidate row of 2026-09-03; lineage M063, M074, M081, M087). `README.md`'s *What the page shows* walks two steps; the smoke test drives the new flow. In this repo: tracking only.

**Out:** the one-file bundle → M089 (this milestone builds on it); a theme control and the client-facing scale text → their standing candidate rows; exposing further generator arguments → its standing candidate row (the disclosure is the home any promoted one would take).

## Acceptance criteria

- [x] AC1: The booted page renders exactly two `section.step` elements and a step bar of two buttons — *Choose scales* and *Choose a format and download*; pressing any `[data-choose]` format card marks it `aria-current`, swaps in that format's `.fmtpanel`, relabels `#downloadBtn` for the format, and leaves the second step on screen — each read from the DOM after the press.
- [x] AC2: Every format setting — enumerated by a DOM query as each named radio group plus each non-radio `input` inside `.fmtpanel` — sits inside a `<details>` element that is closed on first paint and after every format card press, whose `<summary>` names the current value of each of its settings; changing a setting's value updates that summary, read from the DOM after the change.
- [x] AC3: With every disclosure left closed, the eight builds of M089's AC1 produce bundles whose questionnaire and `.json` entries match M089's captured bundles by M089's AC2 comparison; and with each setting moved off its default one at a time — A4, original numbering, shuffle, block `Wave 2 Screening`, prefix `W2SCR`, form `wave2_screening`, required unticked — the built questionnaire for that setting's format reflects it while the other two formats' questionnaires are unchanged by the same comparison.
- [x] AC4: Every control that changes the visible step, enumerated by a DOM query for `[data-goto]` and `#stepbar button`, is crossed by a real Tab-then-activate sequence with the arrived-at step's heading focused and ringed; each `<summary>` is reached by Tab and toggled by Enter and by Space; and `#status` is the page's only announcing region, `#log` staying `aria-live="off"`, by a DOM query for `[aria-live]` and `[role=status]`.
- [x] AC5: Across six cells — widths 360, 768 and 1280 px in light and dark — and each view (step one; step two per format with the disclosure closed and open), a containment check over every rendered control reports 0 clipped and no horizontal document scroll.
- [x] AC6: The start-up log line, the two `README.md` passages on ticking every scale, and the identifier that carries the probe's answer each say only what the probe establishes — a gap-free run of scale items from 1 — with no claim that nothing is left out; the log line and both passages are quoted after the change, and a grep of the builder repo for `tilesExactly` and `nothing left out` returns no hit as a supporting check.
- [ ] AC7: `README.md`'s *What the page shows* walks two steps and names the disclosure; `tests/smoke.spec.js` drives the two-step flow to a bundle download; `node tests/plants.mjs` exits 0; the builder PR's smoke workflow is green.

## Coverage

- AC1 → T1, T6
- AC2 → T2, T6
- AC3 → T6
- AC4 → T6
- AC5 → T6
- AC6 → T3
- AC7 → T4, T5

## Tasks

- [x] T1: Merge steps two and three (`index.html:426-597`, `STEP_IDS`, `showStep()`, `setFormat()`, `markFormatChoice()`): cards select in place, the step bar drops to two buttons, the *Choose a different format* control and step three's heading swap go.
- [x] T2: Wrap each `.fmtpanel`'s fieldsets in a `<details>` with a `<summary>` rendered from the current values; close it in `setFormat()`; refresh the summary on `input`/`change`; token-layer styles for the summary and its focus ring.
- [x] T3: Rename `tilesExactly` and rewrite the `index.html:1525` log line and the two README passages to state a gap-free run from 1.
- [x] T4: Update `tests/smoke.spec.js` (`#stepbar button[data-goto="1"]` then `[data-choose="docx"]`, `smoke.spec.js:117-124`) for the two-step flow and keep `tests/plants.mjs` green.
- [x] T5: Rewrite `README.md`'s *What the page shows* for two steps and the disclosure.
- [x] T6: Run the AC1–AC5 checks on the served branch: DOM reads, the eight-build comparison against M089's captures, the seven off-default probes, the keyboard walk, the six-cell containment sweep; record digests in the work log.

## Work log

- 2026-09-11: created by /milestone-plan.
- 2026-09-11: criteria audit ran in full mode ([O] fresh reader): M090 AC2 enumerates settings as radio groups plus inputs, AC3 fixes one probe value per text setting and points "unchanged" at M089's comparison, AC4 enumerates step-changing controls by DOM query and states `#log` as non-announcing, AC5 names its widths, AC6 quotes the passages with the grep demoted to a supporting check.
- 2026-09-11: plan gate chose two steps with cards selecting in place over keeping three steps with folded options, or one unstepped page, because it removes a screen while keeping M053's one-format-on-screen promise; falsified by visitors reporting the download as hard to find on the second step.
- 2026-09-11: plan gate chose folding all seven settings over keeping paper size and the required flag visible because every setting has a gated package default and the summary line shows the values; falsified by a visitor missing a non-default they needed (A4 the likeliest).
- 2026-09-11: plan gate chose absorbing the log-line candidate row here over leaving it because this milestone edits the README sections its promotion condition names.
- 2026-09-11: implement started; branches `m090-builder-two-step-flow` (here) and `m090-two-step-flow` (builder). Question gate: Jeff asked for light beautification alongside the redesign; taken as a minor amendment within T1/T2's styling — every new colour reads from the existing token layer, so the contrast promises stand, and AC5's six-cell sweep covers the new elements. No other choice was open.
- 2026-09-11: T1+T2 landed in one builder commit (the merged markup and the disclosures are one region): `STEP_IDS` two entries, `setFormat()` closes every `details`, marks the card and relabels the button, card press no longer calls `showStep()`; `FORMATS[].heading` and `#step3h` gone; `refreshSummaries()`/`settingsSummary()` write each `[data-summary]` span from the controls on `input`/`change`. Styling: step-number discs, current-card check, disclosure chevron, larger download button. Screenshots at 360/768/1280 in both schemes read clean by eye.
- 2026-09-11: T3–T5 in one builder commit: `tilesExactly` → `itemsRunFromOne`; log line now "the scales' items together run from 1 with no gaps: true"; the naming-section and *Ticking every scale* passages state the gap-free run and the tail the probe cannot see; the mid-build "keeping the module" log line dropped its "cover … 1..N" wording too. *What the page shows* walks two steps and names the disclosure and its opening summary. `smoke.spec.js` keeps the `data-goto="1"` then `[data-choose="docx"]` route (still the visitor's path) with its comment rewritten; two "On the Word screen" leads became "Under *Word settings*".
- 2026-09-11: T6 on the served branch (builder commit 6a9d059, `tests/serve.mjs` on localhost, driven headless by `evidence.mjs`, `probe-fix.mjs`, `compare.py`, `compare_probes.py` in the session scratchpad). AC1: `section.step` count 2; step bar "1 Choose scales", "2 Choose a format and download"; each card press (qualtrics, redcap, docx) leaves `aria-current` on that card alone, one `.fmtpanel` shown, `#downloadBtn` relabelled, `#step2` visible. AC2: seven settings (radio groups papersize, numbering; inputs shuffle, blockName, idPrefix, formName, required) each inside its format's `details`, all `open === false` at first paint and after every press; each of the seven changes rewrote the summary (e.g. "Word settings A4 · numbered 1 to n · original order", "REDCap settings form “wave2_screening” · no item required"). AC3: eight default builds with every disclosure closed, one download event each, `compare.py` ALL OK against the M089 baseline (`.txt` sha bc73b984bccd/13d122901fcd, inner csv db794565f74b/5a2e1c16ec8a, docx 405/405 and 5/5 rows); bundle digests word d75dd5803efb, word-shuffled 1048dc605fae, word-module 3790e9775be1, word-module-shuffled 8ac991fded82, qualtrics 0f9f2924c7ed, qualtrics-module 2dadd38754d0, redcap 60422c7c468c, redcap-module fe3635e06123. Seven probes ALL OK: A4 pgSz 11909×16834 vs 12240×15840; original numbering first items 66/109/118 vs 1/2/3; shuffle crosswalk true vs false; block line `[[Block:Wave 2 Screening]]`; `W2SCR` ids; form column all `wave2_screening`; required `n` on every item row vs `y`; the other two formats unchanged by the AC2 comparison in each. The a4 and original probes were re-run (`probe-fix.mjs`) after the first pass left the shuffle box ticked from the last default Word build — a script fault, not a page one. AC4: five step-changing controls each reached by Tab and activated by Enter, focus landing on the arrived-at step's `h2` with a 3px solid outline; each of the three `summary` elements reached by Tab, toggled by Enter (closed→open→closed) and by Space (open→closed... recorded [F,T,F,T,F]); `[aria-live],[role=status]` returns `#status` (polite, status) and `#log` (off) only. AC5: 42 cells (2 schemes × 3 widths × 7 views), 0 clipped controls, no horizontal scroll in any.
- 2026-09-11: AC7 local half: `npx playwright test tests/smoke.spec.js` 1 passed (twice, before and after the comment fix); `node tests/plants.mjs` exit 0 — unplanted passed, 8/8 plants red on named assertions (a A1, b A7, c A1, d A4/A5/A6, g A4, h A5/A6, e A2, f A3). The builder PR's smoke workflow is review's to open and watch.
- 2026-09-11: claim audit: 50 claims read, 1 corrected — index.html (the `markFormatChoice` comment said the card mark lands "from the first paint"; it lands when `setFormat()` runs at the end of `main()`, so now "as soon as the controls are shown"); the reader also flagged a pre-existing comment now stale ("three places" for the recap sentence → two), fixed in the same builder commit d205bed. That commit changes comments only (diff 6a9d059..d205bed has no non-comment line), so the T6 evidence stands.
- 2026-09-11: all tasks checked; status → review.

## Decisions

## Review

Fresh evidence gathered 2026-09-11 on builder branch `m090-two-step-flow` at d205bed. The page was served on localhost by `tests/serve.mjs` and driven headless by this session's copies of `evidence.mjs`, `compare.py` and `compare_probes.py`. The probe loop now resets the shuffle box before the off-default probes, which is the script fault T6 hit. The default branch has not moved in either repo since the branches were cut.

- AC1: `section.step` count is 2. The step bar buttons read "1 Choose scales" and "2 Choose a format and download". Each card press (qualtrics, redcap, docx) leaves `aria-current` on that card alone and shows that format's `.fmtpanel` only. Each press relabels `#downloadBtn` (Qualtrics file, REDCap dictionary, Word form, each "(.zip bundle)") and leaves `#step2` visible. Pass.
- AC2: the DOM query yields seven settings. They are the radio groups papersize and numbering and the inputs shuffle, blockName, idPrefix, formName and required. Each sits inside its own format's `details`. All three disclosures read `open === false` at first paint and after each card press in AC1 and each of the seven presses here. Each change rewrote its summary from the DOM (A4, HiTOP-SR item numbers, shuffled order, block "Wave 2 Screening", prefix "W2SCR", form "wave2_screening", no item required). Pass.
- AC3: eight default builds ran with every disclosure closed at each build and one download event each. `compare.py` reports ALL OK against the M089 baseline. The Qualtrics `.txt` entries are byte-identical (sha bc73b984bccd and 13d122901fcd). The REDCap inner csv entries are identical (sha db794565f74b and 5a2e1c16ec8a). The Word entries match on 405 of 405 and 5 of 5 item rows, on headers and on crosswalk flags. The `.json` entries are equal outside buildDate and itemOrder. The seven off-default probes report ALL OK. A4 gives pgSz 11909×16834 against 12240×15840. Original numbering gives first items 66, 109, 118 against 1, 2, 3. Shuffle gives crosswalk true against false. The block line `[[Block:Wave 2 Screening]]` is present. `W2SCR` ids are present and absent from the default. The form column is all `wave2_screening`. Required is `n` on every item row against `y`. In each probe the other two formats' module questionnaires are unchanged by the AC2 comparison. Pass.
- AC4: the query finds five step-changing controls (two step-bar buttons, Continue, Change the selection, Back to scales). Each was reached by Tab from the element before it and activated by Enter. Focus landed on the arrived-at step's `h2` with a 3px solid outline every time. Each of the three `summary` elements was reached by Tab from its card. Each toggled closed, open, closed under Enter twice and again under Space twice, with a 3px solid ring while focused. The query `[aria-live],[role=status]` returns `#status` (polite, role status) and `#log` (aria-live off) only. Pass.
- AC5: 42 cells ran (light and dark, widths 360, 768 and 1280, step one and step two per format closed and open). Each cell had at least 16 rendered controls, 0 clipped, and no horizontal document scroll. Pass.
- AC6: a grep of the builder repo for `tilesExactly` and `nothing left out` returns no hit (node_modules and test-results excluded). The log line at `index.html:1592` reads "the scales' items together run from 1 with no gaps: ${itemsRunFromOne}." The README naming passage (lines 199 to 206) says the page "confirmed with the package that the instrument's scales, taken together, hold a run of item numbers from 1 with no gaps in it". The *Ticking every scale* passage (lines 302 to 311) says "That is all the probe asks: it reads no separate count of the instrument's items, so it cannot see a tail of higher-numbered items no scale claims." Neither claims that nothing is left out. Pass.

- AC7, local half: *What the page shows* walks two numbered steps and names the folded settings line and its opening summary (README.md lines 84 to 130). `tests/smoke.spec.js` presses the second step-bar button then the Word card and reads a bundle download: 1 passed locally. `node tests/plants.mjs` exit 0, the unplanted copy passed and all 8 plants red on named assertions (a A1, b A7, c A1, d A4/A5/A6, g A4, h A5/A6, e A2, f A3). The PR smoke run is read at step 8 before the box is ticked.

Consistency gate: `cairn_validate.py` exit 0 (24 advisory warnings, none new). No DESIGN.md principle changed, so the impact report is skipped. Toolchain slot: `devtools::document()` leaves no diff. README.Rmd is older than README.md, so they are in sync. `pkgdown::check_pkgdown()` reports no problems. NEWS.md needs no entry because this repo's diff is tracking only, as M089 handled it. `devtools::check()` gives 0 errors, 0 warnings, 0 notes. Smoke test 1 passed locally. Plant matrix: recorded below with AC7.

Independent review, three lenses, fresh context. Blame-history lens (Sonnet): zero findings. It traced the three-step to two-step collapse, the removed *Choose a different format* control, the heading swap, the probe rename, the focus and live-region code and the README lineage, and found each consistent with recorded intent. Prior-review lens (Sonnet): both PR-comment probes returned 0 inline comments, so the thread walk was skipped. One finding from the archived record:

- P1 `index.html:834,842,1605`: `setFormat()` now calls `markFormatChoice()`, and `main()` calls `setFormat()` at load, so the Word card carries `aria-current` at first paint. M053's review fixed that same state in commit e2c0600. Confirmed against the code. The implement session chose it knowingly and the comment records why: the panel below the cards is already Word's, so an unmarked row would leave that unsaid. In M053 the format screen stood alone, and the mark would have announced a choice nobody made.

Diff-bug lens (Opus), twelve findings, each confirmed or refuted against the code:

- F1 `index.html:533-546`: the shuffled-form warning (`#shuffleNote`) now sits inside the Word disclosure, which `setFormat()` closes on every card press, so a visitor can build a shuffled form with the warning out of sight. Confirmed. The summary reads "shuffled order" but carries none of the warning.
- F2 `README.md:156`: "The page's third step says the same" names a step that no longer exists. Confirmed.
- F3 `README.md:268-270`: "They sit on the screen of the format they reach: the Qualtrics screen carries ... the REDCap screen ..." describes the removed per-format screens. Confirmed.
- F4 `index.html:1616-1617`: a card press no longer moves focus or announces anything, where before it called `showStep(2)` and focused the new heading. Confirmed. The card keeps focus and gains `aria-current`, and `#status` stays the only announcing region as AC4 requires.
- F5 `README.md:105`: "Only the current format's settings are on the page" overstates what the code does, since all three panels stay in the DOM with two hidden. Confirmed as wording.
- F6 `index.html:339-343,354`: `#stepbar button` declared twice, the second overriding `align-items`. Confirmed, a dead declaration.
- F7 `index.html:1585-1592`: the probe also requires ascending order, which the name and log line do not say. Refuted: `hitop_module()` sorts its items (`R/module.R:102`), so a gap-free run from 1 is exactly what a true answer establishes.
- F8 `index.html:861`: `settingsSummary('docx')` reads the checked paper-size radio without a guard. Confirmed only for a programmatic state with no radio checked, which the page never produces.
- F9 `index.html:869-873`: a summary rewritten under focus is not announced. Confirmed, minor.
- F10 `index.html:488`: `aria-current` inside a plain group is weaker than a radio or `aria-pressed` set. Confirmed as a semantics point on the pattern M053 chose, on unmodified lines.
- F11 `README.md:260`: "The page's on-screen notice says which of these applies" points at the crosswalk sentence now inside the closed disclosure. Confirmed, the same fold as F1.
- F12 `index.html:857-859`: the "(default)" marker only appears after a box is emptied, since the defaults are written into the boxes. Confirmed, cosmetic.

Triage at the gate, 2026-09-11, Jeff deciding:

- F1 and F11: fix now. Jeff asked whether the warning is still needed given the metadata file. It is: the `.json` records the printed order on `item_order`, but no scoring function reads that attribute, so the columns must be reordered by hand before scoring. Jeff wants the scoring functions able to consult it, which is package work and becomes a candidate row at hygiene. Meanwhile `#shuffleNote` moved out of the Word disclosure to sit below it inside the panel, shown whenever the box is ticked.
- F4: fix now. A card press writes "Ready. <format label> chosen." to `#status` while the page is idle (status text starting "Ready."), so a build's progress or a failure message is never overwritten.
- P1: rejected with reason. The mark now says which format the visible settings and download button belong to. The code comment records the reasoning.
- F2, F3, F5: fixed now as README corrections (second step, settings lines, "shown" for "on the page"). The *What the page shows* walk gained two sentences on the warning and the status line.
- F7: rejected, refuted. F6: rejected, a dead declaration a formatter would catch. F8: rejected, a state the page never produces. F9 and F12: rejected, cosmetic. F10: rejected, unmodified lines and the pattern M053 chose.

Re-verification after the fix commit (builder commit bd0b5b5): AC1, AC2, AC4 and AC5 re-run in full by the trimmed sweep, same results (2 steps, 7 settings each closed after every press, 5 controls and 3 summaries crossed, 42 cells 0 clipped and no horizontal scroll). Two added reads: with shuffle ticked and the disclosure closed after two card presses, `#shuffleNote` is outside any `details`, not hidden, and has a rendered height. After the Word and REDCap presses `#status` reads "Ready. Word form chosen." and "Ready. REDCap dictionary chosen." AC3 stands from the first run: the fix touched markup placement, a status write and README prose, none on the build path. AC6 stands: the grep and the quoted passages are unchanged. Smoke and plants re-run: recorded below.
