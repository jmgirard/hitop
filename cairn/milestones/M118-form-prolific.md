# M118: A study link can ask hitop-form to take the participant's Prolific ID and send them to a completion URL

- **Status:** review
- **Priority:** normal
- **Depends on:** M117
- **Driving RR:** —
- **Principles touched:** GP3
- **Resolves:** —
- **Surface tier:** user-facing — the deployed form page, its link builder and the package's article
- **Branch/PR:** `m118-form-prolific` in hitop and in hitop-form

## Goal

Two link fields make hitop-form fit a Prolific study: `prolific: true` takes `PROLIFIC_PID` from the page's address as the participant identifier and writes `STUDY_ID` and `SESSION_ID` into the row and the file, and `complete` names an `https://` address the participant is sent to after a confirmed send and offered after a saved file.

## Scope

**In:** In hitop-form: `parseLink()` reads the two fields. The start screen skips the identifier field when the address carries a Prolific ID. `buildCsv()`, `buildRow()` and `storeSql()` carry `prolific_study` and `prolific_session`. The outcome screens act on `complete`. The builder gains a "Recruit through Prolific" box and a completion field, and prints the link with Prolific's three placeholders. README and tests. In hitop: the online-collection article's Prolific route, NEWS, and a page-written fixture with its reader test.

**Out:** The reader's acceptance of the two columns → M117. A completion URL other than `https://` → nowhere, because the store address has the same bar. Custom completion codes per outcome, which Prolific offers per study → a candidate row, promoted when a study needs a code for the unconfirmed-send outcome. Another recruiter's parameters (SONA, CloudResearch) → a candidate row. `complete` already takes any `https://` address.

## Acceptance criteria

- [x] AC1: `parseLink()` accepts `prolific` only as `true` or `false`, refuses `prolific: true` beside a `participant`, and accepts `complete` only as an `https://` address with no user name and no password, through a `checkCompleteUrl()` that `link.html` shares. It refuses any other value by name with the value shown, as it refuses `shuffle`. Tests fire the refusal for `prolific` as a string, a number and `null`, for the participant conflict, and for `complete` as `http://127.0.0.1`, `http://localhost`, a `javascript:` address, a string that is no URL, an address with a user name only, an address with a password, and a number.
- [x] AC2: Under `prolific: true`, the row and the file always carry `prolific_study` and `prolific_session` after `submitted` and after `item_order` when that column is present, holding `STUDY_ID` and `SESSION_ID` from the page's address, or the empty string when the address lacks one or still holds a `{{%…%}}` placeholder. When the address carries a `PROLIFIC_PID` that is not blank and not a placeholder, it is the participant identifier and the start screen shows no identifier field. When it is absent, blank or a placeholder, the start screen asks as today. Without `prolific: true`, the three parameters are ignored. Tests: a save against the fixture file `tests/fixtures/responses-hitopbr-prolific.csv`, a send to a web address and one to a Supabase table, each of the three with and without `shuffle` and asserting the header or key order; a walk whose address has a real `PROLIFIC_PID`, `STUDY_ID={{%STUDY_ID%}}` and no `SESSION_ID`, asserting both cells are `""`; the absent, blank and placeholder `PROLIFIC_PID` cases each showing the field; and a walk without the field whose address carries all three parameters and whose file and posted row are as before, with and without `shuffle`.
- [x] AC3: The builder's "Recruit through Prolific" box puts `prolific: true` in the link, and its "Completion URL" field puts `complete` in it. The builder refuses the completion address through `checkCompleteUrl()`, probed with one refused form of AC1, and refuses the box beside a filled participant field, each by name. With the box checked, the printed link ends in `&PROLIFIC_PID={{%PROLIFIC_PID%}}&STUDY_ID={{%STUDY_ID%}}&SESSION_ID={{%SESSION_ID%}}`, and the Supabase SQL gains `"prolific_study" text` and `"prolific_session" text` after `"submitted"` and after `"item_order"` when present, equal to two new fixture files written by rule and never captured from the builder, one with the random-order box and one without. Unchecked, the SQL equals the existing fixtures byte for byte.
- [x] AC4: With `complete` in the link, a confirmed send issues one navigation request to the completion address, and the sent screen's heading is not rendered before that request. The two saved screens (no store, unconfirmed send) show a link to the address after the file name, labelled by its host, and issue no request to the address within five seconds of the screen. Without `complete`, the existing outcome-screen assertions (the sent text in network N4, the saved-screen tests in save.spec.js and send.spec.js) pass unmodified. Tests fulfil the completion address through `page.route()`, count its requests, assert the link's `href` on both saved screens, and assert the network set on the confirmed walk equals the page's files, the export, the store and the completion address.
- [x] AC5: The hitop-form README's "Make a study link" and "What the participant sees" sections, its file and row descriptions and test table, and hitop's online-collection article describe the Prolific route: the box, the placeholders pasted into Prolific's study URL, the completion URL from the study's page, the two columns, and that a preview passes a 24-character ID, each cited to the Prolific page it comes from (the URLs in the source note's Provenance block). The article's "Who holds the data" section says that a Prolific ID identifies the participant to Prolific, and that the page's host receives the three Prolific parameters with each page load. hitop's NEWS carries the entry.
- [ ] AC6: hitop ships `tests/testthat/fixtures/responses-hitopbr-prolific.csv`, a file the page saved on a walk with `prolific: true` and `shuffle: true`, with a provenance row in the fixtures README. A test reads it through `read_form_responses()`, asserts the two columns and `item_order`, and scores it through `score_hitopbr()` against means recomputed from the package tables.
- [ ] AC7: `npx playwright test` is green on the checkout with every new test present. In hitop, `devtools::test()` is clean, `pkgdown::check_pkgdown()` passes and the article renders against the installed package.

## Coverage

- AC1 → T1, T6
- AC2 → T1, T2, T6
- AC3 → T2, T4, T6
- AC4 → T3, T6
- AC5 → T5
- AC6 → T5
- AC7 → T6, T7

## Tasks

- [x] T1: In `form.js`, `parseLink()` reads `prolific` and `complete` and refuses the participant conflict. A `checkCompleteUrl()` reuses `checkStoreUrl()`'s parse with `https:` only. `boot()` reads `PROLIFIC_PID`, `STUDY_ID` and `SESSION_ID` from the address under `prolific: true`, and treats a `{{%…%}}` value as absent. The page's header comment names the two fields.
- [x] T2: `runForm()` takes the identifier from the address. `buildCsv()`, `buildRow()` and `storeSql()` write `prolific_study` and `prolific_session` after `submitted` and `item_order`. `fileName()` is unchanged.
- [x] T3: `finish()` and `showSaved()` act on `complete`: `location.assign()` on a confirmed send, and a labelled link on the saved screens.
- [x] T4: `link.html` gains the "Recruit through Prolific" box, its hint, the completion field and the placeholder suffix. The participant-field conflict and the completion refusal show in the alert. Add the `storeSql()` fixtures `supabase-hitopbr-prolific.sql` and `supabase-hitopbr-prolific-shuffle.sql`, and the CSV fixture `responses-hitopbr-prolific.csv` written by rule, each with its fixtures-README row.
- [x] T5: README sections and the fixtures README in hitop-form. In hitop: the walk-saved fixture and its test, the `online-collection.Rmd` Prolific section and "Who holds the data", and `NEWS.md`. Purl and run the article.
- [x] T6: Tests: guard (AC1), save and send (AC2), link (AC3), send and network (AC4). Plant-check each by reverting the feature it covers once. Create the download promise before the walk with its own timeout.
- [x] T7: `npx playwright test` on the checkout, and `devtools::test()` and `pkgdown::check_pkgdown()` in hitop. Jeff merges the hitop-form PR from his terminal, because the merge guard reads the session's repo. Dispatch the deployed-page run after the merge.
- [x] T8: The review-return fixes. F1: `checkCompleteUrl()` refuses a non-text value with the value shown, and G11 asserts it. F2: the article's completion-URL sentence cites article 445178. F4: L12 probes with `http://localhost`. F3: the no-store save and the unconfirmed sends assert no `p.complete` element. F11: the form.js header comment names what link.html imports. F13: `buildCsv()` drops the redundant copy.

## Work log

- 2026-09-23: created by /milestone-plan, from the ROADMAP's Prolific candidate row (lineage M111–M113). D-071 records the link fields and the columns. The Prolific facts are in `cairn/references/prolific2026help.md`, written at the plan commit.
- 2026-09-23: criteria audit ran in full mode ([O] fresh reader, agent af61f6ee): 13 findings on this file, all fixed before the gate. The pair sits after `item_order` so D-070(b)'s sixth column stands; the columns are always written under `prolific: true` and a placeholder writes the empty string; `parseLink()` refuses the participant conflict; the "without" walk carries all three parameters; more refused forms for `complete` and `prolific`; the SQL change is conditional and the fixtures written by rule; the builder's refusal goes through the shared check; the "unchanged" and timing claims bound to named assertions; the README sentence-to-test claim dropped; AC6 adds the page-written fixture and its reader test; Coverage gained T1 and T2. The second read returned 6 findings, fixed: the password probe restored; the save and send tests run with and without `shuffle` and assert the order; an absent and a placeholder `STUDY_ID`/`SESSION_ID` walk asserts `""`; the docs cite Prolific's pages, not the source note; "Who holds the data" names the three parameters the host receives; T4 names the CSV fixture.
- 2026-09-23: step 2 chose recording all three parameters over `PROLIFIC_PID` alone because Prolific asks that all three be saved and `SESSION_ID` tells a returned submission from a first; falsified by a store whose export cannot take two more columns.
- 2026-09-23: plan gate chose a redirect on a confirmed send and a link on the saved screens over a link everywhere and over a redirect on every outcome, because Prolific recommends the redirect and a participant with a saved file must see its name first; falsified by a report of participants leaving the saved screen without the file.
- 2026-09-23: plan gate chose any `https://` completion address over Prolific's host alone because other recruiters use the same return-URL pattern and the check is the store address's; falsified by a study whose wrong host went unrefused at the builder.

- 2026-09-24: from M117's review (F3): the online-collection article's printed tibble now shows `prolific_study` and `prolific_session` as `NA` with no sentence about them, and the modules article and the PID-5 vignette still say "five lead columns ... a sixth, `item_order`". The article route here describes the two columns in all three.

- 2026-09-24: /milestone-implement started; status in-progress; branch `m118-form-prolific` in both repos. No question gate: the plan fixed every choice that matters. T1 done in hitop-form: `parseLink()` reads `prolific` and `complete`, refuses the participant conflict, `checkCompleteUrl()` shares a `parseAddress()` with `checkStoreUrl()`, `readProlific()` reads the three parameters with a `{{%…%}}` value as absent, and `runForm()` takes the identifier from the address. Guard tests G10 and G11 green (64 passed).
- 2026-09-24: T2 done in hitop-form: `leadColumns()` and `leadValues()` give `buildCsv()`, `buildRow()` and `storeSql()` one lead order, the two columns after `submitted` and `item_order`. Save tests S11 to S15 and send tests T13 and T14. Minor amendment: the three fixtures T4 names were written in T2 because the send tests read them, and a fourth was added, `responses-hitopbr-prolific-shuffled.csv`, the page's capture under `prolific: true` and `shuffle: true`, which AC6 copies into hitop as `responses-hitopbr-prolific.csv` (the by-rule file of that name in hitop-form is the no-shuffle comparison fixture). Save, send, link and network specs green (76 passed).
- 2026-09-24: T3 done in hitop-form: a confirmed send with `complete` calls `location.assign()` and draws no sent screen; the two saved screens add a "Then continue to <host>." link after the file name. Tests S16, T15, T16 and N7. T15 reads the headings the page draws through a mutation observer and an exposed function, because a route handler that reads the page while holding the navigation request deadlocks.
- 2026-09-24: T4 done in hitop-form: the "Recruit through Prolific" box and hint, the "Completion URL" field, the participant-field refusal, the completion refusal through `checkCompleteUrl()` with the builder's own prefix, the placeholder suffix appended to the printed link as text, and `storeSql()`'s fourth argument. Link tests L10 to L13; L9's hint locator scoped to its box now that two `label.check` exist (29 passed).
- 2026-09-24: T5 done. hitop-form README: steps 6 and 7 of "Make a study link" with the four Prolific pages cited, the participant screens, the file and row shape, the SQL sentence, the test table. hitop: `responses-hitopbr-prolific.csv` copied from hitop-form's `responses-hitopbr-prolific-shuffled.csv` at `2bd57b9` into `tests/testthat/fixtures/` and `inst/examples/` with README rows; its reader test; the online-collection article's Prolific section, its column sentences and "Who holds the data"; the modules article and the PID-5 vignette name the two columns; `?read_form_responses` says the page writes them; NEWS. `devtools::document()` rerun; the article renders against the installed package with the three identifier columns printed; `pkgdown::check_pkgdown()` clean.
- 2026-09-24: T6 done. The tests were written with each task (G10, G11; S11 to S16; T13 to T16; L10 to L13; N7), the download promise created before each walk through `awaitDownload()`. Twelve plants, each a one-line reversal applied by `sed` and restored by `git checkout`, each went red on the tests that cover it: the `prolific` type check (3 failed), the participant conflict (1), the `https:` bar on `complete` (3), the two columns (14), the identifier from the address (3), the placeholder read (2), the printed suffix (3), the SQL columns (2), the builder's participant refusal (1), the builder's completion check (1), a sent screen drawn before the navigation (2), and the saved-screen link (2).
- 2026-09-24: T7's local half done: `npx playwright test` 177 passed on the checkout; `devtools::test()` FAIL 0, WARN 0, SKIP 15 (pre-existing merge-base skips), PASS 20006; `pkgdown::check_pkgdown()` clean. The hitop-form merge from Jeff's terminal and the deployed-page dispatch belong to review.
- 2026-09-24: claim audit: 150 claims read, 2 corrected — hitop-form/tests/guard.spec.js (a number is refused as not text, no value shown), hitop/vignettes/articles/online-collection.Rmd (the two columns sit after `submitted` or `item_order`, not after the item columns). One risk fixed: `WRITE_FIXTURES` would have overwritten the by-rule prolific CSV with a capture; its case is now `byRule` and the write skips it. The reader's one re-read of the three sites is pending; checkpoint committed before it returns.
- 2026-09-24: the reader's re-read found all three corrected sites correct. All tasks checked, local checks clean; status review. Two branches await review: hitop `m118-form-prolific` (docs, fixture, test, NEWS) and hitop-form `m118-form-prolific` (the page, the builder, tests, fixtures, README).
- 2026-09-24: /milestone-review returned the milestone to in-progress. AC1 fails on F1 (a `complete` number is refused with no value shown), AC3 on F4 (L12 probes a form AC1 does not list), AC5 on F2 (the article's completion-URL sentence carries no citation). AC2, AC4 and AC6 verified. The other lenses found nothing. Defect return 1. The fixes and the fix-now findings F3, F11 and F13 go to /milestone-implement, the rest to the re-review gate.
- 2026-09-24: /milestone-implement resumed on the return. T8 added as a minor amendment carrying the six fixes. All six edited in both repos. Each fix was planted once by reversing it and its test went red: F1 on G11's number probe, F3 on the no-store save and the HTML-body unconfirmed send, F4 on L12 with the builder's check bypassed. The article renders with the new citation. The full hitop-form suite and the claim-audit reader are running at this checkpoint. hitop-form's changes are staged, to be committed on green.
- 2026-09-24: the full hitop-form suite passed, 177 of 177, and the six fixes are committed at hitop-form b781ea7. T8 done.
- 2026-09-24: claim audit: 154 claims read, 1 corrected — hitop-form/form.js (the header comment now names `PROLIFIC_PARAMS` among link.html's imports, hitop-form 2nd commit after b781ea7). The reader also flagged "asks that all three be saved" in the README and the article, then withdrew it on a verbatim fetch: article 445178 says to configure the software to save the parameters, as the source note's line 14 quotes. Both sentences stand as written. The reader's one re-read confirmed the header comment.
- 2026-09-24: all tasks checked, local checks clean; status review. The re-review's fresh runs are its own.

## Decisions

## Review

- 2026-09-24 sync: `origin/main` is unmoved under both branches (hitop 7 commits ahead at b2b85a23, hitop-form 6 ahead at 1768052). No PR exists in either repo. `cairn_validate` exits 0 with 24 advisories (dangling early D-ids and one references staleness), none a gate item.
- AC1 evidence: the hitop-form suite ran this session, 177 passed in 1.4 min. G10 refuses `prolific` as `"true"`, `1` and `null`. Each message names the field and the value, and no Begin button or item renders. G10 also refuses `prolific: true` beside `participant: "g10"` naming both, and accepts `prolific: false` beside a participant. G11 refuses `complete` as `http://127.0.0.1`, `http://localhost` and `javascript:alert(1)` as not starting with `https://`, with the value shown. It refuses `not a url` as not a web address, with the value shown. It refuses `https://user@example.com/done` and `https://user:pass@example.com/done` as carrying a user name or password, with the value shown. It refuses `7` as not text. It accepts the Prolific completion address. `checkCompleteUrl()` at `form.js:235` shares `parseAddress()` with `checkStoreUrl()`, and `link.html` imports it for the builder's field.
- AC2 evidence: same run. S11 saves the HiTOP-BR under `prolific: true` with the three parameters in the address. It sees no identifier field, `participant` equal to the `PROLIFIC_PID`, and the header `study … submitted, prolific_study, prolific_session, hitopbr_01 …` against the by-rule fixture `responses-hitopbr-prolific.csv`. Under shuffle the pair follows `item_order`, read as cells 6 and 7 by `expectShuffled()`. S15 checks the committed capture `responses-hitopbr-prolific-shuffled.csv` against its own `item_order`. T13 posts the row under `prolific` to a webhook and to a Supabase store, with and without shuffle. The Supabase keys equal the matching SQL fixture's column lines, 7 or 8 lead columns plus 45. S12 walks with a real `PROLIFIC_PID`, `STUDY_ID={{%STUDY_ID%}}` and no `SESSION_ID`, and reads `["", ""]`. S13 shows the identifier field for an absent, a blank (`%20%20`) and a placeholder `PROLIFIC_PID`. The placeholder walk writes the typed identifier with both columns filled. S14 and T14 carry all three parameters with no `prolific` field, with and without shuffle. The file and the row then equal the pre-existing shape with no Prolific key. `leadColumns()` at `form.js:455` is the one lead order for `buildCsv()`, `buildRow()` and `storeSql()`.
- AC3 evidence: same run. L10 checks the box's label and five hint clauses. An unchecked build carries no `prolific` field and no placeholder. A checked build decodes to `prolific: true`, and the printed text ends in `&PROLIFIC_PID={{%PROLIFIC_PID%}}&STUDY_ID={{%STUDY_ID%}}&SESSION_ID={{%SESSION_ID%}}`. The link opened as printed asks for the identifier. L11 refuses the box beside participant `l11` by name, with no link built. L12 puts `complete` in the link, and refuses `http://example.com/done` through the shared check with the builder's prefix and no link built. L13 compares the SQL under the box to `supabase-hitopbr-prolific.sql` byte for byte, and with the random-order box to `supabase-hitopbr-prolific-shuffle.sql`. Both fixtures are spliced by rule from the existing files, as the fixtures README records, the pair after `"submitted"` and after `"item_order"`. L7 is unchanged and green: unchecked, the SQL equals `supabase-hitopbr.sql` and `supabase-hitopbr-shuffle.sql`.
- AC4 evidence: same run. T15, for a webhook and a Supabase store, fulfils the completion address through `page.route()` and sees exactly one GET to it. A mutation observer records every heading drawn. The set is `{"HiTOP-BR"}` with no "Thank you", and there is no download. N7 asserts the network set after Finish equals the page's files, the export, the store and the completion address, reached once. S16 (no store) and T16 (a 500 from the store) show the saved screen with `p.complete a` after `code.filename`, in document order `CODE, A`. The link's `href` is the address and its text is `app.prolific.com`. Zero requests reach the address after a five-second wait, and the page stays on its own URL. N4 and the pre-existing save and send screen tests are unmodified and green.
- AC5 evidence: read this session. hitop-form README steps 6 and 7 of "Make a study link" cover the box, the three placeholders cited to the API reference, saving all three and the completion URL cited to article 445178, the 24-character preview ID cited to 445131, and the redirect cited to 445127. "What the participant sees" covers the identifier from the address and the completion link after the file name. "Where the file lands" and the sheet section place the pair after `submitted` or `item_order`, empty on a placeholder. The Supabase SQL sentence and the test table's five rows are updated. hitop's `online-collection.Rmd` gains "The Prolific route" with the same four citations, plus the sheet and column sentences. Its "Who holds the data" says the Prolific ID identifies the participant to Prolific, and that the host receives the three parameters with each page load. The four URLs are the source note's Provenance block. `NEWS.md` carries the entry with no milestone number. The article rendered this session against the installed package and prints the two identifiers.
- AC6 evidence: `tests/testthat/fixtures/responses-hitopbr-prolific.csv` equals hitop-form's `responses-hitopbr-prolific-shuffled.csv` at 2bd57b9 with the CR bytes removed, and `inst/examples/` holds an identical copy. It holds 0 CR bytes and `data-raw/check_line_endings.R` passes. The fixtures README and the examples README each carry its provenance row naming the capture command, the link fields and the Prolific ID. `devtools::test()` ran this session: FAIL 0, WARN 0, SKIP 15 (the pre-existing merge-base skips in six files), PASS 20006. The test at `test-read_form_responses.R:1035` reads the file through `read_form_responses()`, asserts the header order `… submitted, item_order, prolific_study, prolific_session`, the result's lead order, both cells as 24-character hex values that differ, `participant` as the Prolific ID, `item_order` as a rearrangement of 1 to 45, and each item's value from the pattern at its position. It scores through `expect_hitopbr_export()`, which calls `score_hitopbr()` against `table_means()` built from `hitopbr_items` and `hitopbr_scales`.
- AC7, partial: `npx playwright test` 177 passed on the checkout, `devtools::test()` FAIL 0, WARN 0, SKIP 15, PASS 20006, `pkgdown::check_pkgdown()` no problems, and the article rendered against the installed package. `devtools::check()` finished after the return: 0 errors, 0 warnings, 0 notes in 6 m 22 s. Not ticked, pending the re-review's fresh run.
- Consistency gate, run before the return: `cairn_validate` exit 0. `devtools::document()` leaves no diff. `README.md` and `README.Rmd` share one commit (52067e18) and the branch touches neither. `pkgdown::check_pkgdown()` finds no problems. `NEWS.md` carries the entry. No new top-level file. No DESIGN principle changed, so `cairn_impact` is skipped. `devtools::check()` 0 errors, 0 warnings, 0 notes.
- Independent review, three fresh lenses. [S] blame-history: no findings. It read the form.js history of `checkStoreUrl()`, the lead-column writers, the sent and saved screens and the unload guard, the reader's history, D-071, D-070, D-064 and D-038, and the M111 to M117 archives. [S] prior-review: no prior-review evidence contradicted. It read the M096, M108 and M111 to M117 archived Review sections and LESSONS. Both repos' PR-comment probes returned empty, and the thread walk was skipped. M117's carried-forward article item is closed by this diff. [O] diff-bug: no logic bug in the page, the builder or the reader, all four new fixtures rebuilt from their stated rules and matching, the paths without `prolific` or `complete` unchanged except F5. Its 14 findings, ranked as reported, each verified against the code before disposition:
  - F1 (AC1 fails as written): `complete` as the number `7` is refused as "it is not text." with no value shown, through `parseAddress()` at `form.js:248`. AC1 requires the value shown, as `shuffle` shows it. The claim audit changed the G11 header to describe the gap rather than fix it. Disposition: return. Fix at implement: `checkCompleteUrl()` refuses a non-string before `parseAddress()` with the value shown, and G11 asserts it, leaving the store's "its url is not text." message as it is.
  - F2 (AC5 fails as written): the article's "Give the completion URL from the study's page on Prolific" sentence at `online-collection.Rmd:181` carries no citation. The fact is article 445178, "Returning Participants to Prolific", which the README's step 7 cites. Disposition: return. Fix at implement: add the citation to that sentence.
  - F4 (AC3 fails as written): L12 probes the builder's completion refusal with `http://example.com/done`, which is not one of AC1's refused forms. Disposition: return. Fix at implement: probe with `http://localhost`.
  - F3 (AC4's "unmodified" clause): `save.spec.js` on `origin/main` has no saved-screen test, so that part of the clause is vacuous, and no test asserts `p.complete` is absent without `complete`. The assertions that exist (N4, the T6 unconfirmed walks) pass unmodified, so the criterion holds. Disposition: fix now at implement, with the return: S1's no-store save and one T6 walk assert no `p.complete` element.
  - F5 (outside the criteria): `parseAddress()` checks credentials before the scheme, so a store address `http://user@example.com` now gets the credentials message where it got the scheme message. No test covers it. Disposition: for the re-review gate, proposed fix now (check the scheme first in `checkStoreUrl()` as before, or accept the new order and say so).
  - F6 (outside): after `location.assign()` the form page stays with its buttons disabled and "Sending…" showing, so a Back from Prolific or a failed navigation shows no sent screen. Disposition: for the re-review gate, proposed candidate row.
  - F7 (outside): any `https://` completion host is accepted, so a crafted link can send a participant anywhere, with a confirmed send from the attacker's own webhook. Follows the plan-gate choice (D-071). Disposition: for the re-review gate, proposed reject as the recorded choice, its falsifier standing.
  - F8 (test gap): the "without the field" walks carry `participant: 'p001'`, so no test covers a link with no participant, no `prolific` field and a real `PROLIFIC_PID`, where the start screen must still ask. Disposition: for the re-review gate, proposed fix now.
  - F9 (outside): the docs do not warn against also turning on Prolific's own URL-parameter option, an open question in the source note. Disposition: for the re-review gate, proposed candidate row.
  - F10 (outside): `PROLIFIC_PID` is not checked for format, so any address text becomes the identifier, a leading `=` included. A typed identifier has the same exposure. Disposition: for the re-review gate, proposed reject.
  - F11 (trivial): the form.js header comment says link.html imports `encodeConfig()` alone. Disposition: fix now at implement.
  - F12 (trivial): the credentials refusal repeats the address, password included, in the alert. The password is already in the link. Disposition: for the re-review gate, proposed reject.
  - F13 (trivial): `buildCsv()` spreads an already fresh array. Disposition: fix now at implement.
  - F14 (information): the article does not say the completion host sees the navigation; the default referrer policy sends the origin alone. Disposition: for the re-review gate, proposed reject.
- 2026-09-24 return: AC1 (F1), AC3 (F4) and AC5 (F2) fail as written. Status back to in-progress. Defect return 1 of this milestone.
- 2026-09-24 second pass, sync: `origin/main` unmoved under both branches (hitop 02beba88, hitop-form ae4ce0a). No PR in either repo. All seven boxes unticked at the start of this pass and re-ticked only against the evidence below. `cairn_validate` exit 0, 24 advisories, none a gate item.
- AC5 evidence, pass 2: read this session. The article's "The Prolific route" now cites article 445178 at both facts it takes from it, saving the three parameters and the completion URL's form and section ("Returning Participants to Prolific"), and the rendered HTML holds two links to that article. The other three citations (the API reference for the placeholders, 445131 for the 24-character preview ID, 445127 for the recommended redirect) stand. The README's steps 6 and 7, "What the participant sees", the file and row descriptions, the SQL sentence and the test table are as recorded in the first pass. "Who holds the data" is unchanged from the first pass. `NEWS.md` carries the entry and no milestone number. The article rendered against the installed package this session.
- AC1 evidence, pass 2: the hitop-form suite ran this session at ae4ce0a, 177 passed in 1.5 min, 0 failed. G10 and G11 as in the first pass, with one change: `complete` as `7` is now refused as "it is not text, and it is 7.", the value shown, through the type check that `checkCompleteUrl()` runs before the shared parse. The F1 plant (the value dropped from that message) went red on G11's number probe during T8.
- AC2 evidence, pass 2: same run. S11 to S15, T13 and T14 pass unchanged from the first pass, so the evidence recorded there stands at this head.
- AC3 evidence, pass 2: same run. L10, L11 and L13 as in the first pass. L12 now probes the builder's completion refusal with `http://localhost`, one of AC1's refused forms, and reads "The completion URL could not be used: it must start with https://, and it is "http://localhost"." with no link built. The F4 plant (the builder's check bypassed) went red on L12 during T8. L7 unchanged and green.
- AC4 evidence, pass 2: same run. T15, T16, S16 and N7 as in the first pass. The no-store save (the S1 loop) and every unconfirmed send (the T6 loop and the Supabase unconfirmed loop) now also assert that the saved screen holds no `p.complete` element without a `complete` field. The F3 plant (the link always drawn) went red on both during T8. N4 unmodified and green.
- Independent review, pass 2, three fresh lenses at hitop-form ae4ce0a and hitop 0c45a189. [S] blame-history: one finding, the first pass's F5 restated (the store address's credentials check now runs before its scheme check), nothing else contradicting recorded intent or D-071, D-070, D-064 or D-038. [S] prior-review: no prior-review evidence contradicted, the six fix-now items of the first pass present as described, the seven deferred ones not claimed as fixed, both PR-comment probes empty. [O] diff-bug: no acceptance criterion fails as written and no logic bug. Its 13 findings, ranked as reported, with the first-pass label where one applies:
  - P2-F1 = F5 (outside): the store-address message order. P2-F2 = F6 (outside): the form left disabled after the redirect. P2-F3 = F7 (outside): any `https://` completion host. P2-F5 = F8 (test gap): no walk with no participant, no `prolific` field and a real `PROLIFIC_PID`, and none with `prolific: false` beside the parameters. P2-F10 = F10 (outside): no format check on `PROLIFIC_PID`. These carry their first-pass proposed dispositions to the gate.
  - P2-F4 (AC4 holds): the "pass unmodified" clause is vacuous for save.spec.js and the existing assertion lines are byte-identical. Noted, no action.
  - P2-F6 (AC1 holds): a blank participant beside `prolific: true` is accepted, because a blank identifier is none before the conflict check. Noted, consistent with the page's rule.
  - P2-F7 (AC2 holds): the shuffled prolific save is checked for shape and order, not against a fixed file, since a random order cannot match one. Noted.
  - P2-F8 (doc): "asks that all three be saved" called a stretch. The claim audit's reader withdrew the same point on a verbatim fetch, and the source note's line 14 quotes the page. Proposed reject.
  - P2-F9 (outside): the builder gives no warning when the Prolific box is checked with no completion URL or no store. Proposed reject: both are legal links, the README says the completion URL is optional.
  - P2-F11 (doc): the form.js header says the navigation is "the one further request" though a saved-screen link makes one when followed, and README step 6 omits the blank-ID case. Proposed fix now, two comment and prose edits.
  - P2-F12 (info): an older cached page ignores the two fields; `readProlific()` takes the first of repeated parameters. Noted.
  - P2-F13 (trivial): the "not text" branch in `parseAddress()` cannot fire for the completion check. Proposed reject: it still serves the store check.
