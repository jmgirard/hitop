# Roadmap

_Last hygiene check: 2026-09-02 (twenty-seventh pass, at M084 review: M084 archived and its row marked done; the finding-absorbing "four remainders" candidate row dispositioned under records-hygiene §7 rather than extended — split into a promotion-ready warning-contract row and a blocked-remainders row; M079's terminal row pruned under the five-row retention rule; no lesson added, none retired. Bytes: `ROADMAP.md` under 24,000; `LESSONS.md` 45 lines / 19,992 bytes, under 20,000.)_
_Pre-migration history: see `cairn/legacy/` and git log (M001–M017 done there; IDs continue — next new milestone is M086)._
_Release 0.2.0 prepared 2026-08-29: NEWS consolidated, `document()` no diff, `R CMD check` 0/0/0, pkgdown and URLs clean. Tag and GitHub release pending the maintainer._

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M085 | The `rename_*`/`label_*` family's warning contract | review | — | normal | milestones/M085-rename-label-warning-contract.md |
| M084 | The `label_*()` family's unpadded-item report | done | — | normal | milestones/archive/M084-unpadded-item-report.md |
| M083 | `label_pid5()` | done | — | normal | milestones/archive/M083-label-pid5.md |
| M082 | `rename_pid5_items()` | done | — | normal | milestones/archive/M082-rename-pid5-items.md |
| M081 | Every item number the package ships is an integer | done | — | normal | milestones/archive/M081-item-number-integers.md |
| M080 | The shipped PID-5 datasets name item columns as the online exports do | done | — | normal | milestones/archive/M080-pid5-item-column-names.md |

## Candidates

- The three `ku_*` datasets store their 550 response columns as doubles where all five `sim_*` datasets store theirs as integers, so the package's real and simulated example data disagree on the type of the same kind of value. Left out of M081, which retypes item numbers only: these hold answers, and `data-raw/ku_data.R` reads a university network drive so cannot be re-run — evening them out needs an in-place conversion script against a pinned commit plus its own proof that no answer moved. `R/sysdata.rda` carries the same split (`pid_instructions$options$value` integer, the two HiTOP ones doubles), and internal data is out of reach of the `data(package = "hitop")` sweep M081 shipped. Promote if a user reports the disagreement, or when the `ku_*` datasets are next regenerated — added 2026-09-02, extended and compressed 2026-09-02 — lineage: M081 (plan gate; review finding 8)
- Twenty verification-reach gaps four reviews left open, none a wrong result or verdict today; each archive summary names its own, and this row exists to keep them visible. M079's and M080's ten run to test reach, not behavior — missing guards, unstated ordering and zero-row assumptions, sweeps blind to a `paste0()`-built name, a pad width pinned as a literal in two suites, and `check_pid_item_names.R` running in no workflow. M071's and M073's ten are M073's export-vignette sweep and M071's Table 1 extraction guards. Promote when the builders, their tests or the PID-5 generators are next edited, if either sweep returns a wrong verdict, or when the proof PDF is repinned — added 2026-08-30 and 2026-09-01, merged 2026-09-02, compressed 2026-09-02 — lineage: M071 (findings 1, 5, 7, 8), M073, M079, M080
- The builder smoke test M076 ships drives the Word download path only and goes red on a `webr.r-wasm.org` or `jmgirard.r-universe.dev` outage after one retry rather than reporting the outage as its own outcome; both declined at M076's plan gate. Seven deferred review gaps are named in its archive summary (F10, F11, F13 in part, F18-F20, F22). Promote if a generator breaks in Qualtrics or REDCap alone while the smoke test stays green, if false reds become routine, or when the harness is next edited — added 2026-08-31 — lineage: M076
- Two comments in the builder's `index.html` (above `tilesExactly` and above the R call setting it) say its start-up probe detects scale *overlap*. It cannot: `hitop_module()` sorts and de-duplicates items (`R/module.R:102`) before `identical(as.integer(items), seq_along(items))`, so overlapping scales pass. M074 fixed the shipped prose and the adjacent `downloadStem` comment, leaving these two; comment accuracy, not a live defect. The `as.integer()` there is now a no-op, so it can go in the same edit. Promote when a second instrument reaches the page, or when those comments are next edited — added 2026-08-31, extended 2026-09-02 — lineage: M063, M074 (finding 5), M081
- D-045(d) and its *Context* claim no succeeding call's return value changes; M060's own review disproved it. `norm_pid5(scored, scores = character(0), append = FALSE)` and the same `interval_hitopsr()` call each returned a 0x0 tibble before M060 and abort with `hitop_empty_selection` after, and the Context's differing-rows claim holds only under `append = TRUE`. `NEWS.md` was corrected at the M060 gate; the fix here is a superseding entry, `DECISIONS.md` being append-only. Promote when `DECISIONS.md` is next appended to, or if a reader takes D-045(d) as a statement of what the release did — added 2026-08-28 — lineage: M060 (review pass 2, finding 1)
- The HiTOP-BR scale printed `p-Factor` here is `p-factor` in the introduction manuscript — 15 occurrences, the capitalized form absent. `data-raw/verify_hitopsr_names.R` records the divergence in a comment but checks nothing. Both spellings derive the same stem, so no scored column moves; what changes is `hitopbr_scales$Scale`, the Word scoring page, and `available_scales()`. Still IP1 content needing a source and sign-off, and D-042 admits the manuscript for HiTOP-SR names only. Promote when a HiTOP-BR scale name is next sourced, or if a reader is surprised by the capitalization — added 2026-08-27 — lineage: M059 (AC4's second criteria audit)
- `Prolific data HiTOP-SR.sav` (Development Sample 2, N = 780) is on the `references/sources/` shelf, so Table 1's alphas, means and SDs could be recomputed rather than only transcribed. M041 verifies the transcription against the printed table and an item-count oracle — enough for a transcription error, not a typesetting one. Recomputation needs its own decisions, and is IP2 condition one for a package-wide Green & Yang (2009) categorical omega, which D-043 leaves unscheduled. Promote if a printed-table discrepancy is investigated with the table the suspect, or if a reviewer asks where the statistics come from — added 2026-08-27, compressed 2026-08-31 — lineage: M041
- Three descriptor-format gaps M054's review left open, none reachable from a file the package wrote: the reader takes JSON strings where the format documents numbers; `is_module()` accepts the deprecated `hitop_subset` class, falsifying the round-trip-identity claim for it; and `write_module()` checks the class but not the `nItems`/`items` invariant, so a hand-assembled module writes a file the reader refuses. Separately, a descriptor records scales but not the column names the export produces, so `score_hitopsr()` still needs `items` typed out. Promote on any user report, or when M055 lands — added 2026-08-24 — lineage: M054 (plan gate; findings 6, 10, 11)
- The builder app exposes fewer generator arguments than the package does, triaged down at three gates in three families. Word typography and header: no `title`, `font_size` or `font_family`, so every Word download is the default header at 10pt Times New Roman, and `title` sits under IP1 (D-037). Online exports: no `include_instructions` or `breaks`, so instructions always ship and pagination is every 15 items. Scoring key: no `include_scoring`, so the Word download always carries the key page. Promote per family: a study-specific header or larger print form; an unpaginated or instruction-free export; a form without the key — added 2026-08-22, compressed 2026-08-31 — lineage: M045, M049/M050
- Neither HiTOP-SR online generator checks that `id_prefix`, `block_name` or `form_name` is a name its target system accepts — a space in `id_prefix` or a capital in `form_name` yields a dictionary REDCap or Qualtrics rejects at import, with nothing said at generation. M050 adds type and shape guards but not format rules: no citable naming spec is on the `references/` shelf, and IP2 bars inventing one. Promote once such a spec is ingested, or if a user reports an import rejected on a name the package accepted — added 2026-08-23 — lineage: M050
- The five `*_qualtrics.txt` byte-locked artifacts in each of `inst/extdata/` and `pkgdown/assets/downloads/` are protected only by their directory's `-text` rule: carrying neither a NUL byte nor a CR, they are invisible to M047's line-ending guard and git reads them as text — the pairing that let a Windows checkout mutate them at M020. A blanket `*.txt -text` would freeze every ordinary text file, so `.txt` gained no extension rule where `*.qsf` did. The md5 lock still catches a byte change, so this is guard reach. Promote if an artifact `.txt` moves outside those directories — added 2026-08-22, compressed 2026-08-31
- The builder page follows the operating system's colour scheme and offers no control of its own, declined at the M051, M052 and M053 plan gates and left unplanned at M074's. Its token layer already states both schemes, so a control means an explicit override on `:root`, a stored choice, and a re-run of M051's contrast, focus and containment checks in both schemes. Promote if a visitor asks for a theme control — added 2026-08-24, narrowed 2026-08-30 — lineage: M051, M052, M053
- The builder's picker shows each scale's brief clinician definition and nothing else: the client-facing text and the 17 subscale definitions were put out of scope at M057's plan gate and stay only in `vignettes/articles/scales-hitopsr.Rmd`. Showing the client text means `available_scales()` returning a `Client` column beside `Brief`, a public return-value change owing NEWS; the subscale rows need a home in a picker that offers scales. Promote if a clinician wants that wording where they choose scales — added 2026-08-30, split from the builder-page row — lineage: M057
- M075 merges only the REDCap generators' export step; each still assembles its own data dictionary inline. Promote if a third instrument needs a REDCap generator, or if the two builders diverge — added 2026-08-31, replacing the generator-hygiene row M075 absorbed — lineage: M045, M075
- Three reach gaps M075's review left open on the REDCap writer, none a wrong result today; `milestones/archive/M075-redcap-zip-temp-path.md` names each — no assertion of its own on cleanup after a *successful* export, `~/out.zip` and Windows UNC destinations given a `getwd()` prefix, and a `{zip}` floor sourced from release notes and never exercised. Promote on a tilde or UNC destination reported refused, or when the writer is next edited — added 2026-08-31 — lineage: M075 (findings 1-3)
- Three blocked remainders M082 and M083 left on the PID-5 rename and label helpers, each declined at their plan gate: `label_pid5(target = "scales")` leaves `validity_pid5()`'s columns unlabelled (D-018 fixes those names to the scale-development papers, none on the shelf — IP2 work needing sign-off) and `score_pid5(calc_se = TRUE)`'s `_se` columns unlabelled (`calc_se` deprecated under D-049); both families take `version` rather than inferring it. M082's review deferred five more on `rename_pid5_items()`, none wrong on documented input — duplicate output names, ignored or duplicated `item_cols`, a duplicated regex escape, and a cli-wrapping-coupled test; each stated in full in the M082 review section, in git at `d867aa71^:cairn/milestones/M082-rename-pid5-items.md`. Promote per remainder: a citable source for the validity names; a user reporting an unlabelled `_se` column, an unwanted `version` argument, or a duplicated column name — added 2026-09-02, split 2026-09-02 (its unblocked items promoted to the row above) — lineage: M044, M077, M080, M082 (findings 2, 6, 9, 14, 15), M083
- The AST-walk self-check certifying that `test-plot_pid5.R` reads no plot internals discriminates by the *receiver* of a `data` extraction but not the *accessor*: `attr(p, "data")`, `p[[key]]` with a variable index, `do.call("[[", list(p, "data"))` and `base::`-qualified accessors all read past the guard (verified green). No such read exists in the file today, so this is guard strength, not a live defect. The same walk treats only `for` as a loop, so `while`/`repeat` bodies escape the in-loop `info =` rule. Promote on any such read reaching that file, or on the guard being reused for a second test file — added 2026-08-13 — lineage: M040
- M060's output-column collision guard is the only cli-message guard in the suite whose probes vary the message's cardinality and the colliding column's position in `data` — a 30-column probe (past cli's 20-item elision) and a first-column probe, added at the M060 repair after a message naming 20 of 76 colliding columns reached review. The package's other message-content reports (`validity_pid5()`, `norm_pid5()`, `interval_hitopsr()`) each assert on one exemplar. Promote when one of those is next extended, or if a caller reports a truncated list — added 2026-08-28 — lineage: M060
- Removing `calc_se`, the `_se` columns it adds, `calc_sem()` and the engine's `mask_se_na` flag — a GP2 scored-output change for the next breaking release. M069 deprecated it instead (it defaults to `FALSE`, so the warning reaches only deliberate use) and absorbed the standing `mask_se_na` row, retired as moot at its plan gate: unifying output bound for deletion buys nothing (DESIGN Known issue #4). The two HiTOP forms gain `interval_*()` in exchange; the PID-5 has none, so removal leaves its callers `reliability_pid5()`. Promote at the next breaking release, or on a user reporting the warning as noise — added 2026-08-30, compressed 2026-08-31 — lineage: M013, M069
- The full-form and brief-form PID-5 vignettes say nothing about `calc_se`, where the other four carry a paragraph saying it is deprecated, what the number is, and what to use instead. M073 dropped the standing ask for worked examples there — the argument warns and is scheduled for removal — and its `rank_scales()` half graduated into M073, leaving those two vignettes with no mention. Promote if a reader reports the deprecation warning unexplained by the vignette they followed — added 2026-08-30, narrowing the 2026-08-04 vignette-coverage row, compressed 2026-08-31 — lineage: PR #43, M039, M069, M073
- Two width promises, each resting on one hand measurement and guarding no later edit. `plot_pid5()`'s documented ~7-inch figure-width floor for value labels was measured on one device with one font, with essentially no margin at the worst case, and the binding constraint is panel width, which differs by an inch or more between levels. And M036 proves the PID legend's two lines fit the printable column visually, confirming that build only. Promote if a clipped label is reported at or above the documented width, or if a mid-phrase wrap reaches a committed form unnoticed — added 2026-07-31, merged 2026-08-26 — lineage: M036, M039, M040
- Hierarchical PID-5 profile display in the shape of Jeff's HiTOP-SR mockup — one block per level, each a table of scale names, `T [95% CI]` values and a per-row glyph plot with SD gridlines, indent markers and a distinct glyph per level. Shared in chat 2026-08-04, committed nowhere. Adapting it needs decisions `plot_pid5()` does not make: the PID-5 hierarchy is two levels against the mockup's four, and the 10 non-domain-defining facets have no parent. Its CI bars and cutoff fills are the two rows above, each blocked by its own condition. Promote when hierarchical HiTOP-SR scoring is finalized, or if the two-level PID-5 form is wanted on its own — added 2026-08-04, compressed 2026-08-31
- An anchor catching an *upward* percentile displacement (every row taking its successor's value) — M034 closes the downward direction only; its second anchors happen to cover the upward direction on 34 of the 66 T-scored columns, so 32 would need a third hand-read anchor, and no single anchor can serve both directions on the 14 columns with no interior T differing from both neighbours. Promote if an upward displacement ever reaches the shipped dataset — added 2026-07-31, figure corrected 2026-07-31 at M034 review — lineage: M034
- PID-5-BFP (36-item) data, scoring, exports — awaiting materials — added 2026-07-16 — `legacy/ROADMAP.md` Phase 1
- HiTOP-HSUM scoring + reliability + tutorial — awaiting Society feedback — added 2026-07-16 — `legacy/ROADMAP.md` Phase 1
- Severity bands / elevation thresholds on PID-5 profile plots — D-029 ships M038's plot with none, because no source in `references/` or `SOURCES.md` supplies boundaries and shading by severity characterizes a score; `devel/plotting.R`'s prototype bands (T = 40/80, green→red) are the shape wanted. Promote if a published, citable set of PID-5 profile elevation thresholds is ingested as a `references/` page — added 2026-08-01 — lineage: M038
- Confidence intervals on PID-5 profile plots — superseded 2026-08-13 by Johannes Zimmermann's norms proposal (four artifacts supplied by Jeff; not on the `references/sources/` shelf — observed 2026-08-13). Method: the regression-based true score with scale correction (Schmukle, 2025, *Assessment*, 33, 817-825). Blocker: markon2024 prints no reliability coefficient this repo has ingested; the one candidate, Krueger et al. (2012) Table 2 (PMC3413381, unverified), mismatches the norms three ways; Jeff has a further source. Promote once a per-scale reliability traceable to the norming sample is ingested — added 2026-08-01, corrected 2026-08-04, compressed 2026-08-31 — lineage: M038
- Multi-source `pid_norms` schema — Johannes Zimmermann's proposal (2026-08-13) adds six columns plus a `pid_norm_sources` tibble and gives `norm_pid5()` `source`/`age`/`gender`/`ci` arguments; every statistic stays a shipped table cell computed in `data-raw/`, so D-022 is untouched. Three package-side issues it leaves: an `as.integer()` T at `R/norm_pid5.R:272` against decimal percentile-based T (Jeff chose double when the schema moves — GP2, owing NEWS and a D-entry); a censored row fitting neither branch of `norm_select()`'s tie-break at `R/norm_engine.R:82`; Title Case labels needing a camelCase crosswalk. No German row ships until the corrected OSF tables (hwxnj) are citable (IP3). Promote when those are public, or if a second norm source arrives — added 2026-08-13, compressed 2026-08-31
- Guidance on reading T against percentile at the floor of the PID-5 norms — derived 2026-08-13 from shipped `pid_norms`: 22 of the 66 T-scored columns sit above T = 40 at the lowest printed raw, median floor T = 39, where the printed percentile is 0.00 on most columns against the ~0.136 that T implies under normality. This is the linear-T floor pathology Johannes Zimmermann raised against the HiTOP-SR app (RR02, D-022). The book prints empirical percentiles beside the linear T and `norm_pid5()` returns both; missing is documentation on which to trust where. Promote if a reader takes a floor T as a meaningful elevation — added 2026-08-13, compressed 2026-08-31
- Multi-respondent PID-5 profile plots (one line per person, or panels per person) — M038 aborts on any input with more than one row; group profiles need their own decisions about legends, coloring, and how many series is too many. Promote if a researcher is blocked plotting a sample rather than an individual — added 2026-08-01 — lineage: M038
- PID-5 demographically stratified norms — the sex/age-stratified tables appear nowhere in the markon2024 appendix and still await the maintainer; would extend `pid_norms` with no new export — added 2026-07-30, narrowed 2026-07-31 (the facet half graduated to M033) — lineage: M025
- PID-5 Informant Form (IRF) norms — markon2024 A–10/A–11 (domain, trait) and A–12 (SRF/IRF descriptives + T-score difference) arrived 2026-07-30; the package has no IRF surface today, so this is a new instrument version, not a `pid_norms` extension — added 2026-07-30 — lineage: M025
- HiTOP-SR/BR normative data and norming functions — source workbooks arrived 2026-07-30 (`HiTOP-SR-Final.xlsx`, `B-HiTOP overview.xlsx`); both carry sample descriptives, not raw→T lookup tables, so the norming design differs from `pid_norms` — added 2026-07-30, corrected 2026-08-06 (maintainer): no official SR or BR norms exist and the development workbooks are not trustworthy for clinical norming, so IP3 bars a norming function until published, citable tables exist — this row is norming *research*, not a plannable milestone — `legacy/ROADMAP.md` Phase 3
- Clinical reporting & release (individual reports, bass-ackwards analyses, CRAN submission, package paper) — added 2026-07-16 — `legacy/ROADMAP.md` Phase 4
- Someday-maybe cluster (HiTOP-SR/BR validity scales) — added 2026-07-16, narrowed 2026-08-01 (the `devel/plotting.R` plotting-helpers half graduated to M038) and 2026-08-21 (the Shiny scoring app half graduated to M045, which deletes the `inst/shiny/app.R` stub) — `legacy/ROADMAP.md`
- Multi-language download UI: per-language buttons on the instrument download pages won't scale once translations arrive; a selector or grouped layout is wanted — added 2026-07-17 — lineage: M021
- Score subset-collected HiTOP-SR data with no descriptor in hand, by working out from the column names which scales are fully present — needs its own decisions about partial coverage and name matching; useful for data that arrived from elsewhere. Promote if a researcher is blocked scoring a short form they did not generate — added 2026-08-01 — lineage: M037
- "Module" now names two things: a chosen set of scales (`hitop_module()`, M043) and the HiTOP-HSUM, whose Society name is the Harmful Substance Use *Module*. No user-facing text conflates them today, but the collision bites when modularization reaches other instruments, since "an HSUM module" would be ambiguous. IP1 bars renaming the Society's instrument, so any fix is on this package's side. Promote when modularization extends beyond the HiTOP-SR, or if a user reports the ambiguity — added 2026-08-21 — lineage: M043
- Generalize modularization to BR/PID-5: extend the subset-descriptor + subset generation/scoring to HiTOP-BR (overlapping scales, e.g. p-Factor spans all items) and PID-5 (facets partition, domains derive from facets) — added 2026-07-17 — lineage: M024 — annotated 2026-08-21: M043 renames this family to "modules" and deliberately precedes this row so the generalization inherits one vocabulary — annotated 2026-08-26 (M057 review): two latent traps to clear when a second instrument arrives, in `validate_module_instrument()`'s supported-set derivation and `scale_definitions()`'s `ref$Subscale` read; neither is reachable while both maps hold exactly `hitopsr`
