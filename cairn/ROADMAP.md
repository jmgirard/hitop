# Roadmap

_Last hygiene check: 2026-08-29 (M063 done and archived — the builder names both files of a build for the format, scope and shuffle that made it, verified over eight driven builds giving sixteen distinct names, with the merged builder commit `afb1535` deployed byte-identical. The R package is untouched: `check()` 0/0/0, `test()` PASS 15504. Four of the review's eight prose findings were fixed before the merge and three routed to the builder-page candidate row at Jeff's disposition. Both byte budgets were over at the start of this pass — `ROADMAP.md` 39,318 and `LESSONS.md` 25,286 — and the widest candidate rows and the previous stamp were compressed to bring this file back under. M064 and M065 stay planned.)_
_Pre-migration history: see `cairn/legacy/` and git log (M001–M017 done there; IDs continue — next new milestone is M066)._

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M063 | Every file the builder writes says which form it belongs to | done | M062 | normal | milestones/archive/M063-builder-download-naming.md |
| M064 | The builder refuses loudly when the package it installs no longer matches it | planned | — | normal | milestones/M064-builder-version-guard.md |
| M065 | The generators announce the descriptor they write | planned | — | normal | milestones/M065-descriptor-write-announced.md |
| M062 | Both files a builder download produces reach the visitor's disk | done | — | normal | milestones/archive/M062-builder-download-handover.md |
| M061 | The reliability family invents scale display names instead of reading them | done | — | normal | milestones/archive/M061-scale-name-consistency.md |
| M060 | The scoring and conversion family refuses two argument shapes it lets fall through | done | — | normal | milestones/archive/M060-append-collision-empty-selection.md |
| M041 | Confidence intervals for HiTOP-SR scale scores | done | M059 | normal | milestones/archive/M041-hitopsr-score-intervals.md |

## Candidates

- D-045(d) and its *Context* state something M060's own review disproved: that no succeeding call's return value changes. Measured against the merge base, `norm_pid5(scored, scores = character(0), append = FALSE)` and the same call to `interval_hitopsr()` each returned a 0x0 tibble before M060 and abort with `hitop_empty_selection` after it, and the Context's claim that a zero-length selection reached `data.frame()`'s differing-rows error holds only under `append = TRUE`. `NEWS.md`, the user-facing surface, was corrected at the M060 review gate; `DECISIONS.md` is append-only, so correcting the D-entry means a superseding entry rather than an edit, and the milestone's Scope prose carries the same claim in the archive. Nothing shipped is wrong — the abort is what AC2 requires and what D-045(b) decides; the record of its consequences is. Promote when `DECISIONS.md` is next appended to, or if a reader takes D-045(d) as a statement of what the release did — added 2026-08-28 — lineage: M060 (review pass 2, finding 1; NEWS half fixed at the gate at Jeff's selection)
- The HiTOP-BR scale this package prints as `p-Factor` is printed `p-factor` in the introduction manuscript — 15 occurrences, with the capitalized form appearing nowhere in the document. `data-raw/verify_hitopsr_names.R` sees it only incidentally: Table 1's Superspectra and Spectra block holds the eight HiTOP-BR scales, and M059 pins that block by name rather than reconciling it, so the divergence is recorded in the script's comment and nowhere checked. Both spellings derive the same stem, so `score_hitopbr()` returns `hbr_pFactor` either way and no scored column would move; what changes is the printed name in `hitopbr_scales$Scale`, on the HiTOP-BR Word questionnaire's scoring page, and in `available_scales()`. It is still IP1 content needing a source and sign-off, and D-042 admits the manuscript for HiTOP-SR scale names only, so reading a HiTOP-BR name from it is a decision of its own. Promote when a HiTOP-BR scale name is next sourced, or if a reader is surprised by the capitalization — added 2026-08-27 — lineage: M059 (AC4's second criteria audit)
- `Prolific data HiTOP-SR.sav` (Development Sample 2, N = 780) is on the `references/sources/` shelf, so Table 1's alphas, means and SDs could be recomputed from the responses they were printed from rather than only transcribed. M041 verifies the transcription against the printed table and a CI-runnable item-count oracle, enough to catch a transcription error but not a typesetting one in the source. Recomputation needs its own decisions — the paper's exclusion rules, its alpha estimator, what counts as agreement — and would surface rounding differences to adjudicate, so it is not folded into M041. It is also the prerequisite for a package-wide Green & Yang (2009) categorical observed-score omega, which RR05 judges best-defined for the interval formula and D-043 leaves unscheduled: calibrating this pipeline against the printed table is IP2 condition one for shipping any coefficient the source does not print. Promote if a discrepancy between the printed table and the package is investigated with the printed table the suspect, or if a reviewer asks where the statistics come from — added 2026-08-27 — lineage: M041 (plan and implement gates)
- Three descriptor-format gaps M054's review left open, none reachable from a file the package wrote: the reader takes JSON strings where the format documents numbers (`"items": ["66"]`); `is_module()` accepts the deprecated `hitop_subset` class, so `write_module()` writes one and `read_module()` returns a `hitop_module`, falsifying the round-trip-identity claim for that class; and `write_module()` checks the class but not the `nItems`/`items` invariant `module_engine_inputs()` guards, so a hand-assembled module writes a file the reader refuses. Separately, a descriptor records scales but not the column names the export produces (`HSR_007` from Qualtrics, `hsr_007` from REDCap), so `score_hitopsr()` still needs `items` typed out; a `columns` field was declined at the M054 plan gate because those names exist only once a generator has run, and matching columns silently is the failure GP2's warnings prevent. Distinct from the M037 no-descriptor row below. Promote on any user report, or when M055 lands — added 2026-08-24 — lineage: M054 (plan gate; findings 6, 10, 11)
- The builder app exposes fewer generator arguments than the package does, triaged down at three gates in three families. Word typography and header: no `title`, `font_size` or `font_family`, so every Word download carries the default header at 10pt Times New Roman — typography and a label, and `title` sits under IP1 as participant-facing text (D-037). Online exports: no `include_instructions` (Qualtrics) or `breaks` (both), so every online download carries the administration instructions and paginates every 15 items — dropping instructions is IP1's scope and needs sign-off, `breaks` is pagination only; M050 guards both package-side. Scoring key: no `include_scoring`, so the Word download always carries the key page a package caller can turn off — not a hazard the app introduced, since `inst/extdata/hitopsr_A4.docx` carries the same key. Promote per family: a study-specific header or larger print form; an unpaginated or instruction-free online export; a participant-facing form without the key — added 2026-08-22 — lineage: M045 (finding 4), M049/M050 (plan gates)
- Neither `generate_qualtrics_hitopsr()` nor `generate_redcap_hitopsr()` checks that `id_prefix`, `block_name`, or `form_name` is a name its target system accepts — a space in `id_prefix` or a capital in `form_name` yields a dictionary REDCap or Qualtrics rejects at import, with nothing said at generation. M050 adds type and shape guards but not format rules: no citable REDCap or Qualtrics naming spec sits on the `references/` shelf, and IP2 bars inventing one. Promote once such a spec is ingested, or if a user reports an import rejected on a name the package accepted — added 2026-08-23 — lineage: M050 (plan gate, out of scope)
- The five `*_qualtrics.txt` byte-locked artifacts in each of `inst/extdata/` and `pkgdown/assets/downloads/` are protected only by their directory's `-text` rule: carrying neither a NUL byte nor a CR, they are invisible to M047's line-ending guard and git's content sniff reads them as text — the pairing that let a Windows checkout mutate them at M020. `*.qsf` gained an extension rule at M047; `.txt` did not, since a blanket `*.txt -text` would freeze every ordinary text file. A path-scoped rule, or a guard clause keyed to the manifest, would close it; the md5 lock still catches an actual byte change, so this is guard reach, not a live defect. Promote if an artifact `.txt` is moved or added outside those two directories, or if the md5 lock trips again on a line-ending conversion — added 2026-08-22 — lineage: M047 (review finding 10, partially addressed)
- The browser module builder ([jmgirard/hitop-builder](https://github.com/jmgirard/hitop-builder)) has no CI and no smoke test, so nothing but a visitor notices when the deployed page stops working — a scheduled job loading the page in a headless browser and building one form would catch it, at the cost of a check that depends on webR's CDN and r-universe both answering. The version-pin and stalled-install halves graduated to M064 (a declared minimum `hitop` version refused at boot, an install timeout, and a release-walk step), which also settles the pin-versus-checklist question: r-universe serves only its current build and `webR.installPackages` takes no version, so a hard pin is unavailable. Promote if the page is found broken by a visitor rather than by M064's refusal, or when a `hitop` release changes a surface the page calls — added 2026-08-22, narrowed 2026-08-28 — lineage: M045 (review findings 1, 2, 6, 12), M064
- Six builder-page presentation and wording items deferred at gates rather than rejected. The page follows the operating system's light or dark setting with no control of its own, unlike the package's pkgdown site and its three-state switch — declined at the 2026-08-24 M051 gate as a control the stepped-flow rework should place, again in M052's scope, which placed the page's controls without one (corrected M052), and a third time at the 2026-08-24 M053 plan gate. And M057's picker tooltips carry only the clinician brief for the 76 scales: the client-facing definition text (30-40 words, too long for a popup) and the 17 subscale definitions both stay in `vignettes/articles/scales-hitopsr.Rmd`, which already publishes them, since the builder picks scales and not subscales. M063 adds three wording items, none a wrong answer on any current input: the handover guard's rationale in the page notice, `README.md` and the `download()` comment still reads "under the name the new one is about to take", written when every build shared one of two stems and now overstating the hazard; the `-module` prose in both files says "unless you ticked every scale", omitting `wholeInstrument()`'s `tilesExactly` gate (true of HiTOP-SR as shipped, and `README.md:243` carries the same shape); and nothing tells a visitor holding a pre-M063 `hitopsr-module.json` that the pair naming changed, M063-D1 having rejected an explicit whole-instrument marker on a renaming rationale its own scheme also incurs. Promote if a visitor asks for a theme control, if a clinician wants the client-facing wording where they are choosing scales, or when the builder's prose is next edited — added 2026-08-24, widened 2026-08-26, extended 2026-08-29 with M063's three as an explicit disposition at Jeff's selection — lineage: M051, M052, M053, M057 (plan gate), M063 (review findings 4-6)
- `vignettes/articles/modules-hitopsr.Rmd` enumerates how a module form differs from a full-instrument one — 1..n numbering, `randomize`, the crosswalk — and is no longer the complete list it reads as. M048's module-aware header is missing (NEWS and `?generate_docx_hitopsr` carry it, so nothing is false); its `randomize` paragraph claims 1-to-n numbering and a crosswalk without the `renumber = FALSE` case, where neither holds; and both it and `?generate_docx_hitopsr` give the reorder recipe as `collected[order(item_order)]` without saying it holds only when the collected columns are in printed *position* order, so under `randomize = TRUE, renumber = FALSE` a reader who keyed columns by the printed number double-permutes. Promote when the article is next edited, or on a reader surprised by the header, a missing crosswalk, or a double-permuted score — added 2026-08-23, widened 2026-08-24 twice — lineage: M048 (finding 2), M049 (finding 3), M055 (finding 8)
- Two generator-hygiene gaps the {zip} change surrounded but did not address: `build_redcap_zip()` writes its intermediate CSV to a fixed `file.path(tempdir(), "instrument.csv")`, so two REDCap generations in one session's forked workers can clobber each other, and its `file.remove()` is unguarded against a `zip::zip()` error; and `zip` entered Imports with no version floor although `mode = "cherry-pick"` needs zip >= 2.2.0. Promote if a parallel generation returns a wrong dictionary, or if an old-{zip} install reports an unused-argument error — added 2026-08-22 — lineage: M045 (review findings 9, 10)
- The package's three item-column naming patterns disagree: `ku_hitopsr` uses `hsr001`, `sim_hitopsr` uses `hsr_1`, `rename_hitopsr_items()` produces `HSR_1`. Each is internally consistent and nothing is wrong today, but a reader moving between the two datasets, or following the renamer's output, must rewrite the selection idiom each time — the idiom M044 set out to make routine. Settling on one renames a shipped dataset's columns, a GP2 change owing NEWS and a D-entry, so it is not a doc fix. Promote if a user reports a selection failure moving between the datasets, or when a data-regeneration milestone next touches the `*_hitopsr` datasets — added 2026-08-21 — lineage: M044 (review finding, rejected at triage as pre-existing)
- The AST-walk self-check certifying that `test-plot_pid5.R` reads no plot internals discriminates by the *receiver* of a `data` extraction but not the *accessor*: `attr(p, "data")` reads it (ggplot2 4.x objects are S7, so properties are attributes), as do `p[[key]]` with a variable index, `do.call("[[", list(p, "data"))` and `base::`-qualified accessors — all verified green. No such read exists in the file today, so this is guard strength, not a live defect. The same walk treats only `for` as a loop, so `while`/`repeat` bodies escape the in-loop `info =` rule. Promote on any read of a plot's internals reaching that file past the guard, or on the guard being reused for a second test file — added 2026-08-13 — lineage: M040 (findings below the actioning threshold)
- M060's output-column collision guard is the only cli-message guard in the suite whose probes vary the message's cardinality and the colliding column's position in `data`: a 30-column probe (past cli's 20-item inline elision) and a first-column probe were added at the M060 repair, after a message naming 20 of 76 colliding columns reached review. The package's other message-content reports — `validity_pid5()`'s six alerts, `norm_pid5()`'s coding reports, `interval_hitopsr()`'s two conditions — each assert on one exemplar, and would not see the same elision or a position-blind read. Promote when one of those reports is next extended, or if a caller reports a truncated list — added 2026-08-28 — lineage: M060 (defect return 1; the three probe axes ship in the tests, held out of AC1 at the amendment gate)
- `score_hitopsr()` and `score_hitopbr()` return a `_se` value beside a scale score that is `NA`, where `score_pid5()` masks it — the engine gates this on `mask_se_na`, which only `score_pid5()` passes as `TRUE` (`R/score_engine.R:126`, `R/score_pid5.R:152`), its comment calling the SR/BR behavior historical rather than chosen. Reproduced 2026-08-13 on `sim_hitopbr` with one item of respondent 1 set `NA`: `score_hitopbr(items = 1:45, missing = "complete", calc_se = TRUE)` returns `hbr_antagonism = NA` beside `hbr_antagonism_se = 0.3273268`, because `calc_sem()` drops the missing item while `missing = "complete"` drops the whole score. No help page or vignette mentions it. Surfaced by the `calc_se` help-page hotfix (PR #45). Unifying the two changes scored output — a GP2 change owing NEWS and a D-entry — and which behavior is right is the open question. Promote if a user reports a standard error beside a missing score, or when the next milestone touches the scoring engine — added 2026-08-13
- Vignette coverage gaps, both demonstrated only on their help pages: `calc_se` appears in no PID-5 vignette but the short-form one, so the 220-item and 25-item forms have no worked example — and the BF case differs rather than repeats, its 5 domains scoring straight from items with no facet layer, so its standard errors are item-based where the SF's domain ones are not; and `rank_scales()`, though exported and in `_pkgdown.yml`, appears in none. Promote if a reader is unable to work either out from the help page — added 2026-08-04 — lineage: the vignette standard-error hotfix (PR #43), M039 (scoped out at the plan gate)
- Two width promises, each resting on one hand measurement and guarding no later edit. `plot_pid5()`'s documented ~7-inch figure-width floor for value labels was measured on one device with one font — the worst case (a three-digit value at the axis maximum on a facet profile) needed ~0.402in against ~0.382in of padding at 7in, essentially no margin, and the binding constraint is panel width, which differs by an inch or more between `level = "domain"` and `level = "facet"`; a per-level floor, or a measurement repeated across graphics devices, would replace the single number (annotated 2026-08-06: M040 makes the padding conditional on `labels`, leaving the floor where it stands). And M036 proves the PID legend's two lines fit the printable column visually, which confirms that build only; Word's line breaking is not observable from `document.xml`, so any repeatable width-budget check there is an estimate. Promote if a clipped label is reported at or above the documented width, or if a mid-phrase wrap reaches a committed form unnoticed — added 2026-07-31/2026-08-04, merged 2026-08-26 — lineage: M036, M039, scoped out of M040
- Hierarchical PID-5 profile display in the shape of Jeff's HiTOP-SR mockup — one block per level, each a table of scale names, `T [95% CI]` values and a per-row glyph plot with SD gridlines, using indent markers (`>`, `>>`) and a distinct glyph per level. Shared in chat 2026-08-04 and committed nowhere, so it lives only in that conversation. Adapting it needs decisions `plot_pid5()` does not make: the PID-5 hierarchy is two levels against the mockup's four, and the 10 non-domain-defining facets have no parent to nest under. Its CI bars and cutoff-based fills are the two rows above, each blocked by its own condition. Promote when hierarchical HiTOP-SR scoring is finalized, or if the two-level PID-5 form is wanted on its own — added 2026-08-04
- An anchor catching an *upward* percentile displacement (every row taking its successor's value) — M034 closes the downward direction only; its second anchors happen to cover the upward direction on 34 of the 66 T-scored columns, so 32 would need a third hand-read anchor, and no single anchor can serve both directions on the 14 columns with no interior T differing from both neighbours. Promote if an upward displacement ever reaches the shipped dataset — added 2026-07-31, figure corrected 2026-07-31 at M034 review (the row was written pre-M034 and said 41, which was the pre-M034 count) — lineage: M034 (which absorbed M033's original second-anchor row)
- Maintainer-run verification tooling in `data-raw/` carries known gaps across four families, none a wrong answer on shipped data. **M035's norms-book comparison** runs only by hand — committing the norms CSVs or the book extraction as fixtures would duplicate `pid_norms` in CI at ~42 KB with a copy that can drift, its own decision — and localizes a failure poorly: a duplicated `(version, scale, key)` aborts on a bare `stopifnot`, an unparseable book cell aborts inside the extraction, and `mutate_norms_book_check.R` cannot tell a crash from a clean run. **M058/M059's two rename verifiers** ran green at both reviews but are weakly guarded: `verify_hitopsr_rename.R` leaks a `git worktree` registration per run (its `on.exit()` never fires under `Rscript`, its `dir.exists()` guard cannot fire), asserts only that the two Word footers differ rather than that only the build stamp changed, prints manifest rows instead of asserting them, assumes unique `Scale` values so its position oracle is exercised on one of four tables, blanks a whole cell on substring match, never asserts the unrebuilt exports lack the *new* name, and keys its diff to the M058 merge base so any later branch reports its own changes as unexpected (left as the M059 implement gate's choice). M059's review adds seven hardening items on the same scripts and their shared extractor — watermark stripping that can eat a digit-adjacent fragment, `\bScales\b` reading a scale name as a section header (0 of 93 today), a Superspectra partition assuming that section is Table 1's last, an oracle that empties if both prose phrasings of the count vanish, a surviving-fragment control that misses a glued fragment, and a case-class rewrite applied after metacharacter escaping — plus four on the suite: AC6's Word sweep floors at two files so `R CMD check` inspects the installed pair, AC2's self-check asserts a planted flip directly rather than through the loop it validates, AC5 reduces `==` with `&` so an `NA` manifest cell would index phantom rows, and AC1's probe cannot distinguish the two `missing` settings. **M041's shared extractor**: `stray_is_watermark()` (`data-raw/hitopsr_table1.R:289`) calls `grepl(t, "ForPeerReview", fixed = TRUE)` with the arguments reversed, so any substring of the watermark is accepted as watermark rather than reported as stray text (the run matched all 744 cells, so this is a weakened guard, not a wrong answer). **M061's characterization harness** (`data-raw/verify_m061_characterization.R`): its missing-probe guard works at export granularity, so deleting the `hitopsr_module` or `pid5_SF` path still reports "40 cells compared; 0 differ" and exits 0; `without_names()` drops the display-name column, so a branch returning none reduces to the base shape and the change lands in `renamed_cells`, printed but never added to `differ`; and it assumes `origin/HEAD` is set and ignores `git archive`'s exit status. Also from M058: the also-known-as note telling a `hsr_nssi` holder where the column went sits only on `?hitopsr_items`; adding it to `?score_hitopsr` was written at the second review and reverted, since AC2's own sweep allow-list can only be widened by a criterion amendment. Promote if a norms defect reaches a release by a path where the M035 script was not re-run, if a discrepancy is investigated and the output fails to localize it, when either rename verifier or the M061 harness is next re-run (D-041's accepted-paper reconciliation is the known occasion), if the harness's value-and-condition pattern is copied, or if a user asks where `hsr_nssi` went — added 2026-07-31, clustered 2026-08-27, extended 2026-08-28 twice as explicit dispositions at Jeff's selection, compressed 2026-08-29 — lineage: M035, M058 (findings 1, 5-8, 10-12), M059 (F1-F8, F11, F12), M041 (finding 10), M061 (findings 3, 4, 8)
- PID-5-BFP (36-item) data, scoring, exports — awaiting materials — added 2026-07-16 — `legacy/ROADMAP.md` Phase 1
- HiTOP-HSUM scoring + reliability + tutorial — awaiting Society feedback — added 2026-07-16 — `legacy/ROADMAP.md` Phase 1
- Severity bands / elevation thresholds on PID-5 profile plots — D-029 ships M038's plot with none, because no source in `references/` or `SOURCES.md` supplies boundaries and shading by severity characterizes a score; `devel/plotting.R`'s prototype bands (T = 40/80, green→red) are the shape wanted. Promote if a published, citable set of PID-5 profile elevation thresholds is ingested as a `references/` page — added 2026-08-01 — lineage: M038
- Confidence intervals on PID-5 profile plots — added 2026-08-01, corrected 2026-08-04, superseded 2026-08-13 by Johannes Zimmermann's norms proposal (four artifacts supplied by Jeff 2026-08-13: `hitop_package_norms_proposal.md`, `norms_de_prolific2024.R`, a 9,984-row norms CSV and its metadata; not on the `references/sources/` shelf — observed 2026-08-13). Method: the regression-based true score with scale correction (Schmukle, 2025, *Assessment*, 33, 817-825) — estimate `50 + sqrt(rel) * (T - 50)`, bounds `± z * 10 * sqrt(1 - rel)`, falling back to the observed-score interval where the reference group does not fit; it displaces the row's earlier `SEM = 10 * sqrt(1 - alpha)` framing. Blocker unchanged 2026-08-13: markon2024 prints no reliability coefficient this repo has ingested, and the only known candidate — Krueger et al. (2012) Table 2 (PMC3413381, figures unverified) — mismatches the norms three ways (N=264 development sample vs the book's N=995 weighted norming subsample, 220-item form only, alpha not ordinal omega). Jeff has a further reliability source to supply (2026-08-13). Promote once a per-scale reliability traceable to the norming sample is ingested and verified — lineage: M038
- Multi-source `pid_norms` schema — Johannes Zimmermann's proposal (2026-08-13) adds `source`, `group`, `tscore_lo`, `tscore_hi`, `censored`, `n` columns plus a `pid_norm_sources` tibble, and gives `norm_pid5()` `source`/`age`/`gender`/`ci` arguments with group-fallback rules; every statistic stays a shipped table cell computed in `data-raw/`, so D-022 is untouched. Three package-side issues found 2026-08-13 that it does not address: `_t` is `as.integer()` at `R/norm_pid5.R:272` over an `int` `pid_norms$tscore` while percentile-based T carries decimals (Jeff chose 2026-08-13 to widen to double when the schema moves — a GP2 change owing NEWS and a D-entry); a censored row carries `tscore`, `percentile` and both bounds `NA`, satisfying neither branch of `norm_select()`'s tie-break key at `R/norm_engine.R:82`; and his scale labels are Title Case with an ampersand against `pid_norms$scale`'s camelCase stems, needing a crosswalk he does not mention. Jeff decided 2026-08-13 that no German row ships until Johannes republishes the corrected OSF tables (project hwxnj) as a citable source, per IP3. Promote when those tables are public, or if any second norm source arrives needing the schema — added 2026-08-13
- Guidance on reading T against percentile at the floor of the PID-5 norms — derived 2026-08-13 from shipped `pid_norms` (per version/scale, the highest-T row at the lowest printed raw): 22 of the 66 T-scored columns sit above T = 40 there, SF depressivity and SF perceptual dysregulation both at T = 45, median floor T = 39, where the printed percentile is 0.00 on most columns against the ~0.136 that T implies under normality. This is the linear-T floor pathology Johannes Zimmermann raised against the HiTOP-SR app, reproduced in the PID-5 US norms — the book's tables are linear renderings (RR02, quoted in D-022). No new data is needed: the book prints empirical percentiles beside the linear T, `norm_pid5()` returns both, and `plot_pid5(metric = )` plots either; what is missing is documentation saying which to trust where. Re-deriving a percentile-based T is barred by D-022. Promote if a reader ever takes a floor T as a meaningful elevation — added 2026-08-13
- Multi-respondent PID-5 profile plots (one line per person, or panels per person) — M038 aborts on any input with more than one row; group profiles need their own decisions about legends, coloring, and how many series is too many. Promote if a researcher is blocked plotting a sample rather than an individual — added 2026-08-01 — lineage: M038
- PID-5 demographically stratified norms — the sex/age-stratified tables appear nowhere in the markon2024 appendix and still await the maintainer; would extend `pid_norms` with no new export — added 2026-07-30, narrowed 2026-07-31 (the facet half graduated to M033) — lineage: M025
- PID-5 Informant Form (IRF) norms — markon2024 A–10/A–11 (domain, trait) and A–12 (SRF/IRF descriptives + T-score difference) arrived 2026-07-30; the package has no IRF surface today, so this is a new instrument version, not a `pid_norms` extension — added 2026-07-30 — lineage: M025
- HiTOP-SR/BR normative data and norming functions — source workbooks arrived 2026-07-30 (`HiTOP-SR-Final.xlsx`, `B-HiTOP overview.xlsx`); both carry sample descriptives (N/mean/SD/skew/kurtosis on a Prolific sample), not raw→T lookup tables, so the norming design differs from `pid_norms` — added 2026-07-30, corrected 2026-08-06 (maintainer): no official SR or BR norms exist, and the development workbooks are not trustworthy for clinical norming, so IP3 bars a norming function until published, citable tables exist — this row is norming *research*, not a plannable norming milestone — `legacy/ROADMAP.md` Phase 3
- Clinical reporting & release (individual reports, bass-ackwards analyses, CRAN submission, package paper) — added 2026-07-16 — `legacy/ROADMAP.md` Phase 4
- Someday-maybe cluster (HiTOP-SR/BR validity scales) — added 2026-07-16, narrowed 2026-08-01 (the `devel/plotting.R` plotting-helpers half graduated to M038) and 2026-08-21 (the Shiny scoring app half graduated to M045, which deletes the `inst/shiny/app.R` stub) — `legacy/ROADMAP.md`
- Multi-language download UI: per-language buttons on the instrument download pages won't scale once translations arrive; a selector or grouped layout is wanted — added 2026-07-17 — lineage: M021
- Score subset-collected HiTOP-SR data with no descriptor in hand, by working out from the column names which scales are fully present — needs its own decisions about partial coverage and name matching; useful for data that arrived from elsewhere. Promote if a researcher is ever blocked scoring a short form they did not themselves generate — added 2026-08-01 — lineage: M037
- The `*_scales` tables store `nItems` as a double while `lengths(itemNumbers)` — what the engines return — is integer (the M037 lesson). M061 settles the *emitted* types: `available_scales()`, `hitop_module()` and the three `reliability_*()` returns are all integer after it, coerced at the boundary rather than in the data. The shipped tables themselves stay double, so a user joining `hitopsr_scales` to a scored output still meets the mismatch. Promote when a data-regeneration milestone next touches the `*_scales` tables — added 2026-08-21, narrowed 2026-08-28 (M061 absorbs the `available_scales()`/`hitop_module()` half) — lineage: M043 (review finding F5), M061
- Two column-shape follow-ups M061 leaves standing, both from its plan gate. (a) A `camelCase` column on the `reliability_*()` return, matching `available_scales()`, so a caller can join a reliability table to scored columns without rebuilding the stem: M061 aligns the display name and renames the column to `Scale` but adds no stem column, the scope having been drawn at names that were wrong rather than names that are absent. (b) `hitopsr_devstats` names its display-name column `scale` and its stem column `camelCase`, where `hitopsr_scales`, `hitopbr_scales`, `hitopsr_definitions` and `hitopsr_subscales` all use `Scale`. The values agree — the gate reconciled all 93 against `hitopsr_scales$Scale` plus `hitopsr_subscales$Subscale` — so nothing is mis-spelled; only the column's own name is out of step. Renaming it is a documented dataset change and a `data-raw/hitopsr_info.R` regeneration touching `interval_hitopsr()`'s `refstats` join. Promote (a) on a user report or with the next reliability-family change, (b) when a data-regeneration milestone next touches `hitopsr_devstats` — added 2026-08-28 — lineage: M061 (plan gate)
- "Module" now names two things: a chosen set of scales (`hitop_module()`, M043) and the HiTOP-HSUM, whose Society name is the Harmful Substance Use *Module*. No user-facing text conflates them today — the HSUM download page uses the instrument's full name and the module family is HiTOP-SR-only — but the collision bites when modularization reaches other instruments, since "an HSUM module" would be ambiguous. IP1 bars renaming the Society's instrument, so any fix is on this package's side. Promote when modularization extends beyond the HiTOP-SR, or if a user reports the ambiguity — added 2026-08-21 — lineage: M043
- Generalize modularization to BR/PID-5: extend the subset-descriptor + subset generation/scoring to HiTOP-BR (overlapping scales, e.g. p-Factor spans all items) and PID-5 (facets partition, domains derive from facets) — added 2026-07-17 — lineage: M024 — annotated 2026-08-21: M043 renames this family to "modules" and deliberately precedes this row so the generalization inherits one vocabulary — annotated 2026-08-26 (M057 review): two latent traps this row must clear when a second instrument arrives — `validate_module_instrument()` derives the supported set from `module_scale_tables()` alone while `available_scales()` also needs a key in `module_definition_tables()`, so an instrument added to the first map only is declared supported and then aborts inside the lister; and `scale_definitions()` reads `ref$Subscale`, so a definitions table without that column yields a zero-row match and misreports the cause. Neither is reachable while both maps hold exactly `hitopsr`
