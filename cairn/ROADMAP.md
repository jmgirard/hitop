# Roadmap

_Last hygiene check: 2026-08-29 (second pass. M066 opened and archived in one step to record the builder's step-bar chevrons — hitop-builder PR #8, squash-merged `efea175`, Pages green — which had run as ad-hoc work outside every tier and appeared nowhere in tracking; the archive summary says so plainly. Both byte budgets were over and the previous stamp said otherwise: `ROADMAP.md` 34,039 against 24,000 and `LESSONS.md` 25,711 against 20,000, where the earlier pass that day had left ROADMAP over at 34,039 and grown LESSONS from 25,286. Both brought under budget here by compressing the widest rows and entries against the archives that already hold their detail. The builder-page candidate row, now carrying findings from five milestones, was left standing at Jeff's disposition for the next session. M041's terminal row pruned under the five-row retention rule; M064 and M065 stay planned.)_
_Pre-migration history: see `cairn/legacy/` and git log (M001–M017 done there; IDs continue — next new milestone is M067)._

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M066 | The step bar shows its three buttons run in order | done | — | normal | milestones/archive/M066-builder-stepbar-chevrons.md |
| M063 | Every file the builder writes says which form it belongs to | done | M062 | normal | milestones/archive/M063-builder-download-naming.md |
| M064 | The builder refuses loudly when the package it installs no longer matches it | review | — | normal | milestones/M064-builder-version-guard.md |
| M065 | The generators announce the descriptor they write | planned | — | normal | milestones/M065-descriptor-write-announced.md |
| M062 | Both files a builder download produces reach the visitor's disk | done | — | normal | milestones/archive/M062-builder-download-handover.md |
| M061 | The reliability family invents scale display names instead of reading them | done | — | normal | milestones/archive/M061-scale-name-consistency.md |
| M060 | The scoring and conversion family refuses two argument shapes it lets fall through | done | — | normal | milestones/archive/M060-append-collision-empty-selection.md |

## Candidates

- D-045(d) and its *Context* claim no succeeding call's return value changes; M060's own review disproved it. `norm_pid5(scored, scores = character(0), append = FALSE)` and the same `interval_hitopsr()` call each returned a 0x0 tibble before M060 and abort with `hitop_empty_selection` after, and the Context's differing-rows claim holds only under `append = TRUE`. `NEWS.md` was corrected at the M060 gate; `DECISIONS.md` is append-only, so the fix is a superseding entry. Nothing shipped is wrong — the record of its consequences is. Promote when `DECISIONS.md` is next appended to, or if a reader takes D-045(d) as a statement of what the release did — added 2026-08-28 — lineage: M060 (review pass 2, finding 1)
- The HiTOP-BR scale printed `p-Factor` here is `p-factor` in the introduction manuscript — 15 occurrences, the capitalized form absent. `data-raw/verify_hitopsr_names.R` records the divergence in a comment but checks nothing. Both spellings derive the same stem, so no scored column moves; what changes is `hitopbr_scales$Scale`, the HiTOP-BR Word scoring page, and `available_scales()`. Still IP1 content needing a source and sign-off, and D-042 admits the manuscript for HiTOP-SR names only. Promote when a HiTOP-BR scale name is next sourced, or if a reader is surprised by the capitalization — added 2026-08-27 — lineage: M059 (AC4's second criteria audit)
- `Prolific data HiTOP-SR.sav` (Development Sample 2, N = 780) is on the `references/sources/` shelf, so Table 1's alphas, means and SDs could be recomputed from the responses rather than only transcribed. M041 verifies the transcription against the printed table and an item-count oracle — enough for a transcription error, not a typesetting one. Recomputation needs its own decisions, and is IP2 condition one for any package-wide Green & Yang (2009) categorical omega, which D-043 leaves unscheduled. Promote if a printed-table discrepancy is investigated with the table the suspect, or if a reviewer asks where the statistics come from — added 2026-08-27 — lineage: M041
- Three descriptor-format gaps M054's review left open, none reachable from a file the package wrote: the reader takes JSON strings where the format documents numbers; `is_module()` accepts the deprecated `hitop_subset` class, falsifying the round-trip-identity claim for it; and `write_module()` checks the class but not the `nItems`/`items` invariant, so a hand-assembled module writes a file the reader refuses. Separately, a descriptor records scales but not the column names the export produces, so `score_hitopsr()` still needs `items` typed out. Promote on any user report, or when M055 lands — added 2026-08-24 — lineage: M054 (plan gate; findings 6, 10, 11)
- The builder app exposes fewer generator arguments than the package does, triaged down at three gates in three families. Word typography and header: no `title`, `font_size` or `font_family`, so every Word download is the default header at 10pt Times New Roman, and `title` sits under IP1 (D-037). Online exports: no `include_instructions` or `breaks`, so instructions always ship and pagination is every 15 items. Scoring key: no `include_scoring`, so the Word download always carries the key page. Promote per family: a study-specific header or larger print form; an unpaginated or instruction-free online export; a participant-facing form without the key — added 2026-08-22 — lineage: M045, M049/M050
- Neither `generate_qualtrics_hitopsr()` nor `generate_redcap_hitopsr()` checks that `id_prefix`, `block_name` or `form_name` is a name its target system accepts — a space in `id_prefix` or a capital in `form_name` yields a dictionary REDCap or Qualtrics rejects at import, with nothing said at generation. M050 adds type and shape guards but not format rules: no citable naming spec is on the `references/` shelf, and IP2 bars inventing one. Promote once such a spec is ingested, or if a user reports an import rejected on a name the package accepted — added 2026-08-23 — lineage: M050
- The five `*_qualtrics.txt` byte-locked artifacts in each of `inst/extdata/` and `pkgdown/assets/downloads/` are protected only by their directory's `-text` rule: carrying neither a NUL byte nor a CR, they are invisible to M047's line-ending guard and git reads them as text — the pairing that let a Windows checkout mutate them at M020. A blanket `*.txt -text` would freeze every ordinary text file, so `.txt` gained no extension rule where `*.qsf` did. The md5 lock still catches an actual byte change, so this is guard reach, not a live defect. Promote if an artifact `.txt` is moved outside those directories, or if the md5 lock trips again — added 2026-08-22 — lineage: M047
- The browser module builder ([jmgirard/hitop-builder](https://github.com/jmgirard/hitop-builder)) has no CI and no smoke test, so nothing but a visitor notices when the deployed page stops working — a scheduled headless-browser build would catch it, at the cost of depending on webR's CDN and r-universe both answering. The version-pin and stalled-install halves graduated to M064, which also settles pin-versus-checklist: r-universe serves only its current build and `webR.installPackages` takes no version, so a hard pin is unavailable. Promote if the page is found broken by a visitor rather than by M064's refusal, or when a `hitop` release changes a surface the page calls — added 2026-08-22, narrowed 2026-08-28 — lineage: M045, M064
- Six builder-page presentation and wording items deferred at gates rather than rejected: no light/dark control of the page's own (declined at the M051, M052 and M053 gates); M057's picker tooltips carrying only the clinician brief, with the client-facing and subscale definitions left in `vignettes/articles/scales-hitopsr.Rmd`; and M063's three wording items. None is a wrong answer on any current input; the archives carry the detail. Promote if a visitor asks for a theme control, if a clinician wants the client-facing wording where they choose scales, or when the builder's prose is next edited — added 2026-08-24, widened 2026-08-26, extended 2026-08-29 — lineage: M051, M052, M053, M057, M063
- `vignettes/articles/modules-hitopsr.Rmd` enumerates how a module form differs from a full-instrument one and is no longer the complete list it reads as: M048's module-aware header is missing (NEWS and the help page carry it, so nothing is false); its `randomize` paragraph omits the `renumber = FALSE` case; and both it and `?generate_docx_hitopsr` give the reorder recipe as `collected[order(item_order)]` without saying it holds only when the collected columns are in printed *position* order. Promote when the article is next edited, or on a reader surprised by the header, a missing crosswalk, or a double-permuted score — added 2026-08-23, widened 2026-08-24 twice — lineage: M048, M049, M055
- Two generator-hygiene gaps the {zip} change surrounded but did not address: `build_redcap_zip()` writes its intermediate CSV to a fixed `file.path(tempdir(), "instrument.csv")`, so two REDCap generations in one session's forked workers can clobber each other, and its `file.remove()` is unguarded against a `zip::zip()` error; and `zip` entered Imports with no version floor although `mode = "cherry-pick"` needs zip >= 2.2.0. Promote if a parallel generation returns a wrong dictionary, or on an old-{zip} unused-argument error — added 2026-08-22 — lineage: M045
- The package's three item-column naming patterns disagree: `ku_hitopsr` uses `hsr001`, `sim_hitopsr` uses `hsr_1`, `rename_hitopsr_items()` produces `HSR_1`. Each is internally consistent and nothing is wrong today, but a reader moving between the two datasets must rewrite the selection idiom each time — the idiom M044 set out to make routine. Settling on one renames a shipped dataset's columns, a GP2 change owing NEWS and a D-entry. Promote if a user reports a selection failure moving between the datasets, or when a data-regeneration milestone next touches the `*_hitopsr` datasets — added 2026-08-21 — lineage: M044
- The AST-walk self-check certifying that `test-plot_pid5.R` reads no plot internals discriminates by the *receiver* of a `data` extraction but not the *accessor*: `attr(p, "data")`, `p[[key]]` with a variable index, `do.call("[[", list(p, "data"))` and `base::`-qualified accessors all read it past the guard (verified green). No such read exists in the file today, so this is guard strength, not a live defect. The same walk treats only `for` as a loop, so `while`/`repeat` bodies escape the in-loop `info =` rule. Promote on any such read reaching that file, or on the guard being reused for a second test file — added 2026-08-13 — lineage: M040
- M060's output-column collision guard is the only cli-message guard in the suite whose probes vary the message's cardinality and the colliding column's position in `data` — a 30-column probe (past cli's 20-item inline elision) and a first-column probe, added at the M060 repair after a message naming 20 of 76 colliding columns reached review. The package's other message-content reports (`validity_pid5()`, `norm_pid5()`, `interval_hitopsr()`) each assert on one exemplar. Promote when one of those is next extended, or if a caller reports a truncated list — added 2026-08-28 — lineage: M060
- `score_hitopsr()` and `score_hitopbr()` return a `_se` value beside a scale score that is `NA`, where `score_pid5()` masks it — the engine gates this on `mask_se_na`, which only `score_pid5()` passes as `TRUE` (`R/score_engine.R:126`), its comment calling the SR/BR behavior historical rather than chosen. Reproduced 2026-08-13 on `sim_hitopbr`: `hbr_antagonism = NA` beside `hbr_antagonism_se = 0.3273268` under `missing = "complete"`. No help page or vignette mentions it. Unifying the two changes scored output — a GP2 change owing NEWS and a D-entry. Promote if a user reports a standard error beside a missing score, or when the next milestone touches the scoring engine — added 2026-08-13
- Vignette coverage gaps, both demonstrated only on their help pages: `calc_se` appears in no PID-5 vignette but the short-form one, so the 220-item and 25-item forms have no worked example — and the BF case differs rather than repeats, its 5 domains scoring straight from items; and `rank_scales()`, though exported and in `_pkgdown.yml`, appears in none. Promote if a reader is unable to work either out from the help page — added 2026-08-04 — lineage: PR #43, M039
- Two width promises, each resting on one hand measurement and guarding no later edit. `plot_pid5()`'s documented ~7-inch figure-width floor for value labels was measured on one device with one font, with essentially no margin at the worst case, and the binding constraint is panel width, which differs by an inch or more between domain and facet levels. And M036 proves the PID legend's two lines fit the printable column visually, which confirms that build only. Promote if a clipped label is reported at or above the documented width, or if a mid-phrase wrap reaches a committed form unnoticed — added 2026-07-31/2026-08-04, merged 2026-08-26 — lineage: M036, M039, M040
- Hierarchical PID-5 profile display in the shape of Jeff's HiTOP-SR mockup — one block per level, each a table of scale names, `T [95% CI]` values and a per-row glyph plot with SD gridlines, using indent markers and a distinct glyph per level. Shared in chat 2026-08-04 and committed nowhere, so it lives only in that conversation. Adapting it needs decisions `plot_pid5()` does not make: the PID-5 hierarchy is two levels against the mockup's four, and the 10 non-domain-defining facets have no parent to nest under. Its CI bars and cutoff-based fills are the two rows above, each blocked by its own condition. Promote when hierarchical HiTOP-SR scoring is finalized, or if the two-level PID-5 form is wanted on its own — added 2026-08-04
- An anchor catching an *upward* percentile displacement (every row taking its successor's value) — M034 closes the downward direction only; its second anchors happen to cover the upward direction on 34 of the 66 T-scored columns, so 32 would need a third hand-read anchor, and no single anchor can serve both directions on the 14 columns with no interior T differing from both neighbours. Promote if an upward displacement ever reaches the shipped dataset — added 2026-07-31, figure corrected 2026-07-31 at M034 review — lineage: M034
- Maintainer-run verification tooling in `data-raw/` carries known gaps across four families, none a wrong answer on shipped data; the detail sits in each milestone's archive summary. M035's norms-book comparison runs only by hand and cannot tell a crash from a clean run. M058/M059's two rename verifiers ran green but are weakly guarded, and M059's review adds eleven further hardening items on them and the suite. M041's `stray_is_watermark()` (`data-raw/hitopsr_table1.R:289`) has its `grepl()` arguments reversed, so any watermark substring is accepted. M061's characterization harness misses a deleted export path. Promote if a norms defect reaches a release unchecked, if a discrepancy fails to localize, or when either rename verifier or the M061 harness is next re-run (D-041's accepted-paper reconciliation is the known occasion) — added 2026-07-31, clustered 2026-08-27, extended 2026-08-28 twice, compressed 2026-08-29 — lineage: M035, M058, M059, M041, M061
- PID-5-BFP (36-item) data, scoring, exports — awaiting materials — added 2026-07-16 — `legacy/ROADMAP.md` Phase 1
- HiTOP-HSUM scoring + reliability + tutorial — awaiting Society feedback — added 2026-07-16 — `legacy/ROADMAP.md` Phase 1
- Severity bands / elevation thresholds on PID-5 profile plots — D-029 ships M038's plot with none, because no source in `references/` or `SOURCES.md` supplies boundaries and shading by severity characterizes a score; `devel/plotting.R`'s prototype bands (T = 40/80, green→red) are the shape wanted. Promote if a published, citable set of PID-5 profile elevation thresholds is ingested as a `references/` page — added 2026-08-01 — lineage: M038
- Confidence intervals on PID-5 profile plots — added 2026-08-01, corrected 2026-08-04, superseded 2026-08-13 by Johannes Zimmermann's norms proposal (four artifacts supplied by Jeff; not on the `references/sources/` shelf — observed 2026-08-13). Method: the regression-based true score with scale correction (Schmukle, 2025, *Assessment*, 33, 817-825), displacing the row's earlier `SEM` framing. Blocker: markon2024 prints no reliability coefficient this repo has ingested, and the only known candidate — Krueger et al. (2012) Table 2 (PMC3413381, figures unverified) — mismatches the norms three ways. Jeff has a further source to supply. Promote once a per-scale reliability traceable to the norming sample is ingested and verified — lineage: M038
- Multi-source `pid_norms` schema — Johannes Zimmermann's proposal (2026-08-13) adds six columns plus a `pid_norm_sources` tibble and gives `norm_pid5()` `source`/`age`/`gender`/`ci` arguments; every statistic stays a shipped table cell computed in `data-raw/`, so D-022 is untouched. Three package-side issues it does not address: an `as.integer()` T at `R/norm_pid5.R:272` against decimal percentile-based T (Jeff chose to widen to double when the schema moves — a GP2 change owing NEWS and a D-entry); a censored row satisfying neither branch of `norm_select()`'s tie-break key at `R/norm_engine.R:82`; and Title Case labels needing a crosswalk to camelCase stems. No German row ships until the corrected OSF tables (project hwxnj) are citable, per IP3. Promote when those tables are public, or if any second norm source arrives — added 2026-08-13
- Guidance on reading T against percentile at the floor of the PID-5 norms — derived 2026-08-13 from shipped `pid_norms`: 22 of the 66 T-scored columns sit above T = 40 at the lowest printed raw, median floor T = 39, where the printed percentile is 0.00 on most columns against the ~0.136 that T implies under normality. This is the linear-T floor pathology Johannes Zimmermann raised against the HiTOP-SR app, reproduced in the PID-5 US norms (RR02, quoted in D-022). The book prints empirical percentiles beside the linear T and `norm_pid5()` returns both; what is missing is documentation saying which to trust where. Promote if a reader ever takes a floor T as a meaningful elevation — added 2026-08-13
- Multi-respondent PID-5 profile plots (one line per person, or panels per person) — M038 aborts on any input with more than one row; group profiles need their own decisions about legends, coloring, and how many series is too many. Promote if a researcher is blocked plotting a sample rather than an individual — added 2026-08-01 — lineage: M038
- PID-5 demographically stratified norms — the sex/age-stratified tables appear nowhere in the markon2024 appendix and still await the maintainer; would extend `pid_norms` with no new export — added 2026-07-30, narrowed 2026-07-31 (the facet half graduated to M033) — lineage: M025
- PID-5 Informant Form (IRF) norms — markon2024 A–10/A–11 (domain, trait) and A–12 (SRF/IRF descriptives + T-score difference) arrived 2026-07-30; the package has no IRF surface today, so this is a new instrument version, not a `pid_norms` extension — added 2026-07-30 — lineage: M025
- HiTOP-SR/BR normative data and norming functions — source workbooks arrived 2026-07-30 (`HiTOP-SR-Final.xlsx`, `B-HiTOP overview.xlsx`); both carry sample descriptives, not raw→T lookup tables, so the norming design differs from `pid_norms` — added 2026-07-30, corrected 2026-08-06 (maintainer): no official SR or BR norms exist and the development workbooks are not trustworthy for clinical norming, so IP3 bars a norming function until published, citable tables exist — this row is norming *research*, not a plannable milestone — `legacy/ROADMAP.md` Phase 3
- Clinical reporting & release (individual reports, bass-ackwards analyses, CRAN submission, package paper) — added 2026-07-16 — `legacy/ROADMAP.md` Phase 4
- Someday-maybe cluster (HiTOP-SR/BR validity scales) — added 2026-07-16, narrowed 2026-08-01 (the `devel/plotting.R` plotting-helpers half graduated to M038) and 2026-08-21 (the Shiny scoring app half graduated to M045, which deletes the `inst/shiny/app.R` stub) — `legacy/ROADMAP.md`
- Multi-language download UI: per-language buttons on the instrument download pages won't scale once translations arrive; a selector or grouped layout is wanted — added 2026-07-17 — lineage: M021
- Score subset-collected HiTOP-SR data with no descriptor in hand, by working out from the column names which scales are fully present — needs its own decisions about partial coverage and name matching; useful for data that arrived from elsewhere. Promote if a researcher is blocked scoring a short form they did not generate — added 2026-08-01 — lineage: M037
- The `*_scales` tables store `nItems` as a double while `lengths(itemNumbers)` — what the engines return — is integer (the M037 lesson). M061 settles the *emitted* types: `available_scales()`, `hitop_module()` and the three `reliability_*()` returns are all integer after it, coerced at the boundary rather than in the data. The shipped tables stay double, so a user joining `hitopsr_scales` to a scored output still meets the mismatch. Promote when a data-regeneration milestone next touches the `*_scales` tables — added 2026-08-21, narrowed 2026-08-28 — lineage: M043, M061
- Two column-shape follow-ups M061 leaves standing, both from its plan gate. (a) No `camelCase` stem column on the `reliability_*()` return, so a caller cannot join a reliability table to scored columns without rebuilding the stem. (b) `hitopsr_devstats` names its display-name column `scale` and its stem column `camelCase`, where four sibling tables use `Scale`; the values agree, so only the column's own name is out of step, and renaming it is a documented dataset change and a regeneration touching `interval_hitopsr()`'s `refstats` join. Promote (a) on a user report or with the next reliability-family change, (b) when a data-regeneration milestone next touches `hitopsr_devstats` — added 2026-08-28 — lineage: M061 (plan gate)
- "Module" now names two things: a chosen set of scales (`hitop_module()`, M043) and the HiTOP-HSUM, whose Society name is the Harmful Substance Use *Module*. No user-facing text conflates them today, but the collision bites when modularization reaches other instruments, since "an HSUM module" would be ambiguous. IP1 bars renaming the Society's instrument, so any fix is on this package's side. Promote when modularization extends beyond the HiTOP-SR, or if a user reports the ambiguity — added 2026-08-21 — lineage: M043
- Generalize modularization to BR/PID-5: extend the subset-descriptor + subset generation/scoring to HiTOP-BR (overlapping scales, e.g. p-Factor spans all items) and PID-5 (facets partition, domains derive from facets) — added 2026-07-17 — lineage: M024 — annotated 2026-08-21: M043 renames this family to "modules" and deliberately precedes this row so the generalization inherits one vocabulary — annotated 2026-08-26 (M057 review): two latent traps to clear when a second instrument arrives, in `validate_module_instrument()`'s supported-set derivation and `scale_definitions()`'s `ref$Subscale` read; neither is reachable while both maps hold exactly `hitopsr`
