# Work Log

<!-- Append-only, newest first. One entry per work session or merged change. -->
<!-- Format: ## YYYY-MM-DD — Title (M-refs) / What / Why-decisions / Follow-ups. Never fabricate history. -->
<!-- Boundary rule: Architecture → DESIGN. Direction → ROADMAP. Tasks → MILESTONES. History → LOG. -->
<!-- This log starts fresh for the canonical repo (v0.1.0); the old fork's log was deliberately not imported (D-006). Pre-tracking history: see git log and NEWS.md. -->

## 2026-07-10 — Reviewed, merged & closed out M17 (M17)

- **What:** Verified all 14 M17 acceptance criteria against PR [#17](https://github.com/jmgirard/hitop/pull/17) (branch `m17-docs-polish`) by **execution**, not by trusting checkboxes. **Scope integrity:** `git diff --name-only origin/main...m17-docs-polish` touches only NEWS.md, tracking docs, and 11 vignette/article `.Rmd`s — no `R/`, `data/`, `NAMESPACE`, `man/`, `DESCRIPTION`. **Counts (C2/C3/C7):** independently recomputed all 12 numeric claims from fresh `score_*()`/`data` output — SR 76/483/152, BR 8/55/16, PID-5 FULL 30 scores / 250 append / 5 validity, SF 60 SE, BF 5 domains — every prose number matches. **Stale refs (C1):** `grep` for `ku_hitoppro`/`hitop001`/`hitop%03d`/`pro_`/`score_hitoppro` across `vignettes/*.Rmd` returns nothing. **Omissions (C4/C5):** `pid5bf_scoring.Rmd` has zero "WORK IN PROGRESS" and a `reliability_pid5(version="BF")` chunk; `hitopbr_scoring.Rmd` has a `reliability_hitopbr()` section — both execute. **Headings (C6):** only `###` remaining are SR's Method 1/2 sub-sections (legitimately nested). **Links (C9):** 37 distinct `../reference/*.html` targets all resolve to exported objects; 24 `inst/extdata/*` links all exist on disk. **Boilerplate (C10):** "subscale definitions" appears only on the one page that links a definitions article (hitopsr); "real validation sample" only on the three pages linking a `ku_*` dataset. **Grammar/markup (C11/C12):** no "domains scales", no stray `<br>`, no `row mt-4 mb-4`/bare `col-md-4`. **Gate:** `document()` **no-diff** (NAMESPACE/man/R unchanged), `test()` FAIL 0 SKIP 1 **PASS 615** (unchanged; docs-only), `check()` **0 errors | 0 warnings | 0 notes** (1m53s incl. vignette rebuild), `pkgdown::build_site()` **exit 0** rendering all 6 downloads + 5 tutorials + the scales article. Fresh-context general-purpose subagent (had not seen the implementation) independently recomputed the counts, executed the reliability chunks, confirmed link resolution and the `#details`-anchor logic, and returned **PASS — no blockers, no nits**. Merged PR #17 to `main` (`05dac61`) after Jeff's yes/no authorization; re-verified the key criteria on up-to-date main (no stale refs, BR 55 cols, no WIP, load_all OK). M17 → DONE.
- **Why / decisions:** No D-entry — documentation accuracy/consistency only, no architecture or API change. **C8 reconciliation:** the criterion as written ("each `download-*.Rmd` links its own `generate_redcap_<instrument>#details`") was over-specified. `generate_redcap_pid5sf`/`pid5bf` use `@inheritParams generate_redcap_pid5` and so have **no `\details` section** of their own (confirmed: their `.Rd` files have `details=0`, the other four have `details=1`); their download pages therefore correctly point the "Import Instructions" link at `generate_redcap_pid5.html#details` (a valid anchor holding the instrument-generic REDCap import steps) — pointing at their own page would be a dead anchor. The real defect C8 targeted — the PID-5 page's *wrong-family* link to `generate_redcap_hitopsr` — is fixed. C8 marked satisfied with this note rather than failing the milestone over a mis-specified ideal. No DESIGN Known issue closed (all four are scoring/keying debt); no ROADMAP phase outcome completed (this is documentation polish, not a Phase 2/3 end-state). Doc sync limited to MILESTONES (moved to Completed) + this LOG.
- **Follow-ups spawned:** — Two out-of-scope items surfaced during review, both presented to Jeff (not yet milestones): (1) add the import `@details` to `generate_redcap_pid5sf`/`pid5bf` so their reference pages carry the REDCap instructions like the other four (R/roxygen change); (2) purge/untrack the git-tracked `vignettes/.quarto/_freeze/` cache, which still holds old HiTOP-PRO content that a future quarto rebuild could resurrect. M16 (norm-ready official-range scoring) stays PLANNED until PID-5 normative data arrives; it is the only Active milestone, so no READY milestone remains.

## 2026-07-10 — Vignette & pkgdown instrument-page accuracy and polish (M17)

- **What:** Implemented M17 on branch `m17-docs-polish` (docs-only; no `R/`, `data/`, keying, or generated-data files touched — `git status` shows only NEWS.md, MILESTONES.md, and 11 vignette/article `.Rmd`s). **Scoring tutorials:** (A) `hitopsr_scoring.Rmd` — fixed three stale references left from a partial port of an old "HiTOP-PRO" vignette (visible in `vignettes/.quarto/_freeze/hitoppro_scoring/`): column names `hitop001`–`hitop405`→`hsr001`–`hsr405`, dataset `ku_hitoppro`→`ku_hitopsr`, format string `hitop%03d`/`hitop%d`→`hsr%03d`/`hsr%d` (the code chunk already used `hsr%03d`, so only prose was wrong). (B) `hitopbr_scoring.Rmd` — corrected the scale count 7→8 (intro + "Basic Scoring") and the appended/SE column counts 54→55 and 14→16, and added a Scale Reliability section using `reliability_hitopbr()`. (C) `pid5bf_scoring.Rmd` — removed the "WORK IN PROGRESS" stamp, added a proper intro paragraph, and added a Scale Reliability section using `reliability_pid5(version = "BF")`. (D) `pid5sf_scoring.Rmd` — added the `library(tibble)` include and a Simple Standard Errors demo for parity with SR/BR. (E) Harmonized all five tutorials to `##` top-level headings (SR's `###` Method 1/2 kept as genuine sub-sections). While re-deriving every count against fresh `score_*()` output I caught a further stale claim in `pid5_scoring.Rmd` — "245 columns instead of 220 or 25" predated FULL/SF domain scoring; FULL now appends 30 scores (25 facets + 5 domains) so it is **250 … or 30**. **pkgdown instrument pages:** fixed a mis-targeted REDCap "Import Instructions" link on `download-pid5.Rmd` (pointed at `generate_redcap_hitopsr`, a copy-paste from the SR template, → `generate_redcap_pid5`); grammar "domains scales"→"domain scales" on the three PID download pages; reconciled the "Instrument Information" card so only HiTOP-SR (the one page that links a definitions article) promises "subscale definitions", and the "Example Datasets" card so only pages that link a `ku_*` dataset (hitopsr/hitopbr/pid5sf) claim "a real validation sample"; normalized the download-card row markup across all six pages to `row mt-4` + `col-md-4 mb-4`, removing two stray `<br>`s and fixing hitopbr's features-row indentation. Added one NEWS.md bullet under 0.2.0.
- **Why / decisions:** No D-entry — documentation accuracy/consistency only, no architecture or API change. Scope was "fixes + finish + harmonize" (no visual redesign of the tutorials, no restyling toward the download-page card look) per Jeff's planning-time decision; `scales-hitopsr.Rmd` deliberately stays out of the navbar (reachable from the SR download page) and was not restructured, only verified to build. Every numeric/name claim was re-derived against actual code output rather than trusted, which is what surfaced the extra `pid5_scoring.Rmd` (250/30) and confirmed the BR (8/55/16) and SR (76/483/152) counts. Verification: `devtools::check()` **0 errors | 0 warnings | 0 notes** (2m07s, incl. rebuilding the five package vignettes); `pkgdown::build_site()` exit 0, rendering all six download pages, five tutorials, and the scales article; spot-checked the rendered HTML so corrected prose matches the rendered tibble dimensions (BR 411×8/×55/×16, PID-5 100×250, BF has the reliability table and no "WORK IN PROGRESS", SR has zero stale PRO tokens). All 37 distinct `../reference/*.html` targets resolve to exported objects and all 24 `inst/extdata/*` download links exist on disk. `download-hitopsr.Rmd` needed no changes (its boilerplate was already accurate).
- **Follow-ups spawned:** — M17 review pending (`/review-milestone M17`). M16 (norm-ready official-range scoring) stays PLANNED until PID-5 normative data arrives.

## 2026-07-10 — Reviewed, merged & closed out M15 (M15)

- **What:** Verified all 7 M15 acceptance criteria against PR [#16](https://github.com/jmgirard/hitop/pull/16) (branch `m15-api-polish`) by **execution**, not by trusting checkboxes. (C1) `score_*()` formals show `missing` and no `na.rm`/`apa_scoring`/`alpha`/`omega`/`tibble`; each level independently recomputed — `missing="available"` == hand `rowMeans(na.rm=TRUE)`, `"complete"` == `rowMeans(na.rm=FALSE)`, `"apa"` == hand `apa_mean` (rounded proration) on FULL Anhedonia; the three agree on complete rows 1–3; `score_pid5` default ≡ `missing="apa"` and `score_hitopbr` default ≡ `missing="available"` (defaults unchanged); `score_hitopbr(missing="apa")` errors. (C2) `reliability_pid5/hitopsr/hitopbr` all in `getNamespaceExports`, return `scale`/`nItems`/`alpha` (omega omitted when `FALSE`), BF → 5 rows; `score_pid5(...)` prints nothing (0 captured lines). (C3) `reliability_hitopbr()$alpha` == independent `calc_alpha` per scale on `ku_hitopbr`; `reliability_hitopsr()` reverse-keys HSR 310 (`5-x`) before alpha; NA-safe (all-NA, no abort) on a zero-variance matrix. (C4) `tibble` absent from all five formals; scoring/validity/rank/reliability outputs all `tbl_df`. (C5) `rank_scales(reverse="hsr_wellBeing", srange=c(1,4))` reflects so row1 top = anxiety / row2 = wellBeing (vs unreflected row1 = wellBeing), default column `top_scales`, custom `name` + `append=FALSE` → 1-col tibble, errors on non-subset `reverse` and missing `srange`. (C6) DESCRIPTION 0.2.0, NEWS `# hitop 0.2.0`, `_pkgdown.yml` lists the 3 reliability fns, DESIGN D-012 present. (C7 gate) `document()` **no-diff**, `test()` FAIL 0 SKIP 1 **PASS 615**, `check()` **0 errors | 0 warnings | 0 notes** (2m39s incl. vignette rebuild + `--run-donttest`). No-scoring-change confirmed structurally: `git diff --name-only main...HEAD` touches no `data/`, `*_items`/`*_scales`, `sysdata`, or generated-data files. Fresh-context general-purpose subagent (had not seen the implementation) traced the engine `missing`→`scale_fun`/`domain_narm` mapping (each level ↔ exactly one old flag combo, `domain_narm <- missing == "available"` collapsing the old `if(apa_scoring) FALSE else na.rm`), confirmed `mask_se_na` untouched, the reflection math/validation correct, `alpha`/`omega`/`tibble` fully removed from signatures and internals, and the oracles genuine (hand-computed PID fixtures preserved, not code-derived) — **no blockers**. Merged PR #16 to `main` (`9c23e9d`) after Jeff's yes/no authorization; re-verified criteria on up-to-date main. M15 → DONE.
- **Why / decisions:** D-012 (written during implementation) records the four changes and rationale. Two review nits, both **fixed at review** (test-only, no logic change): (1) the BR reliability test read `hitopbr_scales$itemNumbers` on both sides (a plumbing/names check, not a table oracle), so a hardcoded Detachment (items 7,12,30,31,36,37) `calc_alpha` oracle was added — now a genuine independent recompute (suite 614→615); (2) stale `apa_scoring` wording in a `test-score_pid5.R` title + section header corrected to `missing`. Doc sync done inline: ROADMAP Phase 4 "pre-CRAN API stabilization (M15)" outcome marked done; DESIGN Function-families (Scoring/Reliability), signature convention, internal-utilities (`prep_items`), and D-012 were updated in the implementation commit; CLAUDE.md scoring-signature line updated. No DESIGN Known issue closed — the four remaining are scoring/keying debt (the `_se`-where-`NA` unification, #4, still awaits sign-off); Phase 2 was already DONE, so no phase marker advanced.
- **Follow-ups spawned:** — M16 (norm-ready official-range scoring + input rescaling) stays PLANNED until PID-5 normative data arrives; it is the only Active milestone, so no READY milestone remains. `_se`-where-`NA` unification (DESIGN Known issue #4) still deferred pending sign-off.

## 2026-07-10 — Pre-CRAN API polish: reliability family, `missing=`, `rank_scales`, drop `tibble` (M15)

- **What:** Implemented the five breaking API changes on branch `m15-api-polish` (last of the five 2026-07-10 audit milestones), bumping to **v0.2.0**. (1) **Returning reliability family:** new exported `reliability_pid5(version=)`/`reliability_hitopsr()`/`reliability_hitopbr()` ([R/reliability_pid5.R](../R/reliability_pid5.R) etc.), each a thin wrapper over a new unexported `reliability_engine()` ([R/reliability_engine.R](../R/reliability_engine.R)) that returns a per-scale tibble (`scale`, `nItems`, `alpha`, `omega`; a column present iff requested, both default `TRUE`); per-scale `calc_alpha`/`calc_omega` stay `tryCatch`-wrapped so a degenerate scale or absent lavaan yields `NA` not an abort. Removed the print-only `alpha`/`omega` args from all three `score_*()` and deleted the engine's reliability/print block. (2) **`missing=` consolidation:** `score_pid5()` gains `missing = c("apa","available","complete")` replacing `apa_scoring`+`na.rm`; `score_hitopsr()`/`score_hitopbr()` gain `missing = c("available","complete")` replacing `na.rm` (no `apa` — the proration rule is PID-5-specific). Engine maps the level to `scale_fun`/`domain_narm` via `switch`; the old contradictory-flags warning is gone. Defaults reproduce the old defaults exactly. (3) **`rank_scales()`:** new `name` (default `"top_scales"`) names the output column (was hard-coded `"out"`); new `reverse`/`srange` reflect reverse-directioned scales via `sum(srange) - value` before ranking so a scale like HiTOP-SR `wellBeing` (higher = healthier) ranks on the shared "higher = more elevated" metric; validates `reverse ⊆ scales` and requires `srange` with it. (4) **Dropped `tibble`** from `score_pid5/hitopsr/hitopbr`, `validity_pid5`, `rank_scales` (+ engine) — all now always return a tibble; `rank_scales(append=FALSE)` returns a one-column tibble named by `name` rather than a bare vector. Extracted the shared validate → extract → coerce → reverse-key step into `prep_items()` ([R/util.R](../R/util.R)), now used by both engines. Updated the three scoring vignettes to call `reliability_*()`, DESCRIPTION Version, four NEWS bullets, `_pkgdown.yml` (3 new reference entries), and the CLAUDE.md/DESIGN signature docs.
- **Why / decisions:** D-012 records all four changes and the rationale. Oracle strategy: the `missing=` refactor is behavior-preserving per level, so the existing hand-computed APA/available fixtures were re-pointed to `missing=` (never re-derived from the code) and a new block asserts the three levels agree on complete data and diverge as specified under missing; the reliability family is checked by **independent recomputation** — `reliability_hitopbr()` vs hand `calc_alpha` per scale on `ku_hitopbr` (ports the M13 print oracle), plus reverse-keying oracles that hand-reverse HSR item 310 / PID FULL's 16 reverse items and match one scale's alpha, plus NA-safety on a zero-variance scale. Scoring `srange` semantics deliberately untouched (norm-range reconciliation is M16). `mask_se_na` unification (DESIGN Known issue #4) still deferred. `document()` clean (only intended `.Rd`/NAMESPACE changes); `test()` FAIL 0 SKIP 1 **PASS 614** (was 590; +24 net after dropping the old reliability-wiring/`tibble`-toggle tests); `check()` **0 errors | 0 warnings | 0 notes** (2m00s incl. vignette rebuild + `--run-donttest`). No keying/`data/`/`sysdata`/generated-data files touched.
- **Follow-ups spawned:** — M15 review pending (`/review-milestone M15`). M16 (norm-ready official-range scoring + input rescaling) stays PLANNED until normative data arrives. `_se`-where-`NA` unification (Known issue #4) still awaits sign-off.

## 2026-07-10 — Reviewed, merged & closed out M14 (M14)

- **What:** Verified all 5 M14 acceptance criteria against PR [#15](https://github.com/jmgirard/hitop/pull/15) (branch `m14-validation-hardening`) by execution, not by trusting checkboxes. (C1) `validate_items()` type message says "character or integer vector"; length message names both expected (`220`) and actual (`100`). (C2) presence guard aborts naming the offender in **both** `score_pid5` (engine path) and `validity_pid5` (direct path), for a bad NAME (`not_a_column`) and a bad POSITION (`ncol+50`). (C3) caught-condition `call` (`rlang::catch_cnd()` → `rlang::call_name()`) equals the exported function for all seven surfaces — `score_pid5`/`score_hitopsr`/`score_hitopbr`/`validity_pid5`/`rank_scales`/`calc_alpha`/`calc_omega` (proves the `score_engine()` threading blames the wrapper, not the engine). (C4) `grep -rl globalVariables R/` → exactly one file (`hitop-package.R`), its list contains all 10 datasets incl. `pid_scales`, and `R/zzz.R` is gone. (C5) gate. Standard gate on the branch and re-verified on merged `main`: `document()` **no-diff** on `NAMESPACE`/`man/`/`R/`; `test()` FAIL 0 SKIP 1 **PASS 590** (was 585; the +5 are all in `test-validate.R`, `tests/` diff **+85/−0** so no existing numeric assertion was edited); `check()` **0 errors | 0 warnings | 0 notes** (2m10s). No-scoring-change confirmed structurally: `git diff --name-only main...HEAD` touches no `data/`, `*_items`/`*_scales`, `sysdata`, `NAMESPACE`, or `man/`. A fresh-context general-purpose subagent (had not seen the implementation) traced the `call`/`.envir` split on both the engine-threaded and direct-call paths — confirmed every path resolves to the exported function and none to `score_engine`/`cli_assert`/a `validate_*` helper — checked the cli messages are well-formed and the tests non-tautological (real caught-condition `call`, end-to-end through exports), and returned **PASS-WITH-NITS**. Merged PR #15 to `main` (`a0da7cf`) after Jeff's yes/no authorization; re-verified all criteria on up-to-date main. M14 → DONE.
- **Why / decisions:** No D-entry — hardening, no architectural/API change (the only new function param, `score_engine(call=)`, is internal `@noRd`). Three review nits, all dispositioned **no-change** (none affect correctness or the criteria): (1) an `NA` `items` position prints "out of range: NA" rather than a dedicated message — still aborts cleanly instead of the cryptic base-R error, so acceptable; (2) `validate_items_present()`'s "type already checked" assumption is documented and unreachable in the real call order (a factor/list aborts at `validate_items()` first); (3) wrong-**type** attribution isn't separately asserted end-to-end (wrong-length and missing-items are) — low value. Warning attribution stays descoped (cli_warn exposes no `call`), matching criterion 3's abort-only scope. DESIGN's Internal-utilities and User-communication sections were updated in the implementation commit; no DESIGN Known issue closed (all four are scoring/keying debt) and no ROADMAP phase outcome completed (Phase 2 already DONE; the audit's code-quality milestones aren't individual ROADMAP outcomes), so doc sync is limited to MILESTONES + this LOG.
- **Follow-ups spawned:** — M15 (API polish: `reliability_*()` return family, drop the vestigial `tibble` arg, name `rank_scales()`'s output column, `na.rm`/`apa_scoring` → `missing=` consolidation) is the **last** audit milestone; its dep (M13) is DONE so it is ready to plan, but every item is a deliberate breaking change needing Jeff's per-item sign-off at planning time. The `_se`-where-`NA` unification (DESIGN Known issue #4) still awaits sign-off.

## 2026-07-10 — Input-validation & messaging hardening (M14)

- **What:** Implemented M14 on branch `m14-validation-hardening` — the fourth of five audit milestones (validation/messaging only, no scoring-behavior change). (1) `validate_items()` ([R/util.R](../R/util.R)) now splits the old single combined check into a **type** assertion ("must be a character or integer vector", reporting `{.cls {class(x)}}`) and a **length** assertion that names both the expected count and the actual (`Expected {n} item{?s} but got {length(x)}`). (2) New `validate_items_present(data, items, call)` aborts before the base-R `data[items]` extraction, naming the character columns absent from `data` (`setdiff(items, names(data))`) or the integer positions outside `1:ncol(data)` — wired in after `validate_item_uniqueness()` in both [score_engine.R](../R/score_engine.R) and [validity_pid5.R](../R/validity_pid5.R) (the two `data[items]` sites). (3) **Call attribution:** `cli_assert()` gained a `call` arg (default `rlang::caller_env()`) forwarded to `cli_abort(call = ...)`, plus `.envir = rlang::caller_env()` so message `{glue}` interpolates in the validator's frame (where `x`/`n` live) while `call` still blames the user-facing function; every validator (`validate_data`/`validate_items`/`validate_items_present`/`validate_scales`/`validate_range`/`validate_item_uniqueness`) got the same `call` param, and `score_engine()` gained a `call = rlang::caller_env()` param it threads to all its validators so aborts blame the **wrapper**, not the engine. Direct callers (`validity_pid5`, `rank_scales`, `calc_alpha`/`calc_omega`) get correct attribution via the `caller_env()` default. (4) **globalVariables cleanup:** added `pid_scales` to the single top-level `utils::globalVariables()` list in [R/hitop-package.R](../R/hitop-package.R) and **deleted `R/zzz.R`** (its `.onLoad`-based `globalVariables(pid_items, pid_scales)` was the wrong idiom — ineffective for the check NOTE — and duplicated an inconsistent subset of the real list). Test-first: 5 new `test-validate.R` blocks (type-vs-length messages incl. expected/actual numbers; `validate_items_present` fire/no-fire at helper level + end-to-end bad-name and bad-position through `score_pid5` and `validity_pid5`; call-attribution via `rlang::catch_cnd()` + `rlang::call_name()` across score/validity/reliability/rank; a valid-call no-error invariant). Standard gate: `document()` **no-diff** (NAMESPACE/man unchanged — the only roxygen touched is `@noRd` `score_engine`); `test()` FAIL 0 SKIP 1 **PASS 590** (was 585; +5); `check()` **0/0/0** (2m22s). Two NEWS.md bullets.
- **Why / decisions:** No D-entry — hardening, no architectural or API change (existing signatures unchanged; `score_engine`'s new `call` is an internal `@noRd` param). Warning attribution was **descoped from the plan's wording**: `cli::cli_warn()` has no `call` argument, so `warn_item_order()`'s and the engine's `apa_scoring`/`na.rm` warnings stay unattributed; the fix scopes to aborts, which is exactly what acceptance criterion 3 tests. Presence guard aborts (not warns) per Jeff's planning-time decision — scoring can't proceed without the columns and base R already errors, just cryptically. Reliability surface (`calc_alpha`/`calc_omega`) included per Jeff's decision — free, since threading `call` through `cli_assert` fixes their 13 asserts automatically (their `caller_env()` is already the exported fn). The `.envir`/`call` split in `cli_assert` was the one implementation subtlety: the first test run failed because the new `{class(x)}`/`{n}` message globs were being interpolated in `cli_assert`'s frame (no `x`), fixed by pointing `.envir` at the caller while keeping `call` for attribution. Updated DESIGN's Internal-utilities and User-communication sections to describe the `call` threading; no Known-issue closed (all four are scoring/keying debt untouched here). No keying/data/generated-data files changed.
- **Follow-ups spawned:** — M15 (API polish: `reliability_*()` return family, `tibble` arg drop, `rank_scales` output-column naming, `na.rm`/`apa_scoring` consolidation) is the last audit milestone; its deps (M13) are DONE, so it is implementable. The `_se`-where-`NA` unification (DESIGN Known issue #4) still awaits Jeff's sign-off.

## 2026-07-10 — Reviewed, merged & closed out M13 (M13)

- **What:** Verified all 6 M13 acceptance criteria against PR [#14](https://github.com/jmgirard/hitop/pull/14) (branch `m13-scoring-engine`) by execution. Structural: `score_engine()` exists in [R/score_engine.R](../R/score_engine.R), has `@noRd`, is absent from `NAMESPACE`, and has no `man/` page; each pipeline element (reverse loop `low = srange`, `MARGIN = 1, FUN = calc_sem`, `reliability_df`, `apa_mean`/`rowMeans(data_items`) greps to `score_engine.R` **only**; all three wrappers call `score_engine(`; `rename_hitopsr_items()` is in its own file with 0 references left in `score_hitopsr.R`; `score_hitopbr()` has `alpha`/`omega` args + roxygen + man entries. Tests: `git diff main...` on `tests/` is **51 insertions, 0 deletions** in `test-score_hitopbr.R` alone (existing assertions unedited). Standard gate on the branch and re-verified on merged `main`: `document()` **no-diff** on `NAMESPACE`/`man/`/`R/`; `test()` FAIL 0 SKIP 1 **PASS 565**; `check()` **0/0/0**. Behavior-preservation re-confirmed by re-running the characterization harness against the `main` golden → **all 114 configs `identical()`**. A fresh-context general-purpose subagent (had not seen the implementation) independently loaded both branches and compared `score_*()` output with `all.equal(tolerance = 0)` across all six scoring paths incl. `calc_se = TRUE` — **all byte-identical** — and validated the BR alpha oracle as genuine ground truth (independent `calc_alpha` recomputation, guards its no-reverse assumption); returned **PASS-WITH-NITS**, no blockers. Merged PR #14 to `main` (`19abaaa`) after Jeff's yes/no authorization; re-verified criteria on up-to-date main. M13 → DONE.
- **Why / decisions:** Two review nits. (1) The engine's header carried `@noRd`/`@param` inside a plain `#` comment block, so the tags were inert (the function is unexported only because it has no `#' @export`); the intended effect held but the tags were misleading — fixed at review by converting the header to a real `#'` roxygen block with `@noRd` (commit `cdf43ed`; re-confirmed no `score_engine.Rd` generated, `document()` still no-diff, `check()` still 0/0/0). (2) The validation reorder (engine's `validate_data` now runs after the pid wrapper's `match.arg`) is observable only when BOTH `data` and `version` are invalid — no valid-input effect — so left as documented; not a defect. No keying content in the PR, so the sign-off gate was N/A.
- **Follow-ups spawned:** Unifying the `_se`-where-`NA` masking across instruments (DESIGN Known issue #4) — a HiTOP-SR/BR output change needing Jeff's sign-off; candidate for M15. M14 (input-validation hardening) is now unblocked (its deps M11+M13 are DONE) and is the next audit milestone.

## 2026-07-10 — Scoring engine consolidation + uniform reliability (M13)

- **What:** Implemented M13 on branch `m13-scoring-engine` — the third of five audit milestones (a behavior-preserving refactor plus one small additive feature). (1) Extracted the ~100-line pipeline that `score_pid5()`/`score_hitopsr()`/`score_hitopbr()` each hand-maintained into one unexported, data-driven `score_engine()` ([R/score_engine.R](../R/score_engine.R), `@noRd`): validate → extract → coerce → reverse-key → per-scale score (`apa_mean` vs `rowMeans`) → optional FULL/SF domain scores + domain SE → optional `_se` block → optional alpha/omega print → append → tibble. The three exported functions are now thin wrappers that resolve only their instrument data (reverse-item positions, per-scale item-number lists, and — FULL/SF — the domain map) and call the engine (score files down to 148/75/75 lines, ~90 of the pid file being roxygen). (2) `score_hitopbr()` gained `alpha`/`omega` args (default `FALSE`), passed through to the engine — closing an incompleteness (BR runs the identical `rowMeans` pipeline and its scales already reliability-check via `calc_alpha`/`calc_omega`, M5), with matching roxygen (`@param`×2 + reliability `@details` + `@return` note) and 3 new tests in `test-score_hitopbr.R` (print-wiring + scores-unchanged, omega no-error, and an independent-recomputation oracle asserting the exact printed alpha table on `ku_hitopbr`). (3) Relocated `rename_hitopsr_items()` verbatim from `score_hitopsr.R` into its own [R/rename_hitopsr_items.R](../R/rename_hitopsr_items.R). `document()` regenerated only `score_hitopbr.Rd` (new params) and `rename_hitopsr_items.Rd` (source-file pointer); NAMESPACE unchanged; no `score_engine.Rd` (`@noRd`). Standard gate: `test()` FAIL 0 SKIP 1 **PASS 565** (was 561; +4 from the BR tests), `check()` **0/0/0** incl. `--run-donttest` and vignette re-build.
- **Why / decisions:** Behavior-preservation was verified by an out-of-band **characterization harness** (scratchpad, uncommitted): captured every `score_*()` return value + printed reliability text + warnings across the full argument matrix (version × `apa_scoring`/`na.rm`/`calc_se`/`append` × `alpha`/`omega`) on all `sim_*`/`ku_*` datasets on `main` (114 configs), then re-ran on the branch and asserted `identical()`. First pass surfaced **4 mismatches** — `ku_hitopsr`/`ku_hitopbr` at `na.rm = FALSE, calc_se = TRUE`: the shared engine carried `score_pid5()`'s `sems_scales[is.na(out)] <- NA` masking (SE → `NA` where the score is `NA`), which `score_hitopsr()`/`score_hitopbr()` **never had**. Rather than silently adopt pid5's (arguably nicer) masking for SR/BR — a scoring-output change outside a refactor's remit — I preserved each function's exact prior behavior behind a `mask_se_na` flag (pid wrapper passes `TRUE`; SR/BR default `FALSE`); re-run → **all 114 `identical()`**. This pre-existing inconsistency is now recorded as DESIGN Known issue #4, and unifying it (needs sign-off) is deferred. Added **D-011** (engine design); resolved and removed old Known issue #3 (pipeline triplicated), renumbering srange #4→#3. The immaterial validation reorder (engine's `validate_data` now runs after the pid wrapper's `match.arg`) is noted in MILESTONES; `apa_scoring`'s bool check moved into the engine keeping its original relative order (after `validate_items`). No keying/data/generated-data files changed.
- **Follow-ups spawned:** Unifying the `_se`-where-`NA` masking across instruments (DESIGN Known issue #4) — a HiTOP-SR/BR output change needing Jeff's sign-off; candidate for M15 alongside the reliability-return redesign. M14/M15 remain PLANNED (M14 depends on M13, now implementable).

## 2026-07-10 — Reviewed, merged & closed out M12 (M12)

- **What:** Verified all 9 M12 acceptance criteria against PR [#13](https://github.com/jmgirard/hitop/pull/13) (branch `m12-docs-examples`) by execution. Text criteria confirmed by grep: 27/27 exports have an Rd `\examples` section; `pid_items` `@format` says 15 cols, `hitopbr_items` 8, `pid_scales` gained a `@format`+`@examples`; DESCRIPTION Title Case + markdown-free Description (0 asterisks in the block); README checkbox `[x]`; vignette typo gone; `validity_pid5` BF-PNA note present; lifecycle badge still `experimental`. Standard gate: `document()` no-diff; `test()` FAIL 0 WARN 0 SKIP 1 **PASS 561**; `check()` **0/0/0** including `checking examples ... OK`, `checking examples with --run-donttest ... [14s] OK` (docx examples executed), and vignette re-build. Confirmed the "no behavior change" criterion mechanically — every added/removed line in `R/*.R` is a roxygen `#'` line (non-comment diff is empty), and no `data/*.rda`, `R/sysdata.rda`, or `NAMESPACE` is touched, so the keying-content sign-off gate is N/A. A fresh-context general-purpose subagent (had not seen the implementation) ran every example under `load_all()` and returned **PASS with two CONFIRMED minor findings**, both in `R/data.R` `@format` text: (1) the new `pid_scales` block labeled the first column `Facet`, but `pid_scales[["BF"]]`'s first column is actually `Domain` (FULL/SF use `Facet`) — a newly-introduced inaccuracy in a doc-accuracy milestone; (2) `hitopbr_items` `@format` said `\item{PFactor}` but the column is `Pfactor` (pre-existing, but inside the exact `@format` block M12 already edits). I verified both against the loaded `.rda` and fixed them (commit `150224a`): `pid_scales` now documents "Facet (named `Domain` in the BF element)", `hitopbr_items` says `Pfactor`. Re-ran `document()` (only the two intended `.Rd` changed) and `check()` again → **0/0/0**. Merged PR #13 to main (`8d4f350`) after Jeff's yes/no authorization; re-verified the criteria on up-to-date main (`document()` no-diff, all doc facts present). M12 → DONE.
- **Why / decisions:** No D-entry — documentation work, no architectural or API change. No DESIGN Known-issue closes (all four are scoring/validation debt, untouched by M12). No ROADMAP phase outcome completed (Phase 2 was already fully DONE; the README "unit testing" checkbox is a mirror update, not a new outcome) and no CLAUDE.md tooling change, so tracking-doc sync is limited to MILESTONES + this LOG. The two `@format` bugs are exactly the failure mode a fresh-context reviewer exists to catch (self-review would likely have trusted the FULL-element inspection I did at planning time and missed BF's `Domain` column); fixing at review rather than shipping a known-inaccurate `@format` in a milestone whose entire purpose is doc accuracy. The `sim_hitopbr` column-name fix (`hitopbr_*` not `hbr_*`) and the two review fixes are doc/example-only — no data regenerated.
- **Follow-ups spawned:** — M13 (scoring-engine consolidation) is next in the agreed audit order (its deps are met); M14/M15 remain PLANNED.

## 2026-07-10 — Documentation accuracy & examples (M12)

- **What:** Implemented M12 on branch `m12-docs-examples` — the second of five milestones from the 2026-07-10 retrospective design audit (docs, no scoring change). (1) Added runnable `@examples` to all **27 exported functions** (previously none had any): the 4 scoring/validity fns use the `sim_*` datasets; `calc_alpha`/`calc_omega` use the HiTOP-BR Detachment scale of the real `ku_hitopbr` data (α=0.78/ω=0.78 — a coherent scale so the omega CFA converges cleanly), with `calc_omega` behind `@examplesIf requireNamespace("lavaan", quietly = TRUE)` (lavaan is Suggests); `rank_scales`/`label_*`/`rename_hitopsr_items` build from scored output / small constructed frames; the 17 `generate_*` fns write to `tempfile(fileext=...)` so nothing lands in the working dir. The 6 `generate_docx_*` examples are wrapped in `\donttest{}` — the HiTOP-SR docx build is ~4.4s, close to CRAN's per-example NOTE threshold on slower machines; the fast Qualtrics/REDCap examples run unconditionally. (2) Fixed dataset docs in [R/data.R](../R/data.R): `pid_items` `@format` 12→15 columns, `hitopbr_items` 5→8; added a full `@format` + `@examples` to `pid_scales` (documented as a named `list(FULL, SF, BF)` of 5-column tibbles — it had none). (3) Fixed a real accuracy bug surfaced while verifying the `label_hitopbr` example: `sim_hitopbr`'s item columns are actually `hitopbr_1..hitopbr_45`, not the `hbr_1..hbr_45` its `@format` claimed — corrected the doc and the example prefix (all other `sim_*`/`ku_*` column-name claims were re-audited and are correct). (4) Rewrote the DESCRIPTION `Title` (Title Case, "Society" capitalized: "Score and Distribute HiTOP Society Questionnaire Instruments") and `Description` (markdown-free multi-sentence paragraph, `'Qualtrics'`/`'REDCap'` single-quoted). (5) Fixed the "example example" typo in [vignettes/pid5_scoring.Rmd](../vignettes/pid5_scoring.Rmd). (6) Checked "Add Package Unit Testing" done in README.Rmd Development Progress and re-knit README.md. Three NEWS.md bullets. `document()` regenerated 32 `.Rd` (Examples additions + the 4 `@format`/Description fixes; NAMESPACE unchanged); `test()` FAIL 0 WARN 0 SKIP 1 **PASS 561** (unchanged — doc-only); `check()` **0/0/0** including `checking examples with --run-donttest ... OK` (docx examples executed, 14s) and vignette re-build.
- **Why / decisions:** No D-entry — documentation work, no architectural or API change. Lifecycle badge kept `experimental` per Jeff (v0.1.0, M15 breaking changes still pending) — no README badge change. `\donttest{}` chosen only for the docx generators (the sole >~4s examples) so routine `devtools::check()`/`run_examples()` stay fast while `--as-cran` still executes them; verified they actually run via `run_examples(run_donttest = TRUE)` and the check's `--run-donttest` pass. The `validity_pid5` BF-returns-only-PNA doc item from the audit was already satisfied by M11's `@details`, so it needed no change (verified). The `sim_hitopbr` column-name fix is a doc/example correction only — no data regenerated, `sim_hitopbr.rda` untouched. No keying/data/generated-data files changed.
- **Follow-ups spawned:** — M13 (scoring-engine consolidation) is next in the agreed audit order; M14/M15 remain PLANNED.

## 2026-07-10 — Reviewed, merged & closed out M11 (M11)

- **What:** Verified all 7 M11 acceptance criteria against PR [#12](https://github.com/jmgirard/hitop/pull/12) (branch `m11-item-guards`) by execution. Standard gate: `document()` no-diff (NAMESPACE/man unchanged); `test()` FAIL 0 WARN 0 SKIP 1 **PASS 561**; `check()` **0/0/0**. Confirmed criterion 4 rigorously — the diff to pre-existing tests is *purely additive* (`git diff` shows zero deleted lines in `test-validity_pid5.R`; no helper/fixture edits), so the oracle suites run unmodified. Behaviorally re-verified all three guards (srange fire FULL/SF, no-fire default + BF; order fire/no-fire incl. zero-padded, mixed-prefix, integer-position, non-numbered; duplicate abort naming the dupes). Diff scope is `R/` + `man/` + tests + NEWS/tracking only — no `data/`, `*_items`/`*_scales`, or `NAMESPACE`, so non-keying (no sign-off gate). A fresh-context general-purpose subagent (had not seen the implementation) returned **PASS-WITH-NITS**: it independently confirmed each guard's fire/no-fire logic and that both helpers are pure side effects (`return(invisible(NULL))`, run before `data[items]`, never reassign `items`/`data`), and flagged two genuine test-quality nits — both "no behavior change" tests were tautological (neither routed through its warning branch: the srange test compared two identical `c(0,3)` calls; the order test compared two non-warning paths). I verified both against the source, then fixed them: the srange test now routes through `srange = c(1,4)` and asserts PNA + ORS against an independent recomputation (ORS `== 4` is bug-discriminating against a hardcoded 3), and the order test now scores warning-triggering misordered *names* and asserts equality with the same columns selected by *position* (proving `warn_item_order()` only messages, never reorders); also added `validity_pid5` to the order no-fire end-to-end coverage (nit #2). Nits #3 (direct `cli_abort`/`cli_warn` vs house `cli_assert`) and #4 (integer-overflow in `as.integer`) verified as correctly dispositioned — deferred to M14 / irrelevant at max item 650. Fix commit `951408e`; suite grew to PASS 561. Merged PR #12 to main (`fb2ba48`) after Jeff's yes/no authorization. M11 → DONE.
- **Why / decisions:** No new D-entry — no architectural change (a validation/messaging layer, not a scoring change). Doc sync on close-out: updated DESIGN Known issue #4 (the silent `srange` case is now closed by the M11 warning; cutoff auto-adjustment stays deferred pending sign-off) and the DESIGN Validity blurb (added the srange warning + the shared `items` guards). Known issue #3 (scoring triplication → M13) left open. ROADMAP/CLAUDE.md unchanged (no phase outcome completed, no tooling change). The tautological-test catch is exactly the oracle-strategy failure mode (a test that asserts nothing discriminating); fixing at review rather than shipping known-misleading tests.
- **Follow-ups spawned:** — M12–M15 remain PLANNED from the audit (agreed order M12 docs → M13 engine → M14 hardening → M15 API polish). M14 should retire the direct-`cli_abort`/`cli_warn` pattern in these guards once `cli_assert` threads `call`.

## 2026-07-10 — Guard the silent-wrong-results traps (M11)

- **What:** Implemented M11 on branch `m11-item-guards` — the first of five milestones from the 2026-07-10 retrospective design audit (risk-first, ahead of M12 docs / M13 engine / M14 hardening / M15 API polish). Three guards against silently wrong scores, all warn/abort only (no scoring behavior changes): (1) `validity_pid5(version = "FULL"/"SF")` now warns when `srange != c(0, 3)`, because PRD/SD-TD are raw sums against fixed thresholds (10/11/19) that assume 0-3 coding and — unlike INC/ORS — don't adapt to `srange` (BF computes PNA only, so no warning); placed after `validate_range()` in [R/validity_pid5.R](../R/validity_pid5.R). (2) New `warn_item_order()` in [R/util.R](../R/util.R): warns when every `items` entry is a character name sharing one common prefix + trailing integer and those integers are not ascending (the misordered-mapping trap); integer positions, mixed prefixes, and non-numbered names are left alone. (3) New `validate_item_uniqueness()` in `R/util.R`: aborts on duplicated `items`, naming them. Both helpers are called right after `validate_items()` in all four data-taking functions (`score_pid5`, `score_hitopsr`, `score_hitopbr`, `validity_pid5`). Tests: new `test-item-guards.R` (helper-level fire/no-fire units + end-to-end across all four functions + a "scores unchanged" guard) and an srange block appended to `test-validity_pid5.R`. Roxygen `items` param updated in the three score fns (validity inherits via `@inheritParams`) + a `@details` note in `validity_pid5()` on the srange/cutoff interaction and BF-PNA-only; `document()` regenerated 4 `.Rd`; two NEWS.md bullets. Suite FAIL 0 WARN 0 SKIP 1 **PASS 558** (was 527); `check()` **0/0/0**.
- **Why / decisions:** Warning (not abort or auto-adjust) for the srange case so legitimate non-0-3 workflows stay possible; auto-shifting the cutoffs (cutoff + k×low) changes validity-scale semantics and is deferred pending Jeff's sign-off (recorded in the M11 Notes and DESIGN Known issue #4). Order guard is a names-only heuristic: out-of-order *integer positions* can be a legitimate remap, but numbered *names* out of order almost never are. `validate_item_uniqueness()` calls `cli::cli_abort()` directly rather than the house `cli_assert()` helper, because `cli_assert` forwards a pre-built message string and cli evaluates `{.val {dups}}` glue in `cli_assert`'s frame (where `dups` doesn't exist) — the direct call resolves glue in the helper's own frame (same reason `warn_item_order` calls `cli::cli_warn` directly). This is a symptom of `cli_assert` not threading `.envir`/`call`; folding these guards into a call-aware assertion is left to M14 (which already plans `call` threading through `cli_assert`). No D-entry — no architectural change; DESIGN Known issues #3/#4 were added at planning and stay open (M11 mitigates #4's user-facing risk but doesn't auto-correct the metric). No keying/data/generated-data files touched.
- **Follow-ups spawned:** — M12–M15 already PLANNED from the audit. M14 should retire the direct-`cli_abort`/`cli_warn` pattern in these guards once `cli_assert` threads `call`.

## 2026-07-10 — Reviewed, merged & closed out M10 (M10)

- **What:** Verified all 8 M10 acceptance criteria against PR [#11](https://github.com/jmgirard/hitop/pull/11) (branch `m10-generator-tests`) by execution. Standard gate: `document()` no-diff (NAMESPACE/man/ unchanged); `test()` FAIL 0 WARN 0 SKIP 1 **PASS 527** (grew from 422, only the pre-existing OQ-1 skip); `check()` **0/0/0**. Confirmed the diff is tests + DESCRIPTION/NEWS/tracking only — no `R/`, `data/`, `man/`, or `NAMESPACE`, so non-keying (no sign-off gate). A fresh-context general-purpose subagent (had not seen the implementation) returned **PASS-WITH-NITS**: it independently re-derived all five hardcoded HSUM branching-logic strings from the rule logic in `R/generate_redcap.R` + the `hitophsum_choices` data (count>1, any_other with the {0,1,99} exclusion, comparison `>=`, discrete radio + discrete checkbox) and all three field-type overrides — every one matched — and confirmed the parse-and-compare oracle is genuine (not an output snapshot): set-equality is guarded by preceding count assertions so an empty parse fails, REDCap strings compared with `expect_identical`, mutation-sensitivity holds. Three non-blocking nits (docx content oracle is substring-level; qualtrics choices checked on the first question only; the HSUM discrete comma-list branch is unexercised by current data) — all already acknowledged in D-010/Notes. Merged PR #11 to main (`e53ca1f`) after Jeff confirmed all instruments (not just HSUM) are covered. M10 → DONE.
- **Why / decisions:** No new D-entry at review — `D-010` (parse-and-compare oracle for file generators) was recorded during implementation. Doc sync on close-out: DESIGN Known issue #3 already removed + coverage sentence updated + `D-010` in the merged PR; added a Phase 2 outcome bullet to ROADMAP and advanced its related-milestones range to M1–M10. Coverage-depth note for the record: the shared-path instruments (hitopsr/pid5sf/pid5bf) get smoke + item-count-vs-source because they funnel through `build_qualtrics_txt`/`build_redcap_zip`, which hitopbr + pid5 exercise exhaustively; HSUM's bespoke path and the docx papersize branch get dedicated deep tests.
- **Follow-ups spawned:** — Phase 3 (norms/percentiles + profile visualization) is the next direction via `/plan-milestone`; norming is gated on a PID-5 normative source from Jeff.

## 2026-07-10 — Test the generate_* export family (M10)

- **What:** Added ground-truth tests for all 17 DOCX/Qualtrics/REDCap generator functions (the last implemented-but-untested surface, DESIGN Known issue #3). New `tests/testthat/helper-generators.R` parses each artifact back into R (`read_qualtrics`/`read_redcap_csv`/`read_docx_xml` + `docx_page_size`). `test-generate_qualtrics.R` (item number/text set-equality vs source key, choice pairs, AdvancedFormat header, ID zero-pad width, page-break positions, `include_instructions`/`breaks` toggles, smoke ×5); `test-generate_redcap.R` (unzip→CSV, 15 columns in exact order, instruction row + items, `prefix_NNN` padding, `v, l | ...` choices, `Required` y/n, `<br>` page breaks, smoke ×5, plus an HSUM block hand-deriving the four branching-logic rule types and the quantity field-type/choice overrides); `test-generate_docx.R` (valid docx, item text present, us/a4 `<w:pgSz>` branch, `match.arg` error, smoke ×6). Every HSUM branching string and override was hand-derived from the rule logic + `hitophsum_choices` values and confirmed against actual output before being hardcoded as the oracle. `withr` → Suggests; NEWS.md bullet; DESIGN Known issue #3 removed + coverage sentence updated.
- **Why / decisions:** `D-010` — parse-and-compare oracle for file generators (independent-recomputation analog), not output snapshots; `inst/extdata/` artifacts deliberately not used as oracle; zip/officer skip guards. No `R/` changes: no generator bug surfaced (test-first, all passed against unmodified source). Noted (non-blocking): docx A4 uses rounded 8.27×11.69 in → ~11909×16834 twips (~5 off exact ISO A4); the HSUM discrete-list comma branch is unexercised by current data. Suite PASS 527 (was 422), FAIL 0 WARN 0 SKIP 1; `check()` **0/0/0**.
- **Follow-ups spawned:** — Phase 3 (norms/percentiles + profile visualization) remains the next direction via `/plan-milestone`.

## 2026-07-10 — Reviewed, merged & closed out M9 (M9)

- **What:** Verified all 5 M9 acceptance criteria against PR [#10](https://github.com/jmgirard/hitop/pull/10) (branch `m9-single-row-se`) by execution. Standard gate: `document()` no-diff; `test()` FAIL 0 WARN 0 SKIP 1 PASS 422 (only the pre-existing OQ-1 skip); `check()` **0/0/0**. Independently confirmed the fix is the lone single-row offender: reverted `R/score_pid5.R` to `main` and reproduced the exact `"dim(X) must have a positive length"` crash on a 1-row FULL `calc_se = TRUE` call, then confirmed the restored `drop = FALSE` makes it run. Empirically re-derived the three SE oracles (FULL Anhedonia `sd(c(1,1,1,2,1,2,1,1))/sqrt(8)` with reverse items 30/155→2, SF Anhedonia `sd(c(0,1,2,3))/sqrt(4)`, BF Disinhibition `sd(c(0,1,2,3,3))/sqrt(5)`) and matched the package output; confirmed the single-row-≡-first-row-of-multi-row consistency guard. A fresh-context general-purpose subagent (had not seen the implementation) returned **PASS** — it re-derived all three oracles against `pid_scales`/`pid_items`, audited every other matrix-index path in `score_pid5` (scale scores, domain rowMeans, domain SEs, `apa_mean`, alpha/omega all already `drop = FALSE`-guarded), and ran the tests (score_pid5 PASS 88, interface PASS 39). No keying content in the diff, so no keying sign-off required. Merged PR #10 to main (`235cf45`) after Jeff's yes/no authorization. M9 → DONE.
- **Why / decisions:** No D-entry — a one-line bug fix, no architectural change. Doc sync on merge: removed DESIGN Known issue #4 (the single-row `calc_se` crash, now fixed). Criterion 3 ("multi-row unchanged") is verified by internal consistency (single-row ≡ row 1 of a multi-row score) rather than a golden snapshot, which is adequate since `drop = FALSE` is provably a no-op on multi-row matrices and the full 422-test suite shows no regression.
- **Follow-ups spawned:** — (M9 resolved the sole open follow-up from M8). No READY milestone remains; Phase 3 (norms/percentiles + profile visualization) is the next direction via `/plan-milestone`.

## 2026-07-10 — Fix single-row score_pid5(calc_se = TRUE) crash (M9)

- **What:** Implemented M9 on branch `m9-single-row-se`. Root cause (confirmed by reproduction): in [R/score_pid5.R](../R/score_pid5.R) the facet-SE step `apply(data_items[, x], MARGIN = 1, FUN = calc_sem)` omitted `drop = FALSE`. `data_items` is a matrix (from `cbind`), so a single-row input indexed `data_items[, x]` down to a plain vector and `apply(MARGIN = 1)` errored with `"dim(X) must have a positive length"`. One-line fix: add `drop = FALSE` (the domain-SE path at the adjacent line, and the score/`apa_mean` paths, already had it — this was the lone offender). Test-first: added a single-row `calc_se = TRUE` block to `test-score_pid5.R` (FULL/SF/BF) with hand-computed SE oracles — FULL Anhedonia SE `sd(c(1,1,1,2,1,2,1,1))/sqrt(8)` (all-1s input, reverse-keyed items 30/155 → 2, hardcoded from the official key), SF Anhedonia `sd(c(0,1,2,3))/sqrt(4)`, BF Disinhibition `sd(c(0,1,2,3,3))/sqrt(5)` — plus a single-row-≡-first-row-of-multi-row consistency check guarding multi-row behavior. Added a single-row shape invariant (30/30/5 `_se` columns for FULL/SF/BF) to `test-interface.R`. All 4 new score tests reproduced the crash before the fix and pass after. Suite FAIL 0 WARN 0 SKIP 1 PASS 422 (was 406); `check()` **0/0/0**. No roxygen/keying/data touched.
- **Why / decisions:** Same class of bug as the M3 single-row `validity_pid5()` fix; mirrored its `drop = FALSE` remedy and its single-row test pattern. SE oracle values hardcode the scale's reverse-keyed item values from the official key (independent of the package's keying tables), per the oracle strategy. No D-entry — no architectural change.
- **Follow-ups spawned:** — (resolves the M8 single-row `calc_se` follow-up; DESIGN Known issue to be struck on merge by /review-milestone.)

## 2026-07-10 — Reviewed, merged & closed out M8 (M8)

- **What:** Verified all 10 M8 acceptance criteria against PR [#9](https://github.com/jmgirard/hitop/pull/9) (branch `m8-apa-scoring`) by execution. Standard gate: `document()` no-diff; `test()` FAIL 0 WARN 0 SKIP 1 PASS 406 (only the pre-existing OQ-1 skip); `check()` **0/0/0**. Independently re-verified the scoring against ground truth by reimplementing the APA facet rule with deliberately-dumb explicit code and matching the package on every oracle: FULL Anhedonia (item 1 missing) `1.25` (APA, = round(9×8/7)/8) vs `9/7` (traditional); BF Disinhibition NA at 4/5-missing and Detachment prorated `1` at 1/5-missing (both non-NA under `apa_scoring = FALSE`); half-integer round-up `0.6` (base `round()` would give `0.4`); FULL Disinhibition **domain** NA when its Impulsivity facet is NA while Irresponsibility still scores; `_se` NA exactly where its scale is NA; `na.rm = FALSE` warns and is ignored (result ≡ default); exactly-25%-missing retained (`11/8`); SF Submissiveness NA at 2/4-missing; and `apa_scoring = TRUE ≡ FALSE` on complete data for all three versions. A fresh-context general-purpose subagent (had not seen the implementation) returned **PASS** — it independently reproduced the oracles and confirmed the `a = 0` division-by-zero guard, 25%-boundary inclusivity, `_se`/score column alignment, and `na.rm = FALSE` domain gating, and judged the oracles non-tautological and mutation-catching. Confirmed `pid_items`/`pid_scales`/`pid_domains` artifacts absent from the diff. Merged PR #9 to main (`f5e83a8`) after Jeff's yes/no authorization. M8 → DONE.
- **Why / decisions:** No keying content changed — M8 is a scoring-algorithm change only, so no keying sign-off was required. Doc sync on merge: struck DESIGN Known issue #4 (APA rules not enforced — now resolved) and replaced it with the newly-surfaced pre-existing bug (`score_pid5(calc_se = TRUE)` crashes on single-row input: the SE `apply(data_items[, x], …)` lacks `drop = FALSE`; same class as the M3 single-row `validity_pid5()` fix, present on `main`, not an M8 regression); fixed a stale DESIGN cross-reference (`R/qualtrics_test.R` → `devel/qualtrics_test.R`, dropped the dangling "see Known issues #4"); marked the ROADMAP Phase-2 M8 outcome *(done)*, flipped Phase 2 to **complete**, and advanced the "(current focus)" marker to Phase 3 (Norms & visualization). Two non-blocking review nits left as-is: `test-interface.R`'s "changes values but not output shape" only asserts shape (value-change coverage lives in the score_pid5 oracle block), and APA `_se` uses the traditional `calc_sem` over answered raw items (correct per spec — only SE=NA-where-scale-NA was required).
- **Follow-ups spawned:** single-row `calc_se` crash (DESIGN Known issue #4, no milestone yet). Phase 2 (Trustworthiness) is complete; next direction is Phase 3 (norms/percentiles + profile visualization) or the Phase-1 leftovers (HSUM scoring pending Society feedback; PID-5-BFP materials) — none READY, so `/plan-milestone` is the next step.

## 2026-07-10 — APA-compliant scoring toggle for score_pid5() (M8)

- **What:** Implemented `apa_scoring` on branch `m8-apa-scoring`. `score_pid5()` gains an `apa_scoring = TRUE` (default) argument applying the published APA missing-data/proration rule uniformly across FULL/SF/BF. New internal helpers in [R/util.R](../R/util.R): `apa_mean()` (per scale: NA if `(n−answered)/n > 0.25`, else `round_half_up(partial_sum × n / answered) / n`) and `round_half_up()` (matches the APA "round to the nearest whole number", unlike base `round()`'s round-half-to-even). In [R/score_pid5.R](../R/score_pid5.R) the facet computation branches on `apa_scoring` (APA path uses `apa_mean` over each scale's item matrix; else the prior `rowMeans(na.rm)`); FULL/SF domains under APA are the mean of the 3 primary facet averages, NA if any is NA (`na.rm = FALSE` gating on the already-prorated facets); `_se` columns are gated to NA wherever their scale is NA; a cli warning fires if `na.rm = FALSE` is set under `apa_scoring = TRUE` (na.rm then ignored). Sourced the full-form rule **verbatim** from the APA PID-5 Full Version Adult PDF (p. 8) — the BF rule (M6) is the same 25%/proration rule on 5-item domains. Tests: rewrote the two "tolerates missing via rowMeans" tests to assert both modes; pinned the three independent-recompute + the M7 domain-na.rm tests to `apa_scoring = FALSE` (their rowMeans oracle is only correct on the traditional path); added a dedicated APA oracle block (proration/rounding FULL Anhedonia 1.25 vs 9/7; >25% cutoff FULL Impulsivity → NA; exactly-25% boundary retained; half-integer round-up BF Disinhibition 0.6; domain-gating FULL Disinhibition → NA; SE gating; na.rm-ignored warning; SF parity; apa≡trad on complete data) with arithmetic in comments; `apa_mean`/`round_half_up` unit tests in `test-util.R`; `apa_scoring` validation + shape-invariance in `test-interface.R`. Docs: `score_pid5` roxygen/@details, SOURCES.md (verbatim rule + SF-by-analogy caveat + full-form PDF URL, both "not yet enforced" notes flipped to "enforced (M8)"), NEWS.md (default-behavior-change note), D-009 (planning). Suite FAIL 0 WARN 0 SKIP 1 PASS 406 (was 365); `check()` **0/0/0**.
- **Why / decisions:** Per D-009 and the two planning decisions confirmed with Jeff: (1) **all three versions** covered by one algorithm — the FULL rule was sourced verbatim this milestone, so no BF-only interim was needed; the SF (100-item, 4-item facets) applies the full-form rule **by analogy** since Maples (2015) specifies none (25% of 4 = 1 item), flagged in SOURCES.md; (2) `apa_scoring = TRUE` **governs missing data entirely and `na.rm` is ignored** under it (with a cli warning + docs), keeping one arg = one algorithm. Chose an explicit `round_half_up()` over base `round()` because the APA says "nearest whole number" and a `.5` prorated raw is reachable for these facet sizes (5-item scale, 1 missing, answered sum 2 → 2.5), so the distinction is testable, not academic. No keying content (`pid_items`/`pid_scales`/`pid_domains`) changed — this is a scoring-algorithm change only, so no keying sign-off required. Default scored output changes under missing data (NEWS.md); DESIGN Known issue #4 is resolved by this branch (left for /review-milestone to strike on merge, per M6/M7 pattern).
- **Follow-ups spawned:** — (M8 was the sole remaining open Phase-2 milestone.)

## 2026-07-10 — Reviewed, merged & closed out M7 (M7)

- **What:** Verified all 7 M7 acceptance criteria against PR [#8](https://github.com/jmgirard/hitop/pull/8) (branch `m7-fullsf-domains`) by execution on the shipped datasets: FULL/SF gain 5 domain columns appended after the 25 facets (facets unchanged/in order), BF unchanged; each domain = mean of its 3 primary facet scores (independent recompute matched); `na.rm=TRUE` drops a fully-missing facet, `na.rm=FALSE` yields NA; `calc_se` adds domain `_se` = `sd/sqrt(3)` over the 3 facet scores and none appear without it; alpha print excludes domains (facet-level only); `pid_domains` exported/documented/pkgdown-listed. Gate: `document()` no-diff; `test()` FAIL 0 WARN 0 SKIP 1 PASS 365 (only the pre-existing OQ-1 skip); `check()` **0/0/0**. Confirmed `pid_items`/`pid_scales` artifacts absent from the diff. A fresh-context general-purpose subagent (had not seen the implementation) returned **PASS, no blockers** — it independently re-derived the FULL R1/R2 and SF R3 fixture domain values, confirmed the primary-facet map against the standard DSM-5 PID-5 structure (the 15-facet primary subset, not the 21-facet `pid_items$Domain` grouping), and proved the `test-keying.R` map oracle is non-tautological (asserts against a hardcoded APA transcription, would break if any facet→domain assignment were mutated). Merged PR #8 to main (`4bd7d7a`) after Jeff's yes/no authorization. M7 → DONE.
- **Why / decisions:** No existing keying content changed — the new `pid_domains` map is additive scoring data whose values Jeff approved at planning time (verified 2026-07-09 against the APA source) and which is machine-checked against a hardcoded APA transcription, so no separate keying sign-off was outstanding. Traditional na.rm domain scoring shipped (D-008); strict APA missing-data/proration stays deferred to M8. Doc sync: DESIGN data-model + scoring-family + testing-coverage sections and SOURCES.md already updated during implementation; ROADMAP Phase-2 FULL/SF-domains outcome marked *(M7, done)*. DESIGN Known issue #4 (APA missing-data/proration not enforced) intentionally left open — it is M8's charter, not fixed here.
- **Follow-ups spawned:** — (M8 APA-compliant scoring toggle is the sole remaining open Phase-2 milestone; it layers strict missing-data semantics on top of this `pid_domains` map.)

## 2026-07-10 — PID-5 FULL/SF domain scoring (M7)

- **What:** Implemented FULL/SF personality-trait **domain** scoring on branch `m7-fullsf-domains`. `score_pid5(version = "FULL"/"SF")` now appends the 5 domain scores after the 25 facet scores (`pid_negativeAffectivity`, `pid_detachment`, `pid_antagonism`, `pid_disinhibition`, `pid_psychoticism` — names matching BF's existing domain output), each computed as the `rowMeans` of its 3 primary facet scores (APA key Step 3), honoring `na.rm`; domain `_se` columns mirror facet SEs via `calc_sem` over the 3 facet scores when `calc_se = TRUE`. Added a new exported dataset `pid_domains` (5×4: `Domain`, `camelCase`, `primaryFacets` labels, derived `facetStems`) built in [data-raw/pid_info.R](../data-raw/pid_info.R); only `data/pid_domains.rda` regenerated (`pid_items`/`pid_scales` untouched). Tests: new `test-keying.R` block asserting `pid_domains`' 3 primary facets per domain set-equal to the hardcoded APA full-form Domain Table for all 5 domains, plus map invariants (15 distinct valid facet stems, domain stems = BF's); new `test-score_pid5.R` domain block (hand-computed FULL R1/R2 + SF fixtures with arithmetic in comments, independent recompute of Psychoticism/Antagonism from hardcoded facet stems, na.rm/`na.rm=FALSE` behavior, BF-unchanged, `_se` iff `calc_se`); updated `test-interface.R` column-count assertions (25→30, 50→60). Docs: `pid_domains` documented in [R/data.R](../R/data.R) + added to `_pkgdown.yml` reference index + NEWS.md; `score_pid5` `@details` describes domain output; `pid_domains` added to `globalVariables`. Suite FAIL 0 WARN 0 SKIP 1 PASS 365 (was 329); `check()` **0/0/0**.
- **Why / decisions:** Per D-008 and the two planning decisions confirmed with Jeff: (1) **traditional** `na.rm`-respecting domain means (a domain drops a fully-missing facet under `na.rm=TRUE`; is NA under `na.rm=FALSE`) — the strict APA ≥2-missing/proration rule stays deferred to M8, keeping M7 a clean feature-add; (2) the map is stored as **exported** `pid_domains` (transparent, consistent with `pid_items`/`pid_scales`) rather than internal sysdata. Crucial distinction encoded in the oracle: the scoring map is the **15 primary facets**, NOT the broader 21-facet `pid_items$Domain` grouping. `primaryFacets` (labels) is the single source of truth; `facetStems`/`camelCase` are derived. No `pid_items` keying content changed, so no keying sign-off required (the new `pid_domains` map is additive scoring data, verified against the APA source).
- **Follow-ups spawned:** — (M8 APA-compliant scoring toggle remains the open Phase-2 milestone; it layers strict missing-data semantics on top of this `pid_domains` map.)

## 2026-07-10 — Reviewed, merged & closed out M6 (M6)

- **What:** Verified all five M6 acceptance criteria against PR [#7](https://github.com/jmgirard/hitop/pull/7) (branch `m6-bf-provenance`). Gate: `document()` no-diff; `test()` FAIL 0 WARN 0 SKIP 1 PASS 329 (only the pre-existing OQ-1 skip); `check()` **0/0/0**. Confirmed `pid_items` artifacts absent from the diff. A fresh-context general-purpose subagent (had not seen the implementation) returned **PASS, no blockers** — it independently re-downloaded the APA PID-5-BF PDF and confirmed the Domain Scoring numbers character-for-character, loaded `pid_items` to confirm the stored `Domain` matches, and proved the new oracle is non-tautological by mutating an item's `Domain` and watching both affected `expect_setequal` assertions fail. Merged PR #7 to main (`3751a8f`) after Jeff's yes/no authorization. M6 → DONE.
- **Why / decisions:** No keying content changed (`pid_items` untouched — it already matched the APA source), so no keying sign-off required. Doc sync: DESIGN testing-coverage note now cites BF domain verification (M1/M6); added DESIGN Known issue #4 (APA missing-data/proration not enforced in `score_pid5` → M8); split the ROADMAP Phase-2 outcome so the BF-provenance half reads *(M6, done)*.
- **Follow-ups spawned:** — (M8 APA-compliant scoring toggle and M7 FULL/SF domain scoring remain the open Phase-2 milestones.)

## 2026-07-10 — BF keying provenance + tests (M6)

- **What:** Documented and machine-verified the PID-5-BF domain keying on branch `m6-bf-provenance`. Pulled the authoritative APA *PID-5-BF—Adult* PDF and transcribed its "Personality Trait Domain Scoring" table; confirmed `pid_items$Domain` for the 25 BF items matches it exactly (Neg Affect 8,9,10,11,15; Detachment 4,13,14,16,18; Antagonism 17,19,20,22,25; Disinhibition 1,2,3,5,6; Psychoticism 7,12,21,23,24). Added a BF block to [test-keying.R](../tests/testthat/test-keying.R): a domain-membership oracle asserting `pid_items$BF[Domain == d]` set-equal to the hardcoded APA numbers for all 5 domains, plus invariants (25 items / complete 1:25 / exactly 5 per domain / no reverse-keying) — closing the gap that only 2/5 BF domains (Disinhibition, Detachment) were previously checked against the source, and only transitively through `score_pid5`. Updated [SOURCES.md](SOURCES.md): 3 BF rows in the verification table, a PID-5-BF Sources bullet with the APA PDF URL, and a "Note on BF domain scoring" recording all-forward/average(raw÷5) scoring and the unenforced APA missing-data/proration rule (→ M8). Refreshed the stale `test-keying.R` header comment. `pid_items` untouched (`data/pid_items.rda` + `data-raw/pid_items.csv` unchanged). Suite FAIL 0 WARN 0 SKIP 1 PASS 329 (was 320); `check()` **0/0/0**.
- **Why / decisions:** Kept M6 a pure provenance/keying milestone — no `pid_items` edit (already matches the source) and no scoring-behavior change. The APA missing-data/proration rule surfaced during sourcing (`score_pid5` uses `rowMeans(na.rm=TRUE)` and would score a domain from a single surviving item, contradicting the APA "≥2 missing → don't compute" rule) was spun out to new milestone M8 (`apa_scoring = TRUE` default toggle, per Jeff), not folded in — it spans all PID-5 versions, needs FULL/SF missing-data sources, and shares design with M7. No keying content changed, so no sign-off required.
- **Follow-ups spawned:** M8 (APA-compliant scoring toggle, PLANNED). M7 (FULL/SF domain scoring) remains the other open Phase-2 milestone.

## 2026-07-10 — Reviewed, merged & closed out M5 (M5)

- **What:** Verified all five M5 acceptance criteria against PR [#6](https://github.com/jmgirard/hitop/pull/6) (branch `m5-srbr-reliability-tests`). Independently re-derived every asserted oracle value with a script that never calls the package scorers — SR `romanticDisinterest` reversal (1.6/3.4/1.8/2.8), BR overlap `externalizing`=3.0 & `pFactor`=7/3, and `calc_alpha`=42/45 all reproduced exactly (none back-filled). Confirmed the "independent recomputation" is non-circular: tests assert hardcoded item vectors against `which(hitopbr_items$Externalizing/$Pfactor)` and raw-CSV numbers, then vs manual `rowMeans`, not vs the `*_scales` table the scorer reads. Gate: `document()` no-diff; `test()` FAIL 0 WARN 0 SKIP 1 PASS 320; `check()` **0/0/0**. Fresh-context general-purpose subagent review returned **PASS-WITH-NITS, no blockers** — re-derived the same oracle values, confirmed the `calc_omega` reference is a genuinely separate `lavaan::cfa` fit, and flagged only optional tightening (a shape-only pairwise-NA supplement, a broad `expect_error`, label tests reading text from the table that *is* the oracle). Merged PR #6 to main (`6aaa619`) after Jeff's yes/no authorization. M5 → DONE.
- **Why / decisions:** No keying content touched (only `R/score_hitopbr.R` scoring logic + tests), so no keying sign-off required. The three review nits left as-is — all optional, not correctness issues. Doc sync: removed DESIGN Known issue #1 (reliability + SR/BR scoring tests now exist; renumbered, added generator-tests gap as new #3); updated DESIGN Testing-section coverage note; marked the three completed Phase-2 ROADMAP outcomes *(done)*.
- **Follow-ups spawned:** — (M6 BF provenance, M7 FULL/SF domain scoring remain the open Phase-2 milestones.)

## 2026-07-10 — HiTOP-SR/BR scoring + reliability oracle tests (M5)

- **What:** Added ground-truth oracle tests on branch `m5-srbr-reliability-tests` for the previously-untested HiTOP-SR/BR scoring and reliability functions. New `fx_hitopsr()`/`fx_hitopbr()` fixtures in [helper-fixtures.R](../tests/testthat/helper-fixtures.R) (hand-computed, arithmetic in comments, memberships copied from the source CSVs). [test-score_hitopsr.R](../tests/testthat/test-score_hitopsr.R): exact-value assertions incl. `romanticDisinterest` (the lone reverse-keyed SR item, HSR 310), `appetiteLoss`/`bingeEating`, independent recomputation of `appetiteLoss` from hardcoded numbers, invariants (`_se` present iff `calc_se`, prefix, row count, `na.rm`). [test-score_hitopbr.R](../tests/testthat/test-score_hitopbr.R): exact values incl. the overlapping `externalizing`/`pFactor` supersets, independent recompute of both from the `hitopbr_items$Externalizing`/`$Pfactor` marker columns, no-reverse-keying invariant. [test-reliability.R](../tests/testthat/test-reliability.R): `calc_alpha()` vs a hand-computed reference (42/45) + all three assertion-error paths + pairwise-NA; `calc_omega()` vs a direct independent `lavaan::cfa` fit (`skip_if_not_installed`), mixed-sign-loading warning, degenerate input. [test-label_scales.R](../tests/testthat/test-label_scales.R): `label_hitopsr`/`label_hitopbr` item+scale labels and no-match warnings, and `rank_scales()` high/low/tie/prefix-strip/append coverage. Suite FAIL 0 WARN 0 SKIP 1 PASS 320 (was 259); `check()` **0/0/0**.
- **Why / decisions:** Reliability (`calc_alpha`/`calc_omega`, previously zero coverage) folded into M5 rather than a separate milestone (Jeff). SR fixtures use a representative subset + one independent recomputation rather than all 76 scales (Jeff) — the keying table itself is a keying-test concern, not a scoring oracle. Hardened `score_hitopbr()` with `drop = FALSE` on the mean/SE column subsetting to match `score_hitopsr()` (behavior-preserving: all BR scales have ≥6 items, so no output change; removes a latent single-item-scale crash). No keying content touched, so no sign-off required.
- **Follow-ups spawned:** — (M6 BF provenance, M7 FULL/SF domain scoring remain planned. Closes DESIGN Known issue #1's reliability + SR/BR scoring gap, pending review.)

## 2026-07-10 — Reviewed, merged & closed out M4 (M4)

- **What:** Verified all four M4 acceptance criteria against PR [#5](https://github.com/jmgirard/hitop/pull/5) (branch `m4-check-coverage-ci`). Local gate: `document()` no-diff; `test()` FAIL 0 WARN 0 SKIP 1 PASS 259; `check()` **0/0/0**. CI on the PR: all 7 checks green — the full 5-config R-CMD-check matrix (macOS-release, Windows-release, Ubuntu devel/release/oldrel-1), `test-coverage` (codecov upload authenticated via the `CODECOV_TOKEN` secret), and `pkgdown`. Confirmed badges render in `README.md` (knitted from `README.Rmd`, working tree clean) and `covr` is in DESCRIPTION Suggests (alphabetized). Fresh-context general-purpose subagent diff review returned **PASS, no blockers** — independently confirmed badge image/link URLs resolve to the correct repo + workflow filename, the `codecov-action@v7` token pattern and SHA-pin are current, `codecov.yml` is informational (won't block PRs), and `.Rbuildignore` keeps `codecov.yml`/workflows out of the build. Merged PR #5 to main (`4c54979`) after Jeff's yes/no authorization; the merge push re-triggered all three workflows on `main` (confirming they fire on push to main). M4 → DONE.
- **Why / decisions:** No keying content touched, so no keying sign-off required. "Green on main" is inherently a post-merge confirmation (new workflow files don't run on `main` until they land there) — the green PR runs were the correct pre-merge proxy, and the post-merge `main` runs were watched to green. Removed DESIGN Known issue #2 (renumbered #3–#4 → #2–#3).
- **Follow-ups spawned:** — (M5 reliability + HiTOP-SR/BR oracles, M6 BF provenance, M7 FULL/SF domain scoring remain planned.)

## 2026-07-09 — R CMD check + coverage CI (M4)

- **What:** Added two GitHub Actions workflows on branch `m4-check-coverage-ci` alongside the existing `pkgdown.yaml`. (1) [`.github/workflows/R-CMD-check.yaml`](../.github/workflows/R-CMD-check.yaml) via `usethis::use_github_action("check-standard")` — full standard matrix (macOS-release, Windows-release, Ubuntu devel/release/oldrel-1). (2) [`.github/workflows/test-coverage.yaml`](../.github/workflows/test-coverage.yaml) via `usethis::use_github_action("test-coverage")` — covr → Cobertura → `codecov/codecov-action@v7`. `usethis::use_coverage("codecov")` wrote `codecov.yml` (informational status, no PR comment) and its `^codecov\.yml$` `.Rbuildignore` entry. Added `covr` to DESCRIPTION `Suggests` by hand (usethis errored out before that step — see below). Added R-CMD-check + Codecov badges to the `README.Rmd` badges block (reflowed to one badge per line) and re-knit `README.md` with `devtools::build_readme()`. Local `devtools::check()` still **0 errors / 0 warnings / 0 notes**.
- **Why / decisions:** Full matrix chosen (Jeff) over Ubuntu-only for cross-platform coverage. Every `usethis` call that inserts a badge aborted with "Invalid block specification" because this repo's `<!-- badges: start -->…<!-- badges: end -->` block was a single wrapped line; the workflow/config files were still written, so badges were added manually and the block normalized so future `use_badge()` calls parse. `CODECOV_TOKEN` repo secret already added by Jeff, so coverage upload should authenticate on first run. Resolves DESIGN Known issue #2 (CI); to be removed from DESIGN once workflows are green on main (review step).
- **Follow-ups spawned:** — (M5 reliability + HiTOP-SR/BR oracles and M7 FULL/SF domain scoring remain planned.)

## 2026-07-09 — Reviewed, merged & closed out M3 (M3)

- **What:** Verified all six M3 acceptance criteria by executing them on branch `m3-check-hygiene`: `document()` no-diff; `test()` FAIL 0 WARN 0 SKIP 1 PASS 259; `check()` **0 errors / 0 warnings / 0 notes**; `data/`↔`sysdata.rda` disjoint with all 18 `data/` objects documented; `data-raw/sysdata.R` regenerates `R/sysdata.rda` identically (`internal = TRUE`); single-row `validity_pid5()` FULL/SF returns correct values (PRD 22, SDTD 17 / PRDS 12, SDTDS 8). Fresh-context general-purpose subagent diff review PASSED with no concerns — it independently re-derived the oracle constants from `pid_items`, confirmed the four instructions are byte-identical after the sysdata move, confirmed `setNames`/`write.csv` are genuinely used bare (`generate_redcap.R`), and noted `pid_scales` is covered by the pre-existing `R/zzz.R` `.onLoad` registration (hence correctly absent from the new `globalVariables()`). Merged PR [#4](https://github.com/jmgirard/hitop/pull/4) to main (`856e556`). M3 → DONE, collapsed into Completed.
- **Why / decisions:** No keying content touched, so no keying sign-off required; merge authorized by Jeff via the yes/no gate. The SDTD-denominator fix was confirmed behaviorally inert (equal-length vectors) — a correctness/clarity change whose oracle power lives in the single-row fixtures, as noted at hand-off.
- **Follow-ups spawned:** — (M4 is the next actionable milestone: it can now gate CI on the clean check.)

## 2026-07-09 — Check & dependency hygiene: clean check + single-row validity fix (M3)

- **What:** Took `devtools::check()` from 0 errors / 4 warnings / 2 notes to **0/0/0** on branch `m3-check-hygiene`. (1) **Data:** documented the two HiTOP-HSUM registry datasets (`hitophsum_items`, `hitophsum_choices`) in [R/data.R](../R/data.R); consolidated the four `*_instructions` into a new [data-raw/sysdata.R](../data-raw/sysdata.R) written with `internal = TRUE`, removed their definitions from the per-instrument `*_info.R` scripts, and deleted the duplicate `data/*_instructions.rda` (regenerated `R/sysdata.rda` and verified byte-identical content; D-007). (2) **Deps:** removed unused Imports {glue}/{lifecycle}/{jsonlite} (and their `@importFrom`s); moved unexported `R/qualtrics_test.R` → `devel/` to drop the undeclared {httr2} use. (3) **Cleanup:** removed all 8 redundant `utils::data()` calls (score/label functions now use lazy data directly) and added `utils::globalVariables()` + `@importFrom stats setNames`/`utils write.csv` to silence the no-visible-binding NOTE; escaped 18 `•` bullets in `generate_docx.R` (non-ASCII WARNING); added `@inheritParams` to the six pid5sf/pid5bf `generate_*` variants (undocumented-args WARNING); `.Rbuildignore`d `hitop_hex.png`; removed stale vignette artifact dirs. (4) **Bug fix (test-first):** `validity_pid5()` no longer errors on single-row FULL/SF input — added `drop = FALSE` to the ORS/PRD/SDTD `rowSums()` indexing (DESIGN #9) with hand-computed 1-row regression tests, and corrected the SDTD warning-percentage denominator (`length(prd_vec)` → `length(sdtd_vec)`; behaviorally inert, same length). Full suite FAIL 0 WARN 0 SKIP 1 **PASS 259** (+10). Added a NEWS bullet; added the HSUM datasets to the `_pkgdown.yml` reference index.
- **Why / decisions:** D-007 records instructions as internal-only data owned by `data-raw/sysdata.R`. Per D-004, the single-row fix shipped with ground-truth 1-row fixtures (expected values from the official key) rather than a snapshot. The two out-of-original-scope warnings (non-ASCII, undocumented generator args) were folded in because criterion #1 (0 warnings) can't be met otherwise; recorded as discovered subtasks in MILESTONES. No keying content touched. Resolved DESIGN Known issues #3–#5, #8, #9 (removed); #1/#2/OQ-1/SF-cutoffs remain (renumbered 1–4).
- **Follow-ups spawned:** — (M4 CI can now gate on the clean check; M5 reliability + HiTOP-SR/BR oracles; M7 FULL/SF domain scoring remain planned).

## 2026-07-09 — Reviewed, merged & closed out M2; skill + docs sync (M2)

- **What:** Verified M2's five acceptance criteria by executing them: `document()` no-diff; full `test()` FAIL 0 WARN 0 SKIP 1 PASS 249; `check()` 0 errors / 4 warnings / 2 notes — all pre-existing M3 categories (non-ASCII, dependencies, missing docs, Rd usage, top-level files, global bindings), and a scripted scan confirmed **none reference the added test files** (M2 criterion is "no new problem from the tests", not full clean). Fresh-context general-purpose subagent diff review PASSED — it re-verified every hardcoded item list against `pid_items`, spot-checked 5 hand-computed fixture values, confirmed the "independent recomputation" tests read no package data, and confirmed the `score_pid5()` SE-prefix fix matches `score_hitopbr`/`score_hitopsr`. Merged PR [#3](https://github.com/jmgirard/hitop/pull/3) to main (`ceda794`). M2 → DONE, collapsed into Completed. **Skill fix:** updated `/review-milestone` step 6 to make the PR merge an explicit `AskUserQuestion` yes/no authorization gate (the harness blocks the agent from self-merging without it) and to close out only after the PR is actually merged (`c66f875`). **Docs sync:** DESIGN Known issue #1 already reflected M2; #9 (single-row `validity_pid5()` bug) carried in for M3.
- **Why / decisions:** Per D-004 the `calc_se` invariant asserts the correct `pid_anhedonia_se` name rather than enshrining the prefix bug, so the fix shipped with the test PR. Merge authorized by Jeff via the new yes/no gate. No keying content touched, so no keying sign-off required.
- **Follow-ups spawned:** M3 to fix single-row `validity_pid5()` (#9); M5 reliability + HiTOP-SR/BR oracles; M7 FULL/SF domain scoring. M3 is the next actionable milestone.

## 2026-07-09 — Ported PID-5 scoring/validity oracle tests (M2)

- **What:** Added five test files + `helper-fixtures.R` adapting the fork's oracle suite to this repo's API (`version=` not `score_pid5fsf`; `pid_<camelCase>` columns; required `items=`; `srange`; no `scales`/`id`). Coverage: hand-computed fixtures for FULL facets + 5 validity scales, SF facets + 5 S-variant scales, and **BF 5 domains + PNA** (BF fixtures are net-new — the fork had no BF); independent recomputation from hardcoded official item numbers for a facet/domain per version and INC/INCS; invariants for reverse-keying (FULL applies it, SF/BF do not), NA propagation, row-count, `prefix`/`append`/`tibble`/`calc_se`, version case-insensitivity, per-version column counts, and an `alpha`/`omega` print smoke test. New-file totals FAIL 0 PASS 121; full suite FAIL 0 WARN 0 SKIP 1 PASS 249. Dropped the fork's `scales`/`id`/domain/SDTD-guard-bug/`validate_id` tests (absent from this API). FULL/SF *domain* oracle stays deferred to M7.
- **Why / decisions:** Two production bugs surfaced by the oracles. (1) **Fixed** `score_pid5()` SE columns lacked the `prefix` — emitted `anhedonia_se` vs the documented/`score_hitopbr`-consistent `pid_anhedonia_se` ([R/score_pid5.R:139](../R/score_pid5.R)); per D-004 the invariant test asserts the correct name rather than enshrining the bug. (2) **Recorded, deferred to M3** `validity_pid5()` errors on single-row input (ORS/PRD/SDTD `rowSums(data_items[, items])` drops to a vector; needs `drop = FALSE`) — DESIGN Known issue #9; warning tests use ≥ 2 rows to avoid it. Reliability ground-truth left to M5 (M2 only smoke-tests the print side-effect). No `document()` needed (fix was a string, no roxygen change).
- **Follow-ups spawned:** M3 to fix the single-row `validity_pid5()` bug (#9); M5 reliability oracles; M7 FULL/SF domain scoring + oracle.

## 2026-07-09 — Reviewed, merged & closed out M1; docs sync (M1)

- **What:** Verified M1's acceptance criteria on the branch (`test(filter="keying")` FAIL 0 SKIP 1 PASS 111; `document()` no-diff; full `test()` PASS 128; `check()` shows only pre-existing M3-owned warnings, no new problems), plus a fresh-context general-purpose subagent diff review (PASS — oracle direction non-circular, tibble-safe extraction, no weakened assertions). Merged PR [#2](https://github.com/jmgirard/hitop/pull/2) to main (`c3ea999`). **Docs sync:** M1 → DONE and collapsed into Completed; DESIGN Known issue #1 rewritten (keying tests now exist; scoring/validity=M2, reliability/HiTOP-SR-BR=M5 still outstanding).
- **Why / decisions:** M1's criteria deliberately scope out `check()`-clean (that's M3), so pre-existing check warnings don't block it. Test-only PR, no keying content, so no maintainer keying sign-off required; merge performed by the maintainer.
- **Follow-ups spawned:** M2 and M5 are now unblocked (dependency M1 DONE) and plannable via `/plan-milestone`.

## 2026-07-09 — Ported keying-verification tests (M1)

- **What:** Added `tests/testthat/test-keying.R`, porting the fork's external-source keying oracle to this repo (`PID5→FULL`, `PID5FSF→SF` column renames; tibble-safe `pid_items$FULL[rows]` extraction). 11 `test_that` blocks (10 active + OQ-1 skip) verify `pid_items` against the published sources — reverse items, 25 facets, INC 20 pairs, INC-S 10 pairs (2020 + 2021 Correction), ORS 10, PRD 22, SDTD 16-subset, SF 100 selection + per-facet + no-reverse. `devtools::test(filter="keying")` → FAIL 0 | SKIP 1 | PASS 111. `pid_items` unchanged. Installed local dev deps flextable/officer/snakecase so the suite can load. Also planned M1→READY and added M6 (BF provenance) + M7 (FULL/SF domain scoring) stubs, verified against the APA full-form and BF scoring-key PDFs.
- **Why / decisions:** Fulfills D-005 (external-source oracle) for the FULL/SF keying. Confirmed from the primary APA key (Krueger et al., 2013) that the 5-domain = 3-primary-facet model (Step 3) is official but *unimplemented* here (FULL/SF score no domains) → deferred to M7 rather than forced into M1; the source `test-keying.R` never contained domain blocks, so nothing was deleted. BF Domain-column provenance (APA PID-5-BF, 2013) → M6.
- **Follow-ups spawned:** M6 (BF keying provenance + tests), M7 (PID-5 FULL/SF domain scoring). OQ-1 (SDTD item 38) still open, `skip()`-ed.

## 2026-07-09 — Adopted branch + PR workflow (—)

- **What:** Added "Branching & PRs" rules to CLAUDE.md and tracking-rules.md: milestone work on `m<n>-<slug>` branches, merged to main via PR with the URL recorded in the milestone entry; keying-content PRs require Jeff's explicit sign-off before merge; trivial doc/tracking edits may still go direct to main. Updated /work-milestone (branch before code, open PR at completion) and /review-milestone (review the PR diff, merge on pass) to match.
- **Why / decisions:** main is a distribution channel (pak installs and pkgdown deploys from it) and must stay installable; PRs give CI (future M4) a gate and keying changes a sign-off point.
- **Follow-ups spawned:** —

## 2026-07-09 — Added collaboration rules: primary sources & model choice (—)

- **What:** Added two working rules to CLAUDE.md ("Working with Jeff") and `.claude/skills/shared/tracking-rules.md`: (1) seek primary sources when they would help and ask Jeff to upload any that can't be found/accessed — never substitute secondary descriptions or model memory for keying/scoring content; (2) assume the main session runs on Opus, delegate suitable tasks to Sonnet subagents, and ask Jeff to launch a clean Fable session when one would help.
- **Why / decisions:** Maintainer request; extends the existing subagent model-choice rule (no Haiku for accuracy-critical work).
- **Follow-ups spawned:** —

## 2026-07-09 — Tracking docs bootstrapped from code (M1–M5)

- **What:** The tracking system (CLAUDE.md, project/, .claude skills) was copied in from an older PID-5-only fork; its docs described that fork, not this package. Established ground truth from DESCRIPTION, NAMESPACE, R/, tests/, data/, .github/, vignettes/, and _pkgdown.yml, then rewrote CLAUDE.md, DESIGN.md, ROADMAP.md, and MILESTONES.md to match reality (v0.1.0; PID-5 FULL/SF/BF via `version` arg; HiTOP-SR/BR/HSUM; reliability, label/rename/rank, and generator families; pkgdown + vignettes shipped). SOURCES.md kept (keying provenance re-verified against this repo's data-raw/pid_items.csv); D-001–D-005 preserved as history; D-006 added.
- **Verified while auditing:** the old fork's SDTD guard bug, commented-out SF warnings, placeholder DESCRIPTION, and missing BF support do **not** exist here. Still real: no PID-5/keying/reliability tests, no check/coverage CI. Newly found: 6 undocumented data/*.rda (instructions duplicated in sysdata), {httr2} used undeclared while {glue}/{lifecycle}/{jsonlite} are unused Imports, redundant `utils::data()` calls (work, but pollute the global env), and local test/check blocked by uninstalled Imports (flextable/officer/snakecase). Recorded as DESIGN Known issues #1–#8.
- **Why / decisions:** D-006 (re-baseline on the canonical repo; port — don't copy — the fork's oracle tests).
- **Follow-ups spawned:** M1 (keying tests), M2 (PID-5 oracle tests), M3 (check/dependency hygiene), M4 (CI + coverage), M5 (HiTOP oracle tests). OQ-1 (SDTD item 38) remains open pending the maintainer's check of the physical PID-5 manual.
