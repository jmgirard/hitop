# Work Log

<!-- Append-only, newest first. One entry per work session or merged change. -->
<!-- Format: ## YYYY-MM-DD — Title (M-refs) / What / Why-decisions / Follow-ups. Never fabricate history. -->
<!-- Boundary rule: Architecture → DESIGN. Direction → ROADMAP. Tasks → MILESTONES. History → LOG. -->
<!-- This log starts fresh for the canonical repo (v0.1.0); the old fork's log was deliberately not imported (D-006). Pre-tracking history: see git log and NEWS.md. -->

## 2026-07-10 — HiTOP-SR/BR scoring + reliability oracle tests (M5)

- **What:** Added ground-truth oracle tests on branch `m5-srbr-reliability-tests` for the previously-untested HiTOP-SR/BR scoring and reliability functions. New `fx_hitopsr()`/`fx_hitopbr()` fixtures in [helper-fixtures.R](../tests/testthat/helper-fixtures.R) (hand-computed, arithmetic in comments, memberships copied from the source CSVs). [test-score_hitopsr.R](../tests/testthat/test-score_hitopsr.R): exact-value assertions incl. `romanticDisinterest` (the lone reverse-keyed SR item, HSR 310), `appetiteLoss`/`bingeEating`, independent recomputation of `appetiteLoss` from hardcoded numbers, invariants (`_se` present iff `calc_se`, prefix, row count, `na.rm`). [test-score_hitopbr.R](../tests/testthat/test-score_hitopbr.R): exact values incl. the overlapping `externalizing`/`pFactor` supersets, independent recompute of both from the `hitopbr_items$Externalizing`/`$Pfactor` marker columns, no-reverse-keying invariant. [test-reliability.R](../tests/testthat/test-reliability.R): `calc_alpha()` vs a hand-computed reference (42/45) + all three assertion-error paths + pairwise-NA; `calc_omega()` vs a direct independent `lavaan::cfa` fit (`skip_if_not_installed`), mixed-sign-loading warning, degenerate input. [test-label_scales.R](../tests/testthat/test-label_scales.R): `label_hitopsr`/`label_hitopbr` item+scale labels and no-match warnings, and `rank_scales()` high/low/tie/prefix-strip/append coverage. Suite FAIL 0 WARN 0 SKIP 1 PASS 320 (was 259); `check()` **0/0/0**.
- **Why / decisions:** Reliability (`calc_alpha`/`calc_omega`, previously zero coverage) folded into M5 rather than a separate milestone (Jeff). SR fixtures use a representative subset + one independent recomputation rather than all 76 scales (Jeff) — the keying table itself is a keying-test concern, not a scoring oracle. Hardened `score_hitopbr()` with `drop = FALSE` on the mean/SE column subsetting to match `score_hitopsr()` (behavior-preserving: all BR scales have ≥6 items, so no output change; removes a latent single-item-scale crash). No keying content touched, so no sign-off required.
- **Follow-ups spawned:** — (M6 BF provenance, M7 FULL/SF domain scoring remain planned. Closes DESIGN Known issue #1's reliability + SR/BR scoring gap, pending review.)

## 2026-07-10 — Reviewed, merged & closed out M4 (M4)

- **What:** Verified all four M4 acceptance criteria against PR [#5](https://github.com/jmgirard/hitop/pull/5) (branch `m4-check-coverage-ci`). Local gate: `document()` no-diff; `test()` FAIL 0 WARN 0 SKIP 1 PASS 259; `check()` **0/0/0**. CI on the PR: all 7 checks green — the full 5-config R-CMD-check matrix (macOS-release, Windows-release, Ubuntu devel/release/oldrel-1), `test-coverage` (codecov upload authenticated via the `CODECOV_TOKEN` secret), and `pkgdown`. Confirmed badges render in `README.md` (knitted from `README.Rmd`, working tree clean) and `covr` is in DESCRIPTION Suggests (alphabetized). Fresh-context general-purpose subagent diff review returned **PASS, no blockers** — independently confirmed badge image/link URLs resolve to the correct repo + workflow filename, the `codecov-action@v7` token pattern and SHA-pin are current, `codecov.yml` is informational (won't block PRs), and `.Rbuildignore` keeps `codecov.yml`/workflows out of the build. Merged PR #5 to main (`4c54979`) after Jeff's yes/no authorization; the merge push re-triggered all three workflows on `main` (confirming they fire on push to main). M4 → DONE.
- **Why / decisions:** No keying content touched, so no keying sign-off required. "Green on main" is inherently a post-merge confirmation (new workflow files don't run on `main` until they land there) — the green PR runs were the correct pre-merge proxy, and the post-merge `main` runs were watched to green. Removed DESIGN Known issue #2 (renumbered #3–#4 → #2–#3).
- **Follow-ups spawned:** — (M5 reliability + HiTOP-SR/BR oracles, M6 BF provenance, M7 FULL/SF domain scoring remain planned.)

## 2026-07-09 — R CMD check + coverage CI (M4)

- **What:** Added two GitHub Actions workflows on branch `m4-check-coverage-ci` alongside the existing `pkgdown.yaml`. (1) [`.github/workflows/R-CMD-check.yaml`](../.github/workflows/R-CMD-check.yaml) via `usethis::use_github_action("check-standard")` — full standard matrix (macOS-release, Windows-release, Ubuntu devel/release/oldrel-1). (2) [`.github/workflows/test-coverage.yaml`](../.github/workflows/test-coverage.yaml) via `usethis::use_github_action("test-coverage")` — covr → Cobertura → `codecov/codecov-action@v7`. `usethis::use_coverage("codecov")` wrote `codecov.yml` (informational status, no PR comment) and its `^codecov\.yml$` `.Rbuildignore` entry. Added `covr` to DESCRIPTION `Suggests` by hand (usethis errored out before that step — see below). Added R-CMD-check + Codecov badges to the `README.Rmd` badges block (reflowed to one badge per line) and re-knit `README.md` with `devtools::build_readme()`. Local `devtools::check()` still **0 errors / 0 warnings / 0 notes**.
- **Why / decisions:** Full matrix chosen (Jeff) over Ubuntu-only for cross-platform coverage. Every `usethis` call that inserts a badge aborted with "Invalid block specification" because this repo's `<!-- badges: start -->…<!-- badges: end -->` block was a single wrapped line; the workflow/config files were still written, so badges were added manually and the block normalized so future `use_badge()` calls parse. `CODECOV_TOKEN` repo secret already added by Jeff, so coverage upload should authenticate on first run. Resolves DESIGN Known issue #2 (CI); to be removed from DESIGN once workflows are green on main (review step).
- **Follow-ups spawned:** — (M5 reliability + HiTOP-SR/BR oracles and M7 FULL/SF domain scoring remain planned.)

## 2026-07-09 — Reviewed, merged & closed out M3 (M3)

- **What:** Verified all six M3 acceptance criteria by executing them on branch `m3-check-hygiene`: `document()` no-diff; `test()` FAIL 0 WARN 0 SKIP 1 PASS 259; `check()` **0 errors / 0 warnings / 0 notes**; `data/`↔`sysdata.rda` disjoint with all 18 `data/` objects documented; `data-raw/sysdata.R` regenerates `R/sysdata.rda` identically (`internal = TRUE`); single-row `validity_pid5()` FULL/SF returns correct values (PRD 22, SDTD 17 / PRDS 12, SDTDS 8). Fresh-context general-purpose subagent diff review PASSED with no concerns — it independently re-derived the oracle constants from `pid_items`, confirmed the four instructions are byte-identical after the sysdata move, confirmed `setNames`/`write.csv` are genuinely used bare (`generate_redcap.R`), and noted `pid_scales` is covered by the pre-existing `R/zzz.R` `.onLoad` registration (hence correctly absent from the new `globalVariables()`). Merged PR [#4](https://github.com/jmgirard/hitop/pull/4) to main (`856e556`). M3 → DONE, collapsed into Completed.
- **Why / decisions:** No keying content touched, so no keying sign-off required; merge authorized by Jeff via the yes/no gate. The SDTD-denominator fix was confirmed behaviorally inert (equal-length vectors) — a correctness/clarity change whose oracle power lives in the single-row fixtures, as noted at hand-off.
- **Follow-ups spawned:** — (M4 is the next actionable milestone: it can now gate CI on the clean check.)

## 2026-07-09 — Check & dependency hygiene: clean check + single-row validity fix (M3)

- **What:** Took `devtools::check()` from 0 errors / 4 warnings / 2 notes to **0/0/0** on branch `m3-check-hygiene`. (1) **Data:** documented the two HiTOP-HSUM registry datasets (`hitophsum_items`, `hitophsum_choices`) in [R/data.R](../R/data.R); consolidated the four `*_instructions` into a new [data-raw/sysdata.R](../data-raw/sysdata.R) written with `internal = TRUE`, removed their definitions from the per-instrument `*_info.R` scripts, and deleted the duplicate `data/*_instructions.rda` (regenerated `R/sysdata.rda` and verified byte-identical content; D-007). (2) **Deps:** removed unused Imports {glue}/{lifecycle}/{jsonlite} (and their `@importFrom`s); moved unexported `R/qualtrics_test.R` → `devel/` to drop the undeclared {httr2} use. (3) **Cleanup:** removed all 8 redundant `utils::data()` calls (score/label functions now use lazy data directly) and added `utils::globalVariables()` + `@importFrom stats setNames`/`utils write.csv` to silence the no-visible-binding NOTE; escaped 18 `•` bullets in `generate_docx.R` (non-ASCII WARNING); added `@inheritParams` to the six pid5sf/pid5bf `generate_*` variants (undocumented-args WARNING); `.Rbuildignore`d `hitop_hex.png`; removed stale vignette artifact dirs. (4) **Bug fix (test-first):** `validity_pid5()` no longer errors on single-row FULL/SF input — added `drop = FALSE` to the ORS/PRD/SDTD `rowSums()` indexing (DESIGN #9) with hand-computed 1-row regression tests, and corrected the SDTD warning-percentage denominator (`length(prd_vec)` → `length(sdtd_vec)`; behaviorally inert, same length). Full suite FAIL 0 WARN 0 SKIP 1 **PASS 259** (+10). Added a NEWS bullet; added the HSUM datasets to the `_pkgdown.yml` reference index.
- **Why / decisions:** D-007 records instructions as internal-only data owned by `data-raw/sysdata.R`. Per D-004, the single-row fix shipped with ground-truth 1-row fixtures (expected values from the official key) rather than a snapshot. The two out-of-original-scope warnings (non-ASCII, undocumented generator args) were folded in because criterion #1 (0 warnings) can't be met otherwise; recorded as discovered subtasks in MILESTONES. No keying content touched. Resolved DESIGN Known issues #3–#5, #8, #9 (removed); #1/#2/OQ-1/SF-cutoffs remain (renumbered 1–4).
- **Follow-ups spawned:** — (M4 CI can now gate on the clean check; M5 reliability + HiTOP-SR/BR oracles; M7 FULL/SF domain scoring remain planned).

## 2026-07-09 — Reviewed, merged & closed out M2; skill + docs sync (M2)

- **What:** Verified M2's five acceptance criteria by executing them: `document()` no-diff; full `test()` FAIL 0 WARN 0 SKIP 1 PASS 249; `check()` 0 errors / 4 warnings / 2 notes — all pre-existing M3 categories (non-ASCII, dependencies, missing docs, Rd usage, top-level files, global bindings), and a scripted scan confirmed **none reference the added test files** (M2 criterion is "no new problem from the tests", not full clean). Fresh-context general-purpose subagent diff review PASSED — it re-verified every hardcoded item list against `pid_items`, spot-checked 5 hand-computed fixture values, confirmed the "independent recomputation" tests read no package data, and confirmed the `score_pid5()` SE-prefix fix matches `score_hitopbr`/`score_hitopsr`. Merged PR [#3](https://github.com/jmgirard/hitop/pull/3) to main (`ceda794`). M2 → DONE, collapsed into Completed. **Skill fix:** updated `/review-milestone` step 6 to make the PR merge an explicit `AskUserQuestion` yes/no authorization gate (the harness blocks the agent from self-merging without it) and to close out only after the PR is actually merged (`c66f875`). **Docs sync:** DESIGN Known issue #1 already reflected M2; #9 (single-row `validity_pid5()` bug) carried in for M3.
- **Why / decisions:** Per D-004 the `calc_se` invariant asserts the correct `pid_anhedonia_se` name rather than enshrining the prefix bug, so the fix shipped with the test PR. Merge authorized by Jeff via the new yes/no gate. No keying content touched, so no keying sign-off required.
- **Follow-ups spawned:** M3 to fix single-row `validity_pid5()` (#9); M5 reliability + HiTOP-SR/BR oracles; M7 FULL/SF domain scoring. M3 is the next actionable milestone.

## 2026-07-09 — Ported PID-5 scoring/validity oracle tests (M2)

- **What:** Added five test files + `helper-fixtures.R` adapting the fork's oracle suite to this repo's API (`version=` not `score_pid5fsf`; `pid_<camelCase>` columns; required `items=`; `srange`; no `scales`/`id`). Coverage: hand-computed fixtures for FULL facets + 5 validity scales, SF facets + 5 S-variant scales, and **BF 5 domains + PNA** (BF fixtures are net-new — the fork had no BF); independent recomputation from hardcoded official item numbers for a facet/domain per version and INC/INCS; invariants for reverse-keying (FULL applies it, SF/BF do not), NA propagation, row-count, `prefix`/`append`/`tibble`/`calc_se`, version case-insensitivity, per-version column counts, and an `alpha`/`omega` print smoke test. New-file totals FAIL 0 PASS 121; full suite FAIL 0 WARN 0 SKIP 1 PASS 249. Dropped the fork's `scales`/`id`/domain/SDTD-guard-bug/`validate_id` tests (absent from this API). FULL/SF *domain* oracle stays deferred to M7.
- **Why / decisions:** Two production bugs surfaced by the oracles. (1) **Fixed** `score_pid5()` SE columns lacked the `prefix` — emitted `anhedonia_se` vs the documented/`score_hitopbr`-consistent `pid_anhedonia_se` ([R/score_pid5.R:139](../R/score_pid5.R)); per D-004 the invariant test asserts the correct name rather than enshrining the bug. (2) **Recorded, deferred to M3** `validity_pid5()` errors on single-row input (ORS/PRD/SDTD `rowSums(data_items[, items])` drops to a vector; needs `drop = FALSE`) — DESIGN Known issue #9; warning tests use ≥ 2 rows to avoid it. Reliability ground-truth left to M5 (M2 only smoke-tests the print side-effect). No `document()` needed (fix was a string, no roxygen change).
- **Follow-ups spawned:** M3 to fix the single-row `validity_pid5()` bug (#9); M5 reliability oracles; M7 FULL/SF domain scoring + oracle.

## 2026-07-09 — Reviewed, merged & closed out M1; docs sync (M1)

- **What:** Verified M1's acceptance criteria on the branch (`test(filter="keying")` FAIL 0 SKIP 1 PASS 111; `document()` no-diff; full `test()` PASS 128; `check()` shows only pre-existing M3-owned warnings, no new problems), plus a fresh-context general-purpose subagent diff review (PASS — oracle direction non-circular, tibble-safe extraction, no weakened assertions). Merged PR [#2](https://github.com/jmgirard/hitop/pull/2) to main (`c3ea999`). **Docs sync:** M1 → DONE and collapsed into Completed; DESIGN Known issue #1 rewritten (keying tests now exist; scoring/validity=M2, reliability/HiTOP-SR-BR=M5 still outstanding).
- **Why / decisions:** M1's criteria deliberately scope out `check()`-clean (that's M3), so pre-existing check warnings don't block it. Test-only PR, no keying content, so no maintainer keying sign-off required; merge performed by the maintainer.
- **Follow-ups spawned:** M2 and M5 are now unblocked (dependency M1 DONE) and plannable via `/plan-milestone`.

## 2026-07-09 — Ported keying-verification tests (M1)

- **What:** Added `tests/testthat/test-keying.R`, porting the fork's external-source keying oracle to this repo (`PID5→FULL`, `PID5FSF→SF` column renames; tibble-safe `pid_items$FULL[rows]` extraction). 11 `test_that` blocks (10 active + OQ-1 skip) verify `pid_items` against the published sources — reverse items, 25 facets, INC 20 pairs, INC-S 10 pairs (2020 + 2021 Correction), ORS 10, PRD 22, SDTD 16-subset, SF 100 selection + per-facet + no-reverse. `devtools::test(filter="keying")` → FAIL 0 | SKIP 1 | PASS 111. `pid_items` unchanged. Installed local dev deps flextable/officer/snakecase so the suite can load. Also planned M1→READY and added M6 (BF provenance) + M7 (FULL/SF domain scoring) stubs, verified against the APA full-form and BF scoring-key PDFs.
- **Why / decisions:** Fulfills D-005 (external-source oracle) for the FULL/SF keying. Confirmed from the primary APA key (Krueger et al., 2013) that the 5-domain = 3-primary-facet model (Step 3) is official but *unimplemented* here (FULL/SF score no domains) → deferred to M7 rather than forced into M1; the source `test-keying.R` never contained domain blocks, so nothing was deleted. BF Domain-column provenance (APA PID-5-BF, 2013) → M6.
- **Follow-ups spawned:** M6 (BF keying provenance + tests), M7 (PID-5 FULL/SF domain scoring). OQ-1 (SDTD item 38) still open, `skip()`-ed.

## 2026-07-09 — Adopted branch + PR workflow (—)

- **What:** Added "Branching & PRs" rules to CLAUDE.md and tracking-rules.md: milestone work on `m<n>-<slug>` branches, merged to main via PR with the URL recorded in the milestone entry; keying-content PRs require Jeff's explicit sign-off before merge; trivial doc/tracking edits may still go direct to main. Updated /work-milestone (branch before code, open PR at completion) and /review-milestone (review the PR diff, merge on pass) to match.
- **Why / decisions:** main is a distribution channel (pak installs and pkgdown deploys from it) and must stay installable; PRs give CI (future M4) a gate and keying changes a sign-off point.
- **Follow-ups spawned:** —

## 2026-07-09 — Added collaboration rules: primary sources & model choice (—)

- **What:** Added two working rules to CLAUDE.md ("Working with Jeff") and `.claude/skills/shared/tracking-rules.md`: (1) seek primary sources when they would help and ask Jeff to upload any that can't be found/accessed — never substitute secondary descriptions or model memory for keying/scoring content; (2) assume the main session runs on Opus, delegate suitable tasks to Sonnet subagents, and ask Jeff to launch a clean Fable session when one would help.
- **Why / decisions:** Maintainer request; extends the existing subagent model-choice rule (no Haiku for accuracy-critical work).
- **Follow-ups spawned:** —

## 2026-07-09 — Tracking docs bootstrapped from code (M1–M5)

- **What:** The tracking system (CLAUDE.md, project/, .claude skills) was copied in from an older PID-5-only fork; its docs described that fork, not this package. Established ground truth from DESCRIPTION, NAMESPACE, R/, tests/, data/, .github/, vignettes/, and _pkgdown.yml, then rewrote CLAUDE.md, DESIGN.md, ROADMAP.md, and MILESTONES.md to match reality (v0.1.0; PID-5 FULL/SF/BF via `version` arg; HiTOP-SR/BR/HSUM; reliability, label/rename/rank, and generator families; pkgdown + vignettes shipped). SOURCES.md kept (keying provenance re-verified against this repo's data-raw/pid_items.csv); D-001–D-005 preserved as history; D-006 added.
- **Verified while auditing:** the old fork's SDTD guard bug, commented-out SF warnings, placeholder DESCRIPTION, and missing BF support do **not** exist here. Still real: no PID-5/keying/reliability tests, no check/coverage CI. Newly found: 6 undocumented data/*.rda (instructions duplicated in sysdata), {httr2} used undeclared while {glue}/{lifecycle}/{jsonlite} are unused Imports, redundant `utils::data()` calls (work, but pollute the global env), and local test/check blocked by uninstalled Imports (flextable/officer/snakecase). Recorded as DESIGN Known issues #1–#8.
- **Why / decisions:** D-006 (re-baseline on the canonical repo; port — don't copy — the fork's oracle tests).
- **Follow-ups spawned:** M1 (keying tests), M2 (PID-5 oracle tests), M3 (check/dependency hygiene), M4 (CI + coverage), M5 (HiTOP oracle tests). OQ-1 (SDTD item 38) remains open pending the maintainer's check of the physical PID-5 manual.
