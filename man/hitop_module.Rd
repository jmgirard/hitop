% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module.R
\name{hitop_module}
\alias{hitop_module}
\title{Describe a Module of an Instrument's Scales}
\usage{
hitop_module(instrument = "hitopsr", scales, call = rlang::current_env())
}
\arguments{
\item{instrument}{A string naming the instrument to build a module from.
Currently only \code{"hitopsr"} is supported. (default = \code{"hitopsr"})}

\item{scales}{A character vector of scale names to keep. Names may be given
either as they are printed on the instrument (\code{"Antisocial Behavior"}) or
as the camelCase stems used in scored output (\code{"antisocialBehavior"}), in
any mixture and ignoring case. Duplicates are dropped.}

\item{call}{Internal. The environment blamed by any error this raises. A
default argument is evaluated in this function's own frame, so a direct
call blames \code{hitop_module()}; the deprecated \code{\link[=hitop_subset]{hitop_subset()}} passes its
own frame instead, so a bad argument there names the function the user
actually wrote. (default = this function's frame)}
}
\value{
An object of class \code{hitop_module}: a list with the resolved
\code{instrument}, the canonical display \code{scales} and their \code{camelCase} stems,
the integer \code{items} kept (original instrument numbering, ascending), the
parallel \code{reverse} keying flags, and \code{nItems}.
}
\description{
Builds a validated description of a \strong{module}: a chosen set of
an instrument's scales, administered and scored on its own. Supplying the
result as the \code{module} argument of a generator produces an instrument
containing only the items belonging to those scales. The \code{items} field
below always holds the \strong{original} instrument numbers, whatever a
generator prints: the online exports keep those numbers, while
\code{\link[=generate_docx_hitopsr]{generate_docx_hitopsr()}} numbers a Word form \code{1} to \code{n} down the page
unless asked not to. Supplying the module again to \code{\link[=score_hitopsr]{score_hitopsr()}} or
\code{\link[=reliability_hitopsr]{reliability_hitopsr()}} scores the collected columns either way.

A module naming every scale holds exactly the instrument's own items --
all 405 of them for the HiTOP-SR -- but it is still a module, and
\code{\link[=generate_docx_hitopsr]{generate_docx_hitopsr()}} frames it as one: the form is headed
\code{"HiTOP-SR Module (v1.0)"}, and with \code{randomize = TRUE} it also carries a
405-row crosswalk. Supply no \code{module} at all to get the full instrument's
framing. The Qualtrics and REDCap exports are the same either way.

Use \code{\link[=available_scales]{available_scales()}} to see which scales an instrument offers.
}
\examples{
# Describe a two-scale module of the HiTOP-SR
m <- hitop_module("hitopsr", scales = c("Agoraphobia", "Appetite Loss"))
m

# `$items` holds the original HiTOP-SR numbers, not 1..8 -- this is the
# descriptor, not what any particular generator prints
m$items

# Select the collected item columns by NAME, never by position: `m$items`
# holds item numbers, which are column positions only in a frame that is
# exactly the 405 items in order. `ku_hitopsr` leads with `participant` and
# `biosex`, so `ku_hitopsr[m$items]` would quietly return the wrong columns.
collected <- ku_hitopsr[sprintf("hsr_\%03d", m$items)]
ncol(collected) == m$nItems

}
\seealso{
\code{\link[=available_scales]{available_scales()}} for the scale names this accepts;
\code{\link[=generate_docx_hitopsr]{generate_docx_hitopsr()}}, \code{\link[=generate_qualtrics_hitopsr]{generate_qualtrics_hitopsr()}}, and
\code{\link[=generate_redcap_hitopsr]{generate_redcap_hitopsr()}}, each of which takes a \code{module} argument;
\code{\link[=score_hitopsr]{score_hitopsr()}} and \code{\link[=reliability_hitopsr]{reliability_hitopsr()}} for scoring the result.
}
