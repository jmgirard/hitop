% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_file.R
\name{read_module}
\alias{read_module}
\title{Read a Module from a File}
\usage{
read_module(file)
}
\arguments{
\item{file}{A string giving the path to read from.}
}
\value{
A \code{hitop_module} object. If the file carries an \code{itemOrder}, it is
returned on the object's \code{item_order} attribute --- the same attribute
\code{\link[=generate_docx_hitopsr]{generate_docx_hitopsr()}} returns for a shuffled form.
}
\description{
Reads a module descriptor written by \code{\link[=write_module]{write_module()}} (or
written by hand to the same format) and returns the \code{\link[=hitop_module]{hitop_module()}}
object it describes, ready to pass to \code{\link[=score_hitopsr]{score_hitopsr()}},
\code{\link[=reliability_hitopsr]{reliability_hitopsr()}}, or any of the generators.

The file never supplies keying. The module is rebuilt by passing the
file's \code{scales} through \code{\link[=hitop_module]{hitop_module()}}, so this package's own tables
remain the only source of which items belong to a scale. The file's
recorded \code{items} and \code{nItems}, where present, are checked against that
rebuild, and a disagreement is an error: a descriptor written against
scale tables that have since moved fails loudly rather than scoring
quietly.
}
\section{Errors}{


Every failure below aborts with a condition naming the file, so a caller
may catch a particular one by class: \code{hitop_module_file_missing},
\code{hitop_module_file_invalid_json}, \code{hitop_module_file_missing_field},
\code{hitop_module_file_unsupported_format}, \code{hitop_module_file_unknown_scales}
(which carries \code{\link[=hitop_module]{hitop_module()}}'s own refusal as its parent),
\code{hitop_module_file_items_mismatch}, and \code{hitop_module_file_bad_item_order}.

The list is exhaustive by design: a descriptor that is malformed rather
than merely wrong --- a top level that is a JSON array instead of an
object, or a number field that is not a flat array of numbers --- is
refused as \code{hitop_module_file_invalid_json} or as the mismatch condition
for the field it spoils, never as a bare R coercion error.
}

\section{The descriptor format}{


The file is JSON, with these fields:

\describe{
\item{\code{format}}{The format version, a \code{"major.minor"} string. This
release writes \code{"1.0"}.}
\item{\code{package}, \code{packageVersion}, \code{buildDate}}{The package that wrote
the file, its version, and the date it was written. Recorded for the
reader; \code{\link[=read_module]{read_module()}} ignores all three.}
\item{\code{instrument}}{The instrument the module belongs to.}
\item{\code{scales}}{The module's scales, as they are printed on the
instrument. \strong{Required}: these are what the module is rebuilt from.}
\item{\code{items}, \code{nItems}}{The original instrument item numbers the module
covers, and how many there are. Cross-checked on read: the order they
are written in carries no meaning --- \code{\link[=read_module]{read_module()}} compares them as
a set --- but a repeated number is an error, and the printed order of
a shuffled form belongs in \code{itemOrder} instead.}
\item{\code{itemOrder}}{The printed order of a shuffled form: a permutation
of \code{items}. Optional --- a form printed in instrument order carries
none. \code{\link[=read_module]{read_module()}} returns it on the module's \code{item_order}
attribute, the same attribute \code{\link[=generate_docx_hitopsr]{generate_docx_hitopsr()}} returns, and
\code{\link[=write_module]{write_module()}} writes it back from that attribute, so a descriptor
read and written again keeps the order it recorded. The generators'
\code{descriptor} argument sets the attribute for you.}
}

\code{format}, \code{instrument}, and \code{scales} are required. The fields and the
version string are a public contract and change only deliberately.
}

\examples{
m <- hitop_module("hitopsr", scales = c("Agoraphobia", "Appetite Loss"))

f <- tempfile(fileext = ".json")
write_module(m, f)

m2 <- read_module(f)
m2
identical(m2, m)

file.remove(f)

}
\seealso{
\code{\link[=write_module]{write_module()}} to write the file; \code{\link[=hitop_module]{hitop_module()}} to build a
module without one.
}
