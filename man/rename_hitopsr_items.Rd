% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rename_hitopsr_items.R
\name{rename_hitopsr_items}
\alias{rename_hitopsr_items}
\title{Rename Columns to Standard HiTOP-SR Item Names}
\usage{
rename_hitopsr_items(
  data,
  method = c("original", "text"),
  item_cols = NULL,
  item_text = NULL,
  prefix = "HSR_"
)
}
\arguments{
\item{data}{A data frame containing the HiTOP-SR items.}

\item{method}{A string specifying the matching method: \code{"original"} to match
against the legacy item pool names in \code{hitopsr_items$Original}, or \code{"text"}
to match against the literal item prompt text. (default = \code{"original"})}

\item{item_cols}{An optional character vector of current column names to
be renamed. Required if \code{method = "text"}.}

\item{item_text}{An optional character vector of item texts corresponding
exactly to the columns specified in \code{item_cols}. Required if \code{method = "text"}.}

\item{prefix}{An optional string to add before each standardized item number.
(default = \code{"HSR_"})}
}
\value{
A data frame with renamed column names for the matched HiTOP-SR items.
}
\description{
Rename data frame columns to standard HiTOP-SR item names based on either
legacy ("Original") variable names or item text matching.
}
\examples{
# Rename legacy item-pool columns to standard HiTOP-SR item names
legacy <- head(hitopsr_items$Original, 3)
df <- as.data.frame(matrix(0, nrow = 2, ncol = 3,
                           dimnames = list(NULL, legacy)))
names(rename_hitopsr_items(df, method = "original"))

}
