% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability_pid5.R
\name{reliability_pid5}
\alias{reliability_pid5}
\title{Estimate PID-5 scale reliability}
\usage{
reliability_pid5(
  data,
  items,
  version = c("FULL", "SF", "BF"),
  srange = c(0, 3),
  alpha = TRUE,
  omega = TRUE
)
}
\arguments{
\item{data}{A data frame containing (at least) all the PID items (numerically
scored and in order).}

\item{items}{A vector of column names (as strings) or numbers (as integers)
corresponding to the PID items in order. Items must be supplied in
instrument order; duplicated entries are an error.}

\item{version}{A string indicating the version of the PID to score: "FULL",
"SF", or "BF". Will be automatically capitalized. (default = \code{"FULL"})}

\item{srange}{An optional numeric vector specifying the minimum and maximum
values of the items, used for reverse-coding. (default = \code{c(0, 3)})}

\item{alpha}{Optional logical; if \code{TRUE}, include a column of Cronbach's alpha
per scale. (default = \code{TRUE})}

\item{omega}{Optional logical; if \code{TRUE}, include a column of McDonald's omega
(total) per scale, estimated via a one-factor CFA (requires the \pkg{lavaan}
package). (default = \code{TRUE})}
}
\value{
A \link[tibble]{tibble} with one row per scale and columns \code{scale},
\code{nItems}, and (when requested) \code{alpha} and \code{omega}.
}
\description{
Compute per-scale internal-consistency reliability — Cronbach's alpha and
McDonald's omega — for the Personality Inventory for DSM-5: full version
(PID-5, 220 items), short form (PID-5-SF, 100 items), or brief form (PID-5-BF,
25 items). Reliability is estimated on the reverse-keyed item responses, at
the facet level for FULL/SF and the domain level for BF (the same scales
\code{\link[=score_pid5]{score_pid5()}} outputs, before FULL/SF domain aggregation).
}
\details{
Alpha is computed by \code{\link[=calc_alpha]{calc_alpha()}} (covariance-based, pairwise
deletion) and omega by \code{\link[=calc_omega]{calc_omega()}} (one-factor lavaan CFA, FIML). A scale
whose estimate cannot be computed (e.g. too few items or, for omega, a
non-converging CFA or an uninstalled \pkg{lavaan}) is returned as \code{NA}
rather than aborting the call.
}
\examples{
# Facet-level reliability for the full PID-5 (alpha only)
reliability_pid5(sim_pid5, items = 1:220, version = "FULL", omega = FALSE)

}
