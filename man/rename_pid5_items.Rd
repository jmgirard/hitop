% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rename_pid5_items.R
\name{rename_pid5_items}
\alias{rename_pid5_items}
\title{Rename Columns to Standard PID-5 Item Names}
\usage{
rename_pid5_items(
  data,
  version = c("FULL", "SF", "BF"),
  method = c("number", "text"),
  item_cols = NULL,
  item_text = NULL,
  from_prefix = "pid_",
  prefix = NULL
)
}
\arguments{
\item{data}{A data frame containing the PID-5 items.}

\item{version}{A string specifying the PID-5 form the items belong to:
\code{"FULL"} (220 items), \code{"SF"} (100 items), or \code{"BF"} (25 items). Matched
case-insensitively. (default = \code{"FULL"})}

\item{method}{A string specifying the matching method: \code{"number"} to rename
columns spelled \code{from_prefix} followed by an item number, or \code{"text"} to
match against the literal item prompt text in \code{pid_items$Text}.
(default = \code{"number"})

The three forms number their items independently, so \code{"number"} reads the
digits as an item number of the form named by \code{version}: under
\code{version = "SF"}, \code{pid_7} is short-form item 7, not the full-form item the
short form numbers 7. Data labelled by full-form item numbers must be
renamed with \code{version = "FULL"} first, or matched with \code{method = "text"}.}

\item{item_cols}{An optional character vector of current column names to
be renamed. Required if \code{method = "text"}.}

\item{item_text}{An optional character vector of item texts corresponding
exactly to the columns specified in \code{item_cols}. Required if
\code{method = "text"}.}

\item{from_prefix}{A string matched literally at the start of a column name
under \code{method = "number"}, before the item number. The default is the
spelling this package's own PID-5 datasets used before they were renamed
to match the exports. (default = \code{"pid_"})}

\item{prefix}{A string pasted literally before each standardized item
number, which is zero-padded to the width of the form's largest item
number. \code{NULL} resolves to the form's own stem: \code{"pid5_"}, \code{"pid5sf_"} or
\code{"pid5bf_"}. (default = \code{NULL})}
}
\value{
A data frame with renamed column names for the matched PID-5 items.
Columns that could not be matched keep their names. Under
\code{method = "number"}, a column spelled like an item of the instrument whose
number names no item of this form, and under \code{method = "text"}, an
\code{item_text} entry matching no item of this form, are skipped and named in
a warning of class \code{hitop_unmatched_items}, which callers may catch or
suppress by class. A column not spelled like an item number is left alone
and not reported.

Two other warnings carry a class of their own. Under \code{method = "number"},
if no column at all is named \code{from_prefix} followed by a number, nothing
is renamed and the report is \code{hitop_no_columns_matched}. Under either
method, if some but not all of the form's items were renamed, the
completeness report is \code{hitop_incomplete_rename}.
}
\description{
Rename data frame columns to the standard PID-5 item names of one form,
matching either on the item number carried in the current column name or on
the literal item prompt text. The standard names are the ones the package's
REDCap and Qualtrics exports write and the shipped datasets carry:
\code{pid5_001} to \code{pid5_220} for the full form, \code{pid5sf_001} to \code{pid5sf_100} for
the short form, and \code{pid5bf_01} to \code{pid5bf_25} for the brief form.
}
\examples{
# Rename columns named as this package's datasets were before the rename
df <- data.frame(pid_1 = c(0, 1), pid_2 = c(2, 3), age = c(30, 40))
names(suppressWarnings(rename_pid5_items(df, version = "FULL")))

}
