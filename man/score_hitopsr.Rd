% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_hitopsr.R
\name{score_hitopsr}
\alias{score_hitopsr}
\title{Score the HiTOP-SR Instrument}
\usage{
score_hitopsr(
  data,
  items,
  srange = c(1, 4),
  prefix = "hsr_",
  missing = c("available", "complete"),
  calc_se = FALSE,
  append = TRUE,
  module = NULL,
  subset = NULL
)
}
\arguments{
\item{data}{A data frame containing the HiTOP-SR items (numerically coded):
all 405 of them, or, when \code{module} is supplied, that module's items.}

\item{items}{A vector of column names (as strings) or numbers (as integers)
corresponding to the HiTOP-SR items held in \code{data} — all 405, or, when
\code{module} is supplied, that module's items. Items must be supplied in
instrument order; a misordered mapping silently scores the wrong items, so a
warning is issued when the names share a common prefix and trailing number
but those numbers are not ascending. Duplicated entries are an error.}

\item{srange}{An optional numeric vector specifying the minimum and maximum
values of the HiTOP-SR items, used for reverse-coding. (default = \code{c(1, 4)})}

\item{prefix}{An optional string to add before each scale column name. If no
prefix is desired, set to an empty string \code{""}. (default = \code{"hsr_"})}

\item{missing}{A string selecting how missing item responses are handled when
computing scale scores. \code{"available"} (the default) averages whatever items
are present (\code{rowMeans(na.rm = TRUE)}); \code{"complete"} returns \code{NA} for any
scale with a missing item (\code{rowMeans(na.rm = FALSE)}). (default =
\code{"available"})}

\item{calc_se}{An optional logical indicating whether to calculate a
standard error for each scale score: the SD of the items the respondent
actually answered divided by the square root of how many of those items
they answered. Each one summarizes how much a respondent's answers varied
within a scale. It is not a standard error of measurement — no reliability
estimate enters it — so it does not give a confidence interval for a
respondent's true score; for measurement precision see
\code{\link[=reliability_hitopsr]{reliability_hitopsr()}}. (default = \code{FALSE})}

\item{append}{An optional logical indicating whether the new columns should
be added to the end of the \code{data} input. (default = \code{TRUE})}

\item{module}{An optional \code{hitop_module} object, as returned by
\code{\link[=hitop_module]{hitop_module()}}, describing a module of the instrument. When supplied,
\code{data} and \code{items} hold only that module's item columns — in ascending
instrument order, as the \verb{generate_*_hitopsr()} forms lay them out — and
only that module's scales are scored. When \code{NULL}, all 405 items are
expected and all 76 scales are scored. (default = \code{NULL})}

\item{subset}{Deprecated. The former name of \code{module}; supplying it warns.
Supplying both \code{module} and \code{subset} is an error. (default = \code{NULL})}
}
\value{
A \link[tibble]{tibble} containing all scale scores and standard
errors (if requested) and all original \code{data} columns (if requested).
}
\description{
Create a data frame with scores on all the HiTOP-SR scales.
}
\details{
For per-scale reliability estimates (Cronbach's alpha, McDonald's
omega), use \code{\link[=reliability_hitopsr]{reliability_hitopsr()}}.
}
\examples{
# Score all HiTOP-SR scales from the simulated data
score_hitopsr(sim_hitopsr, items = 1:405, append = FALSE)

# Score data collected with a two-scale module. Select the item columns
# by name: `m$items` holds original HiTOP-SR numbers, which are column
# positions only in a data frame that is exactly the 405 items in order.
m <- hitop_module("hitopsr", scales = c("Agoraphobia", "Appetite Loss"))
collected <- sim_hitopsr[paste0("hsr_", m$items)]
score_hitopsr(collected, items = names(collected), module = m, append = FALSE)

}
