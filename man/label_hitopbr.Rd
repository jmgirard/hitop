% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_hitopbr.R
\name{label_hitopbr}
\alias{label_hitopbr}
\title{Label HiTOP-BR Columns with Semantic Descriptions}
\usage{
label_hitopbr(data, target = c("items", "scales"), prefix = "hbr_")
}
\arguments{
\item{data}{A data frame containing HiTOP-BR items or scales.}

\item{target}{A string specifying what to label: \code{"items"} to label raw item
columns with questionnaire text, or \code{"scales"} to label computed scale columns.}

\item{prefix}{A string specifying the prefix used on the column names. Item
columns are expected as the prefix followed by the item number zero-padded
to two digits (\code{hbr_01} to \code{hbr_45} under the default, the pattern the
shipped datasets and the package's REDCap export use; the Qualtrics export
writes \code{HBR_01}, matched by \code{prefix = "HBR_"}). Columns that carry the
prefix and a number that is not one of those expected names are not
labelled, and a warning of class \code{hitop_unpadded_items} names them, in a
sentence per kind: a number without that padding is reported as not
zero-padded to two digits, and a number outside 1 to 45 is reported as out
of range, whatever its padding. That warning is raised whether or not any
other column matched. Scale
columns are expected as the prefix followed by the scale's \code{camelCase}
name, which is what \code{\link[=score_hitopbr]{score_hitopbr()}} writes under its own default
\code{prefix}. (default = \code{"hbr_"})}
}
\value{
A data frame with labeled columns. If no column matched the expected
names at all, \code{data} is returned unchanged and a warning says so; the
\code{hitop_unpadded_items} report still names every prefixed item column it
found.
}
\description{
Add literal item text or clean scale names as attributes to data frame
columns for the HiTOP Brief Report (HiTOP-BR), making them readable by
data viewers and reporting packages.
}
\examples{
# Attach item text as a `label` attribute to the raw item columns
labeled <- label_hitopbr(sim_hitopbr, target = "items")
attr(labeled$hbr_01, "label")

}
