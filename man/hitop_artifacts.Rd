% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{hitop_artifacts}
\alias{hitop_artifacts}
\title{Distribution Artifact Manifest}
\format{
A \link[tibble]{tibble} with one row per artifact build and 7
columns:
\describe{
\item{file}{Artifact file name in \verb{inst/extdata/}}
\item{instrument}{Instrument the artifact administers}
\item{format}{Artifact format: \code{"docx_us"}, \code{"docx_a4"}, \code{"qualtrics"},
or \code{"redcap"}}
\item{instrument_version}{Version of the instrument itself}
\item{build_date}{Date this build of the artifact was generated}
\item{md5}{MD5 checksum of the built file}
\item{changes}{What changed in this build}
}
}
\usage{
hitop_artifacts
}
\description{
Version manifest for the prebuilt instrument artifacts distributed in
\verb{inst/extdata/} and on the package website's download pages. Each build of
an artifact adds a row (the full history is kept), so the latest row per
\code{file} describes the currently distributed file. Artifact revisions are
identified by build date; the instrument version (e.g., \code{"1.0"}) is the
version of the instrument itself and changes only when its publisher
revises it. To check which build you have, compare your downloaded file's
MD5 checksum (e.g., \code{tools::md5sum()}) against the \code{md5} column.
}
\examples{
hitop_artifacts
}
\keyword{datasets}
