% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/norm_pid5.R
\name{norm_pid5}
\alias{norm_pid5}
\title{Convert PID-5 scale scores to normative T scores and percentiles}
\usage{
norm_pid5(
  data,
  scores,
  version = c("FULL", "SF", "BF"),
  srange = c(0, 3),
  prefix = "pid_",
  append = TRUE
)
}
\arguments{
\item{data}{A data frame containing scored PID-5 columns.}

\item{scores}{The score columns to convert, as column names or column
positions (mirroring the \code{items} argument of \code{\link[=score_pid5]{score_pid5()}}). Each column
must be numeric (or logical) and each may be named only once.}

\item{version}{Which PID-5 version the scores came from: \code{"FULL"} (220
items), \code{"SF"} (100 items), or \code{"BF"} (25 items). The normative tables
differ by version.}

\item{srange}{The response range the items were coded on, as
\code{c(low, high)}. Any four-option coding is accepted and reconciled to the
official \code{c(0, 3)} range before lookup; a coding with a different number of
options is not convertible. See Details.}

\item{prefix}{The prefix \code{\link[=score_pid5]{score_pid5()}} applied to its output columns, used
to match a score column back to its scale. Matched literally, not as a
regular expression: a column name that does not begin with exactly this
string keeps its whole name and is reported as uncovered.}

\item{append}{Whether to return the input \code{data} with the conversion columns
appended (\code{TRUE}, the default) or the conversion columns alone.}
}
\value{
A \link[tibble]{tibble} with a \verb{_t} column for every converted scale
whose normative rows carry a T score (the five domains, plus the brief
form's total) and a \verb{_ptl} column for every converted scale, alongside all
original \code{data} columns if requested. The four validity scales
(\code{INC}, \code{INCS}, \code{ORS}, \code{PRD}) are distributed as percentiles only and get
no \verb{_t} column.
}
\description{
Looks up scored PID-5 columns in the published normative tables shipped as
\link{pid_norms} and returns a T score and a percentile for each. Scores are
produced by \code{\link[=score_pid5]{score_pid5()}} and \code{\link[=validity_pid5]{validity_pid5()}}; this function converts them
and never rescores.
}
\details{
For each named score column the tables are searched for the printed
row whose raw score is \strong{nearest} the observed value, and that one row's
printed T score and printed percentile are returned. Nothing is
interpolated: every returned number is a cell of Markon et al. (2024).

Markon et al. print the tables but give no instruction for reading them —
no rounding, interpolation, or tie rule appears anywhere in the book — so
the rules below are this package's, chosen and defended rather than quoted.
They were settled by an independent review of the tables recorded in the
package's development history as report RR02 (in the project repository,
not the installed package).
\itemize{
\item \strong{Between printed rows.} The nearer row wins. Printed raws step by
0.01-0.07 while attainable scores fall on much coarser grids (a 5-item
brief-form domain mean can only be a multiple of 0.2), so most lookups
land between rows.
\item \strong{Ties.} Where two or more rows are equally near -- a raw printed in
several rows, or a value exactly midway between two rows -- the row whose
T score is nearest 50 is returned. The four validity scales carry no T
score, so a tie there returns the row whose percentile is nearest 0.50.
\item \strong{Scores of 0.} Each domain table prints raw 0.00 across a run of low T
scores, because the linear T the book tabulated predicts a negative raw
there and 0.00 is printed instead. The tie rule returns the run's highest
T, the one row of the run that renders an attainable score. Its printed
percentile is positive on some scales and 0.00 on others; that asymmetry
is a property of the published tables, not of this function.
\item \strong{Scores outside the table.} A score above the highest printed row
returns that row's values, rather than an extrapolation. A score below
the lowest returns whatever an observation \emph{at} the lowest printed raw
returns -- which, on the scales whose tables print a run of 0.00, is that
run's highest-T row and not the table's first row, so the two agree
instead of jumping. A message reports how many observations were capped at
each end. This is reachable in ordinary data: \code{PRD} is a 22-item sum
reaching 66 while its table stops at 55.
\item \strong{Unattainable printed rows.} Five domain tables print rows above the
3.00 ceiling a 0-3 item mean can reach, so the top of those T ranges
cannot be attained. A maximum score returns T = 84 (brief-form negative
affectivity), 87 (brief-form detachment), 93 (brief-form disinhibition),
87 (full-form negative affectivity), or 85 (short-form negative
affectivity) -- each at percentile 1.00. Nothing is wrong with such data
and no message fires.
\item \strong{Comparison tolerance.} All comparisons use an absolute tolerance of
1e-8, so that scores on grids with no exact binary representation (a
short-form domain mean is a twelfth) match the printed 2-decimal raws as
intended.
}

Columns the tables do not cover for the requested \code{version} -- the 25
facets, for instance -- return \code{NA} in both conversion columns with a
message naming them. An \code{NA} score returns \code{NA}.

\strong{Response coding.} The normative tables are built on the official
four-option 0-3 coding. Data collected on a four-option coding that merely
starts elsewhere -- 1-4, say -- carries the same information, so each score
is reconciled to the official range before lookup and the conversion
proceeds. A coding with a different \emph{number} of options is a different
metric: no mapping onto a four-option norm table is defined, so every
conversion column is returned as \code{NA} with a warning, and the items must be
recoded and rescored.

How much a shift moves a score depends on how the scale is computed, so the
reconciliation is applied per scale rather than per item. No published
source states these rules -- Markon et al. give the tables for the official
coding only -- so, like the reading rules above, they are this package's,
derived from each scale's own definition:
\itemize{
\item \strong{Item means} (the five domains, and the brief form's total) are
reconciled by subtracting \code{srange[[1]]}. Shifting every item by a constant
shifts their mean by the same constant.
\item \strong{\code{PRD}} is a plain sum over its 22 items, so the same shift moves it by
\code{srange[[1]]} times the number of items, which is what is subtracted. The
item count is read from \link{pid_items} rather than assumed.
\item \strong{\code{INC} and \code{INC-S}} are sums of \emph{absolute differences within item
pairs}. A constant added to both members of a pair cancels in the
difference, so these are unchanged by a shift and nothing is subtracted.
\item \strong{\code{ORS}} is a count of items answered at the top of the response range --
\code{\link[=validity_pid5]{validity_pid5()}} computes it by comparing each item to \code{srange[[2]]}
rather than to a fixed value (\code{R/validity_pid5.R:153} in the package
sources). A shift moves the top of the range along with the answers, so
the same items are counted and the score is unchanged.
}

A shifted coding raises one warning per call naming which of the requested
scales were adjusted and which were left alone; where every requested scale
turns out to be coding-invariant, it says so rather than claiming an
adjustment. The warning covers the scales the tables actually carry, so a
request the tables cover nowhere raises the coverage warning above instead
and nothing about coding. The official coding is silent.

One consequence is worth stating plainly, because it can put two differently
grounded numbers side by side in the same session: \code{\link[=validity_pid5]{validity_pid5()}}'s
published cut scores are \strong{not} reconciled to a shifted coding. \code{PRD} and
\code{SD-TD} are compared against fixed thresholds that assume 0-3 items, and
\code{validity_pid5()} warns rather than adapting them. So a respondent scored on
a 1-4 coding can receive a reconciled percentile from this function and, from
\code{validity_pid5()}, a validity flag still read against the 0-3 thresholds.
Adapting those cut scores is a separate, deliberately deferred question.
}
\examples{
# Score the brief form, then convert its domains and total
scored <- score_pid5(sim_pid5bf, items = 1:25, version = "BF")
norm_pid5(scored, scores = paste0("pid_", c("detachment", "total")),
          version = "BF", append = FALSE)

}
\references{
Markon, K. E., Fossati, A., Somma, A., & Krueger, R. F. (2024).
\emph{Understanding the Personality Inventory for DSM-5 (PID-5).} American
Psychiatric Association Publishing. The normative tables in \link{pid_norms},
Appendix "Normative Score Distributions" (pp. 113-219), are the source for
every value this function returns.
}
