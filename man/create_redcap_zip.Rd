% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_redcap.R
\name{create_redcap_zip}
\alias{create_redcap_zip}
\title{Create a REDCap Instrument ZIP File}
\usage{
create_redcap_zip(
  items,
  instructions,
  form_name = NULL,
  required = TRUE,
  breaks = 15,
  file_path = NULL
)
}
\arguments{
\item{items}{A data frame containing the questionnaire items. The first column
should contain the item IDs, and a \code{Text} column should contain the item text.}

\item{instructions}{A list containing the instructions and response options.
Expects \code{options$value} and \code{options$label} for choices, and \code{start[1]} for the main instruction text.}

\item{form_name}{Character string. The internal name of the form in REDCap.
Defaults to the lowercase instrument name appended with "_questionnaire".}

\item{required}{Logical. Whether the items should be marked as required. Defaults to \code{TRUE}.}

\item{breaks}{Integer or \code{NULL}. The number of items to display before inserting a page break.
Set to \code{0} or \code{NULL} to disable pagination entirely. Defaults to \code{15}.}

\item{file_path}{Character string. The destination path for the output ZIP file.
Defaults to the current working directory with a filename derived from the instrument.}
}
\value{
Invisibly returns a data frame containing the compiled REDCap data dictionary.
}
\description{
Generates a REDCap-compatible data dictionary from item and instruction
data frames and packages it into an Instrument ZIP file for easy uploading.
}
\section{Importing (Uploading) Instruments into a REDCap Project}{

\enumerate{
\item Log in to your REDCap account and navigate to the desired project.
\item Click on the "Designer" link in the left menu bar under "Project Home and Design".
\item In the main page, under "Data Collection Instruments", look for the "Upload instrument ZIP" option and click the "Upload" button.
\item Click "Choose File", navigate to where you have the measure saved as a ZIP folder, and select the ZIP folder containing your instrument.
\item Click "Upload instrument ZIP" button to begin the import process.
\item Find the imported instrument in your list of measures and review for accuracy.
\item Test the instrument to ensure proper functionality within your project.
}
}

\examples{
\dontrun{
create_redcap_zip(
  items = hitopbr_items,
  instructions = hitopbr_instructions,
  breaks = 15,
  file_path = "instruments/hitopbr_redcap.zip"
)
}
}
