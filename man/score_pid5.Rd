% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pid5.R
\name{score_pid5}
\alias{score_pid5}
\title{Score the Full PID-5 Domain and Facet Scales}
\usage{
score_pid5(
  data,
  items,
  srange = c(0, 3),
  prefix = "pid_",
  na.rm = TRUE,
  calc_se = FALSE,
  append = TRUE,
  tibble = TRUE
)
}
\arguments{
\item{data}{A data frame containing all PID-5 items (numerically scored).}

\item{items}{A vector of column names (as strings) or numbers (as integers)
corresponding to the 220 PID-5 items in order.}

\item{srange}{An optional numeric vector specifying the minimum and maximum
values of the items, used for reverse-coding. (default = \code{c(0, 3)})}

\item{prefix}{An optional string to add before each scale column name. If no
prefix is desired, set to an empty string \code{""}. (default = \code{"pro_"})}

\item{na.rm}{An optional logical indicating whether missing values should be
ignored when calculating scale scores. (default = \code{TRUE})}

\item{calc_se}{An optional logical indicating whether to calculate the
standard error of each scale score. (default = \code{FALSE})}

\item{append}{An optional logical indicating whether the new columns should
be added to the end of the \code{data} input. (default = \code{TRUE})}

\item{tibble}{An optional logical indicating whether the output should be
converted to a \code{tibble::tibble()}. (default = \code{TRUE})}
}
\value{
A data frame containing all scale scores and standard errors (if
requested) and all original \code{data} columns (if requested)
}
\description{
Create a data frame with scores on the full PID-5 domain and facet scales.
}
\references{
\itemize{
\item Krueger, R. F., Derringer, J., Markon, K. E., Watson, D., &
Skodol, A. E. (2012). Initial construction of a maladaptive personality
trait model and inventory for DSM-5. \emph{Psychological Medicine, 42},
1879-1890. \url{https://doi.org/10.1017/s0033291711002674}
}
}
