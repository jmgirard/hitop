% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_pid5.R
\name{score_pid5}
\alias{score_pid5}
\title{Score the Personality Inventory for DSM-5}
\usage{
score_pid5(
  data,
  items,
  version = c("FULL", "SF", "BF"),
  srange = c(0, 3),
  prefix = "pid_",
  missing = c("apa", "available", "complete"),
  calc_se = FALSE,
  append = TRUE
)
}
\arguments{
\item{data}{A data frame containing (at least) all the PID items (numerically
scored and in order).}

\item{items}{A vector of column names (as strings) or numbers (as integers)
corresponding to the PID items in order. Items must be supplied in
instrument order; a misordered mapping silently scores the wrong items, so a
warning is issued when the names share a common prefix and trailing number
but those numbers are not ascending. Duplicated entries are an error.}

\item{version}{A string indicating the version of the PID to score: "FULL",
"SF", or "BF". Will be automatically capitalized. (default = \code{"FULL"})}

\item{srange}{An optional numeric vector specifying the minimum and maximum
values of the items, used for reverse-coding. (default = \code{c(0, 3)})}

\item{prefix}{An optional string to add before each scale column name. If no
prefix is desired, set to an empty string \code{""}. (default = \code{"pid_"})}

\item{missing}{A string selecting how missing item responses are handled when
computing scale scores. \code{"apa"} (the default) follows the published APA
scoring key: a facet or domain-item scale with more than 25\% of its items
unanswered is set to \code{NA}, and otherwise the raw score is prorated to the
full item count and rounded to the nearest whole number before averaging (a
FULL/SF domain is \code{NA} if any one of its three contributing facets is \code{NA}).
\code{"available"} averages whatever items are present (\code{rowMeans(na.rm = TRUE)}).
\code{"complete"} returns \code{NA} for any scale with a missing item
(\code{rowMeans(na.rm = FALSE)}). With no missing items the three agree. (default
= \code{"apa"})}

\item{calc_se}{\strong{Deprecated.} This argument, and the \verb{_se} columns it
adds, will be removed in a future release; a call with \code{calc_se = TRUE}
warns. This package has no interval function for the PID-5, so there is
no replacement for it on this instrument; for measurement precision see
\code{\link[=reliability_pid5]{reliability_pid5()}}. What it does while it lasts:
an optional logical indicating whether to calculate a
standard error for each scale score. For the 25 facets, and for the brief
form's domains and total, this is the SD of the items the respondent
actually answered divided by the square root of how many of those items
they answered. The FULL and SF domain scores are means of three facet
scores rather than of items, so their standard errors are taken one level
up: the SD of the three contributing facet scores divided by the square
root of 3. Standard errors are \code{NA} wherever their scale score is \code{NA}.
Each one summarizes how much a respondent's answers varied within a scale.
It is not a standard error of measurement — no reliability estimate enters
it — so it does not give a confidence interval for a respondent's true
score; for measurement precision see \code{\link[=reliability_pid5]{reliability_pid5()}}.
(default = \code{FALSE})}

\item{append}{An optional logical indicating whether the new columns should
be added to the end of the \code{data} input. (default = \code{TRUE})}
}
\value{
A \link[tibble]{tibble} containing all scale scores and standard
errors (if requested) and all original \code{data} columns (if requested)
}
\description{
Calculate scale scores on the Personality Inventory for DSM-5: full version
(PID-5, 220 items), short form version (PID-5-SF, 100 items), or brief form
version (PID-5-BF, 25 items) from item-level data.
}
\details{
For the FULL and SF versions, the output includes the 25 facet
scores followed by the 5 personality-trait domain scores. Following the APA
scoring key (Step 3), each domain score is the mean of the average scores of
its 3 primary facets (the map is stored in \code{pid_domains}). The BF version
scores its 5 domains directly from its items, and adds a \code{total} score. By
default (\code{missing = "apa"}) all versions apply the APA missing-data and
proration rule; use \code{missing = "available"} or \code{missing = "complete"} for
the traditional \code{rowMeans()} behaviors. For per-scale reliability estimates
(Cronbach's alpha, McDonald's omega), use \code{\link[=reliability_pid5]{reliability_pid5()}}.

\subsection{The PID-5-BF total score}{

\code{version = "BF"} returns a \code{total} column after its 5 domains. Markon et al.
(2024, p. 23) define it as the item-level mean over \strong{all 25 items}, not
the mean of the 5 domain means: the total "can be computed by averaging the
overall score by the total number of items in the measure (i.e., 25)". With
five equal-sized domains the two definitions coincide on complete data and
differ only when items are missing, where the published rule above governs.

The total is scored like any other scale, so \code{missing} applies to it at the
25-item level. Under \code{missing = "apa"} that means it is \code{NA} when more than
a quarter of the 25 items are unanswered (7 or more) and prorated otherwise,
independently of the domains. Because a 5-item domain is dropped at 2
unanswered items while the total tolerates 6, \strong{a total can be reported
alongside one or more \code{NA} domains} (at most 3 of the 5; blanking all five
requires 10 unanswered items, which blanks the total as well). This is the
published rule applied as written, not an oversight.

The FULL and SF versions have no total score: the PID-5 book defines one
only for the brief form.

\strong{Errors.} With \code{append = TRUE}, a column of \code{data} whose name this call
would also produce is an error rather than an overwrite or a duplicated
column: the message names every colliding column. Re-run with
\code{append = FALSE} to return only the new columns, or drop the colliding
columns from \code{data} first. The condition is classed
\code{hitop_append_collision}, so a caller can catch this refusal by name.
}
}
\examples{
# Score the full PID-5 (25 facets + 5 domains) from the simulated data
score_pid5(sim_pid5, items = 1:220, version = "FULL", append = FALSE)

# Short form, using the item column names instead of positions
score_pid5(sim_pid5sf, items = sprintf("pid_\%d", 1:100), version = "SF",
           append = FALSE)

# Brief form (5 domains + the total) with standard errors
score_pid5(sim_pid5bf, items = 1:25, version = "BF", calc_se = TRUE,
           append = FALSE)

}
\references{
Krueger, R. F., Derringer, J., Markon, K. E., Watson, D., &
Skodol, A. E. (2012). Initial construction of a maladaptive personality
trait model and inventory for DSM-5. \emph{Psychological Medicine, 42},
1879-1890. \doi{10.1017/s0033291711002674}

Anderson, J. L., Sellbom, M., & Salekin, R. T. (2016). Utility of
the Personality Inventory for DSM-5-Brief Form (PID-5-BF) in the
measurement of maladaptive personality and psychopathology. \emph{Assessment,
25}(5), 596–607. \doi{10.1177/1073191116676889}

Markon, K. E., Fossati, A., Somma, A., & Krueger, R. F. (2024).
\emph{Understanding the Personality Inventory for DSM-5 (PID-5).} American
Psychiatric Association Publishing. The source for the PID-5-BF total
score's definition (p. 23) and for the normative tables in \code{pid_norms}.

Maples, J. L., Carter, N. T., Few, L. R., Crego, C., Gore, W. L.,
Samuel, D. B., Williamson, R. L., Lynam, D. R., Widiger, T. A., Markon, K.
E., Krueger, R. F., & Miller, J. D. (2015). Testing whether the DSM-5
personality disorder trait model can be measured with a reduced set of
items: An item response theory investigation of the personality inventory
for DSM-5. \emph{Psychological Assessment, 27}(4), 1195–1210.
\doi{10.1037/pas0000120}
}
