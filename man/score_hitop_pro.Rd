% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hitop_pro.R
\name{score_hitop_pro}
\alias{score_hitop_pro}
\title{Score the Full HiTOP-PRO Instrument}
\usage{
score_hitop_pro(
  data,
  items = NULL,
  id = NULL,
  srange = c(1, 4),
  calc_se = FALSE,
  tibble = FALSE
)
}
\arguments{
\item{data}{A data frame containing all HiTOP-PRO items (numerically coded).}

\item{items}{An optional vector of column names (as strings) or numbers (as
integers) corresponding to the 405 HiTOP-PRO items in order. If set to
\code{NULL} (the default), all non-\code{id} columns will be assumed to be the
\code{items} in order.}

\item{id}{An optional vector of column names (as strings) or numbers (as
integers) corresponding to variables from \code{data} to keep in the output. If
set to \code{NULL} (the default), no columns will be retained.}

\item{srange}{An optional numeric vector specifying the minimum and maximum
values of the HiTOP-PRO items, used for reverse-coding.
(default = \code{c(1, 4)})}

\item{calc_se}{An optional logical indicating whether to calculate the
standard error of each scale score. (default = \code{FALSE})}

\item{tibble}{An optional logical indicating whether the output should be
converted to a \code{tibble::tibble()}. (default = \code{FALSE})}
}
\value{
A data frame containing any \code{id} variables as well as all spectrum,
subfactor, scale, and subscale scores.
}
\description{
Create a data frame with scores on all the HiTOP-PRO scales.
}
