% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset.R
\name{hitop_subset}
\alias{hitop_subset}
\title{Describe a Subset of an Instrument's Scales}
\usage{
hitop_subset(instrument = "hitopsr", scales)
}
\arguments{
\item{instrument}{A string naming the instrument to subset. Currently only
\code{"hitopsr"} is supported. (default = \code{"hitopsr"})}

\item{scales}{A character vector of scale names to keep. Names may be given
either as they are printed on the instrument (\code{"Antisocial Behavior"}) or
as the camelCase stems used in scored output (\code{"antisocialBehavior"}), in
any mixture and ignoring case. Duplicates are dropped.}
}
\value{
An object of class \code{hitop_subset}: a list with the resolved
\code{instrument}, the canonical display \code{scales} and their \code{camelCase} stems,
the \code{items} kept (original instrument numbering, ascending), the parallel
\code{reverse} keying flags, and \code{nItems}.
}
\description{
Builds a validated description of a subset of an instrument's
scales, for use with the \verb{generate_*} family. Supplying the result as the
\code{subset} argument of a generator produces a shortened instrument
containing only the items belonging to the chosen scales, \strong{keeping each
item's original number} so that data collected with the shortened form
can still be scored against the full instrument's key.
}
\examples{
# Describe a two-scale subset of the HiTOP-SR
s <- hitop_subset("hitopsr", scales = c("Agoraphobia", "Appetite Loss"))
s

# The item numbers are the original HiTOP-SR numbers, not 1..8
s$items

}
\seealso{
\code{\link[=generate_docx_hitopsr]{generate_docx_hitopsr()}}, \code{\link[=generate_qualtrics_hitopsr]{generate_qualtrics_hitopsr()}}, and
\code{\link[=generate_redcap_hitopsr]{generate_redcap_hitopsr()}}, each of which takes a \code{subset} argument.
}
