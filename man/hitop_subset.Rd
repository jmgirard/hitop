% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{hitop_subset}
\alias{hitop_subset}
\title{Deprecated: Describe a Subset of an Instrument's Scales}
\usage{
hitop_subset(instrument = "hitopsr", scales)
}
\arguments{
\item{instrument}{A string naming the instrument to build a module from.
Currently only \code{"hitopsr"} is supported. (default = \code{"hitopsr"})}

\item{scales}{A character vector of scale names to keep. Names may be given
either as they are printed on the instrument (\code{"Antisocial Behavior"}) or
as the camelCase stems used in scored output (\code{"antisocialBehavior"}), in
any mixture and ignoring case. Duplicates are dropped.}
}
\value{
An object of class \code{hitop_subset}, identical to what
\code{\link[=hitop_module]{hitop_module()}} returns apart from its class attribute.
}
\description{
\code{hitop_subset()} was renamed to \code{\link[=hitop_module]{hitop_module()}} in hitop 0.2.0,
when a chosen set of an instrument's scales became a \emph{module} throughout
this package. It is kept so existing scripts keep running: it warns, then
returns the same descriptor \code{hitop_module()} returns, carrying the legacy
\code{hitop_subset} class. Every function that accepts a module also accepts
that legacy object.

The \code{subset} argument of \code{\link[=score_hitopsr]{score_hitopsr()}}, \code{\link[=reliability_hitopsr]{reliability_hitopsr()}},
\code{\link[=generate_docx_hitopsr]{generate_docx_hitopsr()}}, \code{\link[=generate_qualtrics_hitopsr]{generate_qualtrics_hitopsr()}}, and
\code{\link[=generate_redcap_hitopsr]{generate_redcap_hitopsr()}} was renamed to \code{module} at the same time, and
is deprecated in the same way.
}
\examples{
# Deprecated; use hitop_module() instead
m <- suppressWarnings(
  hitop_subset("hitopsr", scales = c("Agoraphobia", "Appetite Loss"))
)
m

}
\seealso{
\code{\link[=hitop_module]{hitop_module()}}, which replaces this.
}
\keyword{internal}
