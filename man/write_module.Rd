% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_file.R
\name{write_module}
\alias{write_module}
\title{Save a Module to a File}
\usage{
write_module(module, file)
}
\arguments{
\item{module}{A \code{hitop_module} object, as returned by \code{\link[=hitop_module]{hitop_module()}}. An
\code{item_order} attribute, where present, is written as the file's
\code{itemOrder} and must be a permutation of the module's items. A \code{columns}
attribute, where present, is written as the file's \code{columns}. It must be
a character vector with one distinct, non-empty name per module item.
A bad attribute is refused before the file is opened.}

\item{file}{A string giving the path to write to.}
}
\value{
The \code{file} path, invisibly.
}
\description{
Writes a \code{\link[=hitop_module]{hitop_module()}} descriptor to a JSON file, so that a
researcher can keep it beside the form they field and read it back at
scoring time with \code{\link[=read_module]{read_module()}} instead of retyping every scale name.

The file records the scale names, not the keying: \code{\link[=read_module]{read_module()}} rebuilds
the items and their reverse-keying flags from this package's own tables.
The recorded \code{items} are there for a human reader and as a cross-check,
and a file that disagrees with what the package derives is an error rather
than a silent preference for either side.

Before it writes, \code{write_module()} rebuilds the module with
\code{\link[=hitop_module]{hitop_module()}} from its \code{instrument} and \code{scales}. A module whose
\code{items} or \code{nItems} differ from that rebuild is refused, and nothing is
written. The file holds the rebuild's fields. \code{\link[=read_module]{read_module()}} rebuilds
the module from the file's \code{scales}, so it returns a \code{hitop_module} with
integer items. This is also true for a file written
from the deprecated \code{hitop_subset} class, or from a module whose items
are doubles.
}
\section{The descriptor format}{


The file is JSON, with these fields:

\describe{
\item{\code{format}}{The format version, a \code{"major.minor"} string. This
release writes \code{"1.0"}.}
\item{\code{package}, \code{packageVersion}, \code{buildDate}}{The package that wrote
the file, its version, and the date it was written. Recorded for the
reader; \code{\link[=read_module]{read_module()}} ignores all three.}
\item{\code{instrument}}{The instrument the module belongs to.}
\item{\code{scales}}{The module's scales, as they are printed on the
instrument. \strong{Required}: these are what the module is rebuilt from.}
\item{\code{items}, \code{nItems}}{The original instrument item numbers the module
covers, and how many there are. Cross-checked on read: the order they
are written in carries no meaning --- \code{\link[=read_module]{read_module()}} compares them as
a set --- but a repeated number is an error, and the printed order of
a shuffled form belongs in \code{itemOrder} instead.}
\item{\code{itemOrder}}{The printed order of a shuffled form: a permutation
of \code{items}. Optional --- a form printed in instrument order carries
none. \code{\link[=read_module]{read_module()}} returns it on the module's \code{item_order}
attribute, the same attribute \code{\link[=generate_docx_hitopsr]{generate_docx_hitopsr()}} returns, and
\code{\link[=write_module]{write_module()}} writes it back from that attribute, so a descriptor
read and written again keeps the order it recorded. The generators'
\code{descriptor} argument sets the attribute for you, and
\code{\link[=score_hitopsr]{score_hitopsr()}} and \code{\link[=reliability_hitopsr]{reliability_hitopsr()}} read it under
\code{layout = "printed"} to score columns entered in the form's printed
order.}
\item{\code{columns}}{The names that an online export gives the module's
items: one string per item, in ascending item-number order.
Optional. \code{\link[=generate_redcap_hitopsr]{generate_redcap_hitopsr()}}'s \code{descriptor} writes the
dictionary's item field names here, and
\code{\link[=generate_qualtrics_hitopsr]{generate_qualtrics_hitopsr()}}'s writes the questions' \verb{[[ID:]]}
values. A Word form has no columns, so
its descriptor has no field. \code{\link[=read_module]{read_module()}} returns the field on the
module's \code{columns} attribute, and \code{\link[=write_module]{write_module()}} writes it back
from that attribute as a JSON array. \code{\link[=score_hitopsr]{score_hitopsr()}} and
\code{\link[=reliability_hitopsr]{reliability_hitopsr()}} use the attribute as \code{items} when \code{items} is
omitted.}
}

\code{format}, \code{instrument}, and \code{scales} are required. A reader of format
\code{"1.0"} ignores a field it does not know, so release 0.2.0, the first
with \code{\link[=read_module]{read_module()}}, and later releases read a file with \code{columns} and
ignore the field. The fields and the
version string are a public contract and change only deliberately.
}

\examples{
m <- hitop_module("hitopsr", scales = c("Agoraphobia", "Appetite Loss"))

f <- tempfile(fileext = ".json")
write_module(m, f)
cat(readLines(f), sep = "\n")

identical(read_module(f), m)

file.remove(f)

}
\seealso{
\code{\link[=read_module]{read_module()}} to read the file back; \code{\link[=hitop_module]{hitop_module()}} to build a
module in the first place; the \code{descriptor} argument of
\code{\link[=generate_docx_hitopsr]{generate_docx_hitopsr()}}, \code{\link[=generate_qualtrics_hitopsr]{generate_qualtrics_hitopsr()}}, and
\code{\link[=generate_redcap_hitopsr]{generate_redcap_hitopsr()}}, which writes one of these files beside the
instrument it builds.
}
