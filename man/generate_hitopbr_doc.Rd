% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_hitopbr.R
\name{generate_hitopbr_doc}
\alias{generate_hitopbr_doc}
\title{Generate a Word Document for the HiTOP-BR Assessment}
\usage{
generate_hitopbr_doc(
  file = "hitopbr_1.0.docx",
  papersize = c("letter", "a4"),
  title = "HiTOP-BR (v1.0)",
  include_scoring = TRUE,
  base_size = 10,
  font_family = "Times New Roman"
)
}
\arguments{
\item{file}{Character string specifying the output file path. Defaults to \code{"hitopbr_1.0.docx"}.}

\item{papersize}{Character string specifying the paper dimensions. Must be one of \code{"letter"} (8.5 x 11 inches) or \code{"a4"} (8.27 x 11.69 inches). Defaults to \code{"letter"}.}

\item{title}{Character string for the document header title. Defaults to \code{"HiTOP-BR (v1.0)"}.}

\item{include_scoring}{Logical. If \code{TRUE} (default), appends a page break and the scoring instructions table to the end of the document.}

\item{base_size}{Numeric value specifying the base font size in points. Defaults to \code{10}.}

\item{font_family}{Character string specifying the font family to be used throughout the document. Defaults to \code{"Times New Roman"}.}
}
\value{
Invisibly returns the path to the created file (\code{file}).
}
\description{
Creates a formatted Microsoft Word document containing the Hierarchical
Taxonomy of Psychopathology - Brief Report (HiTOP-BR) items, instructions,
and optional scoring keys. The function dynamically adjusts table widths
to ensure a perfect fit within standard page margins.
}
\examples{
\dontrun{
# Generate standard version
generate_hitopbr_doc()

# Generate a customized version for A4 paper without the scoring key
generate_hitopbr_doc(
  file = "hitopbr_a4_noscoring.docx",
  papersize = "a4",
  title = "HiTOP-BR - Patient Copy",
  include_scoring = FALSE,
  base_size = 12,
  font_family = "Arial"
)
}
}
