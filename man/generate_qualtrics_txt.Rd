% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_qualtrics.R
\name{generate_qualtrics_txt}
\alias{generate_qualtrics_txt}
\title{Generate a Qualtrics Advanced Format Import File}
\usage{
generate_qualtrics_txt(
  items_df,
  instructions_list,
  file_path = "qualtrics_import.txt"
)
}
\arguments{
\item{items_df}{A data frame containing the survey items. The first column
must contain the item identifiers (e.g., item numbers), and a column named
\code{Text} must contain the question text.}

\item{instructions_list}{A list containing the survey instructions and response
options. It must include a \code{start} character string for the initial descriptive
block, and an \code{options} data frame containing \code{value} and \code{label} columns for
the multiple-choice options.}

\item{file_path}{A character string specifying the output file path.
Defaults to \code{"qualtrics_import.txt"}.}
}
\value{
Invisibly returns \code{NULL}. The function creates a text file at the
specified \code{file_path} and prints a success message to the console.
}
\description{
Creates a text file formatted for the Qualtrics Advanced Format import tool.
It takes a data frame of survey items and a list of instructions, combining
them into a structured text file with specific block and question tags.
This function is designed to work seamlessly with the package's built-in
datasets.
}
\examples{
\dontrun{
# Generate a Qualtrics text file using the package's built-in datasets
generate_qualtrics_txt(
  items_df = hitopbr_items,
  instructions_list = hitopbr_instructions,
  file_path = "hitopbr_survey.txt"
)
}
}
