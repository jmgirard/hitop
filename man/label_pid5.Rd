% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_pid5.R
\name{label_pid5}
\alias{label_pid5}
\title{Label PID-5 Columns with Semantic Descriptions}
\usage{
label_pid5(
  data,
  target = c("items", "scales"),
  version = c("FULL", "SF", "BF"),
  prefix = NULL
)
}
\arguments{
\item{data}{A data frame containing PID-5 items or scales.}

\item{target}{A string specifying what to label: \code{"items"} to label raw item
columns with questionnaire text, or \code{"scales"} to label computed scale
columns. (default = \code{"items"})}

\item{version}{A string specifying the PID-5 form the columns belong to:
\code{"FULL"} (220 items), \code{"SF"} (100 items), or \code{"BF"} (25 items). Matched
case-insensitively. The three forms number their items independently and
score different sets of scales, so the form named here decides both the
text attached to an item column and which scale columns are recognized.
(default = \code{"FULL"})}

\item{prefix}{A string specifying the prefix used on the column names.
\code{NULL} resolves to the default for the given \code{target} and \code{version}: under
\code{target = "items"}, the form's own stem (\code{"pid5_"}, \code{"pid5sf_"} or
\code{"pid5bf_"}), the pattern the shipped datasets and the package's REDCap
export use; under \code{target = "scales"}, \code{"pid_"}, which is what
\code{\link[=score_pid5]{score_pid5()}} writes under its own default \code{prefix}. (default = \code{NULL})

Item columns are expected as the prefix followed by the item number
zero-padded to the width of the form's largest item number (\code{pid5_001} to
\code{pid5_220} for the full form, \code{pid5bf_01} to \code{pid5bf_25} for the brief
form). A column carrying the prefix and a number that is not one of those
expected names is not labelled, and a warning of class
\code{hitop_unpadded_items} names it, in a sentence per kind: a number padded to
some other width is reported as not zero-padded to the form's width, and a
number outside the form's range is reported as out of range, whatever its
padding. That warning is raised whether or not any other column matched.
Scale columns are expected as the prefix followed by the scale's
\code{camelCase} name.}
}
\value{
A data frame with labeled columns. Columns the named form does not
recognize keep whatever attributes they had. The validity-scale columns
\code{\link[=validity_pid5]{validity_pid5()}} writes and the \verb{_se} columns
\code{score_pid5(calc_se = TRUE)} writes are not labelled. If no column matched
the expected names at all, \code{data} is returned unchanged and a warning says
so; the \code{hitop_unpadded_items} report still names every prefixed item
column it found.
}
\description{
Add literal item text or clean scale names as attributes to data frame
columns, making them readable by data viewers and reporting packages.
}
\examples{
# Attach item text as a `label` attribute to the raw item columns
labeled <- label_pid5(sim_pid5bf, target = "items", version = "BF")
attr(labeled$pid5bf_01, "label")

}
